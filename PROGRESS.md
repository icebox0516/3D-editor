# PROGRESS

> 项目快照：回答「今天重新打开项目，现在到底是什么情况」。保持短；任务看 [TASKS.md](TASKS.md)，决策看 [DECISIONS.md](DECISIONS.md)。

- **项目**：浏览器端三维园区编辑器（React 19 + Three.js WebGL2 + zustand + Vite）。一期已收官稳定，二期进行中，**2397 测试基线全绿**（`npm test` / `check:layers` / `typecheck` 三重门槛）。
- **当前阶段**：二期 —— **Feature / Style / Asset 三层程序化内容体系**（画区域赋程序化样式 + 程序化资产放置，0 外部模型文件）。
- **最新状态（2026-09-16）**：①设计共识 15 条决策落 [DECISIONS.md](DECISIONS.md)；②文档体系切换五件套 + 一期 PLAN 归档 `docs/archive/plan-phase1/`（T001 完成）；③任务拆分为**会话粒度**：T002–T007 六个 epic → 21 个子任务（两级编号 `0XX.N`，验收门在每个 epic 末尾，D16）；④**T002.1 完成**——程序化资产契约落地（D17：glob 自描述注册 + 无参 build + 统一 AssetDescriptor 混排 + 会话私有缓存，冒烟资产垃圾桶，+24 测试）；⑤**T002.2 完成**——资产库混排+缩略图（ContentBrowser 统一描述符混排、分类/搜索/标签/收藏全覆盖；程序化源离屏快照两级缓存，启动自动生成、失败降级占位；浏览器实测刷新零重生成，+19 测试）；⑥**T002.3 完成**——放置+烘焙式变体（AssetSourceRouter 复合源：池与 Ghost 统一接入；mulberry32 确定性 seed 掷骰，缩放/旋转烘 transform、色相烘 instanceColor，同 seed 同结果；Inspector seed 只读行，+48 测试）；⑦**T002.4 完成**——设施资产包 5 种（路灯/公园长椅/垃圾桶升级/消防栓/标识牌，几何全代码生成；材质经用户裁定升 shader 级：拆分派发 threejs-expert 几何+park-shader-agent 纯 onBeforeCompile 注入 9 配方——木纹/拉丝/漆面磨损/铸铁颗粒，零纹理零 uniform，instanceColor 通路完整；长椅避让 GLB 撞名改 asset_parkbench；+63 测试；浏览器冒烟 12 对象 9 配方编译零错误）；⑧**T002.5 完成 → T002 epic 收官**——验收门全项过：5 资产 41 对象 GUI 全链路取证（14 图存 `docs/acceptance/t002/`）；合批实测同资产 8→12 实例 draw 调用零增长、20 连放仅池级 +6、删光回基线；撤销链字节级一致 + seed 存档不变 + 跨资产绑定隔离；全程 console 零错误；缩略图清缓存重生成→持久化→热载 2.7s 就位（T002.4 遗留①关闭）；2143 全绿对账 1989+154。近观材质观感截图留档待用户过目（当日视觉模型服务断连）；⑨**T003.1 完成**——撒点纯函数（D17 抖动网格 v1 唯一采样器：cell 随机流 = mulberry32(hashCell(seed,i,j)) 与遍历顺序无关 → 局部重算/跨块确定性根基；均匀层 U[0,h) 抖动点恒在 cell 内、聚簇层 κ=4 模型簇内密度恒标称 4 倍、smoothstep 边缘衰减；scatterPolygon 全量 + scatterChunk 块级 API，全量=逐块拼接逐位一致；mulberry32 迁 core/random + pointInPolygon 入 core/math；2 万点实测 ~7ms，+25 测试）；⑩**T003.2 完成**——分块实例化管线（D9 承重墙：32m 块网格构造可覆写、每 源×块×资产 一个 InstancedMesh 合批、逐块视锥剔除 visible 开关（主遍+阴影遍同口径零提交）、`recomputeChunks(id, keys, params?)` 局部重算机制（未列块矩阵缓冲逐位不动）+ `computeAffectedChunks` 保守受影响块纯函数（bbox 并集外扩撒点影响半径，domain `scatterInfluenceRadius` 单一真相源）、逐实例 hue 微差复用 T002.3 通路；散布 root 挂 scene 兄弟组天然不参与拾取（D5）；DEV 冒烟面 `window.__scatterSmoke`；主代理审查修 boundingBox 惰性补算；浏览器实测：534 实例渲染可见聚簇正确、平移出视锥 16 块→0 可见 drawCalls 515→3（环境基线）、往返无损、同 seed 重建复原、console 零错误；+24 测试）。观察项：多材质组资产的批次 draw call 数 = 块数×材质组数（trashbin ~515 calls@534 实例）——003.4 植物宜单材质合并，T006 细化批次预算；⑪**T003.3 代码全量完成**（2026-09-17，未提交）——D18 拷问门过 + 数据层五段（ScatterRecipe/覆写白名单/seed 派生/序列化 v2.0 增量放行 +33 测试）；runtime 消费端派 threejs-expert：`RegionScatterSync` 五钩子绑定 + `ScatterChunkManager` 小扩（源级显隐/拾取反查，r186 raycaster 不跳 visible=false 已显式挡）+ `RuntimeViewport` 两根合并拾取（点实例选区域 D18.7）+ dev 配方 `dev.scatter_smoke`（+36 测试，含主代理审查返工：**世界多边形叠加 transform XZ——gizmo 平移表面与散布同动**）；**三重门槛 2331 全绿**（2192+69+70）；浏览器验收中断于「实例可见性确认」（区域误处隐藏态+Triangles 未涨待查，中断点/剩余步骤/驱动经验全录任务书交接快照）；⑫**T003.4 代码全量完成**（2026-09-17，未提交）——两段派发：threejs-expert 几何 4 资产（橡 688/松 136/灌 400/花 136 面，meta 扩 triangleCount/levels 接口位，+45 测试）+ park-shader-agent 材质 7 配方纯 onBeforeCompile 注入（树皮×2/冠层×3/花卉×2，零纹理零 uniform 保 instanceColor 通路，+25 测试）；**用户裁定植物观感需优化——返工待做**（弱点清单见任务书顶部）。tests/app 资产账目 18→22 主代理已修（glob 文件名序：植物 asset_* 前缀组在前）；⑬**T008 程序化树木立项**（2026-09-17）——用户裁定 v1 资产观感不达标、003.4 原 A/B 返工分叉作废，改 **3A 级单树开路**（grill 拷问门逐题裁定 → D19：`build(params?)` 参数化 + 单 seed 三流域域分离派生（shapeSlot 路由 / morphRng 形态 / instanceRng 表现）+ sourceKey=`assetId[:preset]:slot-N` 缓存与池分桶 + 整树合并几何烘焙叶卡（aLeafRand/aBend 槽内固有、不读对象 seed）+ uTime 服务拉前（D12 切分修订）+ Inspector 最小面 + 008.3 尾锚点定调唯一人工阻断门；008.1–008.5 五子任务书已建；**T003 剩余冻结**，41 项未提交变更原状挂账）；⑭**T008.1 完成**（2026-09-17，未提交）——契约增量+uTime 基建全量落地（threejs-expert 实现 + 主代理审查）：`domain/assets/shapeFamily` 三流域派生纯函数 + sourceKeyOf 单一真相源；`ProceduralSourceCache.load(assetId, {seed,preset})` 槽路由（**契约第一锁硬测试**：S1≠S2 同槽 → morphSeed 逐位同、build 仅一次、Geometry/Material 同引用；未声明 shapeFamily 回退 assetId 键行为逐位不变）；池键升级 sourceKey（resolvePoolKey 注入）+ aSeed 逐实例属性（**r186 事实裁定：自定义实例属性必须 geometry 绑定**，桶几何挂 Float32 缓冲、生命周期全对齐 instanceColor；**带 seed 池单实例仍 InstancedMesh**——aSeed 风动不因单棵失效）；`TimeUniformService` uTime 最小服务（renderFrame 单一时钟 + 声明即驱动，shader_test 第二消费者）；DEV 验证资产 asset_seedstack（category 'dev' 浏览器过滤不进产品栏）；+45 测试 → **2376 全绿**三重门槛过（主代理亲自复核）；与 T003 挂账变更交织（Renderer.ts 共文件）原状未提交，拆笔待 T003 恢复或用户指示）。⑮**T008.2 完成**（2026-09-17，未提交）——树几何生成器 slot-0 锚点形态全量落地（threejs-expert 实现）：`tree/broadleafGeometry` 五级递归分枝（骨架 5+领导枝 1 → ×3/×3/×2/×2）+ 锥度管枝干（平行传输标架、径向 12→4、主干底盖防斜穿）+ 冠外层叶卡（L4 外段 + L5 全梢）；**结构计数固定 ⇒ 任意 seed 面数恒定**（皮 20724 面/叶卡 5670 张/合 32064 = meta 实数；锚点树 7.53m 高 6.6m 冠幅 minY 精确 0，8 槽 7.53–8.82m 全带内）；几何数据契约冻结——aLeafRand（逐叶 [0,1) 同卡同值）/ aBend（根 0.12·hw → 尖 0.52+0.44·hw 非降，hw=冠内归一高度）/ 叶卡 UV 0–1 域 / 皮 0 叶 1 恰 2 材质组（皮组属性恒 0）；资产「夏栎」build 无参 = slot-0 锚点 = 缓存路径同一棵树；DEV 出图面 `window.__tree3a`（runtime/tree3aStage：mount/unmount/turntable/view/stats，scene 兄弟组 D5 不拾取，bootstrap 只装配零 three）；+21 测试 → **2397 全绿**三重门槛过；浏览器自检（agent-browser，5175 即用即停）：3 截图存 `docs/acceptance/t008/008.2/`，剪影不规则无几何体感、HUD 32.1k 三角 @60fps、console 零错误；偏离两处落档（账目插入位实测在 asset_shrub 后非简报所称之前；DEV 句柄落 runtime 非组合根内联））。⑯**T008.3 代码段完成**（2026-09-17，未提交，锚点门待裁定）——park-shader-agent 交付（首派撞账户 5h 限额阵亡未留半成品、重置后重派成功）：`tree/tree3aMaterials.ts` 三工厂（叶：SDF 橡叶形 alphaTest+alphaToCoverage/背光透射/aLeafRand 三变奏/双层风动——整树缓摆 0.18Hz 高度²权重 + 叶片快颤 aBend 权重，相位 hash(aSeed)；皮：14 脊深沟+节疤+冠下苔痕+同公式缓摆；叶影裁切深度材质同 SDF 单源+皮组守卫）；uTime 桥接 = 材质级 uniforms 与 shader.uniforms 同引用；TimeUniformService freeze/unfreeze + Renderer 一字公开 + bootstrap 接线 + tree3aStage mountWindDemo/freezeTime；**+22 测试 → 2419 全绿三重门槛过**（主代理复核）；浏览器取证 8 图存 `docs/acceptance/t008/008.3/`（近/中/远冻结机位 + 风动对 1.296% 变化 + 冻结对 0.000% 逐位一致 + 足印绿 35.3%/透空 14.4% + console 零错误）；**本会话主代理视觉通道断连**（Read 图仅 CDN 回传/IAB 截图 guest 失败/analyze_image 400——T002.5 同款），观感首验移交用户锚点门，参考基调（文本+3 外链）录任务书；排查插曲两条落档（场景既有绿物误判/an open 同源导航毁应用状态）。
- **下一步**：①**T008.3 锚点门用户裁定**（代码段已完成：材质+风动全量落地 2419 全绿，近/中/远+风动 8 图存 `docs/acceptance/t008/008.3/`；本会话主代理视觉通道断连 → 像素金丝雀量化佐证已出，观感首验即用户，参考基调与基准链接录任务书；裁定过 → 008.4 放置集成，1–2 轮微调上限）；执行链 008.3【视觉锚点定稿·人工门·待裁定】→008.4（放置集成）→008.5（扩 8 槽+验收门）。②T003 剩余（003.3 验收续走/003.5/003.6/设施返工）**冻结挂起**，T008 树协议稳定后恢复（恢复入口=003.3 任务书交接快照）；41 项未提交变更原状挂账。T002.4 遗留②（长椅 GLB 与公园长椅并存策展）建议并入 T004。挂账：dev server 5173 仍在跑（用户在用），收尾停。
- **参考**：宿主架构现状审计 `docs/architecture-audit/`（14 文档，2026-09-15，集成事实参考）。
