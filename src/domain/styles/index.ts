/**
 * domain/styles 汇总导出：参数元数据形态（StyleParameter / StyleParameterType）。
 *
 * T6.9：v1 旧样式体系（StyleDefinition / MaterialSpec / StyleReference / resolveStyle
 * 三级优先级合成）已随旧要素体系删除；本模块仅保留阶段 6 语义定义与预设元数据
 * 共用的参数表形态（迁自 StyleDefinition.ts，import 路径不变）。
 */
export * from './StyleParameter';
