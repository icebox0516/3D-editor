# 程序化植物系统优化与植物资产库建设说明

## 一、总体目标

当前夏栎已经形成完整的程序化植物资产链路：

```text
植物资产定义
→ 程序化几何生成
→ 树皮/叶片材质
→ ProceduralSourceCache
→ sourceKey / shapeSlot
→ InstancedAssetPool
→ 场景实例化
```

现阶段不再进行整体架构重做，而是在现有架构上继续提升两个核心能力：

```text
第一阶段：
把“夏栎”从可用的程序化树，提升为具有较高植物结构真实性的生产级植物资产。

第二阶段：
把夏栎形成的几何、材质、随机、缓存、实例化、LOD、验收规范沉淀为统一资产模板，
以此建立可持续扩展的程序化植物库。
```

最终目标不是“做几棵树”，而是建立：

> **零外部模型依赖、可程序化生成、可实例化渲染、可批量扩展、可统一管理的园区植物资产体系。**

---

# 二、当前夏栎优化原则

## 2.1 不推翻现有架构

当前以下结构继续保留：

```text
asset_xxx.asset.ts
        ↓
xxxGeometry
        ↓
xxxMaterials
        ↓
ProceduralSourceCache
        ↓
sourceKey / shapeFamily
        ↓
InstancedAssetPool
```

原因：

1. 几何生成、材质、资产注册、缓存、实例渲染职责已经分离。
2. shapeSlot / morphSeed / instanceSeed 已经具备植物形态族扩展基础。
3. InstancedMesh 已经具备批量植物实例化能力。
4. 后续植物类型可以复用整个运行时架构，只新增具体资产实现。

因此后续优化原则为：

> **优化资产生成质量，不重做资产运行时架构。**

---

# 三、夏栎下一阶段优化重点

## 3.1 第一优先级：植物结构真实性

当前夏栎最大的限制不是 Shader，而是植物结构。

当前模式：

```text
枝干
 ↓
L4 / L5
 ↓
随机叶卡
```

需要升级为：

```text
主干
 ↓
一级骨架枝
 ↓
二级分枝
 ↓
三级枝梢
 ↓
末梢簇
 ↓
叶簇
 ↓
叶片
```

重点不是增加更多枝条数量，而是建立更加可信的“枝—簇—叶”关系。

---

## 3.2 叶片从“随机叶卡”升级为“叶簇生成”

现有叶卡虽然能够避免完全规则的视觉效果，但叶片与枝梢之间的生长关系仍然较弱。

后续增加独立的：

```text
LeafClusterGenerator
```

建议基本结构：

```text
BranchTip
    ↓
LeafCluster
    ├── clusterCenter
    ├── clusterDirection
    ├── clusterRadius
    ├── clusterDensity
    ├── leafCount
    ├── leafSizeRange
    ├── orientationRange
    └── shellBias
```

叶簇应具备以下特征：

### 外层

叶片较密，形成完整冠层轮廓。

### 中层

叶片密度逐渐降低，同时保留部分可见枝条。

### 内层

明显稀疏，形成冠层内部空腔。

最终达到：

```text
外轮廓完整
+
内部不填死
+
局部透枝
+
簇与簇之间存在间隙
```

从视觉上由“叶片堆起来的树冠”转变为“多个叶簇构成的树冠”。

---

# 四、夏栎冠形优化

夏栎的 8 个 Shape Slot 不应只是随机 Seed 得到的 8 棵树。

应当建立明确的形态类别：

```text
slot-0  标准型
slot-1  挺拔型
slot-2  展开型
slot-3  偏冠型
slot-4  低冠型
slot-5  高冠型
slot-6  疏松型
slot-7  丰满型
```

这里只规定**形态方向**，不要求所有植物都采用同样的 8 种命名。

核心原则：

> **Shape Slot 是可控的植物形态资产，而不是随机数展示。**

每个 Slot 最终都必须经过视觉验收。

---

# 五、枝干真实性优化

现有枝干生成方案已经具备：

* 多级分枝
* 锥度
* 曲率
* 方向随机
* 子枝连接
* 主干倾斜
* 根部膨大

后续重点不是继续增加级数，而是增加以下结构特征：

### 5.1 分枝主次

需要明显建立：

```text
主枝 > 次枝 > 末枝
```

不能所有分枝视觉权重接近。

### 5.2 枝条长度衰减

随着层级增加：

```text
长度 ↓
半径 ↓
分枝数量 ↓
```

### 5.3 冠层中心留空

主干与大枝进入冠层后，不能被叶簇完全封死。

### 5.4 局部偏向

不同 Shape Slot 可以改变：

```text
枝条展开角度
主冠方向
枝条密度
树冠重心
```

形成真正的树形差异。

---

# 六、树皮优化

现有树皮 Shader 已经具备较好的程序化纹理基础，不继续大量增加 Fragment Noise。

下一阶段只补充：

```text
Shader 微表面
+
少量几何微起伏
```

重点解决：

> 近距离观察时树干仍像“光滑圆柱”的问题。

建议只增加少量低频顶点扰动或局部结构，不制造明显高频 displacement。

目标层级：

```text
远景：
颜色 + 粗糙度即可

中景：
Shader 脊沟结构明显

近景：
轮廓开始具有真实树皮微起伏
```

---

# 七、生产 Shadow Path 完整化

当前 DEV 阶段已经具备叶片 SDF 深度材质，但正式 InstancedMesh 路径没有完整继承这一能力。

因此需要统一资产 Runtime 契约。

建议将：

```text
InstanceSource
```

从：

```text
geometry
material
```

扩展到能够支持：

```text
geometry
material
customDepthMaterial
customDistanceMaterial（需要时）
```

最终形成：

```text
植物表面材质
+
植物 Shadow 材质
+
实例属性
```

保证：

```text
DEV 预览
=
正式场景
```

而不是出现两套不同的植物表现。

---

# 八、aSeed 与实例化几何关系优化

当前 `aSeed` 作为 Geometry Attribute 的设计能够支持普通 InstancedMesh，但与诊断 Split Mesh 共用 Geometry 时存在结构耦合。

后续原则：

```text
正常渲染：
Geometry 共享

特殊诊断：
允许 Geometry Copy
```

不要为了极少使用的诊断路径污染正常实例化路径。

目标：

> 正常植物实例保持最大程度共享；诊断/调试模式单独承担额外成本。

---

# 九、性能优化原则

夏栎当前约 32K 三角形，不作为首要问题。

优先控制：

```text
叶片 Overdraw
Fragment Shader
透明/AlphaTest 成本
Shadow Pass
实例数量
```

后续引入：

```text
High
Mid
Low
```

建议：

### High

完整枝干 + 高密度叶簇。

### Mid

降低：

```text
枝条段数
叶簇数量
叶卡数量
Shader 复杂度
```

### Low

重点保持：

```text
树冠轮廓
颜色层次
整体风格
```

而不是保持内部枝条。

---

# 十、夏栎最终验收标准

夏栎完成不应只验收“代码能运行”。

必须同时通过四类验收。

## 10.1 结构验收

```text
主干自然
枝干层级清晰
枝条粗细递减
叶簇与枝梢存在明确关系
冠层内部存在空腔
```

## 10.2 视觉验收

至少验证：

```text
近景
中景
远景
```

并分别观察：

```text
轮廓
枝干
叶簇
叶片
树皮
阴影
```

## 10.3 变体验收

8 个 Shape Slot 必须：

```text
明显不同
但仍然属于同一树种
```

禁止出现：

```text
只是整体缩放
只是旋转
只是轻微随机
```

## 10.4 性能验收

至少测试：

```text
1 棵
20 棵
100 棵
500 棵
1000 棵
```

重点记录：

```text
Draw Call
GPU 时间
CPU 时间
显存
Shadow 成本
Frame Time
```

---

# 十一、夏栎完成后的植物库建设

夏栎完成后，不应该直接复制代码做第二棵树。

正确做法是：

```text
夏栎
 ↓
抽象共性
 ↓
形成 PlantAssetBase 规范
 ↓
再批量建设植物资产
```

---

# 十二、植物资产统一分层

整个植物库建议分为四类。

## 12.1 乔木

用于：

```text
道路
广场
建筑周边
绿地
园区主景
```

例如：

```text
夏栎
香樟
朴树
榉树
银杏
法桐
悬铃木
国槐
乌桕
栾树
```

---

## 12.2 小乔木 / 景观树

用于：

```text
入口
庭院
建筑节点
景观节点
```

例如：

```text
紫薇
樱花
海棠
桂花
红枫
玉兰
```

---

## 12.3 灌木

用于：

```text
道路绿化
建筑边缘
景观带
分隔带
```

例如：

```text
红叶石楠
海桐
黄杨
杜鹃
金森女贞
```

---

## 12.4 地被 / 草本

用于填充：

```text
绿地
花坛
景观带
树下区域
```

包括：

```text
草坪
低矮地被
宿根植物
观赏草
花卉
```

---

# 十三、不要让所有植物都使用同一套生成器

这是植物库建设最重要的一条原则。

应该采用：

```text
通用植物运行时
+
树种专用几何生成器
+
树种专用材质参数
```

而不是：

```text
一个 BroadleafGenerator
→ 换颜色
→ 换名字
→ 变成十种树
```

建议结构：

```text
PlantRuntime
    │
    ├── BroadleafTreeGenerator
    │       ├── 夏栎
    │       ├── 朴树
    │       ├── 榉树
    │       └── 香樟
    │
    ├── ConiferGenerator
    │       ├── 雪松
    │       ├── 黑松
    │       └── 柏类
    │
    ├── PalmGenerator
    │
    ├── ShrubGenerator
    │
    └── GrassGenerator
```

即：

> **运行时统一，植物形态算法按植物类别分族。**

---

# 十四、建立“植物形态族”

建议先定义少量基础生成族。

### BroadleafTree

阔叶乔木：

```text
树干
+
多级枝干
+
叶簇
```

### Conifer

针叶树：

```text
主干
+
分层枝组
+
针叶/针叶簇
```

### FloweringTree

花木：

```text
主枝
+
枝梢
+
叶簇
+
花簇
```

### Shrub

灌木：

```text
多基干
+
大量低矮分枝
+
球状/自然团簇
```

### GroundCover

地被：

```text
表面分布
+
高度随机
+
密度控制
```

### Grass

草类：

```text
草叶
+
丛簇
+
方向随机
+
风动
```

---

# 十五、每种植物资产统一采用标准模板

每新增一种植物，必须具备：

```text
plant_xxx.asset.ts
plant_xxx.geometry.ts
plant_xxx.materials.ts
plant_xxx.config.ts
```

其中：

### asset

负责：

```text
meta
shapeFamily
LOD
variants
build()
```

### geometry

负责：

```text
树干
枝干
叶簇
叶片
```

### materials

负责：

```text
树皮
叶片
花
果实
风动
Shadow
```

### config

负责：

```text
高度
冠幅
枝干比例
叶片尺寸
颜色范围
密度
风动参数
```

这样新增植物时不会复制大量业务代码。

---

# 十六、植物资产元数据统一标准

每个植物至少需要：

```text
id
name
category
family
shapeFamily
heightRange
crownWidthRange
triangleCount
lods
variants
tags
```

例如：

```text
asset_tree_xxx

name:
夏栎

category:
plant/tree

family:
broadleaf

shapeFamily:
8

height:
7.4m–8.5m

crown:
5.4m–6.6m

lod:
high / mid / low

variants:
scale / rotation / hue
```

这些数据不仅用于代码，还用于后续：

```text
资产管理器
植物筛选
性能预算
自动测试
资产预览
```

---

# 十七、植物库必须建立“视觉标准”

以后新增植物不能只看：

```text
“这棵树能生成”
```

而要统一检查：

```text
① 树种辨识度
② 树形自然度
③ 枝干结构
④ 叶片/叶簇结构
⑤ 冠层空隙
⑥ 材质真实度
⑦ 风动自然度
⑧ 阴影
⑨ 不同形态差异
⑩ 性能
```

最终每个植物都必须拥有：

```text
正面
侧面
45°
近景
中景
远景
```

固定取证视角。

---

# 十八、植物库建设顺序

不要同时铺几十种。

建议按照“基础形态覆盖率”建设：

```text
阶段 1
夏栎
```

建立完整模板。

```text
阶段 2
朴树
香樟
榉树
银杏
```

覆盖典型阔叶乔木。

```text
阶段 3
法桐
国槐
栾树
乌桕
```

补充不同冠形。

```text
阶段 4
雪松
黑松
柏类
```

建立针叶树族。

```text
阶段 5
紫薇
樱花
桂花
玉兰
```

建立花木族。

```text
阶段 6
红叶石楠
海桐
黄杨
杜鹃
```

建立灌木族。

```text
阶段 7
草坪
地被
观赏草
```

建立地被族。

这样可以逐步建立完整园区植物覆盖体系。

---

# 十九、植物资产库最终组织方式

最终建议形成：

```text
Plant Library
│
├── Trees
│   ├── Broadleaf
│   │   ├── 夏栎
│   │   ├── 朴树
│   │   ├── 榉树
│   │   ├── 香樟
│   │   └── 银杏
│   │
│   └── Conifer
│       ├── 雪松
│       ├── 黑松
│       └── 柏类
│
├── SmallTrees
│   ├── 紫薇
│   ├── 樱花
│   ├── 桂花
│   └── 玉兰
│
├── Shrubs
│   ├── 红叶石楠
│   ├── 海桐
│   ├── 黄杨
│   └── 杜鹃
│
└── GroundCover
    ├── 草坪
    ├── 地被
    └── 观赏草
```

---

# 二十、最终要形成的不是“树木集合”，而是植物资产平台

最终目标结构应该是：

```text
                Plant Asset System
                       │
        ┌──────────────┼──────────────┐
        │              │              │
   Geometry        Material       Runtime
        │              │              │
   Tree Family     Shader      Cache / Pool
        │              │              │
        └──────────────┼──────────────┘
                       │
                  Plant Asset
                       │
             ┌─────────┼─────────┐
             │         │         │
           Shape     LOD      Variant
             │         │         │
          形态族      距离级别   实例差异
```

然后最终接入你的园区编辑器：

```text
资产管理器
    ↓
植物
    ↓
夏栎
    ↓
形态选择
    ↓
拖拽 / 点击放置
    ↓
InstancedMesh
    ↓
统一风动 / 阴影 / LOD
```

这样植物就不再是“某个程序化 Demo”，而会成为和建筑、道路、设施一样的正式资产类别。

---

# 二十一、最终建设原则

整个植物系统最终遵循以下原则：

```text
1. 零外部模型依赖
2. 植物结构优先于 Shader 堆效果
3. 树种差异来自形态算法，而不是简单换颜色
4. Shape Slot 是人工可控的固定形态族
5. Geometry / Material / Runtime 分层
6. 所有植物支持实例化
7. 所有植物具有明确 LOD
8. 所有植物经过固定机位视觉验收
9. 所有植物具有性能预算
10. 夏栎形成模板后，再复制“方法”，而不是复制代码
```

## 最终目标

建立一个能够持续扩展的：

> **程序化、GPU 友好、零外部模型依赖、可实例化、可变体化、可统一管理的 Three.js 园区植物资产库。**

夏栎是第一棵标准资产，而不是最终资产。

它完成后的真正价值，是把：

```text
“做出一棵树”
```

升级成：

```text
“建立一种可以持续生产植物的能力”。
```
