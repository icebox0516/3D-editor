可以。你现在最需要的不是让 AI “直接开始写代码”，而是让它先把这份总需求**拆成可执行、可验收、可持续推进的小任务**，并且严格接入你现在的 `AGENTS.md / TASKS.md / PROGRESS.md / DECISIONS.md` 体系。

我建议提示词明确要求 AI 遵循：

**总需求 → 阶段 → 子任务 → 原子任务 → 验收 → 依赖 → 文件边界 → 进度记录**

而不是简单输出一个 TODO 列表。

下面这份可以直接给 Codex / Claude Code / Cursor Agent 使用。

你现在负责把《程序化植物系统优化与植物资产库建设说明》转换为一套可以直接进入当前 Three.js 园区三维编辑器项目执行的细分任务。

# 一、你的角色

你是本项目的“任务架构师 / 技术项目拆解 Agent”，不是普通需求分析助手。

你的工作目标不是写代码，而是：

> 将一份较大的植物系统建设需求，拆解成一组具有明确边界、依赖关系、验收标准、文件范围和完成条件的可执行任务。

这些任务最终会被写入项目现有的：

```text id="k3c4jo"
AGENTS.md
TASKS.md
PROGRESS.md
DECISIONS.md
```

任务必须适合后续由代码 Agent 按任务逐项执行，并支持长期持续推进。

---

# 二、必须先读取项目现状

在拆任务之前，必须先检查当前仓库，不得只根据需求文档凭空规划。

重点读取：

```text
AGENTS.md
TASKS.md
PROGRESS.md
DECISIONS.md
package.json
src/runtime/procedural/
src/runtime/instancing/
src/domain/assets/
以及当前夏栎相关源码
```

重点确认：

```text
1. 当前夏栎已经完成到什么程度
2. 当前已有的 T008.x 任务有哪些
3. 哪些需求已经实现
4. 哪些需求只是接口占位
5. 哪些需求已经有正确架构但还没有产品化
6. 哪些需求存在代码级缺口
7. 哪些优化会影响已有契约
8. 哪些需求可以直接复用现有模块
9. 哪些任务必须先于其他任务执行
```

禁止重复规划已经完成的工作。

---
# 三-A、植物开发的视觉参考前置规则

任何植物资产正式进入程序化开发之前，必须先建立该植物自己的视觉参考基准。

任务拆解时，必须为每一种新增植物预留独立的 Reference Research / 视觉参考任务，不得直接沿用其他植物的参考图、形态规则或视觉判断。

执行要求：

1. AI 必须主动检索真实目标植物的外部图片作为视觉参考。
2. 每种植物至少选取 1～3 张具有代表性的真实参考图，优先覆盖：

   * 整体树形/植株形态
   * 枝干与分枝结构
   * 叶片、叶簇或冠层结构
   * 树皮、花、果实等具有识别度的局部特征
3. 根据参考图归纳植物的真实结构特征，而不是直接模仿某一张图片。
4. 必须将参考结果进一步转换为程序化生成规则，包括但不限于：

   * 高度与冠幅比例
   * 主干比例
   * 分枝层级
   * 分枝方向与密度
   * 冠层空间结构
   * 叶簇密度
   * 叶片方向与尺寸
   * 树皮/叶片/花果等视觉特征
   * 常见自然形态变化
5. 参考图只用于：

   * 视觉定调
   * 生成参数制定
   * 开发过程对照
   * 固定机位验收
6. 参考图不得进入产品运行时资源，不得作为最终模型、贴图或运行时数据依赖。
7. 每种植物完成后，应保留“参考特征 → 程序化规则 → 最终实现”的对应关系，便于后续复查和继续扩展。

必须形成：

```text
真实植物参考
    ↓
植物结构分析
    ↓
参数化生成规则
    ↓
程序化几何 / Shader
    ↓
固定机位视觉验证
    ↓
用户/主代理最终裁定
```

禁止：

```text
没有参考图直接凭经验设计植物结构
拿夏栎的结构规则直接换颜色生成其他树种
仅通过“像一棵树”作为植物真实性验收标准
```

该规则属于所有未来植物资产任务的通用前置约束。

# 四、核心拆解原则

不要把任务拆成类似：

```text
优化夏栎
优化 Shader
完善植物库
```

这种任务过于宽泛，无法直接执行。

必须拆成类似：

```text
P001 夏栎生产 Shadow Path
P001.1 扩展 InstanceSource
P001.2 扩展 InstancedAssetPool
P001.3 接入 customDepthMaterial
P001.4 验证 InstancedMesh Shadow
P001.5 回归已有 GLB 资产
```

每一个最终执行任务都必须满足：

> **一个任务应该能够被一个编码 Agent 在一次有限上下文内完成，并能够明确判断“完成 / 未完成”。**

---

# 五、任务拆解层级

使用四层结构：

```text
阶段
  ↓
子系统
  ↓
任务
  ↓
必要时的子任务
```

建议结构：

```text
P0 夏栎收尾
P1 夏栎植物结构真实性
P2 夏栎生产级渲染能力
P3 夏栎多形态资产化
P4 植物资产标准固化
P5 第一批阔叶乔木
P6 针叶植物族
P7 花木族
P8 灌木族
P9 地被/草本族
P10 植物库与编辑器深度集成
```

但不要机械采用这个顺序。

应根据实际仓库依赖关系调整。

---

# 六、重点要求：先拆夏栎，再拆植物库

不要一开始就同时规划几十种植物。

第一阶段必须聚焦：

> 把夏栎变成第一棵“标准植物资产”。

只有夏栎达到标准之后，才把它抽象成植物资产模板。

因此任务链必须体现：

```text
夏栎真实性升级
        ↓
夏栎生产化
        ↓
夏栎 8 Slot 形态族
        ↓
夏栎视觉/性能验收
        ↓
植物资产模板固化
        ↓
其他植物开始复用模板
```

---

# 七、夏栎任务拆解要求

围绕以下几个方向拆分：

## 1. 植物结构真实性

检查并拆分：

```text
主干结构
一级枝
二级枝
三级枝
枝条长度衰减
枝条粗细衰减
枝条空间分布
冠层中心空腔
枝条与树冠关系
树冠整体重心
```

重点判断哪些应该修改现有：

```text
broadleafGeometry.ts
```

哪些应该新建：

```text
LeafClusterGenerator
BranchDistribution
CrownFormation
等模块
```

但不要为了抽象而抽象。

只有在确实能够被后续植物复用时才建立公共模块。

---

## 2. 叶簇系统

将当前：

```text
枝梢 → 随机叶卡
```

拆成：

```text
枝梢
 ↓
叶簇生成器
 ↓
叶簇空间
 ↓
叶片分布
```

任务需要明确：

```text
叶簇中心
叶簇半径
叶片密度
外围偏置
内部稀疏
叶片方向
叶片尺寸
叶片数量
叶片与枝梢绑定关系
```

同时必须明确：

> 哪些参数属于夏栎特有规则，哪些可以成为所有阔叶树共享规则。

---

## 3. 树形 Shape Slot

当前夏栎已经有 shapeFamily。

不要重新建立另一套随机系统。

任务应该围绕现有：

```text
shapeSlot
morphSeed
instanceSeed
sourceKey
```

继续扩展。

最终要求：

```text
8 个 Slot
=
8 个经过人工筛选的固定夏栎形态
```

而不是简单随机生成 8 棵树。

任务需要明确：

```text
Slot 生成
Slot 取证
Slot 固定
Slot 回归测试
```

---

## 4. 树皮

重点拆：

```text
近景几何微起伏
Shader 继续保持现有结构
避免无意义增加 Noise
```

必须明确：

> 什么情况下用 Geometry，什么情况下继续用 Shader。

---

## 5. Shadow

检查当前：

```text
InstanceSource
InstancedAssetPool
Tree Material
Depth Material
```

建立完整生产 Shadow Path。

必须考虑：

```text
普通 Mesh
InstancedMesh
叶片 SDF
多材质 Geometry
Directional Light Shadow
```

同时保证：

> 不破坏现有 GLB / 非植物资产。

---

## 6. aSeed / InstancedMesh

检查当前 `aSeed` 与共享 Geometry 的关系。

重点评估：

```text
正常 InstancedMesh
诊断 Split Mesh
共享 Geometry
实例属性
资源释放
```

只有确认存在实际问题后才拆修改任务，不要为了理论上的洁癖重构。

---

## 7. LOD

设计：

```text
High
Mid
Low
```

但先分析当前 Runtime 是否已经存在 LOD 接口。

不要直接创建全新的 LOD 系统。

优先复用项目现有能力。

每级需要明确：

```text
Geometry
Leaf Count
Shader Complexity
Shadow
适用距离
目标性能
```

---

# 八、夏栎验收任务必须独立出来

不能把验收隐藏在开发任务里面。

至少建立：

```text
视觉验收
结构验收
8 Slot 验收
Shadow 验收
LOD 验收
性能验收
资源生命周期验收
回归测试
```

每个验收必须有：

```text
输入
操作
预期结果
失败判据
```

例如：

```text
T008.X 500 棵实例化性能验收

前提：
使用同一夏栎 Slot

操作：
场景放置 500 棵

检查：
Draw Call
FPS
Frame Time
GPU 时间
显存
Shadow Pass

通过条件：
...
```

不要写：

```text
性能良好
```

这种模糊描述。

---

# 九、植物资产模板拆解

只有夏栎完成后，才进入这部分。

任务需要从夏栎代码中提取：

```text
资产共性
运行时共性
几何共性
材质共性
风动共性
Shadow 共性
LOD 共性
Shape Slot 共性
性能验收共性
```

然后明确：

```text
哪些保留在 Plant Runtime
哪些属于 Broadleaf Family
哪些属于具体树种
```

最终形成：

```text
Plant Runtime
Plant Family
Plant Asset
```

三级结构。

---

# 十、植物库不要一次性拆成几十个重复任务

后续植物必须按照“形态族”组织。

至少分析：

```text
Broadleaf Tree
Conifer
Flowering Tree
Shrub
Grass
Ground Cover
```

每个族建立一个基础模板。

例如：

```text
Broadleaf Family
    ↓
夏栎
朴树
榉树
香樟
银杏
...
```

不要让每棵树重新实现：

```text
Cache
Instancing
Shape Slot
Wind
Shadow
LOD
```

这些应该由公共系统承担。

---

# 十一、植物种类的选择

请不要简单罗列植物名字。

需要根据园区三维编辑器的实际使用场景分析：

```text
道路
广场
建筑周边
绿地
入口
景观节点
水体周边
庭院
```

然后决定：

```text
第一批最需要哪些植物
```

但不得给植物进行“最好 / 最差 / 推荐分数”等主观评级。

使用：

```text
使用场景
覆盖能力
形态差异
技术代表性
开发优先级
```

说明选择依据。

---

# 十二、每个植物任务必须包含以下字段

最终 TASKS 中，每个任务至少包含：

```text
ID
名称
目标
背景
前置依赖
修改范围
预计涉及文件
实现内容
技术约束
验收标准
回归范围
完成后产物
```

例如：

```text
P002.3
夏栎叶簇生成器

前置：
P002.1 枝梢结构调整

目标：
将末梢叶片从随机散布升级为枝梢驱动叶簇

涉及：
src/runtime/procedural/tree/...

实现：
...

验收：
1.
2.
3.

禁止：
不修改 AssetSource 契约
不引入外部模型
不改变现有 sourceKey 语义
```

---

# 十三、任务粒度要求

必须控制任务大小。

一个任务原则上：

```text
只解决一个主要问题
只跨少量文件
可以独立测试
可以独立提交
可以独立记录 PROGRESS
```

如果一个任务同时涉及：

```text
Geometry
Shader
Cache
Instancing
UI
```

通常说明拆得不够细，应继续拆分。

---

# 十四、依赖关系

输出任务时必须建立依赖图：

```text
P001
 ↓
P002
 ↓
P003
 ├── P004
 └── P005
      ↓
     P006
```

明确：

```text
可并行任务
串行任务
必须等待验收的任务
```

避免后续 Agent 同时修改同一核心文件产生冲突。

---

# 十五、不要制造无意义的架构任务

禁止为了体现“专业”而生成：

```text
重构整个 Asset 系统
重写 ProceduralSourceCache
重新设计 InstancedAssetPool
创建新的植物框架
引入新的第三方库
升级 Three.js
```

除非源码分析证明确实需要。

基本原则：

> **最小改动，优先复用已有架构。**

---

# 十六、输出结果

最终输出必须分为四部分。

## A. 现状判断

先说明：

```text
已经完成
部分完成
存在缺口
无需修改
```

不要把已完成内容重新列成开发任务。

## B. 总任务树

例如：

```text
P0 夏栎收尾
 ├─ P0.1 Shadow
 ├─ P0.2 aSeed
 └─ P0.3 回归

P1 夏栎真实性
 ├─ P1.1 枝条结构
 ├─ P1.2 叶簇
 ├─ P1.3 冠层
 └─ P1.4 树皮

P2 夏栎资产化
 ...

P3 植物模板
 ...

P4 第一批植物
 ...
```

## C. 可直接执行的 TASKS.md 内容

每项任务按照统一格式输出。

## D. PROGRESS / DECISIONS 初始化建议

指出：

```text
哪些结论写入 DECISIONS.md
哪些内容初始化到 PROGRESS.md
哪些内容放 TASKS.md
```

不要修改文件，先输出规划结果。

---

# 十七、非常重要：任务拆完后不要直接编码

当前阶段你的任务只有：

> **分析仓库 → 对照总需求 → 形成任务树 → 明确依赖 → 明确验收 → 生成 TASKS**

禁止直接修改源码。

等任务确认后，再按照 TASKS 中的顺序执行。

---

# 十八、最终判断标准

任务拆解完成后，我应该可以直接把结果交给另一个 Coding Agent，让它只读取：

```text
AGENTS.md
TASKS.md
PROGRESS.md
DECISIONS.md
```

就能够知道：

```text
现在做到哪里
下一步做什么
为什么做
改哪些文件
不能改什么
完成后如何验证
完成后在哪里记录
```

最终要求：

> **任务之间具有明确依赖；每个任务可以独立执行；每个任务可以明确验收；整体能够从夏栎平滑演进到完整植物资产库。**

现在开始执行任务拆解，但不要修改任何源码。
