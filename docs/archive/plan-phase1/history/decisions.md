# 决策日志（全文归档 · append-only · 仅主代理写入）

> 2026-09-11 自 STATUS.md 拆离（按需读取重构）；新决策条目直接追加本表末尾，STATUS 只留最近一条摘要指针。


| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-09-11 | **阶段 6 立项「形状驱动创建与插件化样式引擎」+ 阶段门过门（用户逐题拍板 grilling 9 问 + Q3b/Q3c 复核，本轮会话）**：①核心模型 = 两套创建模式——绘制（纯几何形状→未分类区域→赋语义类型→应用样式预设）+ 资产库（现有 GLB 放置零改动）；Shape/Semantic/Style 三层解耦 ②十项裁决：解禁 Shader 样式引擎与水面波纹动画（顶点编辑限定 T6.8 解禁）；先跑 T5.9 拿基线再进阶段 6；**旧建筑挤出体与点位标记锥要素删除**，新系统 building 为与水面平权的普通类型（立体感由预设 build 内按 height 挤出）；语义枚举十类（unclassified/water/grass/plaza/parking/bare_land/road/building/poi/custom）；数据格式与外部 JSON 导入放最后 T6.9 重设计（旧数据转换规则届时按需门）；点绘制仅 POI/标记类（树走资产库不双轨）；快捷键 1 区域（Shift+1..5 子工具）/2 路径/3 点/4 资产、A 保持 45° 锁定、QWER 不变；赋类型自动迁入类型默认图层（并入命令可撤销）+ 四档作用域保留；线框/X-Ray 改场景级 overrideMaterial 全局覆盖 ③技术契约四处修正（用户授权按工程实际重写）：update/setGeometry 拆分、道路带宽几何留 GeometryBuilder（读 semantic.properties.width）、材质模板共享+overrides 对象 clone+引用计数、样式引擎落 src/runtime/styles/ + TypeScript（v1 的 src/styles/+.js 违反 three 白名单） ④CONTRACTS.md 第三次修订：排除项两解禁一限定、新增「阶段 6 目标契约」节、domain/elements 退役注记（T6.6 删除） ⑤**任务重编号**：原 T6.1–T6.8 → T7.1–T7.8、原 T7.1–T7.5 → T8.1–T8.5（文件 git mv + 内容编号/阶段号/前置依赖同步：T7.1 前置改 T6.9、T8.x 前置改 T7.8；src 内 2 处 T6.5 注释同步 T7.5）；新阶段 6 任务书 T6.1–T6.10 定稿（T6.2/T6.3/T6.4/T6.8 派 threejs-expert） ⑥需求源文档《绘制需求变更.md》重写为定稿 v2（根目录，含 v1→v2 变更对照；派发 T6.1 时随任务书嵌入，届时归档 archive/） | 用户提出绘制需求变更（先画形状后赋类型+插件化样式），与现架构「先选类型再绘制」存在方向性偏差；grilling 前沿 9 问全落定，共识经用户复述确认 |
| 2026-09-11 | T5.8 完成裁定（主代理）：①threejs-expert 一次过（commit bdc5034，955→1007，+52；classifyRightButton/四菜单纯模型/toastReducer/ESC 改写用例；守护三件套复跑全绿）②diff review 通过：contextmenu:requested 走注入回调零契约变更（EventMap 属契约，子代理正确避让）、touches={ONE:null,TWO:DOLLY_ROTATE}、右/中键 down+up 一律不转发工具、defaultPrevented 早退（菜单内 Esc 不外溢全局退出手势——三段 Esc 语义目检实证：菜单内只关菜单/全局退出/焦点自动落菜单项）、隐藏/锁定走 UpdateObjectCommand 真实通道（SceneObject.visible/locked 契约既有字段，非占位）③浏览器目检全过：**手势四项以「动态 import store 读 facade.camera.camera/controls 的相机坐标」为 oracle**——右拖旋转（pos 60,50,80→38,57,88.4 且 target 不动）、中拖平移（pos+target 同动）、滚轮缩放（距离 111.9→98.9）、左键大幅拖拽 pos·target 全不变；右拖后零菜单（drag≠tap）；四类菜单全链（空白菜单六类创建子菜单+四机位切换生效+网格 checked+显示设置 disabled；绘制中首项「取消绘制」点击退出；对象菜单八项+隐藏动作 visible 翻转；大纲行菜单隐藏态反向文案「显示」+移动至图层 Water+Ctrl+Z 撤销+删除 danger+重命名；资产卡四项 disabled 矩阵+添加到场景激活放置）；Toast 4 连发挤掉最旧留 3/aria-live=polite/role=status/手动关闭 3→2/TTL 后归零④**主代理发现并修复 1 组真缺陷（df31f9d）**：ContextMenu trigger 先 close() 后 onAction()——close 同步置空 store.contextMenu，App 上下文处理器读 target 恒 null → 重命名聚焦与添加到场景两动作从未生效（move-to-layer 靠 item.arg 幸免）；修复=trigger 改先派发后关闭 + add-to-scene 资产 id 优先取 target.assetId（模型项不带 arg）+ rename 聚焦延迟 0→150ms（面板恢复 commit 重建输入框会夺回焦点）；两动作修复后复验通过⑤两处用户可见文案微修：帮助弹层 Esc 行去内部任务号「T5.8」⑥**遗留观察项**：pickObject 射线对建筑 mesh/实例模型不命中（rendering 正常、pickInRect 框选正常、页面侧 three 模块 patch 观测到射线命中返回 2 hits 但 map.findId/resolvePick 反查全空）——git worktree 起 faf829d 基线以同方法学**完全复现**，判定既有问题非 T5.8 回归（T5.8 diff 零触及拾取链路）；T5.9 以真实输入复核，若复现立 P1 缺陷单（影响：视口右键对象菜单/左键点选在射线修复前依赖框选与大纲行）⑦方法学第八次增补：合成 PointerEvent 需复用真实鼠标 pointerId=1（three r186 OrbitControls onPointerDown 无 try/catch 调 setPointerCapture，未知 id 抛 NotFoundError 中断监听器挂载）；**IAB 空闲冻结下 cua.move 已不可靠唤醒（旋转量积压至后续事件才结算），手势类断言改用 cua.scroll 唤醒（落左面板避开画布缩放）+ store 相机坐标读数，弃截图字节对比**（冻结期截图逐字节相同会造成「未旋转」假阴性，本次一度误判右拖失效）；InputController 的 pointerup 监听在 canvas（down/up 都必须派发到 canvas，up 落 document 不达）；App 拖放监听为 canvas 原生 addEventListener（drop 探测必须派发 canvas 而非 .ed-viewport 父容器） | T5.8 done（bdc5034 + df31f9d）；下一任务 T5.9 主代理 GUI 验收 + 全量回归（含 pickObject 观察项复核） |
| 2026-09-11 | T5.7 完成裁定（主代理）：①threejs-expert 一次过（commit 9991894，905→955，+50 测试；renderLoopStats/RenderModeState/AxesIndicator/hudModel/ViewportHUD/bootstrap 超集/机位共存回归；守护三件套主代理复跑全绿）②diff review 通过：renderMode 走环境通道（scene 层 coerceRenderMode 单一真相源；恢复 shaded=要素材质 MaterialFactory 重算+实例模板按快照全量还原防脏模板泄漏）、info 口径=autoReset 缺省开帧间快照（轴指示器独立 renderer 零污染主 info）、store.cameraMode 记账（沿 gizmoMode 先例）补齐 CameraPort 无事件通道的 checked 同源缺口、ViewportSessionServices 最小结构类型避免 ui→app 导入③浏览器目检全过（HUD 四角 pointer-events 策略/三芯片联动/三渲染模式像素确认+菜单同源/机位不抢工具/轴指示器姿态联动+开关往返/游标读数含 null 边界/1Hz 指标）④两处产出超清单偏差采纳：RenderModeState+scene 归一函数（node 可测必要拆分）、旧 ViewportHud 工具芯片退役（对象计数归状态栏、工具提示由垂直条/ContextToolbar 承担——信息归并）⑤CONTRACTS 逐键注记建议不采纳（env.grid 先例本就不逐键注记，索引签名即约束）⑥「Triangles 10→28 增长」洗清为冻结帧陈旧轮询值（10 读数全伴 FPS 0；有效读数恒 28=4 空场景+24 单建筑挤出面），方法学第七次增补：1Hz 轮询类断言以 FPS≥20 为读数有效性门槛 | T5.7 done；下一任务 T5.8 派 threejs-expert（手势迁移/右键菜单/Toast——OrbitControls 手势配置 + 右键语义变更） |
| 2026-09-11 | T5.6 完成裁定（主代理，续接会话）：①收尾五项目检全过——绿地芯片切换徽标「建造」→「地形」+垂直条按压位重入建筑→绿地（drawTarget 重入工具保持）；退出双路径（退出钮/再点同项）均回「场景」+中段消失+QWER 自动回选择；tooltip hover 250ms 延迟出现+三要素内容+右贴+aria-describedby+pointerout 即隐；Scene▼ 弹层八模式仅「场景」可选带勾选（像素确认对比度差异）+禁用项点击零切换；收尾截图 3 张入 screenshots/phase5/（首建目录，T5.9 续存）②两处自动化伪影均洗清为非缺陷：tooltip opacity 0 初读=IAB 冻结帧 CSS animation 停滞（滚轮唤醒后 opacity 1+裁剪近景像素确认浮层渲染）；全屏截图偶发陈旧帧（DOM present 但像素缺浮层）→ 以「同调用 DOM 读数+裁剪近景」双证裁决③方法学第六次增补：合成 pointerover 悬停会被后续真实 cua.move 重置（React enter/leave 链重算触发 hide）——悬停断言期间禁真实鼠标事件、唤醒用 cua.scroll（wheel 不触发 pointerover；scroll 桥回复可能挂起超时但页面侧已执行）④键位 Q/1–6 键盘路径不可自动测维持既有裁定，单元级 input.shortcuts.test.ts 锁定 | T5.6 done；下一任务 T5.7 派 threejs-expert（含 runtime 变更：渲染模式 env 通道 + 轴指示器 + getViewportStats 超集） |
| 2026-09-11 | T5.6 中断留档（主代理，会话断开前）：①子代理一次过（toolIA/VerticalToolbar/ContextToolbar/Tooltip/自绘六图标/25 toolIA 测试，commit db710d0，879→905，守护三件套主代理复跑全绿 + build 冒烟；lucide-react@1.44.0 随 commit 入库——首派发被取消但依赖已装，重派简报注明核实即用零重装）②**G 键矛盾裁定（任务书内部冲突）**：contextTools 段「吸附 G」与快捷键段「G→网格显示」矛盾，现状 G=吸附全线一致（DrawToolBase 会话级+菜单 tool.snap+帮助表+README），裁定 G 保持吸附、网格 toggle 不配快捷键点击路由 view.grid——契约/既有行为优先于任务书笔误，机械裁定级 ③diff review 通过：toolIA 键表单一真相源（input.ts/菜单/帮助三处同源）、W/E/R 经共享入口写 store.gizmoMode（键盘点击同路）、placement 工具内所有权保留、注册制（Context 中段要素切换器由 ElementRegistry 驱动）④目检已过项与剩余 5 项（绿地切换徽标变化/退出路径/tooltip hover/Scene▼ 灰显/收尾截图）已留档 tasks/T5.6.md 末尾注记；键盘 Q/1–6 不可自动测（IAB 键盘不达页面既有方法学），单元级锁定判非缺陷 ⑤子代理偏差知悉：toolIA 含薄激活入口节（非纯，键盘点击同路的最小实现）、README 测试计数顺手修正 701→905 | T5.6 in_progress：新会话续 5 项收尾目检 → 标 done → 派 T5.7（threejs-expert） |
| 2026-09-10 | T5.5 完成裁定（主代理）：①子代理一次过（ContentBrowser/browserModel/modelFactory/38 测试，commit 2f87463，852→879，守护三件套主代理复跑全绿；子代理环境 browser-use 不可用，自行以 headless Chrome + 原生 CDP 完成像素自验后删临时脚本——合规兜底）②派发简报含两处任务书修正均落实：**工厂分层勘误**——任务书「app 层工厂被 PlacementTool 调用」与 DAG（editor 禁 import app）矛盾，CONTRACTS 优先裁定工厂落 editor 层 `src/editor/factories/modelFactory.ts`（测试随迁 tests/editor/factories/，机械勘误级非按需门）；**drop 坐标勘误**——任务书「clientXY − boundingRect」错误，RuntimeViewport.toNDC 内部自减 rect，clientXY 原样直传 ③EditorHandle 增补 groundPoint(x,y) 组合根超集（沿 T2.4 getGrid/setEnvironment 先例，主代理预授权）④浏览器目检全过：紧凑 60px（芯片行+搜索+展开钮）/点「建筑」芯片=选分类并展开 280px（过渡停摆伪影经 body.ed-dragging 禁过渡实证 280px 可达，判非缺陷）/左分类纵栏+右卡片网格/合成 DragEvent 拖放全链（dragover 琥珀描边高亮→drop→对象落地→自动选中→Inspector 从环境标签自动切回对象属性显示「行人」）/无效落点（视口上半部地平线之上）notice「无效放置位置」+树计数不变零命令/拖放创建经一次 Ctrl+Z 撤销恢复/点击放置回归（卡片主按钮→放置模式 HUD「放置·路灯」→点击落地→连续放置保持）⑤目检疑点两处均洗清：三次拖放「失败」实为落点在地平线之上（groundPoint null 正确拒绝+notice 已显示，早前未读到是 notice TTL 与读取时序问题）；展开态高度 60px 读取为 IAB grid 过渡停摆伪影 ⑥DESIGN.md §5.8 补记主代理确认落文件（1130c15；零新令牌；categoryMarkColor 数据编码色不违反琥珀唯一性；.ed-lib__* 退役登记） | T5.5 done（2f87463 + 1130c15）；下一任务 T5.6（无门可直接派发；lucide-react 引入锁定最新稳定版） |
| 2026-09-10 | T5.4 完成裁定（主代理）：①子代理一次过（inspectorModel/InspectorPanel/17 测试，commit 3b862a2，831→852，守护三件套主代理复跑全绿）②diff review 通过：分组模型纯函数规范（结构判别零 type 字面量、properties 通用迭代注册制友好、派生值按几何形状产出、多选汇总稳定首现序）；多选只读经 facade={null} 杜绝提交路径（防 P0 误改首对象）；标签态/折叠态归 workspaceStore（inspectorTab 新字段 + resetLayout 复位）③浏览器目检 14 项全过：三标签结构、空态纯净（⌖+引导无设置混入）、模型分组（变换/模型参数[资产引用]/元数据/高级）、建筑分组（含外观组：预设/作用域/参数表单/可见/投射阴影占位 + 建筑参数：高度/底部高度/楼层/面积派生只读 625m²）、高度改值经 focusout 提交→格式化重读 21.00→撤销恢复 10.00 且建筑保留、多选只读汇总（2 对象类型分布 + 9 只读值 + 0 可编辑）、重命名三方同步（检查器/大纲/门面）、图层下拉迁移→未分层→撤销恢复、外观组折叠跨标签切换保持、环境预设切夜景（截图像素确认深蓝夜景渲染）、全局设置结构（网格/单位/快捷键跳帮助弹层/两组 disabled 占位）、「文件→场景设置」action 定位（右面板隐藏时恢复+切全局设置标签）、网格开关关闭（截图确认网格消失）、标签跨选变化保持 ④方法学沿用：IAB 键盘 Enter 不达 React（第一次高度改值实为草稿未提交，undo 掉的是建筑创建——与 T5.3 第五次方法学一致判非缺陷），改用合成 focusout（FocusEvent bubbles）走真实 blur→onBlur→commit 链路验证通过；同 sync evaluate 内点击后读 DOM 必旧值（T4.1 先例）拆步执行 ⑤主代理接线收编 1 处（b1f97ac，沿 T5.2 主代理小接线先例）：菜单栏「设置」disabled 占位按钮解禁→onAction('file.scene-settings')（T5.4 已建 action 通道，2 行 UI 接线非渲染层），「布局」按钮维持 disabled（Docking 归 T6.2）；HMR 后浏览器复验定位生效 ⑥DESIGN.md §5.7 补记主代理确认（零新令牌；`--environment/--grid` 尺寸类随收编退役） | T5.4 done（3b862a2 + b1f97ac）；下一任务 T5.5（无门可直接派发） |
| 2026-09-10 | T5.3 完成裁定（主代理）：①子代理一次过（TabStrip/outlinerModel/OutlinerPanel/16 测试，commit 44ea18d，815→831，守护三件套主代理复跑全绿）②主代理 diff review 通过：树模型纯函数规范（组序对齐 DEFAULT_LAYER_NAME_BY_TYPE、表外类型兜底未分层、展开态折叠集合语义、组批 plan+命令构造）；DrawPanel activateDraw 复用零复制；IconToggle 增 stopPropagation 顺手修复旧场景树点 eye 冒泡触发行选中的 T2.4 遗留隐患 ③浏览器目检全过：双标签切换、类型分组树（六组序+计数、表外类型归未分层实测 fence）、搜索（命中行保留+无匹配空态文案）、类型筛选（两维组合正确）、展开/收起/单组展开、双击/F2 重命名（树/门面/属性面板三方同步 + Esc 取消不提交实测）、组级 eye（双建筑全隐他组不动）、单次撤销全恢复（BatchCommand 合并实证）、单选/Ctrl 加选/Shift 范围/组行全选（选中态三方一致）、创建下拉（六要素入口→draw-polygon 激活→菜单自关）、图层标签（8 层计数、新建图层 9、非空删除两步确认含「N 个对象将移至未分层」契约文案、取消无损、空图层真实删除）④方法学增补（第五次）：本 IAB 环境自动化键盘/真实点击不达页面（cua.keypress 零到达、Playwright click 冻结超时）、原生 blur() 不产生 focusout（capture 监听实证）——Enter 提交/自然 blur 提交两条路径不可自动测，但提交链路本体（focusout→onBlur→commit→UpdateObjectCommand）已验证且代码与 T3.4 验收过的 NumberField 完全同模式，判非缺陷，T5.9 全量回归复核；另「取消仍提交草稿」疑点查实为双重派发伪影（Escape 后再手动 focusout 造成的真实用户不可能序列，单派发复判通过）⑤视觉判读两处误报均洗清：树仅 3 组=视口截断（DOM 六组俱全+243px 滚动余量）；属性面板「与选中不同步」=截图单元格内标签切换与行点击同任务致点击未派发（自动化时序伪影），干净复验三方同步通过；DESIGN.md §5.6 补记主代理确认（零新令牌；ed-menu__popup 升格通用下拉原语） | T5.3 done（44ea18d）；TabStrip 组件与 outlinerModel 就位，T5.4 Inspector 标签化直接复用；下一任务 T5.4（无门可直接派发） |
| 2026-09-10 | T5.2 完成裁定（主代理）：①原派发 15:54 被使用限额中断（20:05 重置），续会核查发现实现已基本完成（未提交）——主代理亲自 diff review（全产出符合任务书与派发简报裁定：七菜单清单/enabled 矩阵/保存四态 reducer/action 路由依赖注入/input.ts 行为等价抽取/DrawGridConfig 增 snapEnabled 开关位且三绘制工具消费「菜单开关 ∧ G 键会话开关」）+ 守护三件套复跑全绿（815=750+65）②浏览器目检发现 1 真缺陷并由主代理修复：视角 checked 滞后——上下文条 ViewButtons 直连相机 Port 改机位无事件通道，App 的 cameraMode 不刷新；修复 = MenuBar 增可选 `onMenuOpen` 回调 + App 提取 `refreshSessionState` 于菜单展开/动作派发时重读机位与吸附开关（三路径复验全过）；主代理亲自修复的理由 = 限额窗口未重置无法派修复子代理 + 10 行纯接线非渲染层（AGENTS.md 禁的是渲染层代码），此处留痕 ③目检全过：键盘全链（↓展开聚焦/↑↓循环/←→切菜单/Esc 关闭+焦点回归/hover 经 mouseover 派生切换）、禁用项灰显+tooltip、保存四态（放置→修改未保存、保存→已保存、createObjectURL 注抛→保存失败+错误提示条不静默、重存恢复）、全选/复制/粘贴/删除真实生效（Models 1→2→1、删除自动清选中）、新建场景复位+提示条、帮助三件（快捷键 14 行弹层/操作说明 HEAD 探测可达启用/关于+Esc）、beforeunload dirty 拦截（reload 后状态原样保留实证）、视图 checked 全矩阵、面板隐藏恢复、吸附开关真实生效 ④两处疑点均洗清为 IAB 帧冻结伪影：删除后 +700ms 读「已保存」（冻结期 500ms 节流计时器不走，唤醒后正确显示修改未保存）；左面板隐藏「未生效」（grid-template-columns 的 dur-2 过渡在冻结主线程停滞于起始值——加 body.ed-dragging 禁过渡后即时收起 48/0/912 + elementFromPoint=canvas 实证非缺陷）⑤方法学增补（第四次）：连续 rAF 渲染页面在 IAB 空闲时整体冻结——含 await 的长 evaluate 会 32s 超时且冻结前动作其实已执行；对策 = 单步 evaluate + Node 侧等待 + cua.move 唤醒后读数；zone 显隐断言须禁过渡或唤醒后即时读 ⑥DESIGN.md §5.5 补记主代理确认（视觉令牌零新增；ed-pulse 保存中呼吸动画为状态指示例外，T5.8 Toast 体系复核）；偏差知悉：edit.cut 路由已实现但菜单禁用占位（P1 解禁即用）、ModeSwitch/ViewButtons 临时迁入上下文条（T5.6/T5.7 收编）、snapEnabled 会话级偏好不随场景文件持久化 | T5.2 done（424ce66）；action id 体系（collectActionIds 契约快照测试锁定）与 EditorActionsCore 共享剪贴板就位，T5.6 快捷键/T5.8 右键菜单直接复用；下一任务 T5.3 |
| 2026-09-10 | T5.1 完成裁定（主代理）：①子代理一次过（workspaceStore/Splitter/PanelFrame/App Grid 重排/7 令牌+DESIGN.md §4·§5.4 登记/12 测试，commit 97a1b01，738→750，守护三件套主代理复跑全绿 + build 冒烟）②两处实现细节知悉：隐藏恢复入口=上下文条三 ZoneToggle（任务书未指定恢复方式，占位条内最小扩充，T5.6 可重定位）；viewportShare 保留 viewH 形参但当前只算宽度占比（任务书两断言口径均宽度，注释已标预留）③浏览器目检全过：七段骨架 DOM 实测与令牌逐位一致（视口占比 1292/1920=67.3%）、拖宽钳制上下限实测（180/320/420）、隐藏左列画布精确回收 260px 无空白（边缘 elementFromPoint 命中即 CANVAS）、折叠 29px 标题条内容挂起他组不受影响、**隐藏面板后偏移位单击选中建筑**（画布自适应+射线映射无错位实证）+ 聚焦近景确认琥珀选中线框完整（远距+JPEG 下视觉判读曾两度误报「无高亮/右面板裁切」，均经局部放大或 DOM 实测洗清）④方法学增补（第三次）：IAB 快速连点触发帧暂停丢点（连击序列需逐点 cua.move 唤醒+状态栏验证）；cua.keypress ESC 在 IAB 桥挂起致调用取消（退出工具改 DOM 点「选择」按钮等价达成）；全屏 PNG 字节 diff 被场景逐帧微变淹没（99% 字节差异）不可作 oracle，近景局部证据为准 | T5.1 验收零缺陷零修复循环；UI 重构 P0 骨架落地，T5.2 起按槽位收编 |
| 2026-09-10 | 调试遗留批次收编（用户裁定「提交」）：上一会话渲染质量改动（深度精度 near/far 1/10000、网格距离渐隐 shader、双视场角聚焦、层距放大、5m 网格三处同源、PCF 告警消除）+ placement.flow 集成回归整体提交（843fdd9，730→738 测试，守护三件套全绿复跑）；.zcode/ 与 AGENTS.md 维持本地不入库 | 主代理 diff 审阅确认全部为实质修复而非调试残留（注释完整、测试配套）；工作树复位干净，T5.1 派发无障碍 |
| 2026-09-10 | **UI 重构计划立项 + T5.1 阶段门过门（用户「全按推荐」7 问）**：①计划覆盖 = 阶段 5（P0）全量 9 任务书 + 阶段 6（P1）中粒度 8 份 + 阶段 7（P2）粗粒度 5 份，T6.1/T7.1 阶段门前定稿各自任务书 ②本轮 grilling 兼作 T5.1 阶段门（省一轮重复拷问）③**设计语言沿用「夜间制图台」**，需求文档 36.2 建议色板（#1E1E1E+蓝选中）不采纳——按用户要求跑 ui-ux-pro-max 设计系统检索，其对桌面专业工具返回通用 Web 方案（Inter+紫罗兰），照搬违反 DESIGN.md 红线；ui-ux-pro-max 价值取 UX 检查项层（SVG 图标/cursor-pointer/hover·focus/150–300ms/触达尺寸），已写入各任务书验收 ④**视口手势按需求 13.1 迁移**（UE/Unity 惯例：左键纯选择、右拖旋转、中键平移、右点=上下文菜单、ESC=唯一退出手势；现状 OrbitControls 默认+右键取消工具废止；README 同步迁移说明）⑤保存语义 = dirty 位 + 下载制（快照对比判定，openScene 计数法会误报），「保存中」瞬态占位，beforeunload 脏守卫；不做 localStorage 场景自动保存，布局持久化归 T6.3 ⑥**CONTRACTS 排除项维持**：Measure/Annotation/Analysis 模式禁用占位，解禁需按需门 ⑦图标 = 引入 lucide-react（最新稳定版锁定）+ 园区特有图标自绘 SVG 登记 DESIGN.md ⑧机械裁定三项：状态栏数据通道零契约变更（EditorHandle 超集 getViewportStats + ui 节流 groundPoint + 渲染模式走 env 通道，沿 getGrid/setEnvironment 先例）；现有功能零丢失映射表（场景树→Outliner 标签1、图层→标签2、资产库→Content Browser、绘制面板→垂直条+Context Toolbar、网格→全局设置、环境→环境设置、视图按钮→HUD、保存打开导入→主菜单）；ui升级.md 归档 archive/ ⑨派发附加规则：含 runtime/three 变更任务（T5.7/T5.8/T6.7/T7.4）派 threejs-expert（AGENTS.md 约定），纯 UI 任务派 general-purpose+frontend-design 技能 ⑩垂直条键位 = 数字 1–6（需求 13.2 未覆盖，Blender 惯例裁定）；Tab 键 P0 不占用（纯三维模式 P1）⑪资产拖拽放置提前入 P0 T5.5（验收 43.3 要求拖入流程，P1「资产拖拽」项理解为增强）⑫测试基线实况：工作树存在上一会话调试遗留未提交改动（runtime 微调 + placement.flow.test.ts），738 测试全绿（730+7 新增回归）；非计划产物，派发 T5.1 前须裁定（提交/丢弃） | 阶段门 grilling 7 问用户全按推荐确认；计划文件已建（tasks/T5.1–T7.5 共 22 份）；PROTOCOL 已同步（阶段门 ×7、UI 规则扩展、里程碑验收扩容） |
| 2026-09-10 | T4.1 完成裁定（主代理）：①子代理一次过（B1 domain 单一真相源归层 / B2 组合根订阅清选中 / B3 getMode 机位恢复，commit 9ab2818，+29 测试=730，守护三件套主代理复跑全绿）②A2 资产入库引发的基线测试漂移（phase1.acceptance 硬编码 8 资产）由子代理单独提交修复（cd9458f，改随 manifest 同源）——主代理 A 组操作的连带影响，处置正确 ③生产 preview 浏览器复验 B1/B2/B3 全过：绘制建筑落 Building 层（保存 JSON 实证）、Delete 删除后属性面板即时回「未选中」、front 进绘制 ESC 退出后 camera.getMode()='front' ④**「ESC 退出无效 / store 冻结」疑点查实为自动化方法学缺陷非应用缺陷（第二次）**——React 18+ setState 异步批处理，同一同步 evaluate 内 click 后立即读 DOM 必读旧值；IAB 帧暂停进一步延迟 flush（跨帧等待或 cua.move 唤醒后 UI 均已更新；store 真实数据经动态 import('/src/ui/store.ts') 验证始终最新）；曾因此一度误判「T4.1 引入回归」，git checkout 基线二分后确认旧代码同样"冻结"→ 假象；方法学经验=交互后断言必须跨帧（evaluate 分离 + 唤醒） | 三观察项收编闭环；疑点证据链（store 动态 import 读数 + 基线二分 + 跨帧复验）完整归档 |
| 2026-09-10 | 交付收尾（A 组）与 T4.1 前置裁定（主代理）：①A1 用户确认补打 tag **v1.0.0**（aa860f2，覆盖 2026-09-09「完工不打 tag」原裁定——用户点头后才补）②A2 真实资产入库：用户桌面熊猫文件夹 树1/树2（各约 1MB 低多边形树 GLB）复制至 assets/models/plant/，npm run assets:scan 重生成 manifest（10 模型；中文 id/缩略图/URL 全链路可用），生产 preview 冒烟通过（3 实例 InstancedMesh + 连续段一条历史 + 视觉确认），commit 5baa400 ③**A3 发现并修复真缺陷**：生产构建 dist 缺 assets/（manifest fetch 命中 SPA fallback 返回 HTML → 清单加载失败；此前 build 只验证过编译通过未运行过）——vite.config.ts 增 closeBundle 拷贝插件，commit bfcdbf2；preview 核心路径（画建筑/放树/保存管线）全过，截图 delivery/ 3 张 ④**B3 按需门用户批准：CameraPort 增补 getMode()**（T3.2 曾驳回同名提案，当时无任务书诉求；本轮体验收编诉求成立）——CONTRACTS.md 已改（CameraPort 注记 + 契约变更规则段第二处增补），语义 = runtime 内部跟踪 mode、启动默认 'perspective'、恢复 setMode(原模式)（预设方位角级，不承诺自由姿态快照）⑤T4.1 方案敲定：B1 归层经 domain 层 DEFAULT_LAYER_NAME_BY_TYPE 单一真相源（bootstrap 导入侧改引同一常量）+ DrawToolBase 完成时解析 layerId（激活时解析有图层中途删除失效风险）；B2 组合根订阅 object:removed 清选中（openScene 既有 selection.clear 不动）；B3 CameraController 增 mode 状态 + DrawToolBase activate 记录 savedMode/deactivate 恢复 ⑥A2 实操新经验：自动化放置点击勿取视口过低位置（射线俯角小 → 落点 z≈-992 极远，一度误判「模型未渲染」；经「定位」聚焦后视觉确认正常——先查属性面板坐标再怀疑渲染） | A 组收尾完成；契约第二处增补留痕；T4.1 任务书已建待派发 |
| 2026-09-10 | T3.4 验收裁定（主代理）：①项 6 点要素缺失=计划层遗漏（提示词要求 DrawPointTool「对应要素放置」但 T1.3 五类要素无 Point 几何），走修复循环第 1 轮补 marker 点位要素（registries+runtime，注册制扩展，commit 30e035f）后复验通过 ②「撤销重做丢属性」疑点查实为**自动化方法学缺陷非应用缺陷**——window 级合成 keydown 到不了 React 根委托、NumberField 的 Enter→blur→commit 链要求输入框先 focus（真实用户必点击聚焦）；单元级同构 5 步栈往返通过 + 带焦点提交（h15/h20/w9 均经保存管线验证）双重洗清；方法学经验=合成键盘事件必须元素级派发且先 focus ③观察项（非缺陷）：绘制要素落「未分层」（任务书未规定自动归层，导入侧有 DEFAULT_LAYER_BY_TYPE 映射）、对象被撤销删除后属性面板残留旧态（松手提交设计的副产品，不影响验收）④验收实操新经验：本环境 CUA 真实 click 不达页面（move 正常），画布交互全部改用元素级合成 PointerEvent（InputController 为普通 addEventListener，兼容）；保存/打开经 URL.createObjectURL 钩子 + DataTransfer 注入走真实序列化管线 | 修复循环第 1 轮闭环；疑点证据链（保存 JSON 本体 + 全新选择读数 + 单元复现 + 带焦点复验）完整归档 |
| 2026-09-09 | T3.2 契约裁定（主代理，沿 T2.4 先例）：①draw:status 事件按预授权落地 CONTRACTS.md（EventMap + DrawStatusPayload，payload=子代理提案：length/area/cursor/error 全可选、完成发空载荷复位）②CameraPort.getMode() 提案**驳回**——退出绘制恢复 perspective 中性机位可接受，T3.3 ViewButtons 提供显式切换，无任务书诉求不开契约口子 ③网格可见性/步长**走环境通道零契约变更**——SceneEnvironment 索引签名收 grid 扩展键（如 env.grid={visible,spacing}），Renderer.applyEnvironment 消费（runtime 内部细节），UI 经既有 EditorHandle.setEnvironment 生效且随场景文件持久化；绘制吸附步长经 editor 层共享配置（bootstrap 构造注入），必要时 EditorHandle 增组合根超集方法（T2.4 先例）——T3.3 派发提示词已带此指引 ④点工具连击去重（双击两次 down 落一枚）与落点管线次序（投影→Shift/A 锁定→G 吸附）为实现层设计，测试锁定 | 唯一预授权变更点按期落地；其余两提案以零变更路径化解，均非需用户拍板的语义变更（网格开关/机位还原不影响验收清单） |
| 2026-09-09 | T3.1 阶段门过门（用户「全按推荐」）：①T3.1→T3.2→T3.3 连续派发不逐个拷问，按需门兜底，T3.4 主代理 GUI 验收 ②点工具连续绘制历史粒度 = 同要素类型连续段合并一条（沿 T2.2 mergeBatch 会话批次原地增长先例，切换要素类型/退出工具另起段；线/面每次完成各一条）——T3.2 派发提示词补注 ③T3.2 整任务派发，限额中断按续作协议兜底 ④T3.4 口径：混合回归=阶段一验收流程完整重跑；自动化交互坑沿 T2.5 实操经验；含 README 更新；完工不打 tag ⑤技术风险四项告知无异议（CanvasTexture 文字 sprite / centerline 端头圆角非目标 / 道路 width=6、网格吸附默认开 1m / DrawSession 零接触 SceneManager） | 阶段门 grilling 5 问用户确认；Q2 为任务书唯一实质语义空隙，已裁定 |
| 2026-09-09 | T2.5 验收语义裁定（主代理）：①mergeBatch 批次=每连续放置段（同资产连续点击）一条历史，切换资产另起一段；「一次 Ctrl+Z 全撤」按段达成（T2.5 清单字面按跨段合并理解会得到无界撤销粒度，判定实现语义更优）②FPS 验收以主线程事件循环延迟（median 1ms/p95 2ms@500 树框选拖拽流）+ T2.3 结构单测 + 交互响应替代（自动化宿主暂停帧呈现，rAF 不可测）③图层透明度滑杆 onChange 只进草稿、onPointerUp/onKeyUp 才发 UpdateLayerCommand（松手提交，避免逐帧命令）——验收中两度疑为缺陷均查实为该设计 | 验收疑点全部闭环且无修复循环；环境限制如实记录 |
| 2026-09-09 | T2.4 契约裁定（主代理，沿 T1.3–T1.5 勘误先例，全部零行为变化）：①RectPickPort（pickInRect 框选）保持 editor/services/ports.ts 实现层可选接口，不并入契约 ViewportPort（阶段 3 无跨任务消费诉求，T2.5 实证后再议）②ToolContext.gizmo 确认无需增补（TransformTool 构造注入 GizmoPort 够用，T1.6 观察项关闭）③EditorHandle={camera,getEnvironment,setEnvironment} 定位为组合根门面超集，UI 边界精神不变（仍不触 runtime/io），EditorFacade 契约不动 ④UpdateObjectCommand/UpdateLayerCommand 补录进 CONTRACTS 基础命令注记（机械勘误） | 子代理报告提出的四点均非需用户拍板的语义变更；框选/环境/图层操作由 T2.5 GUI 验收实证 |
| 2026-09-09 | T2.1 阶段门过门（用户「全按推荐」）：①T2.2–T2.4 过门后连续派发不逐个拷问，按需门兜底，T2.5 主代理验收 ②ModelAsset.file/thumbnail 语义 = 「相对 assets 根」路径，组合根 bootstrap 统一解析为加载 URL（CONTRACTS 勘误注记已加，化解 T1.8 观察项）③T2.5 帧率门槛 = DevTools FPS ≥30 且拖拽/滚轮/框选无肉眼卡顿，否则走修复循环 ④T2.4 按任务书整任务派发，2 轮修复失败再按需门议拆分 ⑤T2.1 收藏真相源 = localStorage（UI 唯一驱动），manifest.metadata 收藏字段仅扫描时尽力保留 | 阶段门 grilling 5 问用户确认；观察项与任务书内部语义空隙全部落定 |
| 2026-09-09 | T1.9 验收发现「首载残影帧」缺陷 → 1 轮修复闭环（commit 22b4960）：根因是 three r186 对 WebGL 上下文丢失三层静默（render() 空转 + console.log 非 error + rAF 不断链），合成器保留布局未稳期旧帧致拾取与画面错位；修复 = RenderLoop（异常捕获+熔断）/ ViewportResizePolicy（逐帧尺寸自检）/ ContextLossWatchdog（丢失可观测+恢复续渲），StrictMode 双挂载经源码排除无毒化（A 从未渲染、dispose 不碰共享 context）。主代理复验：首载即选中、强制 loseContext 后 1 条报错+恢复续渲+拾取正常 | GUI 验收按修复循环闭环；缺陷证据链与复验步骤已存会话记录 |
| 2026-09-09 | T1.8 裁定（主代理）：①PlacementTool 增可选 layerId 参数（缺省 null 向后兼容，归层随 CreateObjectCommand 落地，不绕过 Command）②EditorFacade 接口落 src/editor/（UI 禁止导入 app，契约节名即 editor）③loadManifest 将 file 解析为可加载 URL——观察项：契约 ModelAsset.file「相对 assets 根」与 runtime 当 URL 直用的语义不一致，T2.1 资产清单任务需留意 ④createEditor 经 opts.eventBus 注入共享总线（UI store 桥接需同一实例） | 均为装配必需的向后兼容扩展且全测试锁定；file 语义张力记录待 T2.1 处理 |
| 2026-09-09 | 🎨 T1.8 设计门（用户选定）：全编辑器美学方向 = **A 暗色专业工作台**——深灰蓝底（#16181d 系）+ 琥珀橙单一强调、中性无衬线 + 等宽数字、高密度紧凑布局（Blender/Houdini 系专业工具血统）；T1.8 据此产出 tokens.css + DESIGN.md，后续 UI 任务复用 | 三候选中用户选 A；与任务书建议方向一致，3D 视口视觉焦点最佳 |
| 2026-09-09 | T1.7 裁定（主代理）：①JsonImporter 构造注入 ElementRegistry（parse 签名不变）②defaults.layerName 记入 element.metadata.layerName 由调用方归层 ③xy/xyz-array 目标几何推导 = geometryTypes[0]（road→LineString），ring-array 恒 Polygon ④宽限语义：NOT_CLOSED/顺时针 → 修复后收录（errors 附注）；自相交/顶点不足/坐标非法 → 排除；不可修复优先 ⑤映射声明字段必填，缺失/类型不符整对象排除 ⑥SceneSerializer 缺 id/name/environment 回退默认值 | 契约未规定的语义空隙，全部已测试锁定；layerName 归层留给有图层上下文的调用方 |
| 2026-09-09 | T1.6 裁定（主代理）：①TransformTool 经构造函数注入 GizmoPort（null 安全桩）；若 T2.4 需运行时动态换绑再议 ToolContext 增补 gizmo? 字段（届时按需门）②ToolManager.cancel() = 工具 cancel() + deactivate + emit tool:changed{null}，input.ts 将右键/ESC 统一路由至此 ③随机语义：rotation.y 与 scale 相对资产默认值采样（仅 Y 轴旋转、均匀系数），每枚重摇、移动期间稳定 ④观察项：type='model' 对象内联构造无注册制工厂，T2.x 序列化/校验钩子需要时再评估 | 均为实现层工程决策且已测试锁定；GizmoPort 换绑与 model 工厂属后续任务范围，先记录避免丢失 |
| 2026-09-09 | T1.5 契约裁定（主代理，沿勘误先例）：①updateObject patch 运行时透传任意键（含 Element 扩展字段 style/geometry），类型约束经结构化收窄——契约已加注 ②命令驱动变更共发 2 次 scene:changed（SceneManager source=方法名 + Command source=命令名，命令级用于归因），订阅方必须幂等 ③undo/redo 成功后同样 emit scene:changed(命令名) ④mergeBatch:false = 子命令逐条入栈（每次 undo 撤销一个），默认 true 合并单条 ⑤ChangeStyle/ChangeGeometry 对非 Element 对象 execute 返回 false | 契约签名不含 Element 扩展字段但契约自身命令（ChangeStyle/ChangeGeometry）必须写入它们；双事件归因与幂等订阅是事件驱动渲染的前提；其余为任务书字面要求的自然延伸并已测试锁定 |
| 2026-09-09 | T1.4 契约裁定（主代理，沿勘误先例）：①Port「runtime 实现」以结构化类型达成（runtime 不写 implements ViewportPort，避免 runtime→editor 导入违 DAG），兼容性由 App.tsx 组合根编译期收口 ②MaterialFactory 内置参数键→材质字段别名表，参数终值优先于 MaterialSpec 基线 ③Renderer 构造签名 (canvas, deps) 注入 eventBus/sceneManager/styles | 任务书「实现在 runtime」与 DAG 兄弟层互禁矛盾，结构化类型保语义零行为变化；别名表保证属性面板调参所见即渲染所得；事件驱动同步必须有依赖注入 |
| 2026-09-09 | T1.3 契约勘误（主代理裁定，沿 T1.2 按需门先例）：ToolRegistry/CommandRegistry 签名改用结构化最小类型 RegisteredTool/RegisteredCommand，editor 完整 Tool/Command 为其结构超集，消费侧自行收窄 | 契约原文引用 editor 层类型与自身 DAG 矛盾（按原文字面无法编译）；化解零行为变化，属机械勘误而非用户决策 |
| 2026-09-09 | T1.2 按需门（契约增补，用户确认）：①属性接口 interface→type 别名 ②SceneManager 增补 addLayer/updateLayer/removeLayer ③ResolvedStyle = { styleId, values, material } 定义补全——CONTRACTS.md 已同步；六条行为语义（validateGeometry 的 errors/autoFixed 语义、环统一逆时针、LineString 查自相交、resolveStyle 类型回退与默认值钳制、updateObject 幂等细节、layer.objectIds 派生索引）按推荐接受为既定语义 | interface 无隐式索引签名与 properties: Record 冲突（TS2430 实测）；图层无写入方法则功能不成立；ResolvedStyle 契约只给签名。六条与契约精神一致且已被 108 测试锁定 |
| 2026-09-09 | 拷问门改为分级制（取代上一条「每任务必检」）：阶段门 ×4（T0.1/T1.1/T2.1/T3.1）+ 设计门 ×1（T1.8 美学方向用户选定）+ 按需门（改契约 / 验收 2 轮修复失败 / 超授权偏差时主动发起）；其余 14 任务直接派发 | 契约锁死的任务逐个拷问只会产生仪式性确认与警报疲劳，门的价值取决于拦下真决策的频率；T1.8 视觉定调是唯一契约锁不住的用户口味决策 |
| 2026-09-09 | 新增「任务前置拷问门」：每个任务（含里程碑验收）派发前，主代理须按 grilling 技能向用户发起一轮确认（范围/开放决策/风险，附推荐答案），用户回复后方可开工；用户可显式授予免检范围并记本日志 | 用户指定；大工程需要逐任务的人在环确认，防止理解偏差直接进入实现 |
| 2026-09-09 | 原始需求文档与各阶段提示词归档至 `docs/plan/archive/`（提示词全文已嵌入任务书，需求文档按节引用）；全部任务书与协议的引用路径已同步更新 | 用户指定；工作区根目录只留代码与计划体系，原始输入冻结只读 |
| 2026-09-09 | 计划体系重构为状态外化结构：PROTOCOL / CONTRACTS / STATUS / tasks/T*.md，旧单文件主计划归档至 `docs/plan/archive/`；单一写者原则（子代理零写计划文件） | 旧协议要求每个子代理全量读 65KB 主计划并共同编辑同一状态表——token 浪费 + 注意力稀释 + 多写者冲突 |
| 2026-09-09 | 所有 UI 任务（T1.8/T2.1/T2.4/T3.3）必须先读 frontend-design 技能；T1.8 奠基设计系统 tokens.css + DESIGN.md，后续复用 | 用户指定；避免通用 AI 审美与跨任务视觉不一致 |
| 2026-09-09 | 依赖一律安装 npm 最新稳定版并锁定（2026-09 快照：TS 7.0.2 / Vite 8.2.2 / Vitest 5.0.0 / React 19.2.8 / three 0.186.0），不兼容时回退最近可用大版本并记录 | 用户指定 |
| 2026-09-09 | 提示词组 3 的 ElementDefinition.createRenderer 移除，渲染器注册移至 runtime 侧 RendererRegistry | 违反「Domain 零渲染」分层规则 |
| 2026-09-09 | 提示词组 6 的 ToolContext 不直接持有 renderer，改为 Port 接口注入（Viewport/Camera/Preview/Gizmo） | 避免 editor→runtime 反向依赖，违反分层 DAG |
| 2026-09-09 | 需求文档阶段一验收流程中的资产放置/Gizmo 内容重新分配：占位资产生成提前到 T1.8，Gizmo 留 T2.4，阶段一变换验收走属性面板数值输入 | 文档验收流程混入阶段二能力 |

| 2026-09-11 | **按需读取重构（用户指示）**：①STATUS 瘦身为快照（当前进度 + 活跃任务表 24 行 + 依赖顺序 + 指针），已完成 28 条任务行、决策日志 38 条、会话日志 14 条拆离至 `history/{tasks-done,decisions,sessions}.md`（append-only，不回改）；②CONTRACTS 拆分为「基础契约（全任务必读）+ 分域契约（`contracts/stage6-region.md` 阶段 6、`contracts/legacy-elements.md` v1 旧要素，任务书头行「分域契约」指定必读）」+ 分域索引节；③新增 `METHODOLOGY.md`——自决策日志蒸馏 17 条 GUI 验收方法学（IAB 冻结唤醒/事件合成/断言时序/视觉判读/归因纪律），里程碑验收前必读；④PROTOCOL 同步（文件拓扑表、派发模板必读项、验收规则）；T6.1–T6.10 头行补分域契约标注 | STATUS/CONTRACTS 随任务数线性膨胀（决策单元格已达数百字），每会话开场与每子任务必读成本过高；2026-09-09 拆单文件主计划后的第二轮同向优化 |
| 2026-09-11 | **stage6 分域契约分层审查修订（用户三问：1 成立 / 2 成立且更严重 / 3 部分成立）**：①StyleInstance 暴露 THREE——成立为契约缺陷：类型栖身 runtime 合法、当前设计也无 editor 消费方，但契约未声明可见域，T6.1 等 editor 任务实现者可能直接引用突破白名单（check:layers 拦 import 但拦不住设计歧义返工）→ stage6-region.md 重构为 §0 分层总则（模块×归属层×可见方表 + 跨层交互唯一通路：editor 只改数据经 scene:changed、runtime 订阅 reconcile；ui 读元数据仅经 facade.registries.presets）+ §A 数据契约（零 THREE）/ §B registries 元数据注册 / §C runtime 内部契约（THREE 唯一合法出现处）；②元数据/构建注册分层——成立且更严重：原设计 preset 元数据随插件全在 runtime，而 ui 禁止导入 runtime，T6.5 快选条 / T6.6 预设网格与参数表单的数据通路整体断裂 → 双轨注册（沿 v1 先例 StyleDefinition@registries + MaterialFactory@runtime）：StylePresetMeta + StylePresetRegistry（registries 纯数据）+ runtime 构建路由（import.meta.glob→presetId→build）；插件同文件导出 meta+build，组合根 bootstrap 收割 meta 注入注册表，facade.registries.presets 暴露；一致性守门测试（缺 build 灰显禁用 / 缺 meta 不暴露）；首版取消按预设动态分包（与 meta 随文件走冲突，升级路径=registries meta 文件 + runtime build 文件成对方案，接口不变）；③GeometryBuilder 归属——部分成立：T6.4 任务书已隐式 runtime（threejs-expert + 全落 runtime + 白名单完成定义），但契约是唯一权威、跨任务层级约束必须显式 → §C 声明落位 runtime，输入 domain 纯数据（参数化点列=T6.1 纯函数）输出 BufferGeometry，挤出归 building 预设不归构建器；T6.2/T6.3/T6.4/T6.5/T6.6 任务书与《绘制需求变更.md》§5/§6 同步修订 | 用户契约审查；修订不改变十项裁决中任何用户决策，纯分层技术澄清，与既有 SceneSync/RuntimeObjectMap 事件调解模式一致 |
| 2026-09-11 | **stage6 契约第二轮审查修订（用户五问：1 成立 / 2 核心不成立但序列化半句成立 / 3 部分成立 / 4 成立 / 5 成立且加剧——新旧 ChangeStyleCommand 同名冲突）**：①过渡期共存规则未定义 → 分域契约新增 §D：type:'region' 与旧要素同 Scene 并存；渲染沿 RendererRegistry 按 type 派发（RegionRenderer 注册 'region'，旧渲染器不动）；工具/选择/框选/图层/右键通用动作按 SceneObject 通用字段两用；面板与大纲按 type 分支（未更新前 region 行落兜底组不报错）；**T6.1 交付序列化过渡壳**（shape/semantic/style 三层整体持久化、往返无损，T6.9 v2 接管替换）；GeometryData 降级为运行时内部表达（预览映射：面类→Polygon 点列/line→LineString/point→Point，DrawPreviewState 不改；校验经适配器复用；JsonImporter 至 T6.9），业务几何唯一标准=RegionShape；v1 ChangeGeometry/ChangeStyle 与新三件套过渡期并存、按对象 type 由调用侧派发、T6.7 随旧要素退役；②RegionObject 与 SceneObject「不兼容」核心不成立：Element 同构先例（geometry/style 顶级扩展字段）+ T1.5 勘误（updateObject patch 运行时透传+结构化收窄）已裁决该扩展模式，无签名冲突；「序列化丢扩展字段」半句成立，由序列化过渡壳补齐；③④ID 体系两处修正：全局约束 7 增补 region_ 前缀；§B 显式声明 presetId 为注册表键命名空间 '<category>.<name>'（点式），与 createId 实例 ID 前缀体系是两个空间；⑤命令同名冲突 → 第三件命令改名 ChangePresetCommand（避开 v1 ChangeStyleCommand；T6.1/T6.5/T6.6 任务书与《绘制需求变更.md》§七.4 同步），基础契约基础命令注记加退役对照 | 用户契约审查第二轮；共存与退役语义沿既有注册制派发与 T1.5 勘误先例，无新模式 |
| 2026-09-11 | **stage6 第三轮审计整改落地（独立审计子代理报告 P0=0 / P1=8 / P2=9，结论「整改后可启动」；用户裁定全部按主代理处置意见整改，含两项显式裁决）**。用户裁决原文：①「批准 EventMap 两处增补（app:notify + DrawStatusPayload.vertexCount）」；②「四档样式作用域第四档沿用 v1 现状语义（按 presetId 过滤），标签改『同预设』」。整改落地清单：**A 基础契约**（契约变更规则段第五次修订留痕）——EventMap 增 `app:notify`（runtime→UI Toast 通道，现状 runtime 无合法 Toast 路径）+ DrawStatusPayload 增 `vertexCount`（当前形状顶点计数，仅绘制会话）+ editor/services 节 VertexEditPort 指针注记；**B 分域契约 stage6-region**——§C 参数通路定稿（createStyle 将 semantic.properties 以保留键 `semantic` 并入 params 后调 build，签名不变；building 预设读 params.semantic.height 挤出）、§A ChangeSemanticCommand=语义层一切变更（类型切换=自动归层+presetId 置新类型默认预设+overrides 清空一条历史整体回退；业务参数变更同命令承载）、§A 高度合成（最终 y=shape.baseHeight+transform.position.y；语义注册表含 defaultBaseHeight，数值沿现状 SURFACE_ELEVATION：road 0.06/grass 0.12/water 0.18 其余 0；面板「基准高度」读写 baseHeight、Gizmo/属性面板 Y 读写 position.y）、§D 渲染器措辞校正（现状注册 5 个 building/road/green/water/marker；parking 无独立渲染器本体）、§C road 带宽生成仅 shape.type='line'（polygon+road 平面贴地、width 忽略）、§C 增 VertexEditPort（onDragEnd 回调 before/after 点列，模式沿 GizmoPort 先例：runtime 句柄拖动→Port 回调→editor 转命令）；**C 需求文档《绘制需求变更.md》**——§三业务参数消费分流（road.width→GeometryBuilder 读取、building.height→building 预设 build 内挤出，消除与 5.2 矛盾）+ 高度合成规则句、§5.5 三栏重列对齐代码现状 19 套（迁移 10=water×3+green×3+parking×2+road×2[standard/asphalt]；新增 9=plaza×3+base×3+poi×3 无对照按新设计验收；改造 2=building default+modern；废弃 building glass/tech/night+road tech/night、marker×2 随 T6.7 删；新库合计 21 套）、§二#9 四档作用域语义定稿（当前对象/同类型过滤 semantic.type/当前图层过滤 layerId/同预设过滤 presetId——沿用 v1 现状行为，标签「全部」→「同预设」）、§七.3/§八 同步；**D 任务书**——T6.1（ChangeSemanticCommand 职责扩写+语义注册表 defaultBaseHeight+序列化壳措辞改「三层字段经 SceneSerializer 整体透传往返+反序列化结构校验测试锁定，io 层现状不消费注册制 serialize 勿造无人消费的注册壳」+自动归层遇目标图层被删落「未分层」沿 defaultLayerIdFor 现状）、T6.3（三栏清单逐套列名；视觉回归对照仅迁移 10 套；commit 改 `feat(runtime): migrate 10 presets, add 9 new (plaza/base/poi), rebuild 2 building extrude presets (T6.3)`）、T6.4（三类修改断言拆分：切类型=几何顶点不动/材质随预设重建，改语义参数=几何按参数重建/材质实例不重建走 setGeometry；补 keys=['style'] 时渲染器比对 presetId 前后值分辨切预设 vs 调参数；overrideMaterial 覆盖范围限内容组，Gizmo/绘制预览/网格/环境豁免沿 RenderModeState 现状边界）、T6.5（快选条点击=ChangeSemantic+ChangePreset 经 BatchCommand 合并一条历史；「DrawRegionToolBase」改「扩展现有 DrawToolBase」；顶点读数走 DrawStatusPayload.vertexCount）、T6.6（四档过滤字段映射+第四档标签「同预设」；几何分组基准高度读写 shape.baseHeight 与 Gizmo Y 分工）、T6.7（删除措辞校正 5 个渲染器、parking 无本体勿找不存在对象；含废弃样式定义清理）、T6.8（补 VertexEditPort 跨层通路描述）、T6.10（拖顶点期间 FPS≥同场景基线 80% 且 ≥30 量化口径）；**STATUS** T6.3 行与决策指针同步 | 审计 P1/P2 全部处置；契约-需求-任务书三处口径统一并对齐代码现状（styles.ts 19 套、RendererRegistry 5 渲染器、geometryBuilders SURFACE_ELEVATION、StyleParametersForm 四档过滤、SceneSerializer 整体 stringify），消除「迁移 17/19 套」「旧六渲染器」「DrawRegionToolBase」「第四档=全部」等失实口径；当前任务仍为 T5.9（阶段 5 验收），整改完成择期执行 |
| 2026-09-11 | **T6.1 派发前裁定 + 完成认定（主代理，任务书一处执行的口径裁决）**：①**归层撞键裁决**——任务书要求「DEFAULT_LAYER_NAME_BY_TYPE 扩展十类语义」，经核查不可行：water/road/building/parking 四键同时是旧要素类型与语义类型且目标图层名不同（旧表 water→'Water'，语义 water→'水面'），tests/domain/elements/defaultLayers.test.ts 已锁定旧值，硬扩展必破坏旧要素归层与既有测试（违反任务书自身「旧体系零破坏」验收项）→ 裁定：旧表不动，**语义→默认图层名单一真相源 = 语义注册表 defaultLayerName 字段**（取需求 §四 定稿中文名），旧表与十类语义图层的统一翻转由 T6.7（其任务书本就持有「默认图层调整为十类语义图层；DEFAULT_LAYER_NAME_BY_TYPE 单一真相源」）收口；归层遇图层缺失落「未分层」为任务书明文许可语义（T6.5 验收若需场景中存在语义图层，属 T6.5 范围解决）；②defaultPresetId 取值——任务书仅钉死 unclassified→default_solid，其余九类专属预设 id 随 T6.3 落地，过渡期一律 default_solid（恰为 T6.2 降级兜底基线，语义等价）；③完成认定——commit 95d39bf（+71 测试=1094 全绿），守护三件套主代理复跑全绿，diff review 通过：契约 §A 逐字对齐、ChangeSemanticCommand 类型切换三字段联动（semantic+layerId+style）一条历史、禁区（旧要素/io/ui/runtime/tools）diff 为空、isRegionObject 守卫收窄、快照隔离与幂等重放均有测试锁定 | 撞键冲突为任务书执行层面的工程事实（非契约变更，不触发按需门），主代理依 PROTOCOL 偏差决策授权裁定并在源码（semanticDefinitions.ts 文件头）与任务书执行注记双留痕 |
| 2026-09-11 | **T6.2 派发前契约两处澄清（主代理，分域 §C 与基础契约 EditorFacade；决策日志留痕同步）**：①createStyle 语义属性载体——§C「semantic.properties 以保留键 semantic 并入 params（签名不变）」内部张力：semanticType 为字符串参数无法承载 properties，审计原意（building 预设可读 params.semantic.height）无通路 → 裁定：可选尾参 `semanticProperties?: Record<string, unknown>`（缺省 {}），五参调用形态与既有调用方不变，§C 已修订留痕；②EditorFacade.registries 增补 `presets: StylePresetRegistry`——分域 §B 已明文要求暴露但基础契约 EditorFacade 签名未同步，基础契约已修订留痕（指针注记模式沿 VertexEditPort 先例）。另：EventMap `app:notify` 与 DrawStatusPayload.vertexCount 契约第五次修订已用户批准但实现滞后——app:notify 随 T6.2 落地（降级 Toast 需要），vertexCount 留 T6.5（绘制状态栏需要时） | 两处均为派发时发现的契约文档内部不一致，澄清不改变任何用户裁决语义；实现节奏按消费者任务就近落地 |
| 2026-09-11 | **T6.2 完成裁定（主代理）**：threejs-expert 一次过（commit 41ac98d，+65 测试=1159 全绿），守护三件套复跑全绿，diff review 通过。采纳三项实现侧定稿（均代码级注明+测试锁定）：①**资源归属授权规则**（types.ts）——材质生命周期归引擎（共享模板/独享/写时复制/引用计数统一 disposeStyle 处理，插件 dispose 不得释放材质）、几何归调用方（T6.4 RegionRenderer 创建并拥有，引擎与插件均不释放）、插件 update 实现必须读 instance.material 当前值（引擎可能换绑）、Group 根预设不参与模板共享（文档级规则，引擎不强制，T6.3 插件作者遵守）；②**模板缓存键 = 仅 presetId**（无 params 签名维度——模板恒为默认参数形态，带覆写实例从不入池，不存在分叉需求）；③**updateStyle 增量语义**（未提及参数保持上次生效值，引擎 WeakMap 簿记 lastOverrides）+ 写时复制提升（共享实例改参数先 clone 模板，禁共享材质被单对象改写）。观察项（非缺陷，留 T6.3 简报）：Group 根插件不参与模板共享为文档级规则，引擎 applyMaterialPolicy 不检查根对象类型，T6.3 复杂插件若用 Group 根需自管材质 | 任务书授权范围内实现决策（资源归属为任务书明列待定项），主代理 review 采纳 |
| 2026-09-11 | **T6.3 派发裁决 + 完成裁定（主代理）**：派发简报钉死七项——①预设 id 命名空间 = `<semantic>.<variant>`（water/grass/plaza/parking/road/building/poi 八类语义名 + base 通用；green 组随新语义命名 grass.*；旧 default_solid/default_wireframe/test.shader 三套 id 不改）；②building 挤出路径——build 读 params.semantic.height 自建插件挤出几何（调用方平面轮廓只读不释放）、update 材质参数照常 + height 变化时重建「插件自建挤出几何」（任务书"经 setGeometry 或 rebuild 路径"授权，不触碰授权规则 2 保护的调用方几何）、setGeometry 重存轮廓按当前 height 重挤出、材质分层 v1 不做（单 Mesh 参与 engine 模板共享）；③road×2 supportedShapes=['line']（任务书"线状"逐字执行）——**观察项留给 T6.4/T6.6**：polygon+road 过渡期将落 default_solid 兜底 + Toast，预设可用性届时按需裁定放宽；④预设零时间动画（引擎无实例 tick 通路，静态空间渐变允许）；⑤缩略图 = data-URI SVG 静态色块占位（≤300 字符）；⑥defaultPresetId 随库更新（八类指向专属默认、bare_land 无专属保持 default_solid、unclassified 钉死不动）；⑦视觉对照载体 = 临时 harness 页双跑截图（用完删除），硬门 = 迁移 10 套材质属性逐字段等价单测（新插件经 createStyle 完整引擎路径 vs 旧 MaterialFactory+resolveStyle）。完成认定：threejs-expert 一次过（commit bd908b1，+90 测试=1249 全绿），守护三件套复跑全绿，diff review 通过（24 新文件亲审 8 个关键件：迁移范式/挤出算法/poi 贴图缓存/Shader 双件；像素对照 10 套差异像素全 0，主代理目检 water.standard+road.standard 两张页头"差异像素：0"确认）。**采纳两项实现侧资源模型决策**：①poi.billboard 程序化贴图 = 模块级内容寻址缓存（键=量化 glow 步进 0.1、键空间≤51 即≤3.3MB 有界上界，跨实例复用不随实例释放）——规避引擎模板共享与逐实例 canvas 的归属冲突（模板持有者 dispose 会断共享者的 map），语义对齐 materialPool 模板池；②poi.beam 单位圆柱 + scale.y 供高（update 零几何重建，法线水平向与 uv 渐变均不受 y 缩放影响）。偏差：子代理会话 browser-use 不可用，按 AGENTS.md 兜底回退 Chrome headless CLI 截图（页内自算差异像素数渲染进截图头部，非外部判读） | id 命名与 21 套设计明细为任务书未钉死项（仅 poi 三类有设计指定），主代理派发时统一钉死避免子代理自主漂移；贴图缓存为子代理发现的真实归属冲突（引擎 applyMaterialPolicy 弃用插件新材质并 dispose，若贴图随材质私有无解），主代理 review 采纳 |
| 2026-09-12 | **T6.4 完成裁定（主代理，十一项派发裁决 + 两项交付偏差裁定）**：①**R2 road 预设扩形**——road.standard/road.asphalt supportedShapes 从 ['line'] 扩为 [...SURFACE_SHAPES, 'line']，闭环 T6.3「polygon+road 预设可用性」观察项（契约 §C「polygon 形状 + road 语义走平面贴地渲染」端到端成立；T6.3 派发裁决「road 仅 line」限定的是**带宽生成**语义=几何层，不约束预设形状支撑；matrix/road 测试改锁）；②**R5 RegionRenderer 簿记策略**——wrapper Group 恒为 map 根（RuntimeObjectMap/Gizmo/框选引用稳定），presetId 簿记用**请求值**而非降级后的实例值（非法预设降级场景不重复发 Toast，语义仍满足「切预设 vs 调参数」分辨），overrides 键删除（回默认值）走 dispose+create 路径（T6.2 引擎 updateStyle 增量合并不支持删键，渲染器自记键集检测）；③**R7 渲染模式分遍架构**——单场景 + 相机 layer 掩码（内容恒 0/环境 ENV_LAYER=2/辅助 AUX_LAYER=3）+ 每遍 render + scene.overrideMaterial；环境遍（背景+阴影更新）→ 内容遍（override+背景 null+阴影停更）→ 辅助遍（Gizmo/绘制预览豁免）；**灯光例外 enableAll**（envGroup 整组挪层会连带灯——内容遍需收集灯供 MeshStandardMaterial、阴影 pass object.layers.test(light.layers) 需全层灯不漏投影）；Gizmo 对象挪层后 TransformControls 自身 raycaster 必须 enableAll（r186 getRaycaster 存在）否则手柄拾取失效；④**驳回子代理「building 投影留预设 meta 扩展」**——挤出 Mesh 由 buildingExtrude 预设自建，插件内对自建 Mesh 设 castShadow=true 属插件自身职责（与预设决定自身材质同级），零散落判断零 meta 扩展，定向修复落地；⑤buildingExtrude 增 baseOffset（读源几何 boundingBox.min.y）保证 §A 高度合成对 building 挤出成立；⑥其余裁决：GeometryBuilder 落 src/runtime/geometry/（契约 §0 明示）、line 带宽缺省 DEFAULT_LINE_WIDTH=6（对齐 v1/语义默认）、point 锚点 0.3m 四边形、UV 米尺度、图层联动沿通用路径+layerState 提取扩 Sprite；⑦**已知边界三条**（不修，报告留痕）：Sprite 不受 overrideMaterial 覆盖（three 独立渲染路径，poi.billboard 保持原样）、line/point+面类专属语义时命令层写入形状不兼容默认预设→引擎降级+Toast（按形状选预设归后续任务）、共享模板实例 semantic-only 更新触发写时复制 clone（T6.2 引擎既有行为，材质身份变更但参数语义等价）；⑧**方法学第十八条**：锁屏/深冻下像素取证路径（threejs-devtools-mcp HTTP 变体 + run_js 页内模块图 + ContextLossWatchdog 恢复钩子强制单帧 + 同任务 toDataURL 读回 + 分块传输 + GDI+ 转 PNG） | T6.4 任务书「渲染模式改场景级 overrideMaterial」与「覆盖范围限内容组（网格/环境/Gizmo/预览豁免）」两条要求在 three 单场景下互斥——overrideMaterial 为场景全局属性，唯有分遍渲染（每遍切换掩码与 override）可同时满足；分遍深坑（背景全屏重绘/阴影三遍重算/Sprite 独立路径/TransformControls raycaster 层过滤）逐项对策并测试+像素双锁 |
| 2026-09-12 | **T6.5 完成裁定（主代理，五项实现裁定 + 一项边界修正 + 一项观察项）**：①**七形状激活参数反转**——DrawToolParams 从 { elementType } 反转为 { shape: ShapeType }，parseParams 校验 shape 合法且与本工具 shapeType 一致（误接线 activate 抛错 → ToolManager 复位）。「用哪个工具画哪种要素」的 ElementRegistry 前置约束随旧流程退场，改由 shapeType 抽象承载（AREA_SHAPE_TYPES / SHAPE_TOOL_IDS 单一真相源）；②**回选择工具的组合根钩子**——三步流「完成即回选择」经 setExitToSelect 注入（bootstrap 装配时挂 tools.activate(select)）：工具层不持 ToolManager（契约 ToolContext 无此依赖，依赖倒置不破），应用层策略归组合根；点工具 exitsOnComplete=false 保持连续放置（退出时选中保留，快选条随即可用）；③**数字键 e.code 路由**——Shift+数字组合下 e.key 是标点（!@#$%），键路由必须走物理键 e.code（resolveDigitAction 纯函数，Digit1..4 垂直条 / Shift+Digit1..5 子工具直切）；④**快选条浮出条件 = 单选 + unclassified RegionObject**——已分类对象的完整类型/参数/样式操作归 T6.6 四分组面板，「更多」芯片只跳转检查器；类型芯片点击仅暂存（琥珀描边），样式色样点击才发 BatchCommand[ChangeSemanticCommand（自动归层+默认预设+清 overrides）+ ChangePresetCommand] 一条历史；⑤**vertexCount 契约预授权点落地**（2026-09-11 第五次修订第二增补点，随七形状发出，拖拽类=草稿点数、点工具=1）；⑥**T6.7 边界修正**——旧三工具类（draw-polygon/line/point）就地重构为形状驱动非另存本体，任务书「旧工具本体 T6.7 删」实际删除对象 = 旧要素体系（Element 定义/旧渲染器/DEFAULT_LAYER_NAME_BY_TYPE/custom.ts 旧图标/ChangeGeometry·ChangeStyle 命令/buildDrawGroups 等 Element 驱动纯函数）；⑦**观察项（T6.7）**——十类中文语义图层未建，默认场景 v1 英文八层下赋类型按 defaultLayerName 中文名查不到落「未分层」（分域契约 §D 与 semanticDefinitions 注记明文许可）；中文图层集建立与图层体系统一翻转随 T6.7「十类语义图层」落地 | T6.5 done（c2c11ae，1297→1325）；editor/UI 任务零 runtime 改动，GUI 视觉验收（分裂按钮下拉/快选条观感/七形状手势）按协议归 T6.10 阶段验收重跑 F1 清单；下一任务 T6.6（属性面板四分组重构，general-purpose + frontend-design） |
| 2026-09-12 | **T6.6 完成裁定（主代理，五项实现裁定 + 两项观察项）**：①**大纲 region 分组以兜底形态满足**——DEFAULT_TYPE_LABELS 增 `region: '区域'` 仅作展示名兜底（多选汇总/类型只读行），outliner 分组仍落「未分层」兜底组：契约 §D「大纲分组模型在 T6.5/T6.6 纳入 region 类型」的括号许可（未更新前落兜底组不报错）即其落地形态；正式分组随 T6.7 十类语义图层统一翻转（沿 T6.1 归层撞键裁定「旧表不动」先例，避免 region 组耦合 DEFAULT_LAYER_NAME_BY_TYPE 旧表）。②**圆转换半径取法** = max(w,h)/2、椭圆两半轴 = 宽/2·高/2（任务书只说「包围盒中心+半径」未定取法；代码+测试锁定，改规则单点）。③**退化防御**——空/单点（零尺寸包围盒）转参数化形状时点列原样、不造 options（不虚构几何，数据真相底线）；point 目标取首点、空点列取包围盒中心。④**预设点选与参数变更同走四档作用域**（沿 v1 AppearanceBody 行为语义：同一份 RegionStyle 应用到全部目标；scope=object 即单条命令）；批量语义=各自原 overrides 被替换（v1 现状行为）。⑤**表现样式组内序** 网格→参数→作用域（任务书条目顺序，v1 为预设→作用域→参数）。观察项（T6.10 视觉验收裁决）：a. 当前 presetId 不在 find 命中（如切形状后预设不支持新形状）时网格无高亮项、参数表单仍显示当前预设元数据，运行时按 §C 降级 default_solid——是否加「当前（未列出）」占位瓦片待验收定；b. default_solid 也未注册时网格为空（沿快选条先例）。 |
| 2026-09-12 | **T6.7 完成裁定（主代理，边界七项裁定 + 派发中断处置）**：①**删除边界=数据与消费方全删、契约类型面保留**——旧六类要素 Element（含 Building/Road/Green/Water/Parking/Marker 与 defaultLayers.ts）、registries/definitions 九文件（六要素定义 + 19 套 v1 StyleDefinition）、五旧渲染器 + ElementRenderer + MaterialFactory、v1 ChangeGeometry/ChangeStyle 命令全删；Element 接口/ElementRegistry/StyleRegistry/StyleDefinition/resolveStyle 保留空表装配（CONTRACTS.md 基础契约 EditorFacade.registries 形状与 JsonImporter 签名依赖——删类即改契约超任务授权，T6.9 重设计时一并收口）。②**默认图层 = 十一层**：十语义层名派生自 semanticDefinitions.defaultLayerName（沿 T6.1 归层撞键裁定方向收口，派生一致性测试锁定）+ 「模型」第十一层（MODEL_LAYER_NAME 锚定 domain/assets；任务书「十类语义图层」指语义类目齐备，资产库模式零回归硬要求模型对象保有默认归属层——toolIA 放置归层/bootstrap defaultLayerIdFor('model') 共用）。③**旧文件兜底跳过全集 = 六旧 type**（building/road/green/water/parking/marker，非任务书字面的 building/marker 两类——五渲染器已删后 road/green/water/parking 若载入即不可见僵尸对象，比跳过更差；任务书例举沿自需求文档「建筑/点位删除」表述，实际删除集以 T6.5 边界修正「Element 定义」整组为准）；跳过 + app:notify warn 报数量、region/model 正常载入、layers 原样保留，4 用例测试锁定，代码注记指向 T6.9 按需门。④**复用面保留**：geometryBuilders.ts（polygonSurfaceGeometry/centerlineRibbonGeometry 被 T6.4 GeometryBuilder import，仅删 SURFACE_ELEVATION 死常量、数值源收口 defaultBaseHeight）；applySceneObjectState 自 ElementRenderer 迁 renderers/objectState.ts（Renderer/RegionRenderer 双消费）。⑤**主代理 diff review 拦截资产模式回归**：model 对象「资产」引用行（asset-ref）随旧要素 type-params 组被连带删除，令子代理恢复——挂 Metadata 组不复活旧分组，名称经 AssetRegistry 解析。⑥**两项测试口径裁定**：phase1 绘制完成自动归「未分类」语义层为设计行为（DrawToolBase 按语义 defaultLayerName 归层，undo 回退到该层 id 非 null）；JsonImporter 空注册表过渡态 = 测试内联假 ElementDefinition 重写（33 用例保全映射/表达式/宽限修复语义）+ 空注册表抛错与导入失败错误 Toast 断言（正式重设计归 T6.9）。⑦**派发中断处置**：首派发遭 5h 使用限额中断（子代理已写 ~40 文件未提交实现后返回限额错误），重派缩减勘察简报核实半成品 + 主代理两项裁定续跑收尾——半成品不回退、逐文件对照验收（沿 T6.5 同类处置先例）。验收：grep1（12 旧标识符）src/+tests/ 零命中；grep2（building）剩余命中 16 文件逐项确认合法（语义类型/预设库/资产分类/兜底跳过集/App.tsx 映射示例/退役注记）。观察项（T6.10）：a.「导入 JSON」菜单动作为必然报错过渡态（错误 Toast 非静默，T6.9 重建）；b. regionQuickApply 与 phase1 的 undo layerId 断言口径差异属夹具差异（直接创建 vs 绘制路径），非行为分歧 | T6.7 done（01b53a5，86 文件 +1447/−4536，测试 1377→1292 全绿）；contracts/legacy-elements.md 随本条加退役完成注记；下一任务 T6.8（顶点编辑，派 threejs-expert） |
| 2026-09-12 | **T6.8 完成裁定（主代理，七项裁定，含一项待追认）**：①**VertexEditSessionPort 并入分域契约 §C——待用户追认**：子代理沿 RectPickPort 先例将编辑会话控制接口（beginSession/setPoints/requestVertexRemoval/endSession）写为「可选扩展 Port」并提请裁决；主代理按 PROTOCOL 按需门①（子代理报告提出改契约）发起用户拍板，会话内未获响应，暂按推荐方案执行（并入 §C + CONTRACTS.md 指针注记同步 + STATUS 置顶待追认标记）。定性依据：与 RectPickPort（无能力可退化纯点击）性质不同，SessionPort 为编辑会话通路必要组成（无它功能不成立），且签名最小、模式沿 GizmoPort。零代码影响（代码两态等价），用户否决则回滚两处契约增补即可。②**渲染循环内 vertexEdit.frame() 调用裁定为授权内**：子代理修复前会话半成品真实缺陷——句柄原为世界尺寸（球半径 0.45m），132m 观察距离下屏幕投影约 3px 不可见不可点；改按相机距离自适应缩放至屏幕恒定直径（顶点 13px/中点 9px），语义沿 TransformControls（size=0.9 屏幕恒定）先例；无会话 O(1) 早退、每帧连续渲染决策未动，非按需渲染改造。③**吸附管线注入形态**：DAG 禁止 runtime→editor，组合根闭包 editor/tools/draw/snap 的 gridSnap/orthoLock 纯函数注入 VertexEditImpl（VertexSnapPipeline 接口）——同一函数服务绘制与顶点编辑（单一真相源）；Shift 正交主轴取偏移大者，G 会话开关与全局 snapEnabled 取「与」（与绘制同语义）；A 45° 角度锁定未接入（任务书仅要求 G+Shift，留后续按需）。④**读数通路复用 draw:status**：拖动/插删实时推送 vertexCount/area/length/cursor（StatusBar 零改动复用绘制状态栏），未新增 EventMap 事件（符合任务约束）；删除违例拦截提示同走 draw:status error。⑤**DESIGN.md §5.12 补记（主代理确认）**：视口顶点句柄（琥珀球 #e8a33d=--accent 系、hover/drag 两态、边中点半透明八面体 9px、depthTest=false+renderOrder 恒在上、AUX_LAYER 独立组）、Inspector 激活态 .ed-btn--ghost-active（accent-soft 底+accent-line 描边）、ContextToolbar 编辑态中段（复用 ed-ctx__draw 原语）；CSS 令牌零新增，THREE 侧色值与 tokens 对应关系登记为单一对照源。⑥**子代理接手半成品处置追认**：首派发遭 5h 限额中断留未提交三层实现+53 测试（本会话 git status 起点干净系中断时机所致），重派子代理审查后接手补全而非重写——时间线合理、架构保留、既有质量合格，处置正确。⑦**语义细节确认**：point 类型=单点可拖（移动位置）不可删无中点句柄；参数化形状进入编辑即转 polygon（转换仅存会话内存，首次提交随 after 落库，options 丢弃=缓存失效）——零修改退出不产生历史（endSession 经 update(id,['shape']) 按场景权威数据恢复原参数化形状渲染，测试锁定）；每段拖动/每次插删一条 ChangeShapeCommand（撤销逐步回滚含 options 恢复），拖动期间材质实例不变（mesh.material toBe 引用断言，T6.4 解耦用例扩展）。 |
| 2026-09-12 | **T6.9 按需门裁决（用户拍板）+ 主代理三项派发裁决**：①**v1 旧数据兼容整体不做**——用户原话「不用考虑旧版本，全按新架构来，这样逻辑清晰不出错」（就旧建筑/点位转换规则发问，裁决覆盖全部 v1 旧数据）：SceneSerializer v2 仅支持 version '2.0'，'1.0' 及旧版本在版本门直接报错（含旧版停止支持提示）；不实现 building/marker/旧六要素→RegionObject 转换层（任务书原推荐预案 building→区域/marker 丢弃作废）；bootstrap openScene 旧六类对象跳过兜底（T6.7 防崩措施）删除，v2 反序列化改对象类型 fail-fast 校验（type ∈ {region, model} + region 三层结构 + shape/semantic 枚举合法，路径化错误；id/name/environment 宽容回退语义不变）。②**派发裁决 A——旧契约类型面收口删除**：legacy-elements.md 预留「最终去留随 T6.9 收口」；JsonImporter v2 零注册表依赖后 Element/ElementRegistry/StyleRegistry/StyleDefinition/StyleReference/resolveStyle 零消费者，随本任务删除；StyleParameter/StyleParameterType 保留迁独立文件（语义定义/预设元数据/UI 表单消费）；EditorFacade.registries 删 elements/styles 字段、ToolContext.registries 收窄 { assets }；基础契约同步归主代理验收后。③**派发裁决 B——导入映射 v2 形态**：ImportMapping { version:'2.0', rootArray, semanticType(十类，取代 elementType), geometry{path, format(xy/xyz/ring-array 沿用), shape? 显式覆写 polygon/line/point}, fields(name→对象名，其余→semantic.properties，乘数表达式沿用), defaults(presetId? 缺省语义默认预设；layerName? 缺省语义默认图层名写 metadata.layerName；其余→semantic.properties 默认值) }；shape 推断（无覆写时）：ring-array→polygon（多环取外环、内环丢弃记告警）、xy/xyz 单点→point、≥2 点→line；closed：polygon=true 其余 false；baseHeight=语义 defaultBaseHeight；options 不写。④**派发裁决 C——宽限语义沿用**：NOT_CLOSED 补闭合点/顺时针反转就地修复后收录并附「已修复」说明；SELF_INTERSECT/TOO_FEW_VERTICES/INVALID_COORD 排除报错。提交信息随裁决更新为 `feat(io): scene format v2 (v1 unsupported) and importer redesign (T6.9)`。另：T6.8 遗留追认项（VertexEditSessionPort 并入 §C）随本门再次发起未获响应（用户跳过），维持暂裁执行 + 待追认标记 | 用户主动选择更彻底的「不兼容」方案（拒绝推荐预案的转换分支），以逻辑清晰优先；②③④为任务书/legacy 契约既定收口项与实现侧定稿（主代理权限内），任务书已同步改写 |
| 2026-09-12 | **T6.9 完成裁定（主代理，验收通过 + 中断处置 + GeometryData 评估收口）**：①**派发中断处置（沿 T6.7/T6.8 先例，形态为主代理亲自收尾）**——general-purpose 子代理完成约 90%（源码与测试主体改写，未提交）后遭 5h 使用限额中断返回；Agent 派发通道同被限额阻塞（重置 21:25，距 3h），主代理亲自收尾而非重派：剩余缺口 = phase1/actions 两处 version 断言、actions 过渡态用例改写（T6.7「空注册表导入必败」→ v2 正向：building 映射导入 region/楼层×3 高度/building.default 预设/建筑层归层/一条历史/info Toast）、bootstrap.test region 夹具三层补全（makeObject）——均为 io/数据层收尾，不涉渲染层代码（AGENTS.md 主代理限制不适用），守护三件套由主代理全程复跑。②**验收确认（六项全过）**：v2 往返无损（options 缓存/overrides/业务参数/baseHeight/closed/transform/metadata 深相等）；v1 拒读（版本门含停止支持提示）+ 对象类型 fail-fast（region/model + 三层结构 + 枚举合法，路径化错误，测试锁定 building/marker 拒读）；导入 v2 零注册表（三格式×推断、显式覆写三态与数据不符报错、多环取外环内环告警、floors*3 表达式、语义默认预设/图层/baseHeight、宽限修复坐标回写 points、自相交/顶点不足/坐标非法排除）；旧契约类型面删除（Element/ElementRegistry/StyleRegistry/StyleDefinition/StyleReference/resolveStyle/MaterialSpec；StyleParameter 迁 domain/styles/StyleParameter.ts；EditorFacade.registries 与 ToolContext.registries 收窄）；building.example.json v2；三条路径测试全覆盖。③**GeometryData 评估收口（§D 遗留项）**：保留——绘制预览（DrawPreviewState）与校验适配（validateRegionShape）两处真实内部消费，导入器已直接产出 RegionShape 点列，并入无净收益（分域契约 §D 已更新）。④**契约第六次修订**（CONTRACTS.md）：#9 版本 2.0、io 节 v2 重写、registries 节删两面、ToolContext/EditorFacade registries 收窄、domain/styles 仅存 StyleParameter、ID 前缀移出 element_；分域 stage6-region.md §D 序列化过渡/几何两套标准两行收口、legacy-elements.md 终局注记。⑤**观察项闭环**：T6.7 观察项 a「导入 JSON 菜单为必然报错过渡态」随本任务闭环（v2 导入成功，actions.test 锁定）。测试 1353→1343（删 ElementRegistry/StyleRegistry/resolveStyle/elements/openScene.legacy 旧用例，v2 新用例已补；全绿）；check:layers 297 文件；tsc 0 错 | T6.9 done（22fe12f，54 文件 +1118/−1771）；下一任务 T6.10（阶段 6 GUI 验收 + 全量回归，主代理，以 T5.9 F1 清单为基线） |
| 2026-09-12 | **T6.10 完成裁定（主代理，验收通过 + 两观察项裁决 + 冻结口径裁定）**：①**验收六组全过、零修复循环**——A 七形状三步流/B 三类修改解耦（uuid 引用断言+视觉双证）/C 道路中心线·建筑 height 联动·POI 三预设/D 渲染三档全局覆盖语义/E=T5.9 F1 清单全量重跑**零功能丢失**（会话二补齐七项均真实 UI 路径：编辑菜单复制粘贴删除、图层透明度松手提交含材质基准×图层乘算 0.75×0.5=0.375 实证、缩略图 SVG→PNG 懒加载回填、资产放置+同资产 3 实例合批恰 1 个 InstancedMesh count=3、beforeunload dirty 态 defaultPrevented、pickInRect 全画布 11/空白 0/左半 8 恰当子集）/F 三项（切预设 0–1ms≪100ms；帧成本直接 renderFrame 计时基线 1.73ms vs 拖拽中 0.8ms+tick 补丁逻辑帧双态各 +1/帧+拖拽事件 0.07ms=15k/s；3 轮放 5 删全循环 WebGL geometries/textures 记账恒 13/4 零增长）/G 三件套 1343·297·0+build 5.8s+preview 五项 200/H 九截图+README 终版。②**T6.6 观察项 a 裁决=维持现状不加「当前（未列出）」占位瓦片**：circle→point 构造实证（6 base 瓦片无高亮+参数表单仍显 building.default 元数据+运行时降级 default_solid b0a99f+shape-only 一条历史），理由=任意瓦片点击自愈/参数表单供上下文/运行时安全降级/占位瓦片引入模糊交互语义收益边际。③**观察项 b=沿 regionQuickApply 先例维持**：default_solid 未注册仅合成注册表可达（生产恒注册），空网格+「预设未注册」空态为设计行为不立缺陷。④**F 项 FPS 冻结口径裁定**：宿主深冻 rAF 不转，采「直接 renderFrame 同步帧成本等效容量 + tick 补丁逻辑帧证明 + 拖拽事件吞吐」三证口径（watchdog 强制帧 215ms 为恢复路径成本非渲染成本），满足 ≥30fps·≥基线 80% 且量级余量；待解冻后 IAB 实测为可选补强非必需。⑤**新观察项一条不立缺陷**：粘贴副本 id 双下划线（region__mty…——editorActionsCore.offsetClone 传 `${prefix}_` 而 createId 自拼 `_`；全库无 id 前缀解析消费方、契约无单下划线强制、唯一性完好；纯外观，候选微修随后续任务顺手处理）。⑥方法学第十次增补 19–25 条（run_js 草稿/提交同任务纪律、副作用脚本禁双跑、单条历史三件套、帧性能双口径、PNG 大端坑、投影+像素采样确定性判读、DOM 深冻 DOM 断言代证）；测量事故两条（草稿跨任务假阴性、副作用脚本双跑复活已删对象）当场洗清并沉淀。阶段 6 至此全部收口（T6.1–T6.10 ✅），下一任务 T7.1（阶段门）。 |
| 2026-09-12 | **T7.1 阶段门过门（用户逐题拍板，三轮选择式 grilling 9 问 + 2 问展开复问）+ 阶段 7 任务书定稿**：①**Q1 模式架构 = UE 式聚焦模式**（用户先答「参考传统主流三维编辑器」，展开 Blender/UE/Max 三型对比后选定）：显式模式 = 工作域聚焦层，六模式可显式选择（scene/build/road/terrain/decoration/annotation），模式决定垂直条分组/Context Toolbar/HUD/Inspector 焦点，工具能力不因模式禁用，阶段 6 三步流不变；Measure/Analysis 维持禁用占位（沿 2026-09-10 Q6 裁定）。②**Q2 = 不绑定**：模式不干预绘制后类型选择，快选条照常全类型（用户答「绘制完成后，用户自行选择绑定」）。③**Q3a = 折叠进「更多」**：垂直条本模式组置顶+其余折叠抽屉（保留可达性）。④**Q3b = 仅 HUD 文案**：模式徽标全名+专属色+一行引导，场景内渲染零改动（用户反问「八模式到底是什么」后展开八模式落地含义再裁定；场景 overlay 若做需按需门单列）。⑤**Q4 = 四分裁定**：T7.1 建上下文工具矩阵+接线已有五项（复制/轮廓编辑/楼层步进/节点编辑/道路属性，经 ChangeSemanticCommand）+对齐/阵列 disabled 占位；道路分割/合并并入 T7.6（改名「对齐/吸附/阵列与道路几何工具」）；偏移不做记观察项；建筑自动生成不做留档。⑥**Q5 = Alt+1..6** 六启用模式直切（Shift+1..5 被区域子工具占用故弃；Alt+7/8 不绑目标禁用；菜单+选择器三路同源）。⑦**Q6 = useWorkspaceStore.mode**（T7.1 内存态，T7.3 随布局持久化；徽标经 combineMode(显式, activeToolId, drawTarget) 派生）。⑧**Q7 = T7.2 面板 Docking/Floating/Auto Hide 暂不实现，放入远期规划**（用户裁定；连带：T7.3 前置改挂 T7.1 且布局范围限四区框架内显隐/尺寸、T7.8 清单删 Docking 项、阶段序列 T7.1→T7.3→T7.4→T7.5→T7.6→T7.7→T7.8；选型议题随延后挂起）。⑨**Q8 = 确认序列与口径**：T7.8 清单补全（六联动抽查/矩阵/占位 tooltip）+截图目录笔误修正 phase6→phase7。CONTRACTS 第七次修订：排除项增补三则（Docking 延后远期/自动生成不做/偏移不做），无接口签名变更。T7.1 任务书细粒度定稿（细案见 tasks/T7.1.md），T7.2 deferred、T7.3–T7.8 头部与范围同步更新 | 阶段 6 收口后按依赖顺序进 T7.1；阶段 6 派生模式（deriveMode）与需求 24/25 章显式八模式的张力经门裁定调和为 UE 式聚焦双层（显式层+绘制态派生覆盖）；需求 6.2 道路几何工具计划缺口（分割/合并/偏移无任务承接）经 Q4 裁决补位 |
| 2026-09-13 | **T7.1 完成裁定（主代理，中断收尾 + 表序一致性裁定）**：①**中断半成品收尾处置**（沿 T6.5/T6.7/T6.8/T6.9 第四次限额中断先例，本次为跨会话未留交接的中断）：收尾会话以 typecheck+test 定位差距——三处旧测试口径未同步（toolIA.test「仅 scene 可手选」断言与 MenuState 夹具缺 workMode、menuModel.test id 快照缺 view.mode-* 八项）+ DESIGN.md/README.md 两产出未落，逐项补全而非重写；半成品实现本体（toolIA 纯函数群/ContextActions/ModeGuideCard/VerticalToolbar 抽屉/MenuBar 子菜单/actions 路由/tokens 六色）经逐件 diff review 质量合格全部保留。②**MODES 数组序对齐需求 24 章表序（主代理裁定）**：半成品 MODES 沿 T5.6 旧序（annotation 位5/decoration 位6）而 ALT_MODE_ORDER 按 24 章表序（decoration=Alt5/annotation=Alt6），选择器与视图菜单按 MODES 数组序渲染将出现「标注 Alt 6」排在「装饰 Alt 5」之上、位次与键帽错位 → 裁定数组序对齐表序，三路（选择器/菜单/快捷键）位次一致（i 位 ⇒ Alt+i）；menuModel 子菜单注记与三处测试口径同步。③**完成认定**：守护三件套主代理亲自复跑全绿（1369/1369、check:layers 300 文件 OK、tsc 0 错）+ 生产构建冒烟 304ms 产物齐全 + 零 runtime/three 改动核对（改动面 = ui/app/tests/文档）；验收清单七项过（三路切换/cancel 语义/combineMode 徽标/矩阵八分支/步进器单历史/Alt 边界+守卫/禁用占位两处），六联动中 GUI 目检项（抽屉/引导卡/HUD 两行）沿 T2.1 先例归 T7.8 阶段验收。DESIGN.md §2 增六枚 --mode-* 令牌登记 + §5.13 组件约定（模式专属色使用规则：≤12px 色量、不作大面积填充、不侵犯琥珀语义）；README 增「工作模式」章节 + Alt 键位行 + 操作表两行 | T7.1 done；下一任务 T7.3（工作区布局预设与持久化，前置 T7.1 已满足；localStorage UI 唯一真相源；mode 持久化随本任务落地——门 Q6 裁定） |
| 2026-09-13 | **中期偏差终审六项裁定（交叉验证终审官会话，用户选择式逐项拍板）**：①**VertexEditSessionPort 并入分域契约 §C 追认闭环**（T6.8 暂裁执行挂账就此关闭）。②**图层「合并」纳入 T8.3**（定稿时增「图层面板补全」小节：源图层元素批量移入目标图层+删源图层，一条可撤销批量命令；ui升级 §10.2 图层面板清单唯一缺口）。③**场景树「分组+拖拽调整层级」阶段 8 单列任务书**（ui升级 §9.3 缺口，骨架级功能牵动场景格式 v2 扩展/撤销栈/Outliner，不与 T8.3 混验；编号与插入位置待 T8.1 阶段门定稿）。④**资产库「标签浏览+排序」纳入 T8.3**（定稿时增「资产库补全」小节；manifest tags 字段已备，筛选芯片+排序下拉；V2.0 §模型资产库五项子能力就此收口）。⑤**T7.2 Docking 补重启条件**：T8.5 全量验收后的项目复盘时统一裁决重启与否；期间布局类痛点反馈集中可随时按需门重启（P1 身份保留，承诺不再悬置）。⑥**测量/标注维持 CONTRACTS 排除**：ui升级 §5.1/§24 字面条目以契约为准视为远期能力占位，文档张力就此了断。连带勘误：STATUS 归档指针 39→40、README 1343→1369、README 当前进度补阶段 7 开工说明、T8.3 任务书增补吸收项、STATUS 待追认撤销 | 依据《中期偏差审核总报告》（已归档 docs/plan/archive/，随需求侧/执行侧两份源报告一并冻结；需求侧 88/执行侧 93 双独立报告交叉验证，整体裁定「低偏差·受控欠账型」）：无主需求债三项、T7.2 悬置承诺、测量/标注文档张力、暂裁执行挂账四类治理尾部欠账一次了断；排除清单无变动，不涉 CONTRACTS 修订 |
| 2026-09-13 | **T7.3 完成裁定（主代理，一次过交付 + 六项裁量追认 + 一项口径更新）**：①**完成认定**——general-purpose 子代理 TDD 一次过（零修复循环），commit 0e64e38；主代理守护三件套亲自复跑全绿（1412/1412、check:layers 305 文件、tsc 0 错）+ build 冒烟 312ms + diff review 逐件亲审（sanitize 字段级回退/订阅在恢复后启动防 INITIAL 覆盖/applyLayout 深拷贝防预设表引用污染/import type 循环为类型级编译期擦除/radiogroup 键盘惯例）。②**mode 持久化语义落地（门裁定执行）**：预设与个人布局只含布局字段不含 mode（工作态非布局态，T7.1 裁定延续）；唯一例外「恢复默认」动作 = default 预测 + setMode('scene')；自动快照含 mode，反序列化经 MODES enabled 校验（measure/analysis 不可恢复落 scene）。③**六项裁量追认**：MenuBar「状态全经 props」边界局部放宽（弹层直连 workspaceStore，沿 ContextToolbar/ZoneToggle 先例）；弹层 role=dialog 而非 menu（menu 角色不允许嵌文本输入）；browser.category 校验 = 非字符串/空串回退 'all'（开放 slug 域）；deserialize 字段级回退而非整体丢弃；弹层应用后不自动关闭（matchPreset 实时反馈）；个人布局保存载荷写入前过 sanitize。④**主代理裁定一项**：DESIGN.md §5.13 尾行「mode 为内存态不落盘」随本任务过时，更新为「随工作区快照持久化（T7.3 落地）」；§5.14 工作区布局弹层条目按子代理建议文本落地（视觉令牌零新增）。⑤**弹层 GUI 目检归 T7.8**（沿 T2.1/T7.1 先例）：视觉/交互实况/持久化实况/触达/存储实况/Splitter 防抖六组，清单已录任务书执行留痕 | T7.3 done；下一任务 T7.4（纯三维工作模式 Tab，中粒度任务书已定稿可直接派发） |
| 2026-09-13 | **T7.4 完成裁定（主代理，一次过交付 + 三项偏差追认 + 八项简报裁决执行确认）**：①**完成认定**——general-purpose 子代理（frontend-design 前置）TDD 一次过（零修复循环），commit 4c05857（17 文件 +391/−14，+21 测试 = 1433）；主代理守护三件套亲自复跑全绿（1433/1433、check:layers 305 文件、tsc 0 错）+ build 冒烟 306ms + diff review 逐件亲审（store 幂等写法沿 setMode 先例/togglePure3d cancel 语义三工具族/input 裸 Tab 守卫/zoneVars 条件展开不常驻覆写令牌/视图菜单与契约快照同步）。②**八项简报裁决执行确认**：R1 pure3d 为工作态（不持久化、applyLayout/resetLayout 不触碰，守护测试锁序列化产物无 pure3d 键）；R2 togglePure3d 共享入口（进入 cancel draw-*/placement/vertex-edit——vertex-edit 为任务书「绘制/放置」之外的主代理扩裁，顶点编辑会话同属防隐形状态原则——+关右键菜单；退出只翻位）；R3 裸 Tab 路由（Shift/Ctrl/Alt 叠加让位、preventDefault 阻断焦点遍历、isEditableTarget 复用）；R4 菜单 Tab stopPropagation；R5「极简顶部工具」口径 = MenuBar（ContextToolbar/垂直条/三面板区/状态栏一并隐藏；zone 变量 + 三 token 内联归零 + ed-app--pure3d 类；HUD 底部居中提示芯片 .ed-hud--bc 仅 pure3d 渲染）；R6 视图菜单 view.pure3d 三路同源（Tab/菜单；SHORTCUT_HELP 同步）；R7 ctx.rename 纯三维下先退出（防隐形聚焦输入框）；R8 README 两处补记。③**子代理三处偏差追认**：ContextMenu.tsx 第三处 case 'Tab' 同型补 stopPropagation（简报漏列，子代理自查补全——右键菜单开着时 Tab 只关菜单不切模式，与 R4 原则一致）；CSS border:0 归零（0px 网格槽 + 1px 边框在 border-box 下仍渲染约 1px 残留线，违背隐藏语义）；menuModel.test id 守卫正则段字符类放行数字（view.pure3d 含数字 3，actionId 按裁定不改）。④**DESIGN.md §5.15 落地**（主代理落文件）：ed-app--pure3d 修饰类 + .ed-hud--bc 第五角位提示芯片，视觉令牌零新增。⑤**遗留观察（T7.8 GUI 验收裁决）**：帮助弹层（HelpOverlay）开着时 Tab 仍触发全局切换（对话框非菜单未拦）；pure3d 下 view.panel-* 仍改 hiddenPanels（退出后生效，不拦截——已知可接受）；160ms 网格过渡退出方向短暂内容溢出像素复核归 T7.8 | T7.4 done；下一任务 T7.5（多选增强与批量编辑 Mixed，中粒度任务书已定稿可直接派发） |
| 2026-09-13 | **T7.5 完成裁定（主代理，一次过交付 + 四项偏差追认 + 两项主代理裁决）**：①**完成认定**——threejs-expert 子代理 TDD 一次过（零修复循环），commit 1329fa1（13 文件 +1845/−63，+37 测试 = 1470）；主代理守护三件套亲自复跑全绿（1470/1470、check:layers 309 文件、tsc 0 错）+ build 冒烟 304ms + diff review 逐件亲审（GizmoImpl 增量数学矩阵合成推导复核/TransformTool 微任务聚合边界/multiEditModel 纯模块/InspectorPanel 组体/CSS 与 §5.6 工具条 20px 密度先例比对）。②**派发路由裁决（主代理）**：任务含 Gizmo 成组变换（runtime/three 变更），按 AGENTS.md「凡 three.js 相关实现派 threejs-expert」整任务派 threejs-expert——PROTOCOL 枚举表（T7.7/T8.4）T7.5 未列属枚举缺项，本条即补正记录；单子代理整任务保持「每任务一个子代理」纪律。③**多对象 Gizmo 契约内实现（签名零变更）**：attach(ids) 多目标（单选原行为绕自身原点回归锁定、≥2 质心+单位姿态组枢轴）；拖拽增量 D = proxyFinal ∘ proxyStart⁻¹ 逐目标 after_i = D ∘ before_i（平移=整体平移、旋转/缩放绕质心）；结束同一手势 N 次 onDragEnd 回调——GizmoPort 签名与载荷不变，多回调语义属行为扩展非契约修订（T7.1 门已裁「成组收编进 T7.5·单条历史」，机制授权在门）；TransformTool 微任务冲刷聚合 N>1 → 一条 BatchCommand。④**Mixed 语义落地**：multiEditModel MIXED unique symbol 差异检测；通用组（图层/可见/锁定）任意混选 + 同语义 region 追加组（类型/参数逐键/预设/基准高度）；逐键提交不触碰其余键（载荷 = 各对象自身 properties/overrides 合并该键）；预设参数 presetId 有异时不显示表单只留提示（子代理按数据真相底线裁量，追认）；批量语义类型切换连带自动归层/预设重置为 ChangeSemanticCommand ① 路径契约行为（一条历史整体可撤销）。⑤**四项偏差追认**：TRS 表示域边界（非等比缩放×成员旋转产生剪切、Matrix4.decompose 极分解吸收——与单选 gizmo 既有边界一致，测试注释登记）；既有 TransformTool 4 测试异步化（微任务冲刷后单手势命令异步提交，测试加 await 分隔手势——被测行为契约不变，仿真保真修正）；预设网格选项按首对象 shape.type 过滤（ChangePresetCommand 不校验 shape、runtime 引擎消费侧兜底，混形状多选观察项）；attach 过滤不可解析 id 不整体拒挂（多选选中集驱动下更稳健）。⑥**DESIGN.md §5.6+§5.7 补记与两行过时文案修正**（主代理落文件）：.ed-outliner__select-all / .ed-field__value--multi / .ed-input--mixed / .ed-inspector__mixed-tag，视觉令牌零新增。⑦**像素验收归 T7.8**：多选 gizmo 成组旋转/缩放像素级确认、Mixed 面板观感（沿非里程碑任务不浏览器验收先例） | T7.5 done；下一任务 T7.6（对齐/吸附/阵列与道路几何工具，中粒度任务书已定稿可直接派发） |
| 2026-09-13 | **T7.6 完成裁定（主代理，限额中断收尾 + 两真缺陷 TDD 修复）**：①**中断处置**——threejs-expert 子代理在 commit e442d3b（30 文件 +3633/−150，+86 测试=1556）落盘后、返回报告前遭遇 5h 使用限额中断；沿 T7.1 第四次限额中断先例由主代理收尾：守护三件套复跑全绿（1556/1556、check:layers 321 文件、tsc 0 错）+ build 冒烟产物齐 + 30 文件逐件 diff review（roadGeometry 纯函数/Split·Merge 命令往返/RoadSplitTool 会话生命周期/alignArrayModel 八模式与均布数学/GizmoImpl 吸附纯函数与幂等修正机制/Renderer·PreviewManager 接线/bootstrap 注册与共享对象注入/toolIA 矩阵分支/ContextActions 弹层·popover/App·store·InspectorPanel 三处装配/CSS 令牌复用/八测试文件口径）。②**主代理 TDD 修复两真缺陷（commit 5251404，+3 测试=1559，沿 T5.8 主代理亲修先例）**：㈠ RoadSplitTool.findCandidate 用局部点列直比世界 groundPoint——被 gizmo 移动过的道路（position≠0）拾取偏移，而句柄层 localToWorld 已按世界渲染（视觉与命中不一致）；修复=候选计算加 live position 偏移。㈡ ContextActions 阵列确认后「选中副本集」恒不生效——planArrayCopies 二次调用生成新随机 id 与 arrayCommand 内实际创建的副本 id 漂移，存在性过滤恒空；修复=新增 planArrayExecution（副本一次规划，命令与副本集同源）并以此重构 confirm。两缺陷均先写失败测试确认后修复（测试先行红后绿）。③**关键实现追认（简报裁决正确执行）**：对象吸附=GizmoImpl objectChange 幂等修正（TransformControls 每次 move 从内部增量重算 position，外部修正不累积——translationSnap 同款模式）；命中覆盖网格吸附；候选排除自身/不可见（对象或图层 visible=false）；多选=足迹并集。足迹预览=FootprintGhostPort 可选扩展 Port（RectPickPort T2.4 先例，**主代理裁定暂不并入正式契约**——与 RectPickPort/VertexEditSessionPort 同留扩展位）；model 编辑层无足迹→零尺寸框参与计算、2×2 占位框参与预览。分割交互复用 VertexEditSessionPort 句柄显示+onDragEnd 置空中性化（VertexEditTool 每次 activate 重挂回调，置空安全）。合并邻接=世界坐标判定（points+position）容差 0.5m、四情形拼接丢第二段共享端点。④**契约面**：零 CONTRACTS 既有签名变更；命令族增补注记两行（SplitRoadCommand/MergeRoadCommand）。文档：DESIGN §5.16+§5.13 矩阵行取代、README 编辑交互/矩阵/操作表/用例数、任务书执行留痕。像素级 GUI 验收（对齐弹层/阵列 popover 观感、足迹预览、对象吸附手感）归 T7.8。 | T7.6 done（1470→1559 全绿）；下一任务 T7.7（园区导航小地图，派 threejs-expert） |
| 2026-09-13 | **T7.7 完成裁定（主代理，一次过交付 + 方案预裁定 + 两缺陷 TDD 修复）**：①**完成认定**——threejs-expert 子代理 TDD 一次过（零修复循环），commit ec691d3（20 文件 +2002/−38，+49 测试 = 1608）；主代理守护三件套亲自复跑全绿（1608/1608、check:layers 327 文件、tsc 0 错）+ diff review 逐件亲审。②**方案预裁定（派发简报授权，任务书「第二视口或离屏渲染」留白由主代理拍板）**——采用独立 canvas + 独立小 WebGLRenderer + 派生俯视轮廓 scene（AxesIndicator 第二视图先例）；否决主 renderer scissor 第二视口（renderer.info「上一完整帧快照」口径承诺会被第二 pass 污染 + viewport/scissor 状态与主视口互扰）、否决第二 renderer 直接渲染主 scene（PBR program/几何显存双份上传+光照不一致）；「场景轮廓」语义 = 派生简化俯视图（polygon 面/line 条带/point·model 标记，MeshBasicMaterial 十类语义低饱和制图色带）。③**零契约变更路径**——开关入 workspaceStore.minimapVisible（快照扩展字段，不进 SceneData/io；语义属工作区 UI 偏好）；导航走 CameraController.panTargetTo 公共方法 + app 组合根 setNavigate 接线（CameraPort/EditorFacade/EventMap/ToolContext 全不动；EditorHandle.setMinimapVisible 为组合根超集，沿 getGrid 先例）。④**帧预算**——独立上下文 ×1；draw calls = 可见轮廓数 + 2（标记批恒 1 + overlay 恒 1，装置级测试锁定）；GL 绘制 needsRender 门 + ≥33ms 节流（≤30Hz），相机静止+场景静止零绘制；轮廓重建仅 EventBus 脏即时。⑤**主代理 TDD 修复 commit eba93b9（+3=1611）**——(a) viewFootprint.ts:153 正交角射线起点 y 行误乘 ry（正确 rx；当前主相机 up 恒竖直 → right.y≡0 不可达的潜伏缺陷，滚转正交构造写死数值回归锁定）；(b) 主菜单 view.minimap placeholder 漏转正——主代理简报只列 HUD 两处占位属疏漏，任务书口径「T5.7 占位项启用」应全覆盖；沿 view.pure3d 先例三处接线（menuModel checked+ready 门/actions deps 双方法/App getState 绑定）+ menuModel.test disabledAlways 出列。⑥**子代理偏差追认**——HUD 右下占位按钮移除（小地图 runtime 自建 canvas 沿 axes 先例，无 React ref 透传链路）：接受；轮廓层节流取「EventBus 脏即时」而非 10Hz 定时（事件驱动频率天然达标）：接受；浏览器像素自检因子代理环境无 Browser Use 未执行：按简报归 T7.8。⑦DESIGN §5.10 补记小地图画布行（子代理报告提案文案+主代理增补主菜单入口与退役登记）；GUI 像素验收归 T7.8（验收清单已增补：实时反映场景与视野四边形/拖拽点击定位（点击容差 0 口径=换算精确到像素）/开关持久往返/弱存在感观感）。 |
| 2026-09-13 | **T7.8 完成裁定（主代理，三次会话零修复循环，阶段 7 收口）**：①**完成认定**——九项验收全过：前两次会话覆盖 ①–⑦（六态联动/上下文矩阵/布局预设+刷新恢复/纯三维/多选 Mixed 批量/对齐阵列道路分割合并 4mm 命中/小地图导航持久），三次会话补齐 ⑧ F1 八项回归（MCP 桥页 run_js 文件化脚本通道）：文件管线三路全真实菜单路径（导出 createObjectURL 钩子 8829B；打开 DataTransfer 注入 input.files+change 往返 0→6 原名全恢复；导入 floors*3 映射+未闭合环自动补+单历史三件套）、复制粘贴删除各自单历史三件套、图层透明度规则 19 单任务松手提交 undo 恰回 1.0、环境 select night↔day 不入历史、网格 5→10+drawGrid.spacing 同步、缩略图 loading=lazy+交互 kickThumbnail 离屏快照回填 data:image/png 实证（T5.9「2 真 PNG」机制=交互数）、连放净口径恰 0–1ms+24 模型 1 个 InstancedMesh count=24 实例共享（RuntimeObjectMap.byId 门面后门反查主场景）；守护三件套 1611+327+tsc 0+build 5.56s 且三次会话零源码改动沿用。②**渠道裁决**——锁屏深冻环境 MCP 桥页为唯一可行通道（IAB 僵死），方法学第九/十次增补 26–30（evaluate 传输雷区/桥页替代通道/合成指针捕获 no-op/groundPoint 仿射标定/three 双实例雷区+RuntimeObjectMap 后门+隐藏页 lazy 伪影）。③**观察项不立项**——React file-sep 重复 key 告警（menuModel 同菜单多分隔线同 key，纯 console 无行为影响）记 T8 清扫候选；可选小地图关闭态补拍裁定不做（take_screenshot 无 DOM+IAB 冻结，DOM/store 双证已被任务书接受）。④**阶段 7 正式收口**，下一任务 T8.1 阶段门前定稿任务书再过门。 |
| 2026-09-13 | **T8.1 阶段门过门（网络调研先行 + 选择式 grilling 两批 8 问，用户逐题拍板）+ 阶段 8 任务书全量定稿**：①**调研先行（用户指令）**——general-purpose 网络调研子代理对 Unity/UE/Blender/Figma/three.js/SketchUp/Maya/C4D 官方文档做五能力域（吸附对齐/模板导入/多对象编辑/视口诊断/分组层级）对照审核，报告冻结 archive/research-phase8-mature-editors.md；核心结论：六任务书吻合度高（组=纯组织节点有 UE Actor Folders+Blender Collections 双直接先例、Ctrl+G 跨域惯例、15°=Unity 默认、模板只读源=Unity Hub/Figma duplicate、参考线生命周期与 Figma 官方口径精确一致），两处无先例设计（按类型分节多选、3D 拖拽参考线）论证为领域合理特化维持；**调研低成本增补直落任务书**（rotationSnap 相对步进验收注记/Ctrl 临时反转旁路四家标配/参考线视觉数值化 1px+3-4px 端点/混合值「—（混合）」占位 Unity-dash 惯例/重命名默认值+位数+预览前 8 条/drop indicator 三态/hover 折叠组 800ms 展开/HUD 常规·诊断分组/孤岛档计数角标/模板容量兜底 20 套·1MB/导入三态行 Ant Design Upload 惯例），均不触排除项。②**8 问拍板**：Q1 新任务「场景树分组与层级拖拽」编号 **T8.5**（插渲染模式后、验收前；验收任务顺延 **T8.6**，STATUS 依赖序/T7.2 重启条件/终审⑤三处引用同步）；Q2 T8.1 吸附三件**全做**（角度/高度层/参考线）；Q3 场景格式 **2.0 内增量放行 'group'**（fail-fast 扩枚举、未知类型仍路径化报错、组 transform 恒等纯组织——UE 文件夹/Blender Collections 先例）；Q4 T8.4 **三档含未归类高亮**（孤岛语义=未归语义图层对象高亮+HUD 计数角标；灰模=MeshBasicMaterial 单色选型注记）；Q5 **贴地命令纳入** T8.1（UE End 键先例，y 归 0 或下方最近候选层，单条历史）；Q6 **查找/替换重命名模式纳入** T8.3（Blender/UE 双先例，与前缀+序号互斥共享预览）；Q7 **组行「选择成员」纳入** T8.5（双击组行/右键项=仅选成员，UE 选文件夹先例）；Q8 **「设为默认模板」不纳入**（唯一偏离推荐项，入 T8.2 明确不做）。③**派发路由补正**：T8.1 含 GizmoImpl/参考线渲染 runtime 变更，整任务派 threejs-expert（沿 T7.5 先例，PROTOCOL 枚举表同步）；T8.2/T8.3 general-purpose；T8.4 threejs-expert。④Analysis Mode/高级场景分析/测量标注维持排除，排除清单零变动 | 阶段 8 立项完成：T8.1（中粒度）+ T8.2–T8.4（细化）+ T8.5（新建）+ T8.6（验收顺延重编号）六份任务书定稿待发；本条为 T8.1 派发授权依据 |
| 2026-09-13 | **T8.1 完成裁定（主代理，一次过交付 + 六项偏差追认 + 一项契约请求裁定）**：①**完成认定**——threejs-expert 子代理 TDD 一次过（零修复循环），commit 0098156（31 文件 +1890/−130，+54 测试 = 1665）；主代理守护三件套亲自复跑全绿（1665/1665、check:layers 338 文件、tsc 0 错）+ dev server 起服冒烟 + 31 文件逐件 diff review 亲审（分级同步三路/旋转弧度与相对步进/高度幂等修正/参考线池化与帧内 O(1) 早退/贴地单历史/UI 三路同源装配链）。②**六项偏差追认**：并列候选择先见者（`<` 确定性，T7.6 旧函数保留回归锁定）；恰对齐 d=0 也产出参考线（反馈连续，Figma 同款）；参考线端点=两框延伸并集（随视觉三数值登记）；Ctrl 绘制管线 no-op（门裁定「不影响绘制三键」的忠实执行）；高度吸附移动参考取底面（顶面对齐屋顶线不做）；道路条带足迹为必要补充（footprint 语义升级非回归，影响 T7.6 道路候选从中心线变条带边缘=任务书定义）。③**契约增补请求裁定：SnapTiersConfig/AlignGuidesSink 不并入 CONTRACTS**——沿 ObjectSnapConfig（T7.6）先例，共享配置/结构化 Sink 非 Port 接口，组合根超集注释锚定。④**DESIGN.md §5.12 增补**（主代理落）：对齐参考线行三数值单一对照源（1px 线宽/4px 方形端点/--accent 0xe8a33d）+0.25m 层高+renderOrder 1001+端点并集语义；顶点编辑提示行文案同步（G 网格 / A 45°）。⑤**贴地键位 End**（全局无冲突核实）；SHORTCUT_HELP 增 Ctrl（拖拽中）/End 两行、A 行扩「绘制/顶点编辑」。⑥像素级 GUI 验收（参考线观感/拖拽场景/贴地实操）归 T8.6（沿 T7.5/T7.6 先例） | T8.1 done：测试 1611→1665 全绿；阶段 8 进度 1/6；下一任务 T8.2（场景模板与批量导入，任务书已定稿可直接派发） |
| 2026-09-14 | **T8.2 完成裁定（主代理，一次过交付 + 七项偏差追认 + 契约第八次修订）**：①**完成认定**——general-purpose 子代理（frontend-design 前置）TDD 一次过（零修复循环），commit 12cc253（17 文件 +2465/−80，+38 测试 = 1703）；主代理守护三件套亲自复跑全绿（1703/1703、check:layers 343 文件、tsc 0 错）+ build 冒烟 315ms + 17 文件逐件 diff review 亲审（io 模块族/两内置 JSON 逐字段/actions 路由与 deps/菜单模型与键盘索引/四弹层组件/App saveStateRef 防陈旧闭包/CSS 令牌与 40px 触达/测试断言口径）。②**七项偏差追认**：(a) 示例园区建筑参数 floors→height——semanticDefinitions 唯一规范参数为 height 且 JsonImporter floors*3 产物即写 height，主代理简报笔误、子代理按单一真相源修正；(b) deps 增 saveUserTemplate 注入点——node 无法触 globalThis.localStorage，沿 pickFile 注入先例中转；(c) UserTemplateSaveFailure 拒存原因超集（+invalid-name/storage-unavailable 防御路径+文案）；(d) 非 dirty 直通落点在 App hook（React 代码路径，node 覆盖 fake 两路，GUI 直通归 T8.6）；(e) file.new 旧 actionId 移除（子菜单父项不派发=MenuSubmenuDef 既定语义，全库 grep 无其他派发点）；(f) rename 改到既有名=覆盖合并（io 层防御，UI 层已禁撞名，沿 saveNamedLayout 先例）；(g) 示例园区绿地/湖/南门坐标（简报授权「任选合理」）。③**告警口径裁定**：批量导入三态行 W=JsonImporter errors.length 全量（沿现单文件 notify 既有口径）——ImportError 无 severity 字段，细分告警/错误需契约变更不做；X=文件级解析失败置灰行。④**契约第八次修订**：#7 ID 前缀清单增补 `tpl_`（用户模板 TemplatePayload id；主代理派发简报预授权+验收落档）；内置模板 id 固定字符串（builtin-empty/builtin-sample）属注册表键命名空间先例不入前缀体系。⑤**DESIGN §5.17 登记**（主代理落，据子代理提案+亲审事实）：弹层族外壳 DialogShell/新建确认三行摘要+取消钮初始焦点/命名弹层/模板清单行 24px+::before 40px 触达/导入预览三态行 Ant Upload 惯例零依赖/新建场景子菜单四区 separator+键盘索引跳过。⑥像素级 GUI 验收（子菜单交互/dirty 确认弹层/示例园区 6 对象视觉/另存往返+刷新存活/管理弹层/多文件预览）归 T8.6 | T8.2 done：测试 1665→1703 全绿；阶段 8 进度 2/6；下一任务 T8.3（多对象编辑与工作区增强，任务书已定稿可直接派发） |
| 2026-09-14 | **T8.3 完成裁定（主代理，一次过交付 + 六项偏差追认 + 零契约变更）**：①**完成认定**——general-purpose 子代理（frontend-design 前置）TDD 一次过（零修复循环），commit 40d0667（23 文件 +2139/−235，+56 测试 = 1759）；主代理守护三件套亲自复跑全绿（1759/1759、check:layers 348 文件、tsc 0 错）+ 23 文件 diff review 亲审（MergeLayerCommand 快照 undo/redo 幂等、batchRenameModel 纯函数、multiRegionSectionsOf 分节收敛、弹层/接线/CSS 52 处 var(--) 零硬编码色/测试断言口径——验收用例「建筑×2+道路×1 两节独立收敛」直接入测）。②**六项偏差追认**：(a) 节内混合占位复用 T7.5 `Mixed` 斜体占位控件而非新造「—（混合）」字样——任务书自带「与 T7.5 单类型 MIXED 占位语义同源」条款，T7.5 `Mixed` 已过 T7.8 验收，一致性优先；(b) 资产名称排序 = Unicode 码点字典序（刻意不用 localeCompare——node small-icu 无中文整理数据静默退化、跨环境序不一致，源码注释登记）；(c) 批量重命名入口 outliner-row + viewport-object 两菜单对称补齐（任务书「右键/上下文入口」，视口多选右键同达选中集；actionId `ctx.batch-rename` 快照契约同步）；(d) 图层合并目标选择 = 行内选择条（删除两步确认条同构原语）而非弹层——「明确不做」仅排除目标内容预览弹层，列表选择语义等价，交互同构复用；(e) 布局导入双层拒收语义：结构非法（坏 JSON/非对象/缺 name/layout）整体拒收 Toast、字段级非法沿 sanitize 回退默认——「同款 sanitize 校验」的自然解读，往返无损 node 测试锁定；(f) multiEditFieldsOf 保持 T7.5 兼容签名（同类型全 region 仍产出单 region 字段），组件统一走分节路径（单节退化等价）。③**派发简报边界裁定**（派发前主代理定）：分节仅对全 region 选中集产出（model 混入不出节、通用组仍全量）；纯同类型退化为单节；改名统一 UpdateObjectCommand；「未分类」哨兵 layerId===null 天然不可作源/目标。④**DESIGN 登记**（主代理落，据子代理提案+亲审事实）：§5.6 图层合并行、§5.7 多选语义分节行、§5.8 标签/排序工具行（含码点字典序理由）、§5.14 行尾图标钮/行内重命名卡/导入布局三行、新 §5.18 批量重命名弹层。⑤file-sep 清扫 = MenuBar.tsx:247 key 加 index 一处（对齐同文件 :507 子菜单先例）。⑥像素级 GUI 验收（分节节头观感/批量重命名弹层实操/布局导入往返/合并 undo/标签芯片叠加/多弹层 console 干净）归 T8.6 | T8.3 done：测试 1703→1759 全绿；阶段 8 进度 3/6；下一任务 T8.4（高级渲染模式，**派 threejs-expert**，任务书已定稿可直接派发） |

| 2026-09-14 | **T8.4 完成裁定（主代理，一次过交付 + 预裁决路线全沿 + 零契约变更）**：①**完成认定**——threejs-expert 子代理 fbe2211（21 文件 +1163/−139，+28 测试 1759→1787 全绿；check:layers 348 文件、tsc 0 错、build 330ms 主代理复跑全绿）；首次派发撞 5h 使用限额中断（子代理已写大量未提交改动），限额重置后重派同简报，子代理逐文件 review 继承改动确认与预裁决一致后补齐缺口整体交付——同 T6.8 两段式先例。②**四项预裁决路线全沿**（派发简报钉死、实现无偏差）：normals=自写微型 ShaderMaterial（RGB=世界空间法线，规避 MeshNormalMaterial 视空间编码深坑，顶点着色器含 USE_INSTANCING 分支乘 instanceMatrix，片元直写 framebuffer 不做色彩空间转换=诊断读数语义）；islands 混合池=精确拆分（InstancedAssetPool 诊断分组 API：纯池整网格换层、混合池临时第二 InstancedMesh 共享源 geometry/material 只重建矩阵缓冲、槽位表 1:1 分组感知 update/resolvePick、退出并回单网格释放缓冲）而非「整网格归亮侧」降级；DIAG_LAYER=4（与内容 0/环境 2/辅助 3 互不重叠）；持久化剥离位置=bootstrap.saveScene 组装出口（诊断档 renderMode 置 undefined，JSON.stringify 自然丢弃；SceneSerializer 保持整体透传语义不动）。③**架构要点**——RenderPassPlan.overrideMaterial 由 boolean 改 OverrideToken（none/primary/dim/highlight）经 RenderModeState.resolveOverrideMaterial 解析（islands 双内容遍各取其材、纯函数可测保持）；islands 激活期间增量维护走 attach/update（layerId 键）钩子+scene:changed(removeLayer) 静默换层兜底订阅（成对退订）；拾取射线 raycaster.layers.enableAll（沿 GizmoImpl 先例，DIAG_LAYER 对象点选不失效）。④**零契约变更**——renderMode 六态联合与诊断档会话级语义均未入 CONTRACTS（stage6-region.md 仅留 overrideMaterial 注记一句，无需修订）。⑤**主代理验收**——diff review 逐件过（dispose 泄漏路径 teardownPool 含 teardownSplit 核实）；MCP HTTP 变体视觉冒烟（示例园区 openScene 真实管线装载、session.setEnvironment 同源通道切档、强制单帧页内快照）：灰模统一中性灰遮挡保留✓、法线 +Y 顶面绿色符合世界空间编码且控制台零 shader 编译错✓、孤岛角标 N=0「全部已归类」→ChangeLayerCommand 置 null（真实命令管道）→「未归类对象：1」实时收敛+中央绿地 #FF6A45 高亮其余降暗✓；三项余项（预设组合实测/shaded 往返像素比对/下拉分组像素呈现）+两项观察项（POI 标记诊断档下原色=T6.4 继承边界注记、多遍模式状态栏 Triangles/calls 读 0=three r186 info.reset 逐 render 开头执行且末遍辅助遍常空，T6.4 wireframe 起既有口径非本次回归）**归 T8.6 验收清单**。⑥**DESIGN 登记**：新 §5.19（诊断档语义/分组下拉/角标芯片/诊断配色对照源——CLAY 0x9aa0a6、DIM 0x23272e@0.35、HIGHLIGHT 0xff6a45，视觉令牌零新增）+ §5.10 渲染下拉行指针；方法学增补 31–32 两条（MCP 桥上下文绑定雷区/run_js 传输语义与页内缩小快照法、r186 info 口径）。⑦**测试**：1787 全绿（InstancedAssetPool 诊断分组 10 用例：纯池/混合拆分/拾取反查/增量迁移/增删重算/退出恢复/单例路径/dispose；renderMode 四遍计划与令牌解析；bootstrap clay 保存→重开回退 shaded；hudModel 计数/文案；三路菜单分组）。下一任务 T8.5（场景树分组与层级拖拽，任务书已定稿） | T8.4 done：测试 1759→1787 全绿；阶段 8 进度 4/6；像素验收与观察项归 T8.6 |
| 2026-09-14 | **T8.5 完成裁定（主代理，一次过交付 + 五项偏差追认 + 契约第九次修订 + 里程碑验收全过）**：①**完成认定**——threejs-expert 子代理（改动面裁定：任务书「editor/数据层+UI 为主」但 Renderer.ts:459 未注册类型告警跳过面需静默化 group + gizmo 组壳守卫属 runtime 接线，按 PROTOCOL UI 附加规则③ 单派 threejs-expert 全任务，小地图 isRegionObject/asset 守卫天然跳过零改动）TDD 一次过（零修复循环），commit 1575049（30 文件 +2731/−90，+46 测试 1787→1833 全绿；check:layers 359 文件、tsc 0 错、build 332ms 主代理复跑全绿）；主代理 30 文件 diff review 亲审（hierarchy 森林纯函数环防御、三命令记账与幂等重放、DeleteObjectCommand 组感知 undo 数组位置复原、OutlinerPanel dnd 去抖与计时器清理、CSS 全令牌零硬编码色）。②**五项偏差追认**：(a) 接手中断会话预写测试自相矛盾（「after 锚点带子树」对实际无变化移动期望 execute=true 与同文件 no-op 用例不可兼得）——改写为真实行使用例 + 补显式 no-op=false 断言；(b) 半成品死导入清理（指向不存在 fixture 的模块级失败）；(c) evaluateDrop 拒因优先级 cycle 先于 into-non-group（后代同时非组行时报更根本的不变式违反）；(d) 命令选中联动（GroupCommand 后选中新组壳、UngroupCommand 后选中上提成员，UE 文件夹先例；undo 不回滚选中沿全命令集惯例）——任务书未禁止、链式操作入口；(e) 契约留痕归主代理（已落）。③**契约第九次修订**（阶段门 2026-09-13 预授权 + 任务书「主代理权限内」）：#7 前缀增 `group_`；SceneObject.parentId 注记翻转（自 T8.5 承载真实层级）+ 同父兄弟顺序=数组内顺序 + 'group' 四不变式注记（不传变换/删组不删成员/layerId 独立/可嵌套+环检测）；SceneManager 增补 reorderObjects（沿 addLayer 先例：未知 id 忽略、遗漏追加尾部、幂等静默、变化 emit scene:changed source=reorderObjects）；#9 补记 group 为 2.0 版本号不变前向兼容增量。④**主代理里程碑验收六项全过**（IAB evaluate 通道 + store 动态 import 读数；截图桥两次僵尸按方法学 4b⑤ 换标签恢复，场景经 saveScene→deserialize→openScene 真实管线迁移）：Ctrl+G/Ctrl+Shift+G 往返 + undo/redo 完整复原（计数/canUndo/canRedo 迁移）；拖拽三形态落位（D 挂入组2/D before A 排序/A 根区上提）+ indicator 三态 computed style 判据（into=accent-soft 底+accent-line 描边、before/after=±1px accent 插入线、forbidden=not-allowed 光标）+ 环拒绝 toast 捕获（push 包装暂存法洗清 TTL 时序伪影）+ 800ms 折叠组自动展开（持续悬停 6×220ms 后 expanded、IAB 空闲冻结挂 timer 伪影洗清）；删组仅删壳成员上提 + 4 对象 transform 逐字段不变 + 删组前后视口截图 md5 逐字节一致（ddb47fdd）；序列化往返嵌套一层无损（saveScene→deserialize→openScene 后 id/parentId/顺序全等）+ 未知类型仍路径化报错（objects[0].type）；双击组行仅选成员不含壳 + 单击选壳 Inspector 9 字段全 readOnly + 注记行 + 混合选中异类型分节不崩 + 组行菜单三分组项与「选中组内对象」路由 + 分类视图组壳 0 行 + 计数/折叠。⑤**DESIGN §5.20 登记**（主代理落，据子代理提案+亲审事实）：层级树缩进 14px/叶行色点、caret 折叠态、drop indicator 三态（琥珀=合法 accent 族/danger=禁止族）、拖拽源行 0.45 压暗、视图切换 ghost 双钮；视觉令牌零新增。⑥**两观察项归 T8.6**：组行右键「隐藏/锁定」对纯组织节点语义空转（字段存在不级联无视觉效应，可裁决隐藏）；800ms 自动展开依赖页面活跃（空闲冻结 timer 挂起，真用户交互正常）。下一任务 T8.6（阶段 8 GUI 验收 + 全量回归，主代理亲自执行，阶段收官） | T8.5 done：测试 1787→1833 全绿；阶段 8 进度 5/6；T8.1–T8.5 全一次过零修复循环；T8.6 统括 T8.1–T8.5 像素验收 + T7.2 重启复盘 + 本任务两观察项 |
| 2026-09-14 | **用户预裁两项（T8.6 终审⑤ + 完成定义提前落定，D/E/F 组验收前答出）**：① **T7.2 重启复盘关闭**——Docking/Floating/Auto Hide 维持 deferred 远期规划、不重启（终审⑤「全量验收后统一裁决」提前落定；按需门保留——布局痛点集中仍可随时过门重启并回填阶段）；② **三期 UI 重构整体交付不打 git tag**（完成定义「tag 与否听用户指令」落定为否，交付以 docs(plan) commit 收口） | 用户 2026-09-14 指示；T8.6 剩余 D/E/F 组、修复循环、四问自查、截图归档不受影响，收官时不再重复询问 |
| 2026-09-14 | **T8.6 完成裁定（主代理，阶段 8 收官——UI 重构三期交付完毕；两会话全过 + 修复循环两件闭环 + 契约第十次修订）**：①**完成认定**——主代理亲自两会话执行（首会话：守护基线 1833/359/tsc 0/build/preview + A/B/C 三组全过 + 缺陷①立案；次会话：D/E/F 组 + 四问 + 截图 + 修复循环 + 文档闭环），全程 MCP 桥页通道（锁屏深冻），零回归零修复循环返工。②**D 组全过**：三档实拍；三路同源 checked 全证（HUD 下拉/视图菜单 `#ed-menu-popup-view`/右键 `.ed-context__sub`）；下拉分组 DOM 证据（METHODOLOGY 25 代证）；shaded 往返像素 md5 逐字节一致（61fdb8ab，clay 差异确证）；night+clay 组合恢复 md5 复原；透明度组合 shaded 生效 + clay 不生效（裁设计边界）+ undo 复原基线；AUX 豁免视觉模型确认（灰模上三色轴清晰）；孤岛角标实时闭环。③**E 组两观察项裁决**：①组行隐藏/锁定空转——实证（visible 写入+入 undo 历史+成员不级联+视口 md5 不变）后**裁修复**：objectEntries 对 isGroup 单点过滤（viewportObjectMenu/outlinerRowMenu 两处共用覆盖；isGroup 填充链路核实 App.tsx:554 无缺口；附带确认组壳 NON_RENDERABLE 拾取不可达、viewport-object 侧为纵深防御），general-purpose 子代理 df06da3（+3 测试），桥页复测组行 10 项/叶行保留全对；②800ms 冻结依赖——非缺陷（T8.5 已证），注记收案。④**F 组全过**：F1 八项基线重跑（①含 T8.5 组嵌套导出/打开往返无损升级验证 + T8.2 导入预览确认流适配；③④⑤⑥⑦⑧各按原口径全过、连放 0–2ms 单 InstancedMesh count=8 共享；⑪ assetId 读数口径纠误 asset.assetId 嵌套非回归）；守护三件套四轮全绿（1833→1836→1849，360 文件，tsc 0）+ build 320ms + preview 200。⑤**缺陷①修复**：threejs-expert 子代理 2cc6d7b——GizmoPort 增 resync()（**契约第十次修订**，CONTRACTS 快照同步：attach 一次性快照后命令路径变更须重同步、dragging 自守卫）；TransformTool 激活期订阅 scene:changed→resync（source 过滤核实弃用——SceneManager 与命令层双发且不可区分；deactivate 对称退订）；GizmoImpl attach 重构共享 resolveAndApplyPose 行为零变化、组壳剔除/多选质心/全失效等价 detach 保持；+13 测试；主代理 diff review + 三件套复跑 + 桥页 reload 复测闭环（TransformCommand z=20→proxyZ 跟随 20→undo→双双归 0，修复前漂移；截图 10 号入档；拖拽中抑制按 GizmoImpl.pose 既有口径留浏览器验收、工具层 FakeGizmo 契约建模已断言）。⑥**四问自查全过**（进入=三维编辑器/编辑=Viewport·Outliner→Inspector/搭建=ContentBrowser→Viewport→Inspector/增功能=进入模式上下文零新按钮——证据三期累计+本会话实测）。⑦**观察项三件注记收案**：POI 诊断档原色=T6.4 继承边界；状态栏多遍读 0=r186 口径（方法学 32）；clay 档透明度不视觉生效=override 遮蔽样式设计边界。⑧**截图** phase8/ 14 张（新 10 张 md5 唯一 + t85 删组前后同 md5 故意证据对）；METHODOLOGY 增补 33–36 四条（合成 pointermove button:-1/dragEndCb 微任务旧值/拾取衰减 fresh 判别/正视 Z 柄投影占据）。⑨两会话用户预裁两项（T7.2 远期关闭、不打 tag）已落档 | T8.6 done：测试 1833→1849 全绿；阶段 8 完成 6/6；UI 重构三期（阶段 5/6/7/8）全部交付；STATUS 无活跃任务，新方向待用户定 |
| 2026-09-14 | **阶段 9 立项（主代理，用户口头需求收编：UI 按钮层级 + 平面模型贴地闪烁）**：用户会话提出两议题——①「优化 UI 按钮层级问题」②「模型对数深度缓冲问题（平面模型放到平面上、没加高度即闪烁）」。两轮只读勘察（Explore 代理，证据带行号，关键行号主代理亲验）定性：**①UI 层级**=z-index 四档令牌刻度健在（tokens.css:131-134，全仓 14 处均走令牌、零内联）但五大工具条为带 z-index 的 grid item 各自成层叠上下文，全仓**零 portal**、菜单/下拉/tooltip 全部内联被困宿主内部，对外按 App DOM 顺序（App.tsx:728-824）拼胜负——MenuBar 下拉被 contextbar 盖顶（MenuBar.tsx:244 × app.css:109）、contextbar 弹层压不过视口 HUD z20 芯片（ContextToolbar.tsx:399/ContextActions.tsx:265,382 × app.css:2125）、vtool flyout 盖不过 DOM 靠后的左面板列（VerticalToolbar.tsx:203,300）、主菜单子弹层被父弹层 overflow 裁剪（app.css:861-862×947-959）、Outliner 创建下拉被面板 overflow:hidden 裁剪（OutlinerPanel.tsx:426 × app.css:1261/1271）；右键菜单 fixed+尾部挂载（app.css:962-975 + App.tsx:818）为唯一正确先例=统一样板；DESIGN.md 无层叠专章=**规范缺口而非违例**；pointer-events 策略勘察确认正确非病因。**②贴地闪烁**=主相机 near=1/far=10000 已专调一轮（Renderer.ts:311-317 注释在案）、根因为模型与地面**精确共面平局**——三条创建路径+两条贴地路径全部 y=精确 0 零 epsilon（RuntimeViewport.ts:66/PlacementTool.ts:284/App.tsx:327/modelFactory.ts:27/editorActionsCore.ts:141/GizmoImpl.ts:516,818）；工程既有对症先例=网格抬 0.02+polygonOffset(-1,-1)+depthWrite:false（Renderer.ts:188-216）、区域面片 baseHeight 阶梯 0.06/0.12/0.18（semanticDefinitions.ts:126/90/82），唯独模型两头不沾；**log depth 否决预裁**（精确共面平局不因精度重分配而解；near=1 下 100m 视距≈0.6mm 精度充足；3 处自写 ShaderMaterial 无 logdepthbuf include 兼容回归面：RenderModeState.ts:134-155/grid.preset.ts/Renderer.ts:194-213）——**待 T9.1 阶段门呈报用户确认**（用户原始表述为对数深度缓冲，主方案=模型底面微抬 0.03 单一常量五路落点 + 地面 polygonOffset 双保险，承托层语义统一含道路顶/楼顶）。据此定稿任务书 **T9.1**（UI 弹层层叠治理：七处弹层逃逸+DESIGN.md 层叠专章，general-purpose）/**T9.2**（共面 z-fighting 消除：微抬+双保险，threejs-expert）/**T9.3**（阶段 9 GUI 验收+全量回归，主代理亲自，沿 T8.6 模式）；PROTOCOL 阶段门 ×9（T9.1 派发前，未过门不得派发）、里程碑表增 T9.3、threejs-expert 派发枚举增 T9.2；**无契约变更预期**（如实现需 ViewportPort 等签名变更走按需门） | 阶段 9 立项：任务 53→56（T9.1–T9.3 pending）；T9.1 派发前需阶段门 ×9，log depth 否决预裁为门议题；无代码改动 |
| 2026-09-15 | **T9.1 阶段门 ×9 过门（用户预授权自决 + 社区调研先行）**：用户经定时任务下达常设预授权原则——「遇到决策门请先调研社区成熟方案，然后根据项目实际情况，按你推荐的来，无需我确认」，本门及后续按需门均按此执行（预授权留痕即门授权依据，沿 T8.6 用户预裁先例）。①**调研先行（两议题）**：**弹层层叠**——portal 至根级/body 浮层容器为 React 生态一致标准（Radix Portal、floating-ui 官方对 overflow:hidden 裁剪的推荐解法、MUI/Chakra 同构）；具名分级 z 刻度（MUI drawer→modal→snackbar→tooltip）为设计系统主流实践，与本项目四档令牌（panel/hud/popover/toast）同构；同层 portal 间按挂载 DOM 顺序拼层为公认做法（Radix #1317/#760 已知痛点，DOM 顺序+显式令牌管理）；CSS top layer（popover API/showModal）为新兴替代但迁移面大，不采纳。**共面 z-fighting**——社区共识「精确共面两片元深度值逐位相同，任何深度缓冲精度方案（含 logarithmicDepthBuffer）都不能消除歧义」（threejsroadmap/Babylon 论坛/CG StackExchange 三源一致）；对症方案稳健度排序=epsilon 几何微抬 > polygonOffset > renderOrder+depthWrite；polygonOffset 有掠射角 caveat（因子随坡度，因子值需实测自验——任务书 §2 已有该要求）。②**门内自决五问**：Q1 弹层逃逸路线——七处弹层**首选路线 A（createPortal 至 App 根部浮层容器，z=--z-popover）**（生态标准对齐）；右键菜单既有 fixed 样板维持不动（能工作代码零改动原则）；个别弹层若 fixed 改造成本显著更低可循 ContextMenu 样板走路线 B，验收只认结果语义（任务书原条款，门不收紧）。Q2 Tooltip 取舍——**裁「保留 CSS 方案 + 换边避让（近面板侧向内弹）」**：tooltip 高频瞬态、纯 CSS 逃逸成本高（任务书注记），换边避让恰好覆盖验收项「右缘按钮 tooltip 不被相邻面板遮挡」；Radix 式 portal tooltip 属过度工程。Q3 **log depth 否决确认**——社区证据直接支持预裁三条（平局不解/精度充足/3 处自写 ShaderMaterial 兼容面），另增第四条：log depth 有实测性能代价（three.js 论坛「Beware of logarithmic depth buffer」）；维持否决，主方案=微抬+地面偏置双保险不变。Q4 lift 取值 0.03 与 y 存真值不迁移——确认（社区 epsilon 微抬为最稳方案；旧场景 y=0 由地面偏置兜底+边界注记，任务书原条款）。Q5 z 刻度规范——四档令牌数值不动、DESIGN.md 层叠专章两硬规则+calc 登记制，确认（与 MUI 式具名刻度主流一致）。③**阶段确认**：目标=层叠秩序+共面消隐两体验修补；序列 T9.1→T9.2→T9.3 不变；验收以三任务书清单为准、像素级归 T9.3；风险四项=弹层改造回归面（dialog/toast/HUD 秩序，A 组回归+T9.3 兜底）、诊断档 override 池材质偏置同步（任务书 §2 二选一留痕）、旧场景 y=0 兜底边界、无契约变更预期（如需 ViewportPort 等签名变更走按需门）。④**T9.1 获派发授权**（general-purpose，frontend-design 前置）。 | 用户定时任务预授权（2026-09-15 00:23 触发原文）；调研来源=floating-ui docs/discussion#1210、Radix #760/#1317、MUI z-index 定制文档、threejsroadmap z-fighting 指南、CG StackExchange #95/#8940、Babylon 论坛、three.js #2593/#655、discourse 88495 |
| 2026-09-15 | **T9.1 完成裁定（主代理，一次过交付 + 三项偏差追认 + 零契约变更）**：①**完成认定**——general-purpose 子代理（frontend-design 前置）TDD 一次过（零修复循环），commit 51142e6（13 文件 +635/−141，+15 测试 1849→1864 全绿）；主代理守护三件套亲自复跑全绿（1864/1864、check:layers 363 文件、tsc 0 错）+ build 冒烟 332ms + 13 文件 diff review 亲审（AnchoredPopup 测量-定位-收敛守卫/外点监听 ref 持有防重挂/popupLayer 四策略纯函数与 CSS 锚定一一对应/clamp 防负值死循环/七组件接线逐个核对贴锚 placement 与取消语义/Outliner 键盘首项按 id 全局取/CSS 死锚定清理零残留）。②**三项偏差追认**：(a) `.ed-vtool` 移除 `z-index: var(--z-panel)`——门裁定 2「保留 CSS+换边避让」在 vtool 几何下不可行（左列贴左面板：右弹被盖/左翻出屏/上下翻仍越入），按「验收只认结果语义」补最小解困：去 z 解除层叠陷阱使 tooltip 的 --z-popover 直达根层，网格分格互不重叠无视觉变化，flyout 已 portal 不依赖该 z——几何补全非路线变更，采纳并落 DESIGN §5.21 硬规则②附注（vtool 登记为 panel 档豁免：tooltip 宿主）；(b) 外点关闭范围统一为「锚+本弹层+根浮层容器」——原 containment 各异，统一后两层弹层并开互不误关、宿主工具条其他按钮点击顺带关菜单，逐案核对 hover/点击/双弹层序列等价或更优；(c) 越界收边/翻边统一化——仅在原实现同样越界（只是被盖住看不见）场景介入，沿 ContextMenu 8px 先例，支撑验收「完整可见」；另核实原 `.ed-vtool__menu` 注释「8px」实为 4px（--sp-2），按实际几何实现。③**DESIGN §5.21 层叠专章落档**（主代理落，据子代理提案+亲审事实）：四档令牌语义表/两硬规则（弹层必须逃逸+带 z 工具条内禁渲染浮层）/calc 登记制（dialog 101、splitter 11 在册）/vtool 豁免附注/backdrop-filter 注记；视觉令牌零新增。④**门裁定全沿**：路线 A（createPortal 至 #ed-popup-root）、右键菜单 fixed 样板零改动、四档令牌数值零变更、未引定位库、pointer-events 策略未动。⑤测试：popupLayer.ts 15 单测（挂载目标解析/四策略定位/收边翻边/Tooltip 换边决策），React 壳沿既定方针不进 node 测试；视觉叠放关系归 T9.3 A 组像素验收 | T9.1 done：测试 1849→1864 全绿；阶段 9 进度 1/3；下一任务 T9.2（贴地共面 z-fighting 消除，**派 threejs-expert**，任务书已定稿可直接派发，log depth 否决已经门确认） |
| 2026-09-15 | **T9.2 完成裁定（主代理，一次过交付 + 偏差追认含任务书勘误 + 诊断档更优第三结果 + 零契约变更）**：①**完成认定**——threejs-expert 子代理 TDD 一次过（零修复循环，先加 20 测试确认 19 red→实现→green），commit b45b618（16 文件 +442/−56，+20 测试 1864→1884 全绿）；主代理守护三件套亲自复跑全绿（1884/1884、check:layers 365 文件、tsc 0 错）+ 16 文件 diff review 亲审（常量五路接线逐一核对无双重抬升路径/buildTransform 与 defaultAssetTransform 各自单次施加/collectElevationSnapLevels 全模型判定/贴地命令逐对象 delta 公式/undo 零位移不入历史边界）。②**实现要点**：MODEL_BASE_HEIGHT=0.03 落 domain/assets/ModelObject（沿 MODEL_LAYER_NAME 单一数值源先例；不变式单测锁定 0.02<lift<0.06）；isModelObject 结构判别自 Renderer 私有收编入 domain（editor/runtime 单一实现）；地面 makeGroundMaterial polygonOffset factor=1/units=1（依据=精确共面斜率项相消、units 提供常量分离、与网格线 −1/−1 拉向相机先例的镜像补位；贴地表层 0.06+ 几何间距远大于 1-2 深度量子远景无劣化）；gizmo 吸附候选 collectElevationSnapLevels（拖拽目标全为模型→候选+lift，含 region/组壳/混合/空→裸值）。③**诊断档二选一现更优第三结果**：任务书预设「池内补偏置或注记可接受」二选一，实现核验发现地面在 ENV_LAYER 由环境遍渲染、环境遍 overrideMaterial 恒 'none'（主代理亲验 RenderModeState.ts:88-101），任何档位地面材质不被替换——无需补池、诊断档下不存在共面闪烁面，结论落 makeGroundMaterial docstring。④**偏差追认**：(a) 任务书断言行号勘误——:79 实为 region 断言（y→0 region 语义不变未改），模型断言实际在 :101/:112-113（悬顶面→12+lift、多选→10+lift）；(b) 工厂显式 transform/position 不抬升（调用方已定基准透传），lift 施加于承托面落点三路（buildTransform/defaultAssetTransform/工厂缺省 DEFAULT_REST_POSITION）；(c) gizmo 混合选中（模型+region）保守取裸值（region 精确贴附优先），与贴地命令逐对象判定同源不同形；(d) 3 个文件连带更新（modelFactory/PlacementTool.enhanced:425/phase1.acceptance:269）均属「按 lift 语义更新」范围。⑤**门裁定全沿**：log depth 未开启、renderOrder 未用、near/far 未动、region 阶梯零变动、y 存真值不迁移（旧场景由地面偏置兜底，边界注记在常量 docstring）。⑥回归核验：undo/redo y 往返专测、实例化 composeMatrixInto 逐字应用零改动、三渲染器不渲染主地面代码核验、阴影接收与网格淡出兼容。像素级（放置闪烁对照/连续帧 md5/手动 y=0/诊断档组合/承托层实操）归 T9.3 B 组 | T9.2 done：测试 1864→1884 全绿；阶段 9 进度 2/3；下一任务 T9.3（阶段 9 GUI 验收+全量回归，主代理亲自执行沿 T8.6 模式） |
| 2026-09-15 | **T9.3 完成裁定 + 阶段 9 收官（主代理，单会话零修复循环，56 任务全部完成）**：①**A 组 UI 层叠专项全过**——七弹层逐一核验：文件下拉（popupRoot z100+contextbar 重叠带 40-66px 四点命中全弹层）、子弹层（独立 portal、parentIsRoot=true、父弹层 sw=cw=206 无意外滚动条、右缘自由外伸 234px）、模式下拉（压过顶部中央 HUD 菜单芯片=原症状位）、阵列/对齐弹层（portal+命中，对齐钮 ≥2 选中才现形单选无钮非缺陷）、vtool flyout（208px 压过左面板三高度命中 3/3）、Outliner 创建下拉（7 项 inRoot+withinVp）、Tooltip（换边避让+z 参与链推理代证：tooltip pointer-events:none 设计使 elementFromPoint 天然跳过，非缺陷）；叠放回归=dialog dirty 确认流程实操正常+右键菜单 fixed 样板零改动+toast z1000/fixed 静态核实。**唯一疑点洗清（方法学 37 新课）**：子弹层初测未逃逸（fiber 链缺 HostPortal 层+AnchoredPopup 组件 fiber 缺失=旧版组件闭包在挂），判别链=performance 资源记录见 `?t=` HMR 时间戳 URL+304 再验证 → 跨会话遗留 dev server 变换缓存/浏览器缓存的**陈旧混合模块集**（同文件新旧代码并存）→ 重启 dev server+硬刷新后全过=缓存伪影非产品缺陷；预防=验收会话一律重启 dev server。②**B 组共面专项全过**——长椅真实点击放置管线 y=0.03（Inspector 显示真值）；**三次独立强制帧 SHA-256 全同**（98fb25d6…）；手动 y=0（规则 7b 两步提交链）精确共面**双帧全同**（c0cf4ee1…，polygonOffset 稳赢平局）；承托层双例（y=5 贴地→水面顶 0.18+0.03=0.21；y=0.1 贴地→道路顶 0.06+0.03=0.09，最近下方层语义）；诊断档像素实证（clay [154,160,166]=0x9aa0a6 精确匹配 DESIGN §5.19 登记色、normals [126,253,126] 世界空间 +Y 绿、islands 降暗；**地面像素三档逐字节全同=环境遍 override 恒 none 地面材质不被替换的 T9.2 结论在像素层锁定**）；undo/redo 0.1↔0.09 单命令迁移；saveScene→deserialize→openScene 7 对象复原+y 无损+再存 JSON 逐字节一致。③**C 组基线+守护全过**——八项冒烟（画区域矩形拖拽成域/放模型×3/多选 Ctrl 加选+对齐钮现形/图层 11 行/保存打开逐字节/环境 night↔day/网格 off↔on spacing5/资产库全链路）+T8.2 dirty 确认弹层顺带复验；守护四轮（1849 基线→T9.1 1864→T9.2 1884×终轮复跑 142 文件）+build+生产 preview 200。④**取证通道两课落档（方法学 38）**：IAB 截图桥会话中途退化（activity capture failed/超时/僵尸三态交替）——DOM chrome 证据改 elementFromPoint 命中测试双证（命中测试=真实叠放次序，比截图更确定）；视口像素走强制帧通道（contextlost/restored+同任务 toDataURL，rAF 死亡不影响，SHA-256 页内算避免大字符串传输）；连续帧证明须多次独立 evaluate 各强制一帧（同任务连拍同帧）。⑤**阶段 9 收官**：T9.1/T9.2 双一次过+T9.3 零修复循环；测试 1849→1884（+35）；56 任务全部完成；阶段 5–9（UI 重构四期+体验修补）全部交付 | T9.3 done：阶段 9 完成 3/3；STATUS 无活跃任务；新方向待用户定 |
| 2026-09-15 | **阶段 10 立项门（T10.1 派发前）——「测量与分析」，常设预授权自决（用户定时任务预授权「决策门调研社区成熟方案+按主代理推荐自决无需确认」沿 T9.1 门先例；本会话曾向用户呈报四候选方向选择式提问未获答复，按自主模式沿推荐项继续）**：**立项依据**——测量/分析为已交付产品唯一显式预留「后续版本」功能（toolIA.ts:519-538 禁用占位第 7/8 位 + README「测量/分析为禁用占位（后续版本）」+ 菜单 scene.stats 占位），其余候选（T7.2 重启=用户两度预裁维持远期不动摇；大场景性能专项/导出分享=无既定预留信号）让位。**调研先行**：general-purpose 调研代理完成成熟编辑器测量对照（SketchUp/Blender/Unity/UE5/three.js editor/Web 查看器/mars3d 数字孪生标准形态）+ 空间分析标配对照 + three.js 标签/拾取实现要点，冻结存档 archive/research-phase10-measure-analysis.md。**门内八问自决（问题→推荐→裁定）**：①契约「测量标注」排除项解禁口径→**限定解禁为会话态测量**（不入场景 JSON、不入 Command 历史；社区主流 Blender/UE5/SketchUp 读数均会话态；沿 T8.1 参考线「可见但不入历史」先例；持久化与测量点吸附维持排除入远期池）——裁定采纳；②测量类型集合→四类全做（距离折线每段+总长/高度差三读数（空间+水平+ΔH）/面积水平投影单一口径/三点角度；共享一条点击管线，角度边际成本低且 SketchUp/Blender 通行）——采纳；体积/剖面/贴地距离/可视域不做；③拾取口径→表面优先（content 对象 raycast 交点）+地面兜底+近水平限距 2000m（调研坑③）；不切顶视不锁旋转（测量常贴立面）——采纳；④标注呈现→canvas 纹理 Sprite 池化（DPR 2x/文字变更才重绘/frame() 屏幕恒定字号）+ depthTest:false 恒置顶层（调研坑①明示「永远置顶」策略），否决 CSS2D DOM 浮层（每帧投影同步+层叠秩序耦合+T9.1 刚治理完弹层逃逸）与 troika 新依赖——采纳；⑤契约通路→新 MeasurePort（updateDraft/updateMeasurements/clear，runtime 实现 MeasureOverlay）+ ViewportPort 增 surfacePoint + EventMap 增 measure:status/measure:changed；进行中草稿**不复用** updateDrawPreview（其 Vec2 地面域装不下表面 Vec3 点，草稿通道并入 MeasurePort）——采纳（=第十一次契约修订， CONTRACTS 六处落点 + 分域 contracts/stage10-measure.md）；⑥入口与键位→Alt+7=测量/Alt+8=分析（解绑预留位）；垂直条新增 measure 分组四子工具无数字键（1–4 已占不扩位）；子工具切换 ContextToolbar 芯片沿绘制五芯片先例；删除语义=Delete 删上一条+清除全部（逐条点选拾取不做，远期池）——采纳；⑦分析 V1 范围→场景统计报表（分图层数量/水平投影面积聚合，scene.stats 转正，汇报刚需零渲染成本）+日照时刻分析（时刻滑杆+季节四选+纬度，驱动太阳方位；太阳位置**自研 NOAA 简式纯函数零依赖**否决 suncalc 新依赖；sun 环境扩展键会话级保存剥离沿 renderMode 先例）；视域/通视/剖面/漫游/高程着色/遮挡时长热力图全部入远期池——采纳；⑧任务切分→T10.1 测量核心（threejs-expert）/T10.2 测量模式整合（general-purpose）/T10.3 统计报表+分析模式启用（general-purpose）/T10.4 日照分析（threejs-expert）/T10.5 阶段验收（主代理）五任务依赖序——采纳。**风险登记**：Sprite 文字缩放可读性（DPR+恒定字号对策）→T10.5 A 组像素验收；多遍渲染 shadowMap.autoUpdate 纪律与太阳方位变化交互→T10.4 静态核实；measure:status 与 draw:status 状态栏共存互斥（工具切换清空）→T10.2 测试锁定；测量拾取不得命中 AUX 层→T10.1 测试断言。**顺带修正**：CONTRACTS 变更规则段回溯补登第十次修订（T8.6 GizmoPort.resync，当时 GizmoPort 注记自记第十次但本段漏登记——编号矛盾本次洗清）。**阶段 10 任务书 T10.1–T10.5 全量定稿随本决策落档** | 阶段 10 立项完成，门已过；下一任务 T10.1 直接派发（无门）；STATUS/PROTOCOL/CONTRACTS 同步更新 | 
| 2026-09-15 | **T10.1 完成裁定（主代理，一次过 + 五偏差追认 + 契约勘正两处）**：threejs-expert 五提交交付测量核心引擎——MeasureSession 会话域（四不变式单测锁定，零场景副作用=测量全程 SceneManager/History 零调用断言）/MeasureTool 单实现类四注册键（ε=0.01m 去重作用于吸附管线产出后的点、共线拦截草稿保留、height 2 点/angle 3 点自动完成、G/Shift/A 辅助键 x/z 域 y 保留、不切顶视不锁旋转）/MeasureOverlay 覆盖层（恒驻组+Line/端点/标签三池、AUX_LAYER+depthTest:false+renderOrder 1001 永远置顶、canvas 纹理 Sprite DPR 2x 文字变更才重绘、frame() 屏幕恒定端点 4px/标签字号、dispose 含逐标签纹理全释放、node 无 DOM 跳过标签路径）/ViewportPort.surfacePoint 三分支（表面优先 content raycast/地面兜底/近水平限距 2000m）/组合根装配（单例+measure:changed→覆盖层提交层订阅 dispose 成对退订+EditorHandle.measure 门面出口+input Delete 测量语义路由沿 vertex-edit 先例）。守护：1884→1967 全绿（+83：math 12/session 5/tool 33/surfacePoint 8/overlay 20/wiring 4/路由 1）、check:layers 375 文件 OK、tsc 0——主代理亲跑三件套证实。**五偏差全部追认**：① MeasureKind 物理定义 core/types+editor 再导出（DAG：EventMap 在 core 需引用，core 为唯一公共上游）② measure:status 复位载荷=仅含 kind（初稿「空载荷 {}」与 kind 必填矛盾，取类型安全侧）③ ε 去重作用于吸附产出后（双击同位置产出相同吸附点，语义更自洽）④ input.ts Delete 路由与 EditorHandle.measure 为组合根超集（沿先例）⑤ 缺省色 0xffffff+标签深底浅蓝沿 PreviewManager（T10.2 DESIGN §5.22 令牌登记后统一调换，代码常量已注明）。**契约勘正两处已同步 stage10-measure.md**（①②对应文本，标注「2026-09-15 T10.1 落地勘正」）。**观察项两条**：distance 长链标签随点数线性增长（池上限 256 截断保护，远期增补）；height 未画垂直参考虚线（契约只要求线/端点/标签三件套，远期增补）。工作区外部残留：assets/models/building/建筑1.glb 会话中途出现（非两子代理产物、主代理未动——疑用户手动放置，呈报待用户定夺，不擅自处置） | T10.1 done：1967 全绿；阶段 10 进度 1/5；下一任务 T10.2（general-purpose UI 整合） | 
| 2026-09-15 | **T10.2 完成裁定（主代理，子代理限额中断 → 主代理接管续接交付 + 冒烟全过）**：①**中断处置**——general-purpose 子代理会话中途撞使用限额（[1308]），留下 22 文件 +768/-120 完整未提交实现（无 commit）；用户「继续任务」指令后主代理核验现场接管：跑守护见 1987/1989 两失败=旧 toolIA.test 断言未随 measure 转正更新（另 typecheck 报 MenuState mock 缺 activeToolId）——纯测试对齐两处修复后 1989 全绿+check:layers 376 文件+tsc 0+build 337ms，主代理亲自 diff review 全量过（重点：MeasureOverlay 仅三处色值常量=授权范围；布局持久化纯注释；store 桥接/MeasureSessionPort 结构子集避 ui→app 导入=架构正确），以单 commit 51b26c1 收口。②**交付面**——measure 模式四卡位转正+verticalGroup:'measure'+--mode-measure 藤紫 #ab9ee6（与琥珀参考线/幽灵蓝三方可区分，DESIGN §5.22 单一对照源+MeasureOverlay 常量同源调换）；Alt+7 解绑预留位启用；垂直条测量分组四子工具（无数字键；场景模式展开直接可见/其余模式「更多」抽屉可达）；ContextToolbar MeasureSection（四芯片即切+计数+删除上一条/清除全部空表禁用）；状态栏 describeMeasureStatus 四 kind 读数段（复位=仅 kind 清段）；工具菜单四子工具子菜单 checked 同源；SHORTCUT_HELP 增测量行。③**浏览器冒烟全链路过（新 dev server 5174——5173 被跨会话陈旧实例占用，沿方法学 37 用干净端口）**——距离两点+双击完成（ε 去重生效）、状态栏实时段长/总长 50 m（网格吸附 5m 量化正确）+坐标 X 50.00·Y 0.00·Z 5.00（吸附管线口径）与游标原值 48.85/6.78 区分清晰、完成复位、ESC 弃草稿、Delete 删上一条 1→0、空表双按钮禁用、会话跨工具退出/重激活存活（四不变式之会话生命周期）、Alt+7↔Alt+1 徽标 测量↔场景、截图 phase10/t102-smoke-measure-mode（紫藤徽标/引导卡/覆盖层线段视觉证实）。④**观察项一条记 T10.5**：测量标签 14px 字号在 720p 视口截图中小字不可辨读（数值正确性已 DOM 实证；标签视觉可读性/是否需字号或对比度微调归 T10.5 A 组像素验收）。⑤**冒烟操作课**：工具已激活时再点同名按钮=toggle 退出（首次取证图因此作废重拍——连续冒烟脚本须先核工具 pressed 态再落点） | T10.2 done：1989 全绿；阶段 10 进度 2/5；下一任务 T10.3（general-purpose：统计报表+分析模式启用） | 
| 2026-09-15 | **一期收官裁定（用户指令「将现有任务归档，一期已完成」）**：①阶段 0–9 全部交付 + 阶段 10 已交付 T10.1/T10.2（1989 全绿基线）之上，用户裁定一期完成；余量 T10.3（场景统计报表）/T10.4（日照时刻分析）/T10.5（阶段 10 GUI 验收）**不再执行，状态记 closed（未执行）随收尾归档**——非 done 非 failed，任务书原样冻结供二期复用，T10.2 遗留观察项「测量标签 720p 可辨读性」随 T10.5 关闭转入二期候选池；②`docs/plan/tasks/` 61 份任务书整体 git mv 至 archive/tasks-v1/ 冻结存档；③STATUS.md 重写为收官快照（关闭任务表：57 done + T7.2 deferred + 3 closed；进度真相源转入低维护态）；④PROTOCOL.md 顶部加收官横幅——执行协议转为历史记录，二期如重启须重新过门立项（本条即协议变更留痕）；⑤README 进度段对齐（一期收官表述 + 测试数 1884→1989）；⑥历史三日志收官条目 append-only 追加（本条 + 会话日志） | 用户显式裁定一期完成并要求归档现有任务；T10.3–T10.5 属增强功能（统计/日照）与验收收口，不在需求文档一期承诺范围内（一期承诺 100% 兑现见 archive/需求侧审核报告.md），关闭不构成承诺缺口 |
