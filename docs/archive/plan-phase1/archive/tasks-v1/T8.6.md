# T8.6 · 阶段 8 GUI 验收 + 全量回归（主代理）

> **状态：done（2026-09-14 两会话完成：首会话守护基线 + A/B/C 三组 + 缺陷①立案；次会话 D/E/F 组 + 四问 + 截图归档 + 修复循环两件交付 + 文档闭环；全部一次过零回归）** ｜ 阶段：8（UI 重构 · 高级能力） ｜ 前置依赖：T8.1–T8.5 全部 done ｜ 执行者：主代理亲自（browser-use 技能；锁屏深冻环境按 AGENTS.md 兜底走 threejs-devtools-mcp HTTP 桥页通道，METHODOLOGY 26–32 必读）

## 验收清单

- [x] **A · 高级吸附与自动对齐**（T8.1）：首会话全过（详见下方首会话交接记录）
- [x] **B · 场景模板与批量导入**（T8.2）：首会话全过
- [x] **C · 多对象编辑与工作区增强**（T8.3）：首会话全过
- [x] **D · 高级渲染模式**（T8.4）次会话全过：三档实拍（clay 统一灰/normals 世界空间/islands）；**三路同源 checked 全证**（HUD 下拉、视图菜单 `#ed-menu-popup-view`、右键 `.ed-context__sub` 子菜单——三处 checked 均随 env 同步迁移）；HUD 下拉「常规/诊断」分组 DOM 证据（1 separator + 6 项；480px 画布取证不含 DOM 按 METHODOLOGY 25 DOM 代证）；**shaded 往返像素 md5 逐字节一致**（base==back 61fdb8ab，clay 档差异确证切换生效）；night+clay 组合 + 恢复 md5 复原基线（无残留）；透明度组合：clay 档不视觉生效（override 材质遮蔽样式——裁设计边界，观察项③）+ shaded 档 opacity 0.5 生效且 undo 后 md5 复原基线（管线完好无残留）；AUX 豁免视觉模型判读确认（统一灰模上 RGB 三色轴清晰）；孤岛角标实时闭环（全部已归类 → ChangeLayerCommand 置 null → 未归类对象：1 → undo → 全部已归类）
- [x] **E · 场景树分组与层级拖拽**（T8.5）两观察项裁决：**①组行「隐藏/锁定」空转——裁修复**（实证：点隐藏 → 组壳 visible=false + 入一条 undo 历史 + 成员不级联 + 视口 md5 逐字节不变=纯空转语义误导；修复=objectEntries 对 isGroup 过滤两菜单单点覆盖，general-purpose df06da3，桥页复测组行 10 项无隐藏/锁定、叶行保留且文案随状态，「锁定」同型推定）；**②800ms 展开依赖页面活跃——非缺陷**（T8.5 已证真用户交互正常，IAB 冻结伪影，注记收案）
- [x] **F · 三阶段累计全量回归**：**F1 八项基线重跑全过**（①导出/打开/导入真实管线——升级验证 T8.5 组嵌套导出往返无损 + T8.2 预览确认流适配：注入后预览三态行「2 对象 · 2 告警」→ 确认导入 floors×3 映射 12/18 + 单历史三件套；③复制零历史/粘贴 9→10 单历史/删除 9→8 单历史各自 undo 恰复原；④图层透明度滑杆规则 19 单任务提交 0.65+undo 恰回 1.0；⑤环境预设 select night↔day 生效且 canUndo 不动；⑥网格 #grid-spacing 两步提交双同步 grid+drawGrid 且不入历史；⑦10 卡全 lazy+SVG 占位+点卡 kickThumbnail 回填真 PNG；⑧连放 8 次全链路 0–2ms + 单 InstancedMesh count=8 实例共享；②撤销重做由①③④三件套覆盖）；守护三件套两会话四轮全绿（修复前 1833→1836、修复后主代理复跑 **1849/1849**、check:layers 360 文件、tsc 0 错）+ build 320ms + 生产 preview 4173 冒烟 200
- [x] 截图存 `docs/plan/screenshots/phase8/`（14 张：首会话 3 候选全归 + 次会话 7 张 + 修复对照 1 张 + T8.5 里程碑 4 张；新 10 张 md5 全唯一，t85 删组前后同 md5 为故意证据对）
- [x] 需求文档四十四章四问自查（见收口备注）
- [x] **T7.2 重启复盘**（终审 ⑤）：**2026-09-14 用户预裁维持 deferred 远期、不重启**（原「全量验收后统一裁决」提前落定；按需门保留——布局痛点集中仍可随时过门重启并回填阶段）

## 完成定义

全部通过 → STATUS 标 done + 会话日志；UI 重构三期整体交付**不打 tag**（2026-09-14 用户预裁；交付以 docs(plan) commit 收口）。

## 收口备注（2026-09-14 次会话）

1. **修复循环两件并行交付**（改动面不相交，两会话间零冲突）：①缺陷① gizmo undo 代理漂移——threejs-expert 2cc6d7b：GizmoPort 增 `resync()`（**契约第十次修订**，CONTRACTS GizmoPort 快照同步）；TransformTool 激活期订阅 scene:changed 调 resync（source 过滤核实后弃用——SceneManager 与命令层双发且来源值不可区分，防自反馈改 TransformControls dragging 态守卫，resync 幂等不重挂）；GizmoImpl attach 重构共享 resolveAndApplyPose（行为零变化），组壳语义/多选质心/全失效等价 detach 均保持；+13 测试。桥页 reload 复测（等价路径：TransformCommand z=20 → proxyZ 同步 20 → undo → 双双归 0；修复前漂移在旧值）+ 截图 `10-gizmo-resync-after-undo-fixed.png`；拖拽中抑制按既有口径留浏览器验收（工具层 FakeGizmo 已契约建模断言）。②观察项①——general-purpose df06da3（见 E 行）。
2. **四问自查全过**：①进入页面=三维编辑器（视口主体 + 轻量 HUD 芯片 + 三栏工作区，phase7 全景/phase8 基线截图在案）；②编辑对象第一路径 Viewport/Outliner→Inspector（点选/框选→分节 Inspector→右键上下文菜单路由，三期验收累计 + 本会话 E/F 组实证；菜单三路为同源补充非必经）；③搭建场景 Content Browser→Viewport→Inspector（点卡放置连放 0–2ms → Inspector 调参；模板/批量导入加速起步）；④增功能=进入模式/上下文（T8.4 诊断档进 HUD/视图/右键三路既有分组零新按钮；T8.1 吸附进设置+工具栏开关；T8.5 分组进快捷键+大纲+右键）。
3. **观察项裁定三件**（非缺陷注记，缺陷①与观察项①除外）：POI 标记诊断档原色=T6.4 继承边界；多遍模式状态栏读 0=r186 info.reset 口径（METHODOLOGY 32）；clay 档图层透明度不视觉生效=override 材质遮蔽样式的设计边界（数据保存与常规档生效均实证）。
4. **自动化伪影记录不立项**：`#ed-layout-popup` 偶发处于打开态且 body 合成点击不关（组件关闭挂 document pointerdown，合成 click 不触发——真用户路径无此态）；菜单项查找偶发空读=菜单开合残留时序（方法学 18⑥），重走即过。
5. **通道**：全程 MCP 桥页（HTTP 9300/桥页 9299，SID 沿用）；桥页 reload 自动重挂再验证一次；截图 __T86SHOT 强制单帧法（方法学 31）。

## 剩余节（2026-09-14 首会话交接，下会话从此续做）

**已完成**：守护基线（1833/1833、359 文件、tsc 0、build 324ms、preview 4173 冒烟 200）；**B 组 T8.2 全过**（四区子菜单/示例园区 id 重生成 overlap=0/dirty 三行摘要+确认替换+非 dirty 直通/tpl_ 存储+改名+删除/桥页 reload 刷新存活/三态行+取消零副作用+确认单历史三件套）；**A 组 T8.1 全过**（设置四分项+容差+步长档改 45° 联动+总开关压下不改分项记忆/贴地 15→12 楼顶面层+20→18+多选 BatchCommand/网格吸附拖拽 z=10 精确/对齐参考线 liveZ=20 精确+guides visible+松手即逝+截图/道路边缘 14.35→14 精确/Ctrl 反转 20 vs 19.46 raw 对照/角度步进 15→30→75 连续 15°k/高度层 grid-off raw→15 精确/顶点编辑 A 键 angleLock 双向+Esc/拖拽 undo 多轮）；**C 组 T8.3 全过**（区域×2 公共节+建筑×1/道路×1 分节+高度改值仅建筑生效+undo 复原/图层 Mixed=__mixed__ 提交+undo 完整复原/批量重命名前缀+2 位填充预览楼01/楼02+确认单历史+查找替换 办公→宿舍 只改匹配+undo/布局保存→行内重命名→导出 blob 捕获→删除→导入恢复全往返/图层合并 POI→建筑 ×3 单历史 undo 南门回 POI 层/标签亭 1/10→+搜索车 0/10 AND→清恢复 10/10/console.error+warn 双零）。

**缺陷①（唯一，待修复循环）**：gizmo 拖拽提交后 undo（键盘 Ctrl+Z 真实路径），场景对象回滚但 gizmo 代理不重同步（实测 sceneZ=0 vs proxyZ=20，视觉手柄漂移；数据路径正确——命令 before/after 用场景真值；Q→W 工具切换即恢复）。根因方向：GizmoImpl 无 scene:changed 订阅、TransformTool 仅 selection:changed 重挂（TransformTool.ts:68-72）。截图 `t86-a-defect-gizmo-after-undo.png`。修复派 threejs-expert（runtime 面）。

**剩余**：D 组（T8.4：三档+HUD/视图菜单/右键三路同源 checked、shaded 往返像素 md5、下拉「常规/诊断」分组、环境 night+clay 组合无残留、AUX 豁免 gizmo 抽验、孤岛角标；状态栏多遍读数=0 系 r186 口径注记非回归——METHODOLOGY 32）；E 组两观察项裁决（组行隐藏/锁定空转、800ms 冻结依赖——右键菜单已见入口，纯裁决）；F 组（F1 八项基线重跑，t78-f1* 脚本可复用 + 生产 preview 页面级冒烟 4173 已起）；四十四章四问自查；T7.2 重启复盘已关闭（2026-09-14 用户预裁维持远期，decisions 已落）；截图归档 phase8/ ≥8 张 md5 唯一（已有 3 候选在 `.zcode/investigate/t86/`：drag-mid/align-guides/defect）；METHODOLOGY 正式增补（合成 pointermove 必须 button:-1、dragEndCb 微任务异步致 up 同任务读数为旧值、合成交互累计致 gizmo 拾取衰减-fresh 判别实验定案伪影、正视中心区 Z 柄投影占据）；文档闭环 + commit。

**通道与现场**：宿主锁屏（LockApp active）→ IAB 深冻不可用 → 全程 MCP 桥页（HTTP 9300 / 桥页 9299，SID=`t86.sid`，封装 `.zcode/investigate/t86-call.sh <js>`、截图 `t86-shot.cjs`/`t86-shot3.cjs` + 页内 `__T86SHOT()` 强制单帧缩小快照）；桥页 reload 后自动重挂（多次验证）；store 引导脚本 `t86-01-state.js`（window.__T86S 暂存）；dev 5173 与 preview 4173 均在跑。桥页 localStorage 有本会话遗留（workspace-layouts 含「T86 改名布局」、templates 空、t78-marker）——不影响验收，D 组前可顺手清或忽略。
