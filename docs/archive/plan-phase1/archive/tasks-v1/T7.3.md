# T7.3 · UI 重构 P1：工作区布局预设与持久化

> **状态：done（2026-09-13）** ｜ 阶段：7（UI 重构 · 专业交互） ｜ 前置依赖：T7.1（原 T7.2 延后远期，2026-09-12 门裁定，依赖改挂） ｜ 执行者：general-purpose（纯 UI/store/纯函数，零 runtime/three 改动） ｜ 需求原文：`docs/plan/archive/ui升级.md` 第二十九章/第三十章

## 范围

- 四布局预设：默认（Scene/Viewport/Inspector/Content Browser）/ 极简（仅 Viewport）/ 搭建（强化 Content Browser + Scene）/ 分析（强化 Viewport + Inspector）。
- 自定义工作区：面板尺寸/显隐/标签页状态可保存为个人布局（localStorage，UI 唯一真相源，沿 T2.1 收藏先例）、恢复默认、重新进入编辑器恢复上次工作状态（需求 30 章末段）。
- 顶部「布局」按钮（T5.2 占位）启用：预设切换 + 保存/管理个人布局。
- **门裁定边界（2026-09-12）**：T7.2 面板自由停靠延后远期——「布局」在本任务 = 既有四区框架内的显隐/尺寸/标签状态（含 T7.1 的 workspaceStore.mode 持久化与「恢复默认」重置为 scene）；无停靠位概念。

## 验收方向

四预设一键切换且布局正确；自定义布局保存/恢复/删除；刷新页面后工作区状态还原；布局数据与场景数据解耦（清场景不清布局）。测试：布局序列化纯模块 + localStorage 桩。

## 产出文件

- Create：`src/ui/layout/layoutPresets.ts`（四预设数据表 + 快照序列化/校验钳制 + matchPreset + 命名布局 CRUD，可注入最小 storage）、`src/ui/layout/workspacePersistence.ts`（mount 恢复 + 订阅防抖 300ms 写回，storage/debounceMs/deps 三路可注入）、`src/ui/menus/LayoutMenu.tsx`（顶栏布局弹层：四预设 radio/个人布局/保存命名/恢复默认）、`tests/ui/layout/layoutPresets.test.ts`（26 例）、`tests/ui/layout/workspacePersistence.test.ts`（12 例）
- Modify：`src/ui/layout/workspaceStore.ts`（applyLayout 一次性应用入口；mode 注释换持久化口径）、`src/ui/menus/MenuBar.tsx`（布局按钮解禁挂 LayoutMenu；文件头边界放宽注记）、`src/App.tsx`（initWorkspacePersistence 一行接线）、`src/ui/styles/app.css`（.ed-layout__* 一节，令牌零新增）、`src/ui/index.ts`、`src/ui/tools/toolIA.ts`（注释口径同步）、`README.md`（工作区布局章节 + 操作表行）、`tests/ui/layout/workspaceStore.test.ts`（+5 例）
- DESIGN.md §5.14 由主代理验收后落地（子代理报告建议条目，见执行留痕）

## 验收清单

- [x] 四预设一键切换且布局正确——LAYOUT_PRESETS 数据表（default=INITIAL / minimal 三区全隐 / build 底部 280 展开+右隐 / analysis 左底隐+右 360），尺寸全在钳制域内，数据完整性用例锁定
- [x] 自定义布局保存/恢复/删除——命名布局 CRUD（localStorage `t3d-editor.workspace-layouts`，重名覆盖/空名拒绝/损坏容错）+ 弹层「保存当前布局…」行内命名（重名「将覆盖」提示）；预设与个人布局**不含 mode**（工作态非布局态，T7.1 裁定），唯一例外「恢复默认」= default 预设 + setMode('scene')（T7.3 门裁定）
- [x] 刷新页面后工作区状态还原——initWorkspacePersistence：mount 恢复 `t3d-editor.workspace` 快照（布局字段 + mode；mode 经 MODES enabled 校验，measure/analysis 不可恢复落 scene）→ 恢复之后启动订阅（防 INITIAL 覆盖用户快照）→ 防抖 300ms 写回；StrictMode 双挂载幂等
- [x] 布局数据与场景数据解耦（清场景不清布局）——布局只进 localStorage 两个独立键，不触碰 SceneData/serializer/EditorFacade/EventBus（分层 DAG 强制 ui 禁 io，结构上不可耦合）
- [x] 顶栏「布局」按钮（T5.2 占位）启用——LayoutMenu 弹层三段（预设 radiogroup + matchPreset 命中高亮/自定义态、个人布局列表应用+删除、保存命名卡 + 恢复默认）；role=dialog（含 radiogroup 与文本输入，不用 menu 角色）；Esc/点击外部关闭回焦、↑↓ 预设组循环聚焦
- [x] node 测试：序列化纯模块 + localStorage 桩全覆盖——新增 43 例（layoutPresets 26 + workspacePersistence 12 + workspaceStore 5），1369→1412 全绿
- [x] 视图菜单 view.workspace 占位维持不动（本任务入口仅顶栏按钮，避免菜单契约快照 churn）

## 完成定义

守护三件套（npm test 1412 / check:layers 305 文件 / typecheck 0 错）全绿；build 冒烟 312ms 产物齐全；`src/ui` 零 runtime/io 导入、零 THREE；commit `feat(ui): workspace layout presets and persistence (T7.3)`（0e64e38）。弹层视觉/持久化实况目检项归 T7.8 阶段验收（沿 T2.1/T7.1 先例）。

## 执行留痕（2026-09-13）

- **一次过交付**：general-purpose 子代理 TDD 全程（先测后码，三新测试文件先红再绿），commit 0e64e38（13 文件 +1697/−13），无修复循环。
- **主代理 diff review 亲审通过**：layoutPresets 纯模块（sanitize 字段级回退/钳制/mode enabled 校验/matchPreset 不比 mode/CRUD 容错逐段核）；workspacePersistence（订阅在恢复后启动、防抖收敛、dispose 清定时器）；applyLayout 深拷贝入参 + 钳制入口（预设表无引用污染风险；workspaceStore↔layoutPresets 的 import type 循环为类型级、编译期擦除）；LayoutMenu（radiogroup 键盘惯例/role 语义/空名守卫）；app.css 令牌零新增复用既有原语。
- **六项裁量偏差（子代理报告，主代理追认）**：①MenuBar 边界局部放宽——弹层直连 workspaceStore（沿 ContextToolbar/ZoneToggle 先例），不经 App props；②弹层 ARIA 用 dialog 而非 menu（menu 角色不允许嵌输入框）；③browser.category 校验口径 = 非字符串/空串回退 'all'（开放 slug 域无法枚举）；④deserialize 字段级回退而非整体丢弃；⑤弹层应用后不自动关闭（matchPreset 高亮实时反馈）；⑥个人布局保存载荷写入前也过 sanitize。
- **主代理裁定一项**：DESIGN.md §5.13 尾行「mode 为内存态不落盘」随 T7.3 落地过时，更新为「随工作区快照持久化（T7.3 落地，见 §5.14）」；§5.14 条目按子代理建议文本落地。
- **留 T7.8 目检清单**（子代理报告六项）：弹层视觉（右缘锚定/三段间距/高亮与空占位观感）；交互实况（关闭回焦/↑↓ 聚焦/Enter 保存/重名提示/hover 浮显删除）；持久化实况（改布局切模式刷新恢复、mode 回落、隐私模式降级、StrictMode 无 INITIAL 闪变）；触发钮触达 ≥40px；存储两键实况与损坏回退；Splitter 拖拽防抖收敛。
