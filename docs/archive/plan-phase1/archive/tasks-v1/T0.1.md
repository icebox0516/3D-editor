# T0.1 · 工程脚手架与守护设施

> **状态：done**（仅主代理更新） ｜ 阶段：0 ｜ 前置依赖：无 ｜ 注入提示词：第 1 组（仅骨架部分，见偏差）
> 🚪 阶段门：本任务派发前须经 PROTOCOL.md「任务前置拷问门」阶段级 grilling 确认（阶段目标/任务序列/验收/风险）。

## 必读上下文

1. `docs/plan/CONTRACTS.md`（全局约束 + 接口契约 + 目录结构）
2. `docs/plan/archive/三维园区可视化编辑器需求文档.md` §总体架构·六层架构

## 注入提示词（原文）

> 帮我搭建一个三维园区编辑器的核心项目骨架，严格遵循六层单向依赖架构：core -> scene -> domain -> editor -> runtime -> io。
>
> 【核心约束】
> 1. 全程严格遵守「业务数据与 Three.js 渲染对象彻底分离」原则，业务层绝对不能直接操作 THREE.Object3D
> 2. 所有层单向依赖，下层不能依赖上层
> 3. 纯 TypeScript 实现，只输出逻辑代码，不做 UI
>
> 【任务】
> 1. 初始化目录结构，严格按照以下目录输出文件：src/{core/{id,events,math,types,utils},scene,domain,editor,runtime,io,registries}
> 2. 实现 core 层全部基础能力（types/id/events/math/utils）
>
> 【输出要求】每个文件输出完整的 TS 代码，带类型定义，注释清晰标注每个模块的职责和边界。

## 偏差与补充（优先级高于上方提示词）

- 提示词「不做 UI、纯 TS」在本任务仅指**不写功能 UI**；仍需 Vite + React 壳（后续里程碑要浏览器验收）。占位页不需要设计投入（纯空 div），UI 设计系统在 T1.8 用 frontend-design 技能确立。
- **本任务不实现 core 逻辑**（那是 T1.1），只搭骨架 + 守护脚本，`src/` 各层先放 `index.ts` 占位导出；目录含 CONTRACTS.md 所列的 `ui/`、`app/`。
- **依赖版本策略**：安装时一律 `npm install <pkg>@latest` 并提交 lockfile，禁止旧版本。2026-09 快照（`npm view` 实测）：typescript 7.0.2、vite 8.2.2、vitest 5.0.0、react 19.2.8、three 0.186.0、zustand 5.0.15、@testing-library/react 16.3.3、@types/three 0.185.4（最新即可，允许与 three 小版本错位）、@types/react 19.2.18。若某依赖最新版与工具链不兼容（如 TS7 与某 vite 插件），回退最近可用大版本并在返回报告中记录原因。

## 产出文件

- Create: `package.json`、`tsconfig.json`、`vite.config.ts`、`vitest.config.ts`、`index.html`、`src/main.tsx`、`src/App.tsx`（空视口占位 div）
- Create: `scripts/check-layer-deps.mjs`
- Create: `src/{core,scene,domain,registries,editor,runtime,io,ui,app}/index.ts`（空占位）
- Create: `.gitignore`（node_modules/dist/.vite）
- Modify: `README.md`（已存在的根目录指引；在「当前进度」一节前插入「运行方式」：dev/build/test/typecheck/check:layers 命令，保留其余内容不动）

## 接口（Produces，后续所有任务依赖）

- npm scripts：`dev` / `build` / `preview` / `test`(vitest run) / `test:watch` / `typecheck`(tsc --noEmit) / `check:layers`
- `scripts/check-layer-deps.mjs`：扫描 `src/**/*.{ts,tsx}` 的 import，按 CONTRACTS.md 分层 DAG 与 three 白名单（three 仅 `src/runtime/**`、`src/app/**`、`src/main.tsx`）校验；违规输出 `文件 -> 违规导入 -> 原因` 并 exit 1。`tests/**` 不受 DAG 限制但受 three 白名单限制（runtime 单测允许 three）。

## 验收标准

- `npm run dev` 启动并显示占位页面；`npm test`（0 测试时通过）、`npm run typecheck`、`npm run check:layers` 全绿。
- 故意写一个越层导入能被 `check:layers` 报错（自测后移除）。

## 步骤

- [x] 1. 初始化 package.json / 安装最新依赖 / 五个配置文件（git 仓库已存在）
- [x] 2. 编写 `scripts/check-layer-deps.mjs`（正则解析 `from '...'` 即可，无需 AST 依赖）
- [x] 3. 临时越层导入验证脚本生效，然后删除临时代码
- [x] 4. 运行四条守护命令全绿
- [x] 5. commit：`chore: scaffold vite+ts+react project with layer guard`
