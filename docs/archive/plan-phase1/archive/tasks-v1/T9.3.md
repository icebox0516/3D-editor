# T9.3 · 阶段 9 GUI 验收 + 全量回归

> **状态：done（2026-09-15 单会话全过——A/B/C 三组零修复循环；含一次陈旧 dev server 缓存伪影洗清（方法学 37）；截图 10 张 phase9/ + 强制帧像素通道；METHODOLOGY 增补 37–38）** ｜ 阶段：9（体验修补：层叠秩序 + 共面消隐） ｜ 前置依赖：T9.1、T9.2 ｜ 需求原文：阶段验收惯例任务（沿 T8.6 模式）
> **派发注意**：**主代理亲自执行**（浏览器 GUI 验收不下放，沿 PROTOCOL/browser-use 约定）；验收前必读 `docs/plan/METHODOLOGY.md`。

- **A 组 · UI 层叠专项**（T9.1 逐项像素验收，截图留证）：
  - [x] MenuBar 各下拉完整可见（popupRoot 挂载 z100 + 与 contextbar 重叠带 40-66px 四点命中全弹层 + 截图 a1）；子弹层右展无裁剪无意外滚动条（独立 portal，parentIsRoot=true，父弹层 sw=cw=206 无横滚，右缘自由外伸 +234px，截图 a1b）——初测异常经方法学 37 判别为跨会话 dev server 304 陈旧混合模块伪影，重启后全过
  - [x] ContextToolbar 模式下拉压过顶部中央 HUD 菜单芯片（弹层 4 点命中含芯片位，截图 a2）；ContextActions 阵列弹层（portal+z100+左视口 HUD 区命中，截图 a2b-array）与对齐弹层（多选 ≥2 才现钮——单选无钮非缺陷；portal+命中，截图 a2b-align）
  - [x] VerticalToolbar flyout 覆盖左面板（portal+208px 重叠带三高度命中 3/3；像素照因截图桥中途退化以 DOM 证据收案，方法学 38）
  - [x] Outliner 创建下拉不被 `.ed-panel`/`.ed-side` 裁剪（portal 后 inRoot+withinVp，7 项完整）
  - [x] Tooltip 取舍落档（换边避让实现 + vtool 去 z 解陷；叠放以 z 参与链推理代证——tooltip pointer-events:none 设计使命中测试天然跳过，方法学 38）
  - [x] 叠放回归：dialog dirty 确认弹层 B2 流程实操正常（scrim/card 秩序）；右键菜单（fixed 样板零改动）；toast z1000/fixed 令牌与 DOM 尾位静态核实（T9.1 零触碰，瞬态像素照未捕获以静态证据收案）；HUD/axes/minimap z20 未动
- **B 组 · 共面专项**（T9.2 逐项）：
  - [x] 平面模型放置到空地：长椅真实点击放置管线 y=0.03（Inspector 显示真值）；连续帧稳定性=三次独立强制帧 SHA-256 全同（98fb25d6…，方法学 38 通道）；帧像素归档 b1-lift-0.03-frame.png
  - [x] 手动 y=0 兜底：Inspector 两步提交链（规则 7b）y→0 精确共面后双帧 md5 全同（c0cf4ee1…）——地面 polygonOffset 稳赢平局零闪烁；对照帧 b1-coplanar-y0-frame.png（修复前视觉口径=精确共面态，pre-T9.2 无偏置构建的闪烁复现以根因勘察留档代证）
  - [x] 贴地命令承托层语义：示例园区（6 区域）长椅 y=5 贴地落水域顶 0.18+0.03=0.21；y=0.1 贴地落道路顶 0.06+0.03=0.09（最近下方层语义双例实证，右键菜单真实路径）
  - [x] 诊断档三档：clay 中部像素 [154,160,166]=0x9aa0a6 精确匹配 DESIGN §5.19 登记色、normals=[126,253,126] 世界空间 +Y 绿、islands 降暗；地面像素三档逐字节全同（199,202,202）=环境遍 override 恒 none 地面材质不被替换的 T9.2 结论像素实证；三档帧归档 b3-diag-*
  - [x] undo/redo 往返 y 稳定（0.1↔0.09 单命令迁移）；保存/打开往返 saveScene→deserialize→openScene 后 7 对象复原、y=0.09 无损、再存 JSON 逐字节一致
- **C 组 · 基线重跑 + 守护**：
  - [x] 八项核心链路冒烟：画区域（矩形拖拽成域第 8 对象单历史）/放模型（三把长椅真实管线）/多选（Ctrl 加选 2+对齐钮现形）/图层（11 行渲染正常）/保存打开（B 组逐字节往返）/环境（night↔day 同源通道）/网格（off↔on+spacing 5）/资产库（分类芯片+卡片+放置）；另 T8.2 dirty 确认弹层流程顺带复验
  - [x] 守护三件套四轮全绿（基线 1849→T9.1 1864→T9.2 1884×2 终轮复跑 142 文件/1884 用例/365 文件/tsc 0）+ build 成功 + 生产 preview 冒烟 200
- [x] 收尾：截图 10 张入 `docs/plan/screenshots/phase9/`；METHODOLOGY 增补 37–38 两条；文档闭环（两任务书 done 已落档、STATUS、decisions、tasks-done、sessions）

## 明确不做

- 不做新功能；两任务缺陷走修复循环（≤2 次，超限 blocked 呈报）。（本会话零缺陷零修复循环——A 组唯一疑点经方法学 37 判别为缓存伪影洗清）
