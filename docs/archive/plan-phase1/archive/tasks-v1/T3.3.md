# T3.3 · 绘制模式 UI 与流程串联

> **状态：done**（仅主代理更新；2026-09-09 commit a54567e，684 测试全绿） ｜ 阶段：3 ｜ 前置依赖：T3.2 ｜ 注入提示词：第 10 组任务 4 ｜ **UI 任务**（先读 frontend-design 技能）

## 必读上下文

1. `docs/plan/CONTRACTS.md`（重点：ElementRegistry、draw:status）
2. `C:\Users\admin\.agents\skills\frontend-design\SKILL.md` + `src/ui/styles/{tokens.css,DESIGN.md}`（复用设计系统）
3. `docs/plan/archive/三维园区可视化编辑器需求文档.md` §绘制模式·绘制流程、§吸附、网格与视图
4. 前置源码：`src/editor/tools/draw/**`、`src/runtime/renderers/RoadRenderer.ts`、`src/ui/**`

## 注入提示词（原文，第 10 组任务 4）

> 4. 绘制流程：
> 选择要素类型 -> 选择几何类型 -> 开始绘制 -> 完成几何 -> 创建 Element -> 应用默认样式 -> 生成 Command -> 进入场景

## 偏差与补充（优先级高于上方提示词）

- UI：左侧「绘制」面板 = 要素类型网格（building/road/green/water/parking，来自 `ElementRegistry.list()`）→ 几何类型（按 definition.geometryTypes 过滤）→ 点击激活对应 Draw 工具（params: `{ elementType, perspective: false }`）。
- 顶部工具条：「选择/移动/绘制」模式切换 + 视图按钮（透视/顶/前/侧、F、Home）；底部状态栏显示 draw:status（长度/面积/坐标/错误提示）。
- GridSettings：网格显示开关、尺寸与间距配置（作用于 gridSnap 步长与视口网格渲染）。
- 道路 centerline 模式补全：线绘制完成后 properties.width 默认 6，RoadRenderer 补 centerline 分支（LineString + width → 带状面，端头圆角为显式非目标，主体几何必须正确）。

## 产出文件

- Create: `src/ui/panels/DrawPanel.tsx`、`src/ui/components/{ModeSwitch.tsx,StatusBar.tsx,ViewButtons.tsx,GridSettings.tsx}`（UI 复用设计系统）
- Modify: `src/runtime/renderers/RoadRenderer.ts`（centerline 分支）、`src/App.tsx`（布局：左资产/绘制、右属性、下状态栏）
- Test: `tests/ui/DrawPanel.test.tsx`、`tests/runtime/RoadRenderer.centerline.test.ts`

## 接口（Produces）

DrawPanel（Registry 驱动）；StatusBar 订阅 draw:status → store。

## 验收标准

- DrawPanel 选项由 Registry 驱动（注册假要素类型自动出现，测试验证）。
- RoadRenderer：直线 (0,0)→(10,0) + width 6 → 带状面宽 6、长 10、法线朝上。
- 状态栏随绘制事件更新（事件 payload → store → 渲染，测试断言）。
- 浏览器串测留给 T3.4。
- 守护三连全绿。

## 步骤

- [x] 1. 失败测试：DrawPanel registry 驱动 / centerline 渲染几何 / 状态栏数据流
- [x] 2. 实现通过（复用 tokens.css / DESIGN.md）
- [x] 3. 守护三连
- [x] 4. commit：`feat(ui): draw mode panel, status bar and centerline roads`
