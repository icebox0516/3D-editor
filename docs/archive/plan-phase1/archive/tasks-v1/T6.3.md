# T6.3 · 样式库迁移与新增预设

> **状态：pending** ｜ 阶段：6（形状驱动创建与样式引擎） ｜ 前置依赖：T6.2 done ｜ 执行者：threejs-expert ｜ 需求来源：《绘制需求变更.md（定稿 v2）》第五.5 节 ｜ 分域契约：contracts/stage6-region.md

## 任务背景

样式库按三栏处置对齐代码现状 19 套（src/registries/definitions/styles.ts：building×5 / road×4 / green×3 / water×3 / parking×2 / marker×2，无 plaza、无 base 通用），**新库合计 21 套**（明细与需求文档 5.5 节一致）。旧 StyleDefinition 体系保留共存（旧要素仍在用，T6.7 删除）。

## 验收清单

- [ ] **迁移 10 套**（每套一文件，**同文件导出 `meta`（纯数据）与 `build`**——meta 经 T6.2 组合根收割机制入 StylePresetRegistry；supportedShapes/supportedSemantics/defaultParams 按新语义标注；缩略图先用静态色块/SVG 占位）：
  - water×3：standard / dynamic / tech
  - green×3：grass / landscape / tech（supportedSemantics 标注为 grass）
  - parking×2：standard / tech
  - road×2：standard / asphalt（线状；带宽几何由构建器供给，插件只管材质）
- [ ] **新增 9 套**（无旧对照，按新设计验收）：plaza×3 + base 通用×3 + poi×3——poi = 图标牌（Sprite/Billboard）、光柱（半透明圆柱 Shader）、地面标记（环形贴地）
- [ ] **改造 2 套**：building 保留 default / modern，改造为挤出预设——在 build 内按 `params.semantic.height` 对平面轮廓挤出生成立体（材质分层可选：墙面/顶面；引擎已将 semantic.properties 以保留键 `semantic` 并入 params，分域契约 §C）；height 参数变化经 setGeometry 或 rebuild 路径正确反映
- [ ] **废弃不迁移**：building×3（glass/tech/night）与 road×2（tech/night）随预设库重设计废弃；marker×2 随旧要素体系于 T6.7 删除（本任务不动）
- [ ] 参数元数据完整：所有插件 defaultParams 沿用 StyleParameter 形态（key/label/type/default/min/max/options），可直接驱动属性面板表单（T6.6 消费）
- [ ] 视觉回归对照：**仅对迁移 10 套执行**——与旧渲染对照（每类要素建同参数对象截图比对，允许无损压缩差异；管线为 MaterialFactory 旧路径与插件新路径双跑对照）；新增 9 套与改造 2 套按新设计验收，无旧对照
- [ ] 预设过滤：按 shape×semantic 过滤结果与 meta 声明一致（单测矩阵）

## 完成定义

守护三件套全绿；对照截图存任务报告；commit `feat(runtime): migrate 10 presets, add 9 new (plaza/base/poi), rebuild 2 building extrude presets (T6.3)`。
