# T7.8 · 阶段 7 GUI 验收 + 全量回归（主代理）

> **状态：done（2026-09-13 三次会话收口；九项全过零缺陷，详见各项勾选）** ｜ 阶段：7（UI 重构 · 专业交互） ｜ 前置依赖：T7.1、T7.3–T7.7 全部 done（T7.2 延后远期，2026-09-12 门裁定） ｜ 执行者：主代理亲自（browser-use 技能）

## 验收清单

- [x] 工作模式六态联动抽查（每启用模式一次完整切换流：徽标/垂直条折叠/引导卡/HUD 引导逐项可观测）；三路切换入口一致（选择器/Alt+1..6/菜单）；measure/analysis 灰显 + tooltip；绘制态徽标临时覆盖与恢复 —— 前半会话过（选择器批量巡检+主菜单子菜单+Alt/Tab 键盘代证）；后半会话补像素证据 10a/10b（建造/道路切换 + store mode 断言）+ 模式弹层快照复证 measure/analysis [disabled]
- [x] 上下文工具矩阵按对象类型出现且动作真实生效：建筑（复制/轮廓编辑/楼层步进可撤销）/道路（节点编辑/宽度步进可撤销/分割合并）；对齐/阵列在 T7.6 后为启用态；占位项 disabled+tooltip —— 前半会话过（建筑/道路动作+单条历史）；后半会话补齐合并实操（项⑥内）
- [x] 布局四预设切换 + 自定义布局保存 → 刷新页面恢复（含模式持久化）；布局与场景数据解耦 —— 前半会话过切换/快照/解耦（指纹 8560B 不变）；后半会话补刷新恢复（极简→reload→zone 0px/hiddenPanels true 全复原；恢复默认亦过）
- [x] Tab 纯三维模式进出 + 布局还原 —— 前半会话过（菜单进/退 260px 精确）；后半会话补像素证据 11（p3d=true 断言+全屏视口+芯片）+ 退出还原复证
- [x] 多选批量编辑（改高度/图层/材质各一条历史）+ Mixed 显示语义 —— 后半会话完成：两建筑多选 → 汇总条「已选 2 个对象」+ 高度 Mixed（placeholder='Mixed'+ed-input--mixed+title 提示）；批量高度 12/20→15/15 单历史三件套（undo×1 恰复原+redo 复现）；批量图层 建筑→绿地 单历史 undo 复原（截图 05）
- [x] 对齐/吸附/阵列三工具操作 + 撤销；道路分割/合并 + 撤销 —— 后半会话完成：对齐弹层八项全显（3 对象均布启用）+ focusin 足迹预览（Check 标记）+ Z 均布执行 gap 6m 等距单历史 undo（截图 06）；阵列 popover 3×10m 东 实时预览 hint 刷新+确认 3 副本 posX 10/20/30+副本集自动选中+单历史 undo 9→6（截图 07）；道路分割节点 (-15,2) 点击 4mm 精度两段拆分+合并点列完整复原+各自 undo 闭环（截图 08，MCP 桥页 run_js 通道）
- [x] 小地图定位/拖拽/开关 —— 后半会话完成：点击跳转（中心 0,0→-38,-22 西北向）+ 拖拽平移（→24.8,11.8 东南向）经 groundPoint 差分实证；齿轮关/主菜单 view.minimap 开 两路 + display none/'' 断言；刷新持久往返（关态 reload 后仍 none+快照 false）
- [x] 一期 + 阶段 5/6 核心回归（F 组全量）+ 守护三件套 + 生产 preview 冒烟 —— **三次会话（F1 八项 MCP 桥页 run_js 通道全过）**：①打开/导出真实管线=菜单 文件→导出场景 → createObjectURL 钩子捕获 8829B JSON（ver 2.0/6 对象/11 图层/day）；打开往返=新建 0 对象 → 菜单 打开… → DataTransfer 注入 `input.files`+change → 6 对象原名全恢复；导入 JSON=菜单 导入 JSON… 注入 2 楼 → floors*3 映射 12/18 + 环自动补闭合告警 + 单历史三件套（undo×1 恰回 6/redo 复现 8）；②撤销重做=⑤⑥已大面覆盖（批量高度/图层、Z 均布、阵列、道路分割/合并各自 undo×1 恰复原+redo 复现）；③复制=剪贴板置位零历史、粘贴 8→9 偏移 +1m 副本自动选中单历史、删除 9→8 单历史，各自 undo×1 恰复原；④图层透明度=规则 19 单任务 2 帧 input+1 pointerup 提交 1→0.65、undo×1 恰回 1.0（单历史）；⑤环境预设=Inspector 环境设置 select night↔day、canUndo/canRedo 不动=不入历史设计语义；⑥网格=全局设置 #grid-spacing 5→10 两步提交（规则 7b）、grid.spacing=10 且 drawGrid.spacing=10 吸附步长同步、不入历史；⑦缩略图=10 卡全 `loading=lazy`+SVG 占位 src 正确，点卡交互后 kickThumbnail 离屏快照回填 `data:image/png`（asset_car 实证，即 T5.9「2 真 PNG」机制——交互数即真 PNG 数）；隐藏标签页原生 lazy 网络推迟为环境伪影（visibilityState=hidden，对照非 lazy 同 URL 秒载 96px 洗清，方法学 30）；⑧InstancedMesh 连放=资产卡点击激活放置工具+8 次合成 pointerdown 全链路 1–3ms（含事件路由+OrbitControls+ghost），editor 层净口径（groundPoint+共享工厂+CreateObjectCommand）**0/0/0/0/0/1/1/1ms 恰 0–1ms**；实例共享=RuntimeObjectMap.byId 33 条 → 主场景（parent 链反查）**1 个 InstancedMesh count=24**（24 车辆模型全共享）。守护三件套+build=前半会话全绿（1611/1611、327 文件、tsc 0 错、5.56s）且本会话零源码改动（git status 仅 .zcode//AGENTS.md 不入库）沿用有效
- [x] 截图存 `docs/plan/screenshots/phase7/`（≥8 张）—— 8 张 md5 全唯一：05 多选 Mixed / 06 对齐弹层+足迹预览 / 07 阵列 popover / 08 道路分割（MCP 1280×720） / 09 全景含小地图 / 10a 建造 / 10b 道路 / 11 纯三维；小地图关闭态无独立截图（DOM/store 双证代，废片已删不入库）

## 完成定义

全部通过 → STATUS 标 done + 会话日志；不过项走修复循环（≤2 轮）；不打 tag（沿先例）。

## 收口备注（2026-09-13 三次会话）

1. 可选「小地图关闭态补拍」裁定不做：MCP take_screenshot 仅截主 WebGL 画布无 DOM 面板（方法学 27③），IAB 锁屏深冻不可用；前半会话 DOM/store 双证（display none+快照 false+刷新持久）已被本任务书接受为等效证据，废片不入库。
2. 观察项（非缺陷不立项）：React 菜单分隔线 key 告警（`file-sep` 重复 ×4，menuModel `${menu.id}-sep` 同菜单多分隔线同 key，纯 console 告警无行为影响）——记入 sessions 供 T8 清扫候选。
3. 方法第十次增补：+30（three 双实例雷区：Vite `?v=hash` 查询串不可省、RuntimeObjectMap.byId 门面后门、隐藏标签 lazy 推迟伪影）。
