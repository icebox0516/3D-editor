# T7.4 · UI 重构 P1：纯三维工作模式（Tab）

> **状态：done（2026-09-13）** ｜ 阶段：7（UI 重构 · 专业交互） ｜ 前置依赖：T7.1 ｜ 需求原文：`docs/plan/archive/ui升级.md` 第十四章

## 范围（阶段门前细化）

- Tab 进入/退出纯三维模式：隐藏 Scene Outliner / Inspector / Content Browser / 垂直工具条；仅保留极简顶部工具 + Viewport HUD + 基础操作提示。
- 进入时若有激活绘制/放置工具，先取消（ESC 语义）再进入，防止隐形状态。
- 输入层 Tab 键占用（T5.6 已预留不占用）；文本输入焦点时 Tab 不拦截（isEditableTarget 既有守卫复用）。

## 验收方向

Tab 双向切换瞬时（≤200ms 过渡）、面板隐藏后 Viewport 全幅；纯三维模式下选择/手势/相机操作仍可用；退出后布局精确还原（含面板尺寸）。测试：模式切换 store 纯逻辑 + input 路由。

## 执行留痕（2026-09-13）

- **派发**：general-purpose（frontend-design 前置），自包含简报钉死八项裁决（R1 pure3d 工作态不持久化 / R2 togglePure3d 共享入口含 vertex-edit 扩裁 / R3 裸 Tab 路由 / R4 菜单 Tab stopPropagation / R5 极简顶部工具=MenuBar+zone 变量归零+--bc 提示芯片 / R6 视图菜单 view.pure3d 三路同源 / R7 ctx.rename 防隐形聚焦硬化 / R8 README）。
- **交付**：一次过零修复循环，commit `4c05857`（17 文件 +391/−14，+21 测试 = 1433）。
- **主代理验收**：守护三件套亲自复跑全绿（1433/1433、check:layers 305 文件 OK、tsc 0 错）+ diff review 逐件亲审（store 幂等写法/togglePure3d cancel 语义/input 裸 Tab 守卫/三处菜单 stopPropagation/zoneVars 条件展开/视图菜单与快照同步）+ 生产构建冒烟 306ms。
- **子代理三处偏差追认**：①ContextMenu.tsx 第三处 `case 'Tab'` 同型补 stopPropagation（简报漏列，子代理自查补全）；②CSS `border:0` 归零（0px 网格槽 + 1px 边框残留约 1px 线，违背隐藏语义）；③menuModel.test id 守卫正则段字符类放行数字（`view.pure3d` 含数字 3，actionId 按裁定不改）。
- **DESIGN.md §5.15 落地**（主代理落文件）：ed-app--pure3d 修饰类 + .ed-hud--bc 第五角位提示芯片。
- **遗留观察（T7.8 GUI 验收裁决）**：帮助弹层（HelpOverlay）开着时按 Tab 仍触发全局切换（对话框非菜单未拦）；pure3d 下 view.panel-* 仍改 hiddenPanels（退出后生效，已知可接受）。
