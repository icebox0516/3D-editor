# T7.5 · UI 重构 P1：多选增强与批量编辑（Mixed 显示）

> **状态：done（2026-09-13）** ｜ 阶段：7（UI 重构 · 专业交互） ｜ 前置依赖：T7.1 ｜ 需求原文：`docs/plan/archive/ui升级.md` 第三十二章/第十九章

## 范围（阶段门前细化）

- 多选机制补全：Shift 跨组范围选择、Ctrl/Cmd 增减、框选（既有）、Outliner 类型筛选多选。
- Inspector 多选态从 P0 只读汇总升级为**公共属性批量编辑**：同类型多选→图层/材质/显隐/类型参数（高度等）批量修改（BatchCommand 一条历史）；异值字段显示 `Mixed` 占位且**编辑时不覆盖未改字段语义**（聚焦提交才下发）。
- Gizmo 多对象成组变换（T2.4 遗留观察项「gizmo 单挂首个对象」收编）。

## 验收方向

多选建筑批量改高度/图层/材质为一条可撤销历史；Mixed 字段显示与提交语义正确（不静默覆盖）；gizmo 拖拽多对象同步且单条历史。测试：公共属性求取/差异检测纯模块 + 批量命令序列。
