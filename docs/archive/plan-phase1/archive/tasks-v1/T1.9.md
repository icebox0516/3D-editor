# T1.9 · 阶段一 GUI 验收（主代理执行，不派发子代理）

> **状态：done**（仅主代理更新） ｜ 阶段：1 ｜ 前置依赖：T1.8 ｜ 注入提示词：无

## 必读上下文

1. `docs/plan/archive/三维园区可视化编辑器需求文档.md` **全文**（验收交叉核对）
2. `docs/plan/CONTRACTS.md`（分层校验点核对）

## 执行方式

主代理用 browser-use 技能操控浏览器逐项验收（`npm run dev` 启动）；每项截图存 `docs/plan/screenshots/phase1/`。不过项按 PROTOCOL.md 修复循环处理（带截图与复现步骤派修复子代理，同一项最多 2 轮）。

## 验收清单

- [x] 新建场景：天空 + 地面 + 网格可见，控制台无错误
- [x] 导入示例 JSON（UI 选择 mapping + fixture）→ 出现建筑
- [x] 点击建筑 → 属性面板显示；5 套样式逐一切换生效且几何不变
- [x] 修改高度数值 → 实时更新；Ctrl+Z 撤销 / Ctrl+Shift+Z 重做正确
- [x] 资产列表点击树 → Ghost 跟随鼠标 → 连续放置 3 棵 → 右键退出
- [x] 属性面板改树的 position 数值 → 生效（等效变换验收，Gizmo 属 T2.4）
- [x] 保存 → 刷新页面 → 打开 → 建筑/树/样式/位置完整恢复
- [x] 控制台全程无错误；撤销步数与操作一致（放置 3 棵 = 3 步，与实现声明一致）
- [x] UI 视觉符合 DESIGN.md 方向（截图自查：非通用默认样式）

## 完成动作

勾选本清单、STATUS.md 标 done、commit：`docs(phase1): gui acceptance screenshots`
