# T5.9 · 阶段 5 GUI 验收 + 全量回归（主代理）

> **状态：done** ｜ 阶段：5（UI 重构 · 核心骨架） ｜ 前置依赖：T5.1–T5.8 全部 done ｜ 执行者：主代理亲自（browser-use 技能，不下放）

## 任务背景

阶段 5 收口：按需求文档第四十三章验收标准（43.1–43.5）逐项实操验收 + 一期核心流程全量回归（确认重构零功能丢失）+ 截图存证。

## 验收清单

### A. 结构验收（43.1）

- [x] A1 默认布局（1920×1080）：Viewport 占编辑区 ≥65%（浏览器实测 DOM 尺寸计算并截图标注）；左面板 ≈260px、右面板 ≈320px、Content Browser 紧凑 ≈60px、垂直工具条 48px 常驻 ——实测 1292px/67.3%，260/320/60/48 全中；截图 t59-a1-default-layout.png
- [x] A2 左/右/底部面板均可隐藏；隐藏后 Viewport 自动扩展、无空白残留；面板开合后渲染/拾取无错位（画布自适应） ——三区全隐→1872×992（97.5%）、canvas 同步；截图 t59-a2-all-panels-hidden.png；冻结期 UI 开关恢复滞塞为 IAB 伪影（方法学 4b）
- [x] A3 Viewport 无业务卡片遮挡、无大面积装饰；HUD 四角呈现且不遮挡中心 ——视口内仅 canvas+HUD 三簇+轴指示器（88px），HUD 全部 pointer-events:none；截图 t59-a3-hud-closeup.png

### B. 职责验收（43.2）

- [x] B1 Scene=对象 / Layer=图层 / Inspector=属性 / Content Browser=资产 / Toolbar=工具 / Viewport=三维操作——六区职责单一，无混杂残留（旧 DrawPanel/ModeSwitch/EnvironmentPanel 等已退役） ——源码零组件残留（仅迁移注释）+ DOM 结构双证

### C. 核心交互验收（43.3）

- [x] C1 选择对象 → 修改属性（Transform/样式/类型参数）→ Viewport 实时更新 → 撤销恢复（含 5 步历史栈往返） ——Y=5/scaleX=2/改色 #4a90d9/楼层 6 四编辑；undo×5 清空→redo×5 完整恢复；截图 t59-c1-edit-undo-viewport.png
- [x] C2 拖资产入视口 → 自动创建 → 自动选中 → Inspector 自动打开（右面板隐藏态下拖拽亦然） ——两变体全过；截图 t59-c2-asset-drop.png
- [x] C3 绘制流程：垂直条进入绘制（六类）→ 状态栏读数 → 完成 → Outliner 归组归层 → Inspector 显示类型参数 ——六类全建并归 Building/Roads/Green/Water/Parking/Annotation；建筑 Inspector 外观（预设四档作用域）+建筑参数（高度/底部高度/楼层/面积）；截图 t59-c3-drawing-in-progress.png
- [x] C4 手势全表：左选 / 右拖旋转 / 中键平移 / 滚轮缩放 / 右点菜单 / ESC 退出 / F 聚焦 / Q/1–6/G/W/E/R 键位 ——左选（建筑命中）/右点菜单（viewport-blank 菜单 DOM）/ESC/Q/1–6/W/E/R（gizmo 终态 scale）/G（吸附往返）实测过；右拖/中键/滚轮沿 T5.8 像素证据（CameraPort 无位姿读取）；左选对**模型**不命中见观察项缺陷（修复循环）

### D. 联动验收（43.4）

- [x] D1 Viewport→Outliner→Inspector、Outliner→Viewport→Inspector、Inspector→Viewport 三向一致（含多选部分：框选后 Outliner 多行高亮、Inspector 只读汇总） ——框选 8 对象→大纲 8 行 aria-selected、Inspector「已选 8 个对象」类型计数只读汇总；行点选/改名联动全过

### E. 模式与状态（43.5）

- [x] E1 工作模式派生正确且 HUD/Context Toolbar 可见；切换绘制模式时工具/辅助信息自动变化；禁用模式项灰显占位 ——Scene▼ 八模式 1 启用 7 禁用灰显；键 3 入绘制→HUD 芯片「地形」、Context Toolbar 绘制中段+退出钮、机位自动顶视、ESC 还原
- [x] E2 保存四态流转 + beforeunload 守卫；保存的 JSON 可打开复原（序列化往返） ——已保存→修改未保存→已保存；beforeunload 脏态 defaultPrevented/净态放行（reload 被冻结核吞，改事件级验证）；往返 9 对象含变换/样式覆盖/楼层全恢复

### F. 一期全量回归

- [x] F1 打开/导入 JSON/导出、撤销重做、复制粘贴删除、图层操作（透明度松手提交等既有设计语义）、环境预设、网格设置、缩略图懒加载、InstancedMesh 放置性能（主线程延迟口径） ——打开/导出走真实管线（createObjectURL 钩子+DataTransfer 注入）；透明度 3 帧 input+1 pointerup=单条历史（一次 undo 回 1.0）；环境 night/day 切换；网格 5→10 提交且吸附步长同步；缩略图 2 真 PNG+8 占位 SVG；8 连放主线程 0–1ms、11 模型 136 三角形（实例共享）
- [x] F2 `npm test` / `npm run check:layers` / `npm run typecheck` 全绿；`npm run build` + 生产 preview 冒烟（沿 A3 先例：dist 含 assets、清单可加载） ——验收期 1007 全绿 + 修复合入后 **1023 全绿**/241 文件分层 OK/tsc 0 错；build 884ms，dist 含 assets/models/thumbnails/mappings/manifest，index.html 引用零缺失，preview 4173 冒烟（app/canvas/menubar/浏览器/状态栏全就位）

### G. 收尾

- [x] G1 截图存 `docs/plan/screenshots/phase5/`（≥6 张：默认布局/隐藏面板/拖放/绘制中/HUD 特写/三渲染模式）；README 核对更新 ——8 张 t59-* 入库（三渲染模式：wireframe/xray 新拍 + shaded 由 C1/C2 同着色视图代证，方法学 14b 陈旧缓存帧）；README 进度/门表/UI 规则/测试数已更新
- [x] G2 验收疑点按修复循环处理；方法学坑沿 T3.4/T4.1 实操经验（跨帧断言、元素级事件、坐标先查面板） ——观察项定级：建筑不命中=机位切换后旧坐标伪影（洗清）；模型不命中=实例池射线拾取真缺陷 → 修复循环 1 轮闭环（threejs-expert 限额中断于单例修复+模板合并，主代理接手补类型错/测试笔误并提交；node 新增 16 用例 + 浏览器复验：全画布探针 0→11 命中格、真实点击选中凉亭）；方法学新增 4b/7b/14b/17b 四条

## 完成定义

全部通过 → STATUS 标 done + 会话日志；不过项走修复循环（≤2 轮，仍败则按需门）。不打 tag（沿交付先例，用户点名才打）。
