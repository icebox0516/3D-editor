# T6.8 · 顶点编辑（解禁项 · 末档）

> **状态：done** ｜ 阶段：6（形状驱动创建与样式引擎） ｜ 前置依赖：T6.7 done ｜ 执行者：threejs-expert ｜ 需求来源：《绘制需求变更.md（定稿 v2）》第七.3 节「编辑顶点」+ CONTRACTS 排除项修订（顶点编辑限定本任务解禁） ｜ 分域契约：contracts/stage6-region.md

## 任务背景

排除项「顶点编辑」于 2026-09-11 设计门限定解禁、仅本任务实现。对面/线 RegionObject 的 shape.points 做顶点级编辑，全程走 setGeometry 路径（不重建材质、不丢类型与样式）。

**跨层通路（2026-09-11 审计增补，VertexEditPort——定义在分域契约 §C，模式沿 GizmoPort 先例，基础契约 editor/services 节有指针注记）**：runtime 顶点句柄拖动 → `VertexEditPort.onDragEnd` 回调（一次性 before/after 点列）→ editor 层编辑会话转 `ChangeShapeCommand`（松手提交一条历史）。editor 不触 THREE、runtime 不发命令，依赖倒置经 Port 回调衔接。

## 验收清单

- [x] 编辑模式入口：属性面板「编辑顶点」解禁 + 视口双击对象快捷进入；Esc / 再次点击退出
- [x] 视口顶点句柄：选中对象顶点渲染为可拖拽 handle（点/线/面全支持；参数化形状进入编辑即转 polygon 自由点列，options 缓存失效标注）
- [x] 拖动顶点：节流更新 shape.points → 经 VertexEditPort.onDragEnd 回调 → ChangeShapeCommand（松手提交一条历史）；G 网格吸附与 Shift 正交管线复用；面积/周长读数实时
- [x] 插入 / 删除顶点：边中点 Alt+点击插入、顶点右键/删除键移除（保持 ≥3 顶点面 / ≥2 点线约束，违例拦截提示）
- [x] 撤销粒度：每段拖动/每次插删一条历史；类型与样式全程保留（解耦断言沿用 T6.4 用例扩展）
- [x] 拾取隔离：编辑模式中顶点句柄优先于对象拾取；退出后恢复常规拾取

## 完成定义

守护三件套全绿；拖动期间材质实例不变（引用断言）；commit `feat(runtime,ui): vertex editing with shape-only history and preserved style (T6.8)`。
