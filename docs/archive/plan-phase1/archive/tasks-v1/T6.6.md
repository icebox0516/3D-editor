# T6.6 · 属性面板四分组重构

> **状态：done** ｜ 阶段：6（形状驱动创建与样式引擎） ｜ 前置依赖：T6.5 done ｜ 执行者：general-purpose（先读 frontend-design 技能） ｜ 需求来源：《绘制需求变更.md（定稿 v2）》第七.3 节 ｜ 分域契约：contracts/stage6-region.md + contracts/legacy-elements.md

## 任务背景

Inspector 按「基础信息 / 几何 / 业务类型 / 表现样式」四分组重组，服务 RegionObject 三层模型；旧要素面板分支保留共存（T6.7 删）。复用 tokens.css / DESIGN.md 既定语言，零新起炉灶。

## 验收清单

- [x] 基础信息：名称 / 图层（下拉迁移）/ 锁定 / 可见——沿用既有控件与命令通道
- [x] 几何分组：形状类型下拉（七类可切换，切换走 ChangeShapeCommand 按包围盒/中心自动转换顶点；圆/椭圆编辑半径、矩形编辑长宽）；顶点数 / 面积 / 周长派生只读；**基准高度（读写 `shape.baseHeight`；与 Gizmo / 变换组 Y 值读写 `transform.position.y` 分工——两层叠加生效，分域契约 §A 高度合成）**；「编辑顶点」按钮 disabled 占位（归 T6.8）
- [x] 业务类型分组：类型下拉（十类）+ 按语义注册的业务参数定义动态生成表单（road.width / building.height 等）；改值走命令
- [x] 表现样式分组：预设缩略图网格（**数据源 = StylePresetRegistry 经 EditorFacade.registries.presets，按 shape × semantic 过滤，禁止 import runtime**，选中高亮）；参数控件由插件 defaultParams 元数据自动生成、实时生效（runtime 经 scene:changed reconcile，update 路径）；四档作用域批量应用 = 按作用域过滤的批量 ChangePresetCommand，一条撤销——**过滤字段映射（2026-09-11 审计定稿，沿用 v1 现状行为，源码参照 StyleParametersForm.resolveStyleTargets）**：当前对象=仅自身 / 同类型=过滤 `semantic.type` / 当前图层=过滤 `layerId` / **同预设**=过滤 `presetId` 相同（第四档标签由「全部」改「同预设」）
- [x] 多选只读汇总沿用（含 RegionObject 类型分布）
- [x] 解耦断言（UI 级）：切类型不触发几何重建读数变化、切预设后几何派生值不变、参数改动经 update 即时渲染不闪变
- [x] 旧要素（building/marker/model）面板分支不回归

## 完成定义

守护三件套全绿；DESIGN.md 有新增视觉元素则补记；commit `feat(ui): inspector four-group restructure for region objects (T6.6)`。
