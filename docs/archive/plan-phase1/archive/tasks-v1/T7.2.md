# T7.2 · 面板 Docking / Floating / Auto Hide（延后——远期规划）

> **状态：deferred（2026-09-12 T7.1 阶段门用户裁定：本功能暂不实现，放入远期规划；2026-09-14 T8.6 终审⑤用户预裁维持远期不重启，复盘关闭；重启需按需门过审并回填阶段）** ｜ 原阶段：7（UI 重构 · 专业交互） ｜ 原前置依赖：T7.1 ｜ 需求原文：`docs/plan/archive/ui升级.md` 第二十八章
>
> **门留痕**：当时选型议题（自研 vs dockview 类库）随延后一并挂起；若远期重启，选型按当时工程现状重新评估（红线仍为面板操作不得销毁 Viewport，需求 37.6）。阶段 7 序列已剔除本任务，T7.3 前置依赖改挂 T7.1。

## 范围（阶段门前细化）

- 面板可停靠四边（左/右/上/下）、可悬浮独立窗口（多屏/大属性编辑场景）、Auto Hide（收缩为侧边标签，Hover/点击展开）。
- **技术选型为阶段门核心议题**：自研（沿 T5.1 splitter 体系扩展）vs 引入 dockview 类库（依赖策略与分层影响评估）；倾向自研轻量方案（P0 骨架已含面板注册制雏形），性能红线 = 面板操作不得销毁/重建 Viewport（需求 37.6）。
- 拖拽句柄、停靠预览高亮、悬浮层 z 序（`--z-popover` 档）。

## 验收方向

四边停靠 + 悬浮 + 自动隐藏全路径可用；Viewport 画布与 WebGL 上下文在任意面板操作下存活（拾取/渲染无异常）；布局变更后布局持久化（T7.3 接力）生效。测试：停靠状态机纯模块；GUI 验收归 T7.8。
