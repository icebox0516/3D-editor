# T2.1 · 资产清单与缩略图

> **状态：done**（仅主代理更新） ｜ 阶段：2 ｜ 前置依赖：T1.9 ｜ 注入提示词：第 9 组任务 1 ｜ **UI 任务**（先读 frontend-design 技能）
> 🚪 阶段门：本任务派发前须经 PROTOCOL.md「任务前置拷问门」阶段级 grilling 确认（阶段目标/任务序列/验收/风险）。

## 必读上下文

1. `docs/plan/CONTRACTS.md`（重点：ModelAsset 契约）
2. `C:\Users\admin\.agents\skills\frontend-design\SKILL.md` + `src/ui/styles/{tokens.css,DESIGN.md}`（复用设计系统）
3. `docs/plan/archive/三维园区可视化编辑器需求文档.md` §模型资产库
4. 前置源码：`assets/manifest.json`、`scripts/generate-assets.mjs`、`src/ui/AssetListPanel.tsx`

## 注入提示词（原文，第 9 组任务 1）

> 1. AssetManifest 资产清单：
> - 扫描 /assets/models 目录生成 manifest.json
> - 启动时读取清单，不重复全量扫描
> - 自动生成缩略图缓存机制

## 偏差与补充（优先级高于上方提示词）

- 缩略图两级：①脚本为每个 GLB 生成确定性 SVG 占位缩略图（按类别色 + 名称首字，写 `assets/thumbnails/*.svg` 进 manifest）；②运行时首次加载真实模型后用离屏渲染快照替换内存缩略图并 best-effort 持久化 IndexedDB（key = assetId + file 尺寸）。
- `scripts/scan-assets.mjs`：扫描 assets/models → 重写 manifest.json（增量合并：按 file 路径保留旧 tags/收藏标记）。
- 资产面板 UI（替换 T1.8 迷你列表，复用设计系统）：搜索框 + 分类侧栏 + 卡片网格（缩略图/名称/标签/收藏星标）；收藏状态存 localStorage。

## 产出文件

- Create: `scripts/scan-assets.mjs`
- Create: `src/io/AssetManifest.ts`（manifest 读取与 AssetRegistry 灌注）
- Create: `src/runtime/loaders/ThumbnailCache.ts`
- Create: `src/ui/panels/AssetLibraryPanel.tsx`（替换迷你列表）
- Test: `tests/io/AssetManifest.test.ts`、`tests/scripts/scan-assets.test.mjs`

## 接口（Produces）

- `loadManifest(json): { assets: ModelAsset[] }`
- AssetLibraryPanel 点击卡片 → `tools.activate('placement', { assetId })`

## 验收标准

- scan-assets 对目录增删模型后重跑，manifest 增量正确、旧 tags/收藏保留。
- 面板搜索命中 name/tags；分类过滤正确；收藏跨刷新保留。
- 首次点击资产出现真实缩略图，第二次从缓存取（加载计数断言只渲染一次）。
- 守护三连全绿。

## 步骤

- [x] 1. 失败测试：manifest 灌注 / scan 合并语义 / 缩略图缓存命中
- [x] 2. 实现脚本与运行时缩略图
- [x] 3. 实现资产面板（复用 tokens.css / DESIGN.md）
- [x] 4. 守护三连
- [x] 5. commit：`feat(assets): manifest scanning, thumbnails and asset library panel`
