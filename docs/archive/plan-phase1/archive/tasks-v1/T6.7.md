# T6.7 · 旧要素体系删除与全量清理

> **状态：done** ｜ 阶段：6（形状驱动创建与样式引擎） ｜ 前置依赖：T6.6 done ｜ 执行者：general-purpose ｜ 需求来源：《绘制需求变更.md（定稿 v2）》第二节「明确删除」 ｜ 分域契约：contracts/stage6-region.md + contracts/legacy-elements.md

## 任务背景

新链路全量就位后，删除被取代的旧体系，完成架构收口。删除边界以 T6.2–T6.6 实际落地形态为准（如新引擎仍消费 StyleRegistry 的参数定义形态，则只删按要素绑定的关联，不删被复用部分——diff 时逐项判断并在报告列明）。

## 验收清单

- [x] 删除 building / marker Element（BuildingProperties、MarkerElement 及 registries/definitions 对应项）与旧六要素绘制工具（DrawBuildingTool 等按要素命名变体）
- [x] 删除旧要素渲染器（2026-09-11 审计措辞校正：现状仅注册 **5 个**——BuildingRenderer / RoadRenderer / GreenRenderer / WaterRenderer / MarkerRenderer；**parking 无独立渲染器本体，勿找不存在的 ParkingRenderer**）与 Geometry-Element 强绑定残余；StyleDefinition.elementType 按要素绑定体系按实际消费方处理（含废弃样式定义清理：building glass/tech/night、road tech/night、marker default/warning，见 T6.3 三栏清单）
- [x] 默认图层调整为十类语义图层（water/grass/plaza/parking/bare_land/road/building/poi/未分类/自定义）；`DEFAULT_LAYER_NAME_BY_TYPE` 单一真相源
- [x] 大纲分组、右键菜单、主菜单、帮助弹层、README：六要素文案与入口全量换新；无死链与失效快捷键描述
- [x] **旧场景文件过渡兜底**：反序列化遇已删 type（building/marker）不崩溃——跳过该对象并 Toast 告警数量（正式转换规则归 T6.9 按需门裁决，此处仅防崩兜底并留代码注记）
- [x] 测试重构：删除针对旧体系的用例，新增/保留的用例全部转述为新体系断言；`grep -r "BuildingElement\|MarkerElement\|building" src/`（语义命中逐项人工确认）无残留
- [x] 资产模型对象（ModelObject / PlacementTool）不受影响——属资产库模式，零改动

## 完成定义

守护三件套全绿；旧文件打开兜底行为有测试；commit `refactor(core,ui): remove legacy element system, semantic layers and cleanup (T6.7)`。
