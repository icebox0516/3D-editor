# T2.2 · PlacementTool 放置增强

> **状态：done**（仅主代理更新） ｜ 阶段：2 ｜ 前置依赖：T2.1 ｜ 注入提示词：第 9 组任务 2

## 必读上下文

1. `docs/plan/CONTRACTS.md`（重点：Tool 契约、BatchCommand）
2. `docs/plan/archive/三维园区可视化编辑器需求文档.md` §模型放置
3. 前置源码：`src/editor/tools/PlacementTool.ts`（T1.6 产出）

## 注入提示词（原文，第 9 组任务 2）

> 2. PlacementTool 增强：
> - 放置中滚轮缩放预览、R键旋转预览
> - 随机旋转、随机缩放范围配置
> - 地面吸附
> - 连续放置的 BatchCommand 合并选项（可配置是否合并为一次撤销）

## 偏差与补充（优先级高于上方提示词）

- 滚轮在放置模式下缩放 Ghost 预览（临时 scale，夹在 [0.2, 5]）；R 键旋转 Ghost 45°——均为预览参数，落点时进 CreateObjectCommand 的 transform。
- 地面吸附恒开（y=0，groundPoint 已保证）。
- 随机缩放同时作用 x/y/z。

## 产出文件

- Modify: `src/editor/tools/PlacementTool.ts`
- Test: `tests/editor/PlacementTool.enhanced.test.ts`

## 接口（Produces）

PlacementTool 参数扩展：`{ ..., mergeBatch?: boolean }`（默认 true）。

## 验收标准

- onWheel 改 ghost scale 且夹取；onKeyDown 'r' 旋转 45°。
- 随机旋转/缩放 200 次采样全部落在 [min,max]。
- mergeBatch=true：连续放置 N 次仅 1 条历史（BatchCommand），undo 一次全撤、redo 一次全恢复；false 时逐个。
- ESC/右键退出不删除已放置对象（回归）。
- 守护三连全绿。

## 步骤

- [x] 1. 失败测试：滚轮/R 键 / 随机区间 / BatchCommand 合并开关
- [x] 2. 实现通过（Fake ports）
- [x] 3. 守护三连
- [x] 4. commit：`feat(editor): enhanced placement with batch merge`
