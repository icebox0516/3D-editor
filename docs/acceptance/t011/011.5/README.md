# T011.5 悬铃木视觉验证取证（D30 增量口径 + D35 外派验证子代理）

> 任务：[tasks/011.5-platanus.md](../../../tasks/011.5-platanus.md) ｜ Spec：[docs/research/platanus-reference.md](../../../docs/research/platanus-reference.md) **1.0**（**含文末终审记档降级口径**：树高 ≈12m 弱 Inferred 主代理裁定保守低端、裂深深端可至 2/3、斑块细碎端可至 1/20、容许低位双主枝记档不建模——本验证按此口径判读）｜ 口径：SOP §5 Step 4 单资产层（D30）——三必做 + 疑点触发项
> 环境：Chrome headless（CDP 9333 直驱，tools/cdp.mjs）+ Vite dev（5173）｜ 1920×1080 DPR=1 ｜ WebGL2 ｜ day 预设固定灯光 ｜ freezeTime 冻结风相位（风动双帧除外）｜ asset_tree_platanus slot-0 锚点（High 皮 25098（含果序 115 球 ×8 = 920 并入组 0）/ 叶 3368 / 1684 卡 = 28466 面）
> 判读通道记档：本会话 Read 工具对本地图片仅返回 CDN 转存 URL（011.4 同限制）——证据帧经转存 URL 喂多模态图像分析 MCP 做中性判读（提问不出现树种名/预期答案，011.2 防引导幻觉口径）；机器侧证据（pixel-diff / md5 / stats / console）为本地直读。
> **机位口径修正记档（本轮工具层发现）**：Renderer controls `maxPolarAngle = π/2−0.001`（不钻地面以下）——**view() 负仰角一律被 clamp 至水平**（1500ms 收敛 A/B/C 实证 el−35 = el−20 = el0 同帧）；`minDistance=2` 不咬显式写位（1.2m 帧与 2m 帧异 md5，OrbitControls update() 只 clamp phi）。影响 = 所有「仰视」类机位实际为水平机位（**011.1–011.4 同一行为，历轮仰视标签均为名义标签**——本轮记档不改历轮结论：水平帧干面读向证据仍有效）。本目录树皮/果序帧一律按水平机位（el=0）+ 1200ms 阻尼收敛（damping 0.08，500ms 仅收敛 ~92%）重拍为确定性版。

## D30 三必做结论

### ① Spec 身份判定 —— **通过（五重身份组合指向悬铃木，读向不依赖标注）**

| 距离档 | Spec §7 判据 | 判定 | 证据 |
|---|---|---|---|
| 近距 | 掌状裂大叶（阔卵形宽>长、中央裂片阔三角宽≈长渐尖、裂深约半叶、每裂片 0–2 疏粗齿）+ 互生疏排 + 成对球状果序 + 斑块剥落干面细部 | ✓ | leaf-closeup-3p5m / macro-2p8m-el0 / macro-2p8m-el20 三帧中性判读：**裂口确认**（12 叶清点 3 裂×5 / 4 裂×3 / 5 裂×4——「3 主深裂 + 2 侧浅裂」观感为主、部分全 5 裂，**无全缘主导叶**）；**裂深 1/5–1/2、最深达半**（落 Spec 1/3–1/2 典型 1/2 域 + 深端容差）；**中央裂片宽三角、宽≈长（读比 1:1.2–1.5:1）、先端收尖不细长**（= 阔三角渐尖）；齿 = 稀疏浅齿/细密浅锯齿（近距可辨、量级弱——Spec 疏粗齿 0–2 枚全缘为主的近似端，见缺口复核⑦）；**果序成对**（C7 判读「两枚小圆球并排挂在细长梗上、叶下、球径 ≈1/8–1/10 叶宽、褐绿色」）；大叶平展摊开（叶姿态 Spec ✓）；脉纹弱（个别叶浅色中脉线隐约——离基掌状 3 脉为弱表达层，先例「隐约可见」口径同档） |
| 中距 | 大叶疏簇通透冠（空隙 20–30%、外密内疏、粗质 coarse、单叶轮廓不可辨）+ 干面三色带地图状斑块剥落（最强身份信号）+ 斜上-平展骨架剪影 + 满冠悬垂果球 | ✓ | B25 逆光判读**空隙 20–30%、外密内疏、粗团块质感、团块 ≈1/10 冠幅、冠内枝干剪影透见、中距单叶不可辨**——**「大叶疏簇粗团块」读向成立、非前例细密冠、非稀疏缺陷**（疑点②通过）；baseline 判读**干面大片多色斑驳拼贴中距可辨、辨识度高**；骨架 5–7 根斜上 40–55° 层级递减（Spec 4–7 根 35–60° ✓）；fruit-under-crown-8m 判读 **3 处小球、两处成对、清晰圆点 6–10px、细梗下垂叶下、褐绿 vs 绿叶区分明显**——果序中近距可辨成立（疑点④通过） |
| 远距 | 阔卵-圆头开张轮廓（冠幅比 0.6–0.75 中偏窄大冠）+ 中绿粗质大叶色块 + 斑驳干面剪影 | ✓ | vis-far-50m 判读**阔卵-圆头、顶部圆化微平、冠缘不规则、冠幅/树高 ≈0.62–0.70、最宽 45–55% 树高、大枝斜上开展下部近水平**（全落 Spec）；slots 全景 8/8 同种语言一致（叶色/叶质/树皮三色带/冠形四项一致） |

**身份组合成立**：掌状裂大叶 + 大叶疏簇通透粗质冠 + 三色带大片地图状剥落干皮 + 阔卵-圆头 12m 级大冠 + 叶幕下成对宿存果球——vs 五先例分化明确（叶形第六族：掌状深裂多裂轮廓 vs 五例全缘/锯齿/扇形缺刻；树皮第六语言：冷调三色带大片地图拼贴 vs 纵裂脊沟族/榉树小片暖斑驳/朴树浅斑；果序首个成对球状挂点附属物；体量首个 12m 级上层大乔）。

### ② 统一基线帧 —— `baseline.png`（M25 = 25m/35°/8°，day 预设 + freezeTime，1200ms 收敛）

全族同参数同源（族门横向比较基线 + 用户观感复核窗口）；无校准轮，本帧即交付终态。**12m 级 M25 构图完整性**（点名疑点）：判读树完整入画、树高占画面高 75–80%、冠幅占宽 50–55%、无裁切——**构图通过**；族内体量对比记档（供 011.13 混植）：12m 级在 M25 同参数下体量读向 ≈1.5× 五先例（8–8.5m 级），slots 全景高差 12–18% 带内一致——上层大乔量级读出、无构图溢出。

### ③ 契约缺口记档（本任务零修订，归 011.13）—— 开发候选 10 条逐条复核后的最终版

**A（继承实证，011.1–011.4 已记不重复立项）**：偏冠相干方位偏置 / 干长比率字段化 / 冠幅比涌现映射 / 起伏∝半径挂钩 / 常绿语义位 / 叶族公共 specular·envMap / 树皮沟内层次 / 椭球密度场参考系 / 局部斑片状树皮起伏通道 / 门控型树皮取证机位（缺口 D——悬铃木 = **满干型**皮（材质「全干拼贴」整干有效 + 唯一高度项 smoothstep(3.0,5.5) 为连续过渡非限带，platanusStage 011.3 缺口 D 评估记档），本轮像素复核一致：低干 10m 帧与冠内 1.2m 帧均读出拼贴、无高度门控跳变——**缺口 D 维持榉树单例**）。

**B（开发 agent 十候选，本验证逐条代码+像素复核全部属实）**：

| # | 缺口 | 复核结论（代码位 + 证据） |
|---|---|---|
| 1 | **果序无独立材质组通道**：果序入皮组（组 0），无独立材质/着色语义位 | 属实——platanusGeometry 模块头「果序入皮组（组 0），材质接口约定冻结」；果序顶点 uv v∈[4,5] 果序域为几何身份标记、着色走皮材质褐系近似（「可接受记档」）；stats 实测组 0 槽间 24850–25226 非恒等（果球数随槽 84–131 变）——独立果序材质（如绿褐分档/刺状表面）需家族新组语义 |
| 2 | **一节一叶卡尺度合并抽象无家族语义位**：真节间 3–5cm × 卡径 0.36m ⇒ ≈5–9 节/卡径，每簇 5 候选 = 连续节位合并抽象（「每簇叶量」字段承载） | 属实——platanusShapeProfile「每簇叶量 5 = 疏簇候选数（互生单叶语言的卡尺度合并抽象）」+ platanusGeometry L44 同记档（银杏散生节距同款先例口径）；「节距→卡密度」的显式映射无家族字段，逐叶真节距建模面数不可行——族级若做须新抽象位（挂点密度语义） |
| 3 | **12m 级领导链复利噪声**：领导链复利外伸 ≈×1.9（领导比 ±0.1 → 涌现树高 ±1.4–1.9m；先例银杏 ×1.8 于 8m 级绝对噪声 ×0.65） | 属实——platanusShapeProfile leaderLengthRatio 字段注释探针记档（8 槽涌现带 11.53–12.79，参数域收紧 0.7 收束）；几何侧 totalHeight 11.9+rng×0.7 + 各槽领导比回调记录在案。族级课题：树高绝对值随尺度的噪声放大无补偿通道 |
| 4 | **低位双主枝容差无通道**：终审 C-5 容许低位双主枝变体——结构差异不落连续参数，如需表达须家族契约结构位 | 属实——platanusShapeProfile「容许低位双主枝不进 8 槽（伪差异禁止项纪律）」+ 模块头记档；本轮像素侧 8 槽全景 8/8 单干读向一致（个体内容差未表达——记档状态正确） |
| 5 | **crownWidthRatio 外泄非线性第二数据点**：参数 0.46 → 涌现 0.683（外泄 ×1.48；8 槽带 0.543–0.762） | 属实——platanusShapeProfile crownWidthRatio 字段注释探针回写（外泄 ×1.67 首探 → 角度梯度回调后 ×1.48 落域）；几何 L331-332 回调记录。与银杏 ×1.06–1.60 构成两数据点：外泄系数 = 低枝平展角 × 长度分级的双放大耦合项、非线性且树种依赖——族级参数→涌现映射需含角度域耦合（011.4 C 组缺口新证据，本树为第二数据点） |
| 6 | **果序 Low 省略档间连续性仅记档**：Low 档果球省略（远距亚像素，rng 照常消费），档间连续性无断言保障 | 属实——platanusGeometry「Low 省略（远距亚像素——档间连续性记档）」；本轮像素复核：Mid 25m 果球 3–5 处成对可辨、Low 25m 无小球可辨但「仍读作一棵正常的树、无秃/稀/糊缺失」+ 32m 三档轮廓一致高差 <1–2%——**省略无观感断崖，记档口径在像素侧成立**（档间连续性为读向级、无量化门） |
| 7 | **裂数/裂深/齿幅/脉型共用 aLeafRand 统计近似**：多通道复用同一 rand（裂数 75/15/10 与叶位/树势相关性无法表达）；几何侧无叶位语义通道 | 属实——platanusMaterials「裂深 0.19–0.26 / 裂数 75/15/10 / 齿幅 0.008–0.022 / 脉离基两型 75/25 / 稀 5 脉 15%（多通道复用同一 rand）」+ 缺口候选①注释同记；本轮像素侧新证据：近景 12 叶清点裂数读 3/4/5 裂 = 5/3/4 片——**「5 裂主导 75%」的视觉读向偏弱**（3 主深裂 + 2 侧浅裂观感为主，5 裂叶存在但非主导读数），统计近似 + 浅侧裂窗的表达与 authored 分布有读向偏差（不阻塞身份：掌状裂读向明确、裂深落域；精确分型需叶位/树势 attribute） |
| 8 | **放射角窗 dip 族 SDF 私有实现**：掌状裂「叶基放射角窗 dip 族」（放射角坐标 t=|x|/max(v,0.10) 免 atan 射线族 + dip 窗开窗）为 platanusMaterials 文件内实现，未进公共抽象 | 属实——platanusMaterials 模块头「dip 减法机制可推广、缺刻的顶边挂载不可推广」评估 + 缺口候选②注释（枫/梧桐等族内多裂深裂叶形再现时可提炼家族公共 SDF 模式）；本轮像素侧掌状裂轮廓三帧读出成立（新路径有效性验证过） |
| 9 | **脉层五型公共抽象待收口**：中轴/三出/羽状/辐射(二叉)/离基掌状五型脉分属各资产文件内实现 | 属实——platanusMaterials「离基掌状 3 脉——前例脉型库推广（樟树离基三出同款坐标语义）」文件内实现；五型散布各文件（夏栎中轴/朴树三出/榉树羽状/银杏辐射二叉/悬铃木离基掌状），公共抽象归族级收口评估（011.4 缺口 6 的五型扩展版） |
| 10 | **树高锚 12m 硬编码无联动通道**：totalHeight = 11.9 + rng×0.7 为 platanusGeometry 文件内常数（≈12m 主代理裁定保守低端），无 profile/家族联动位 | 属实——platanusGeometry L891（形态参数域 11.9–12.6m 硬编码 + 涌现带 11.53–12.79 探针注释）；BroadleafShapeProfile 无树高绝对值字段（先例均为 8–8.5m 级同构硬编码）——**家族混植的树高量级锚（12m 级 vs 8m 级）无参数面**，011.13 混植门若需同族不同量级个体须记档处置 |

**C（本验证新增观察级，2 条）**：
1. **裂数视觉读向偏 3 主裂**（并入缺口 7 新证据，见上表）——近景 12 叶清点 3/4/5 裂分布 5/3/4 片，「3 主深裂 + 2 侧浅裂」观感为主；Spec「5 裂典型主相」的近景统计读向偏弱（中距不可辨——无影响）。归 011.13：若需强化 5 裂读向，调主/侧 sinus 深度比或裂数分型权重（属参数域微调，非结构性）。
2. **齿幅近景读向偏细密**：macro 帧判读「细密浅锯齿」vs Spec「每裂片 0–2 疏粗齿、全缘为主」——齿载波（0.008–0.022）在近景读为细齿而非疏粗齿。牺牲顺序首位项（远距即失），不阻塞；归 011.13 参数域参考（若需「全缘为主」读向，齿幅分布可下调稀疏端）。

## 疑点触发项记录（D30：按疑点驱动）

1. **掌状裂叶近景**（点名）：三帧（L3.5 / macro el0 / macro el20）中性判读——裂口确认、无全缘主导叶；裂深最深达半（1/2 典型域）；中央裂片宽三角宽≈长（1:1.2–1.5:1）先端收尖不细长（阔三角渐尖 ✓）；齿弱可辨；**裂数读向见缺口 7/C-1**。**掌状裂身份读出成立** ✓
2. **大叶疏簇冠读向**（点名，防误判稀疏缺陷）：B25 逆光空隙 **20–30%** + 外密内疏 + 粗团块（≈1/10 冠幅）+ 中距单叶不可辨 + 冠内枝干透见；C7 近距大叶 8–15× 枝径、平展摊开、疏簇 + 沿枝互生并存读向。**「大叶疏簇通透冠 coarse」读向成立，非稀疏缺陷**（对照 Spec §枝叶密度判读）✓
3. **树皮满干拼贴**（点名）：M25 主干（baseline 帧中距可辨、辨识度高）+ trunk-lower-10m（三色带奶油白 30–35%/灰绿橄榄 25–30%/灰褐 25–30% + 深褐沟缝 5–10%、大片地图状、单斑 1/8–1/12 干径、新旧对比中-高、新斑光滑哑光）+ bark-1p2m 冠内近距（拼贴 + 板块边缘深色勾边翘曲贴片感 + 红褐枝条）双方位一致——**满干型三色带大片地图拼贴成立、与榉树小片暖斑驳分化明确**（色温冷调含灰绿 / 斑尺度大片 / 对比高，三重分化全读出）✓
4. **果序可见性**（点名）：8m 水平帧 3 处小球（两处成对、一处单/遮挡）、清晰圆点 6–10px、褐绿 vs 绿叶区分明显、细梗下垂叶下；C7 帧成对球 + 细长梗读向；Mid 25m 仍 3–5 处成对可辨——**球径 ≈5–6cm 工程域在 8m 达清晰可辨、成对结构可读、中距隐约可读，Spec 中近距判据全达成**（不磨项：无）✓
5. **风动三证据**（点名：快颤 8–14 rad/s 六树最低 × 幅度 ≤15mm 六树最大）：①run M25 双帧 pctChangedAny **1.04%**、质心 (955,579) 冠部 ✓；②frozen 双帧 pctChangedAny **0.00**（md5 逐字节相同）✓；③C7 近景双帧 pctChangedAny **10.09%**、86.3% 行有差异——大叶快颤清晰可见，单帧无拖影（高频小幅 + 帧率下锐利合理）、无「僵直/过摆」疑点 ✓
6. **console 两轮**：主批次（含 8 机位 + 风动 + 8 槽 + LOD 全流程）回读 **[]**；树皮/果序补充批次回读 **[]**（零错误零警告）✓（注：重拍批 t0115-barkfruit2 为纯机位移动未再收集——两轮口径已足）
7. **LOD 三档**（点名：轮廓连续 + 果序 Low 省略档间连续性）：32m 全景三档轮廓一致（高差 <1–2%、冠形/最宽点同位）、体量连续（Mid ≈85–90% / Low ≈70–75%，无坍缩跳变）、干形/叶色三档一致、粗团块质感保持 ✓；逐档 25m 三帧 + Low 50m md5 全异（011.3 viewLevel API 教训执行）✓；账目：High 25098+3368=**28466** / Mid 5434+1348=**6782** / Low 370+1556=**1926** 全落家族预算行 [≤40000 / 6000–10000 / 1500–3000] ✓；果序 Mid 保留可辨 → Low 省略无断崖（见缺口 6 复核）✓
8. **12m 级 M25 构图 + 族内体量**（点名，不阻塞）：baseline 完整入画 75–80% 画面高 ✓；族内体量 ≈1.5× 五先例读出（记档供 011.13 混植——混植门需关注 12m 级与 8m 级同框的尺度对比语言）✓

## 校准需求：**无**——全部像素读数落 Spec 域内（含终审降级口径容差带），无可参数化修正项。观察级 2 条（C-1/C-2）归 011.13 参数域参考、不构成本轮校准。

## 复现

```bash
# vite dev 5173 + Chrome headless CDP 9333 后：
node docs/acceptance/t011/011.5/tools/cdp.mjs docs/acceptance/t011/011.5/tools/t0115-visual.mjs      # 主批次（8 机位 + 风动双帧×3组 + 8槽 + LOD + console 轮1；1200ms 收敛）
node docs/acceptance/t011/011.5/tools/cdp.mjs docs/acceptance/t011/011.5/tools/t0115-barkfruit.mjs   # 树皮/果序批（console 轮2；帧为 clamp 前首拍——已被下条重拍取代，仅 console 证据有效）
node docs/acceptance/t011/011.5/tools/cdp.mjs docs/acceptance/t011/011.5/tools/t0115-barkfruit2.mjs  # 树皮/果序帧重拍（el=0 确定性版 + 1200ms 收敛；最终帧来源）
node docs/acceptance/t011/011.5/tools/pixel-diff.mjs <a.png> <b.png>                                 # 风动双帧像素判定
```

- 证据帧：本目录 **28 帧**（md5 全异唯 wind-frozen a/b 相同 = 冻结语义；baseline = 三必做统一基线帧）；stats 原始回读 `tools/main-stats.json`
- 判读帧与用途映射：身份五重（leaf-closeup/macro×2、B25/C7、trunk-lower-10m/bark-1p2m×2/baseline、F50/baseline、fruit-8m×2/C7）｜风动（wind-run/flutter/frozen 各 a/b）｜8 槽（slots-panorama-42m/50m-wide）｜LOD（lod-levels-32m + high/mid/low-25m + low-50m）｜辅助（vis-near-12m 结构/果球冠底、fruit-crown-edge-6m、trunk-lower-10m-az110、bark-trunk-1p2m-az110）
- 三门槛（npm test / check:layers / typecheck）：**本轮不跑**——D35 口径 011.13 末轮 QA 子代理统一执行（任务书 Acceptance 节）
- 进程清理：Chrome CDP 9333 + vite 5173 会话尾停（AGENTS.md 进程纪律）
