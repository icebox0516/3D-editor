# T006.6 LOD 选档稳定基准（High 档派生）

> 归属：[T006 Asset Runtime LOD](006-lod-chunking.md) ｜ 状态：**收口 2026-09-23**（Step 1 测试先行 + Step 2 稳定基准实装 done 2026-09-20——Step 2 经用户指令提前于 T011 执行；~~余 Step 3 A/B 实测 + 阈值重锁判定~~ → **Step 3 迁 T021.8 承接、本任务收口〔D41〕**，Step 1/2 交付为 T021 技术基线） ｜ 前置：T006.5（done）

## 背景（D28，2026-09-20 grilling 会话裁定）

用户检查 LOD 系统提出两项疑点，经事实探查 + 五题逐题裁定（DECISIONS.md D28）：

1. **选档基准现状 = 当前档位几何包围球**：两链一致——放置链 `InstancedAssetPool.frameLod` 取当前桶源 `pool.source` 的 `geometry.boundingSphere`（随迁档换源）；散布链 `ScatterChunkManager.frame` 按当前档位键取 `assetStates` 缓存半径。三档几何 bounds 实际不等（档间轮廓连续性容差 ≤0.4m，折算半径差 ~2-5%）——迁档后半径换源、m 读数平移的反馈环存在，但被 15% 迟滞带吸收，无观察病症（定性卫生债）。
2. **演进方向 = High 档派生的稳定资产级半径**（D28.2）：Runtime 在源解析时一次计算并缓存，两链恒用；**剔除不跟随**（逐桶视锥剔除继续用各档真实几何球——剔除用真实包围是正确语义）。
3. **测试先行（D28.6，Step 1）**：`InstancedAssetPool.lod.test.ts` 此前给三档硬 pin 同一 boundingSphere——假前提，不修则本任务改造无可证明的验收面。

## Goal

选档基准与当前渲染档位解耦：同一实例在任意档位下选档度量恒定，消除「迁档→半径换源→读数平移」反馈环；档间 bounds 差异不再影响选档。

## Requirements

- **Step 1 测试先行（本会话执行，验收见完成记录）**：假前提修复（三档真实差异化 bounds）+ 现状语义不变量锁定（档间半径差被迟滞带吸收、迁档半径换源不触发反向换档）；检查散布链测试是否存在同款假前提。
- **Step 2 稳定基准实装（done 2026-09-20，threejs-runtime-agent，用户指令提前于 T011——见完成记录）**：High 档派生基准球缓存（资产级、high 源到达时一次冻结）；两链选档输入切换到稳定基准；剔除路径不动；散布链 chunk 代表口径（最近点 + maxScale，D27.3）不变；新增断言「同一实例任意档位选档度量恒定」。
- ~~**Step 3 A/B 实测记档 + 阈值重锁判定**~~ → **2026-09-23 迁 T021.8 承接（D41）**：原「选档分布 A/B + 阈值重锁判定」升级为 T021 全链标定（表示链阈值 + 迟滞 + 过渡带 + 密度 + 批次 + 预算）的组成部分，基准球口径平移量实测并入其结论记档；本文件不再单独执行。

## Acceptance

- 稳定基准下：同一实例任意档位选档度量恒定（断言锁定）；真实差异化 bounds 下既有选档/迁档/迟滞断言全过。
- ~~Step 3 记档完成（平移量实测 + 重锁判定结论）~~ → 迁 T021.8 承接（D41，2026-09-23）。
- `npm test` / `check:layers` / `typecheck` 全绿。

## Constraints

- 稳定基准 = Runtime 从 High 档几何**派生的缓存值**，不是资产声明字段（D28.4——不违反 D27.12「调度阈值归 Runtime 全局策略常量」；`proceduralProfile` h/w 保持 T016 浏览语义不进选档）。
- 剔除不跟随（D28.2）；`sourceKey` 形态身份语义零触碰（D23.2）；桶键、迟滞、批次控制语义零变化。
- 派发 threejs-runtime-agent（AGENTS.md「多 Agent 按 Step 派遣」）；~~Step 3 为当前剩余步骤，执行时机由当前任务依赖与项目状态决定~~（**2026-09-23 Step 3 迁 T021.8，D41——本句仅存历史语境**）（~~作为 T018 启动前置条件之一（D31.4）~~ → **2026-09-23 用户指令「先跑 T18」取代：Step 3 不再阻塞 T018，改 T018 期间或之后执行**——选档分布实测与视觉环境无关）。

## 完成记录

**2026-09-20 Step 1 done（测试先行——D28 grilling 会话内执行，threejs-runtime-agent 交付 + 主代理 diff 审查与三门槛复验；Step 2/3 未开工）**

**① 假前提修复（三测试文件，零 src/ 改动）**：放置链 `InstancedAssetPool.lod.test.ts` 与散布链 `ScatterChunkManager.lod.test.ts` / `ScatterChunkManager.batch.test.ts` 此前均三档 pin 同一 boundingSphere（放置 r=2 / 散布 r=5）——改为按档差异化：`LEVEL_RADIUS = { High 基准, Mid −4%, Low −2% }`（High ≥ Mid/Low 高档包络不变量，档间轮廓差实测量级 2~5%），`leveledSource(level)` 按档钉球，新增机位工具 `cameraForMeasuredM` / `cameraAtMeasuredM(m, radius)` 按当档半径定标。其余散布测试（ScatterChunkManager.test / RegionScatterSync / RuntimeViewport.scatter）经检查不存在该前提。

**② 既有断言处置**：28 条既有 it 逐条推演——25 条评估余量 ≫ 2~4% 平移保持成立零改动；3 处机位**定标口径**调整（放置/散布迟滞防抖与静止相机按 mid 半径定标、batch 合并成员 culled 角点循环按 low 半径折算），理由均为「条目已迁档后读数按当档半径换算」的现状语义正确锁定，非期望值挪动（例：原越线步读数 4.947 ×2/1.92 = 5.153 > 升档线 5.1 会断言必挂）。

**③ 新增不变量 +6（每链 3 条，本步核心价值）**：
- **(a) 半径差被 15% 迟滞带吸收**：跨线降档 → 同机位下一帧读数平移 +4.2% 零换档 → 读数在名义线 ±3%（≥ 半径差量级）来回跨线 6 次全部保持 mid、桶集/源请求零 churn → 决定性越升档线单次回 high；
- **(b) high→mid 迁档半径换源整链**：判别机位读数 5.2 ∈ [升档线 5.1, 名义线 6) 停留 mid——若下一帧仍用旧 high 半径评估读数 = 5.2×1.92/2 = 4.99 < 5.1 会立即升回 high；停留 mid = 「下一帧读数已用新档半径」+「平移不触发反向换档」双证据；
- **(c) mid→low 读数回缩方向**（Low 半径 > Mid，平移朝升档线的不利方向）：同机位回读 15.72 名义回 mid 带（< 16）但 > 升档线 13.6 不回弹——整链在平移两个方向都锁定。

`ScatterChunkManager`「档间轮廓连续不变量下各档近似同值」自此从注释级假设升格为测试级现状锁定。

**④ 三门槛（主代理复验）**：npm test **201 文件 / 2848 全绿**（基线 2842 + 6）、check:layers **485**、typecheck **零错**；零 src/ 改动，渲染行为零变化。

**⑤ 移交 Step 2 注意点**：稳定基准（High 档派生半径）实装时，(b) 条判别机位断言（4.99 反证逻辑）与 (a)/(c) 的「平移被吸收」叙事将随语义变化**同步改写**——预期行为非缺陷（届时「同一实例任意档位选档度量恒定」断言新增，替换现状锁定）；batch 文件 culled 成员机位 (0,1) 读数 59.95 距 culled 线仅 0.08%（确定性浮点构造无漂移，调 `LEVEL_RADIUS.low` 时需同步关注该循环落点）。

---

**2026-09-20 Step 2 done（稳定基准实装——用户指令提前于 T011 执行；threejs-runtime-agent 交付 + 主代理 diff 审查与三门槛复验；Step 3 待做）**

**① 实装**：新增 `src/runtime/lodReference.ts`（`LodReferenceSphereCache`）——两链共享的 High 档派生基准球缓存，持有独立 Sphere 拷贝（不引用几何球对象，几何归源端释放不影响基准）；模块头注载明 D28.2/D28.4 契约边界。放置链 `InstancedAssetPool`：high 桶源到达（ensurePool）按 sourceKey 冻结派生，`frameLod` 选档输入改取基准球——**整球消费**（代表点 = 基准球心过实例矩阵、radius = 基准半径；只缓存半径会让 d 随档微抖）。散布链 `ScatterChunkManager`：high 源到达（requestAsset）按 assetId 冻结派生，`frame` 选档半径经 `referenceRadiusOf` 取基准（代表点 = 块盒最近点不变）；`AssetSourceState.radius` 死字段删除（唯一消费者是旧选档输入——**勿复活该惰性缓存**，会重引档间平移）。**派生只在 level==='high' 到达路径发生**（mid/low 到达不写缓存，首个解析档不会冒充 High）；冻结后同 key 幂等 no-op（源 evict 重建同 key 几何确定性恒等）。防御回退（会话内不可达——两链结构上 high 先解析：放置 attach 即建 high 桶、散布块注册即请求 high 源，条目首评前基准必已派生；不可达性推演记档于交付报告）：miss 时回退当前档源球过渡一帧，不冻结、不报错、不阻塞。

**② 零变化面核验**：剔除路径（InstancedMesh.computeBoundingSphere 各调用点 / chunk.box 块剔除 / refreshSubmitVisibility）零触碰；评估器 `evaluateLodRepresentation` 签名与语义零改动；Renderer 接线零改动；零 domain / 零资产声明文件（asset.ts / ModelAsset / AssetDescriptor / proceduralProfile 红线遵守）；LOD off / culled / 迟滞 / 桶键 / 迁移 / 拾取语义不变。

**③ 测试改写（+2 → 2850）**：Step 1「平移被吸收」现状语义组按稳定基准语义反转——(b) 判别机位反转（High 口径读数 5.05 < 升档线 5.1 **即时回 high**；半径泄漏 5.26 / 球心泄漏 5.30 双口径停留 mid 的反证一网打尽）；mid→low 改「无回缩」（同机位读数 16.05 恒定零二次换档）+ low 基准泄漏判别（13.55 < 13.6 即时回 mid，误按 low 半径 13.83 会停留 low）；机位定标统一回归 High 口径（Step 1 的 mid/low 半径定标三处调整撤销）；**新增两链各 1 条换档历史无关性测试**（不同起径确定性到达 high/mid/low 后，同机位选档结果一致——含 13.55 升档线内侧判别位）；测试侧新增 `LEVEL_CENTER` 球心应力（Mid/Low 沿视轴 ±0.5——选档若误取当档球心，读数平移 ±0.25（m 口径）≫ 迟滞带 15% 必翻转）。单档资产（唯一源即 high）行为逐位保持（既有单档测试全绿即回归面）。batch 文件 culled 角点 (0,1) 读数 59.943 距 culled 线 0.10%——脆弱点**未消除**（`h += 2` 步进 + 角点渐近比构造的固有性质，与半径口径无关；确定性浮点无漂移，后续调 `LEVEL_RADIUS.high` 需关注该落点）。

**④ 三门槛（主代理复验）**：npm test **201 文件 / 2850 全绿**（Step 1 基线 2848 + 2 = 两链换档历史无关性）、check:layers **486**（485 + 1 新文件 lodReference.ts）、typecheck **零错**。

**⑤ 生产语义影响（Step 3 观察面）**：mid/low 档下选档读数整体 +2~4%（换用 High 半径，m=d/r 反比）→ 实际运行中换档点比旧实现略偏近侧；阈值重锁判定待 Step 3 A/B 实测记档（预期不触发：平移量 = 档间半径差 2~5%，处 15% 迟滞带内）。
