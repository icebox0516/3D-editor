# T009.1 枝干结构真实性

> 归属：[T009 夏栎结构真实性与生产化](009-tree-realism.md) ｜ 状态：**done（2026-09-18）** ｜ 前置：T008.6（参考基准）

## Goal

建立夏栎内部结构参数面（shapeProfile）并升级枝干结构真实性：主枝 > 次枝 > 末枝视觉权重可感知；**冠内空间通透规则显式化**——建立枝干通道、内层密度衰减与局部空腔控制，而非固定挖空。

## Requirements

- **shapeProfile 参数面**：crownCenter / crownHeight / crownWidth / mainBranchSpread / asymmetry / canopyDensity / crownTopBias 等（字段集结合 T008.6 参考分析定稿）；008.2 的 LEVELS / TRUNK / CHILD_PLAN / LEAF 硬编码常量改造为 shapeProfile 消费
- **主次分级**：一级骨架枝与末梢枝的视觉权重拉开（在现有首枝主导 ×1.08 基础上强化层级对比）——所有分枝视觉权重接近即不达标
- **冠内通透**：显式规则替代「叶卡只挂外段」的涌现式空腔——枝干通道（主干大枝进冠不被封死）、内层密度衰减、局部空腔控制
- **slot-0 锚点重校**：以标准组合初值重校 slot-0，锚点基准可辨识（还是那棵夏栎，结构更可信）

## Scope

- 预期触碰：`src/runtime/procedural/tree/broadleafGeometry.ts`、`src/runtime/procedural/assets/asset_tree_3a.asset.ts`（slot → 结构配置路由）
- 不碰：契约层（InstanceSource / sourceKey 语义）、材质（tree3aMaterials 只消费不动）

## Acceptance

- slot-0 过结构验收：主干自然 / 层级清晰 / 粗细递减 / 冠内通透可见（对照 T008.6 参考表「已符合/偏差」项）
- 固定机位取证存档；结构计数测试锁同步更新（同槽确定性原则延续：同 asset + slot + level 生成完全相同 Geometry、同槽重复构建面数恒等；跨 slot 面数差异为 8 槽形态向量的设计预期，受 009.6 LOD 预算约束，不作恒等要求）
- **面数不作硬验收**：记录实际面数，仅在显著超出现有 32K 量级时回落；正式 High/Mid/Low 预算由 009.6 锁定
- `npm test` / `check:layers` / `typecheck` 全绿

## Constraints

- **建立夏栎内部结构参数面，不扩展 ProceduralBuild 公共参数；build 仅负责将 slot 路由到对应结构配置**（防子代理自行发挥扩公共签名）
- D19 契约第一锁不破；同 seed 逐位复现、同槽同引用测试延续

## 完成记录

**2026-09-18 done（procedural-asset-agent 交付，主代理 diff 审查 + 三重门槛复跑 + 四机位视觉裁定全过）。**

**执行链路**：主代理简报派遣 procedural-asset-agent（开工前 Spec Version 校验 = 1.0 通过）→ 交付 → 主代理 diff 审查（契约面核查：公共签名未动 / 恰 2 组 / rng 无条件消费保确定性）→ 三重门槛复跑 **2452 全绿（177 文件，+9 测试）**→ 四机位固定取证 + 视觉通道结构裁定。

**交付物**：
- **shapeProfile 参数面** `src/runtime/procedural/tree/tree3aShapeProfile.ts`（新建）：五组字段（冠形 / 骨架 / 逐级分级 / 冠内通透 / 叶卡与拓扑预算），逐字段标注 Spec 依据与 Evidence Status——**Unknown 项（radiusRatio / lengthRatio / 锥度）明确标「工程设定，无现实基准」，不编造现实依据**；slot-0 标准组合初值 + 8 槽路由表（当前仅 slot-0，未列槽回落）。LEVELS/TRUNK/CHILD_PLAN/LEAF 硬编码常量全部删除改 profile 消费；资产侧 `profileForSeed()` 枚举 8 槽 morphSeedOf 还原 slot → profile（O(8) 纯查表确定性）——**ProceduralBuild 公共签名未动**
- **主次分级**（broadleafGeometry）：①骨架枝 rank 势差（长度 ×1.14→0.81、起径 ×1.0→0.85，强化自 008.2 单点 ×1.08）；②逐级半径比低级陡末级缓 [0.48, 0.54, 0.60, 0.65]（自近平的 0.55+0.03·level）；③L1 末径比 0.7 通体粗壮（Spec「骨架枝粗壮有力」Verified 的方向性表达）；④wander 姿态分级（粗枝 0.12 刚直 → 细枝 0.34 纷乱）。实测各级均起径 [0.0832, 0.0314, 0.0121, 0.0052, 0.0028] 严格递减，L1/L2 = 2.65、L1/L5 = 29.8（测试锁 ≥2.2 / ≥12）
- **冠内通透显式规则**（任务核心，替代「叶卡只挂外段」涌现式空腔）：①**枝干通道**——主干→领导枝连续中轴保护段（r=0.42m）+ 5 骨架枝基段（0.75×、45% 枝长），带内叶卡硬剔；②**内层密度衰减**——q = r/rShell(y) 冠内归一化径向深度密度场（q≥0.55 壳层满密 → q≤0.12 芯层地板 0.05，× canopyDensity × crownTopBias），rng 概率剔；③**局部空腔**——3 个 rng 驱动偏心空腔球（0.5–0.9m）硬剔。叶卡挂点自 008.2 外段限制（L4 0.55 / L5 0.30）放宽至沿途（0.18 / 0.08），内层稀疏交由密度场接管。rng 每卡无条件消费 1 次——**分支无关确定性保持**。slot-0 实测：候选 5670 → 存活 4398（梯度 726 + 空腔 546 + 通道 0），内核保留率 0.330 vs 外壳 0.985（外密内疏结构化证据），通道保护带 0 入侵

**结构计数新旧对照**：皮 20724（拓扑恒等：五级 [6,18,54,162,324]、径向 12→4、主干 14 环）｜叶卡候选 5670 → 存活 4398｜总面 32064 → **29520（-8%，32K 量级内不回落）**。同槽确定性延续：同 seed + 同 profile 两次构建全属性逐位相等；跨槽面数带 28672–30292（009.3 八槽设计预期）。

**测试**：`asset_tree_3a.test.ts` 更新（triangleCount 29520；「面数槽间恒定」收窄为「同槽重复构建恒等 + slot-0 = 声明实数」——D19/D20.1 同槽确定性口径）；`tree3aStage.test.ts` 计数锁 8796/4398；**新建 `broadleafStructure.test.ts` 9 条**（确定性 2 / 主次分级 2 / 通透 4 / canopyDensity 参数驱动 1）。2443 → 2452。

**主代理视觉验收**（四机位固定取证存 `docs/acceptance/t009/009.1/`：near-12m / mid-25m / far-50m（az35 el8）+ backlight-25m（az275 背光看通透），freezeTime 冻结风相位，console 零错误）——任务书四判据全过：**主干自然** ✓（近直立，中距/远距确认）；**层级清晰** ✓（近距确认一级骨架枝明显粗壮、末梢落差可感知）；**粗细递减** ✓（主干→骨架枝过渡良好；轻微观察项见下）；**冠内通透可见** ✓✓（中距 + 背光双确认：外密内疏、逆光透空破碎感、骨架枝剪影可读、地影斑驳非整块实影——通透不挖空整层，冠顶/冠底 1/3 带均有存活卡有测试锁）。

**次要观察（记档不返工，按去向分流）**：①二级→末梢个别分枝点轻微突细 + 少量叶卡悬浮观感（offset 外扩技法）→ 近景微调域，随 009.4 / 009.6 综合裁定；②根部 flare 观感偏弱（实现有 1.32× 指数衰减，25m 下不显；spec basal_flare = Unknown 不判对错）→ 可随 009.4 补研；③冠幅比偏矮胖（远距剪影反馈）→ 已知偏差#2，明示归 009.3；④背光下空隙偏细碎均匀、缺大尺度撕裂口 → voidCount/voidRadius 参数面已暴露，009.3 槽参数可调；⑤DEV 舞台 FPS 读数 10（dev 模式 + 连续渲染瞬时值）→ 非本任务验收项，正式性能口径归 009.7。

**延期项**：spec 附录#16（小枝赭/红褐 + 灰皮孔与主干材质分化）——表达需末级枝独立材质组（破「恰 2 组」冻结契约）或皮材质按枝级分支着色（动 tree3aMaterials，本任务 Scope 排除）；建议去向 009.4 近景工作或材质专项（皮材质可按 uv 弧长/aBend 近似区分枝级，归 park-shader-agent）。

**slot-0 重校摘要**：动了 rank 势差 / 逐级半径比 / L1 锥度 0.62→0.7 / scaffoldThickness 0.55→0.58 / 横展角显式参数化 55–70° / wander 分级 / 叶卡挂点放宽 + 三条通透规则；**已符合项全部保持**（主干倾轴 ≤4°、五级分枝、骨架枝 5+1、挂高 0.55–0.95 干高段、视觉冠底 ≈0.28、干高 40–46%、flare 1.32×、主干锥度 0.42/t^0.85、叶卡朝向/尺寸/UV/aLeafRand/aBend/恰 2 组契约）。冠幅比中位未调中心值——裁决明示归 009.3，本任务仅参数化。
