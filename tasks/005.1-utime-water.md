# T005.1 uTime 广播 + 水面

> 归属：[T005 Animation Pathway](005-animation-pathway.md) ｜ 状态：**not-started** ｜ 前置：无硬依赖（水面预设接线建议在样式体系稳定后）

## Goal

D12 通路交付：全局时钟 + 注册式 uTime 广播 + 可暂停；水面流动作为首个消费者示范「声明即活」。

## Requirements

- **时钟服务**：渲染循环推进全局时间；uTime 广播——材质/样式实例声明 uTime uniform 即自动注册驱动（零逐实例 JS tick）
- **全局暂停**：广播可暂停/恢复（截图取证、场景导出时帧稳定）
- **水面接线**：水面预设接 uTime：流速 / 波幅参数化实时可调（StyleParameter 表达）
- **零开销证明**：未声明 uTime 的材质每帧零额外成本；既有 24 套预设行为零变化

## Acceptance

- 水面持续流动、参数实时生效；暂停后连续两帧截图逐字节一致
- 既有预设全量回归零变化；三重门槛全绿

## Constraints

- 渲染循环保持每帧连续渲染（勿改按需渲染）；时钟/广播服务派 `threejs-expert`，水面 shader 接线派 `park-shader-agent`（同一会话按子系统拆分简报）；动画观感必须截图验收

## 完成记录

（会话结束必写）
