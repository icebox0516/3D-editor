# T009.5 程序化资产公共 Shadow 能力

> 归属：[T009 夏栎结构真实性与生产化](009-tree-realism.md) ｜ 状态：**done（2026-09-19，汇入 009.7 验收收官）** ｜ 前置：无 epic 内硬前置（可与 009.1–009.4 并行；汇入 009.7 验收）

## Goal

补当前真实缺口——**ProceduralSourceCache → InstanceSource → InstancedAssetPool → 正式场景**的生产 Shadow 路径：公共契约扩展 + 正式放置对象投影与叶形裁切。夏栎为第一个实际验证该公共能力的 Procedural Asset。

## Requirements

- **契约扩展**：InstanceSource 增可选 `customDepthMaterial?`（`customDistanceMaterial?` 仅类型位、零实装零消费，点光源影需求出现时再立项）
- **核心链接入**：InstancedAssetPool 4 处建网格（split 亮网格 / 单实例退化 / 主 InstancedMesh / 锚点装饰）挂载深度材质与 castShadow；dispose 语义同步（ProceduralSourceCache / ThumbnailCache / tree3aStage，防泄漏——「池不 dispose 共享模板」原则不破）
- **分口径（不绑成一个验收项）**：
  - **正式放置对象 = 必须支持 Shadow**（投影 + 叶形裁切）
  - **Ghost / Preview = 必须不破坏 Shadow 契约、不要求投影**（预览态不强求 castShadow，避免预览与编辑场景产生额外阴影干扰）
  - **ScatterChunkManager = 仅契约兼容验证**（类型通过、无行为破坏），不做新 Scatter 功能
  - **AssetLoader（GLB 端）= 类型兼容、零行为变化**（回归项非接入项）
- **aSeed 契约兼容实测**：aSeed 共享几何在散布 / Ghost 路径的实际渲染行为验证，修正 InstancedAssetPool 类头「其他网格材质不声明 aSeed」过时断言
- **池类头纪律（D23.6）**：修正过时断言时遵循「稳定契约留类头、实现事实落任务完成记录」——类头只保留 aSeed 必须 geometry 绑定 / 池不 dispose Source / Ghost 不走本池等稳定契约，不借机向类头堆新规则
- **夏栎首验**：tree3a 叶片 SDF 深度材质（现有 `createTree3aLeafDepthMaterial`）接入正式路径——DEV 预览 = 正式场景

## Scope

- 预期触碰：InstanceSource 类型定义、InstancedAssetPool（建网格 + 类头断言修正）、ProceduralSourceCache dispose、ThumbnailCache、AssetLoader 类型兼容、ScatterChunkManager 契约兼容行、tree3aMaterials / tree3aStage 挂载
- 不碰：散布功能开发（T003 冻结）、GLB 行为、散布侧 aSeed 同相位修复（留 T003 解冻后独立任务）

## Acceptance

- 正式场景（产品放置路径）夏栎投影正确 + 叶形裁切正确；DEV 出图面与正式场景影裁切一致
- Ghost 存在时 Shadow 契约无破坏（无投影要求，分开验收）
- GLB / 设施资产回归逐位不变；资源泄漏检查过（dispose 链完整）
- renderer.info 取证（Shadow pass 开销、draw call 变化）；`npm test` / `check:layers` / `typecheck` 全绿

## Constraints

- 派发按 AGENTS.md「多 Agent 按 Step 派遣」执行（本任务为架构/契约/管线类 Step，非着色器创作）
- T003 冻结口径 = 仅契约必要行触碰，不恢复散布功能开发
- 不破坏 D19 锁定规则链与「池不 dispose 共享源」语义

## 进度

- 2026-09-18 先行补丁（用户指令，threejs-runtime-agent 交付）：InstancedAssetPool 三处建网格（singleMesh / 诊断亮网格 / 主 InstancedMesh）已设 `castShadow/receiveShadow = true`（锚点装饰 Mesh 按池边界排除——不进场景仅包围盒），正式放置对象投影链路已通，三重门槛全绿（2443 用例）；两处过时注释同步（tree3aMaterials 头取舍记档 / asset_tree_3a.asset.ts）。**本任务余下职责不变**：customDepthMaterial 契约通道（叶形裁切进产品路径）、dispose 链同步、分口径验收、ScatterChunkManager 契约兼容验证、aSeed 类头过时断言修正。
- **防误判注（2026-09-18，同日更新）**：先行补丁原为用户指令下 threejs-runtime-agent 交付的工作区小改动，2026-09-18 随 008.6 放行、T009 解锁后收编提交（三重门槛 2443 全绿）。本任务状态仍 not-started——castShadow 直通只是投影链路，customDepthMaterial 契约通道（叶形裁切进产品路径）、dispose 链同步、分口径验收等余下职责未动，完整启动归 T009 epic 排期；真实当前任务见 PROGRESS.md「当前子任务」行。

## 完成记录

**2026-09-19 完成（threejs-runtime-agent 交付 + 主代理审查验收，汇入 009.7）**：

- **契约**：`InstanceSource` 增可选 `customDepthMaterial?`（归源所有——与 geometry/material 同生命周期，源端统一 dispose，消费方只挂引用）与 `customDistanceMaterial?`（**仅类型占位**，零实装零消费零 dispose——点光源影立项时一并实装，JSDoc 记档）。
- **池挂载**：InstancedAssetPool 三建网格点（singleMesh / instancedMesh / ensureSplit 亮网格）条件挂载（undefined 不赋值 → GLB/旧资产零变化）；锚点装饰 Mesh 不挂（不进场景，任务边界排除）。
- **夏栎首验**：asset build 返回 `createTree3aLeafDepthMaterial(level)`（档位匹配 Mid=High SDF / Low=Low SDF）；tree3aStage 四挂载点改**同源消费** source.customDepthMaterial（DEV 预览 = 正式场景同一份深度材质，单测锁同一实例），仅 fake build 注入不带字段时回退自建；模块头三处取舍记档改写（asset/materials/stage）。
- **dispose 链**：ProceduralSourceCache.releaseSource（evict/dispose 共用）、ThumbnailCache.captureProcedural finally、tree3aStage.disposeSource 均补深度材质释放；池任何生命周期点不 dispose（源所有权，spy 反向断言）。
- **aSeed 类头修正（D23.6）**：过时断言「Ghost/锚点/散布网格材质不声明 aSeed」改写为实际行为——T008.3 起夏栎材质声明 aSeed，非实例绘制（Ghost/锚点装饰共享同几何材质）读缓冲首元素/GL 缺省 0，确定性相位无未定义行为；实测细节落本记录不进类头。
- **零改动面**：ScatterChunkManager / AssetSourceRouter / AssetLoader（typecheck 即证契约兼容；GLB 行为零变化）。
- **验收（取证 `docs/acceptance/t009/009.5/`）**：产品路径（放置命令管线）3 棵逆光 22m + 影近景 15m——**SDF 叶形裁切影确认**（锯齿叶缘 + 冠影内透光斑驳，非整卡剪影）；DEV 同机位对照同源一致；Ghost 激活态半透明在位、自身不投影、已放置树影完好、无渲染异常；drawCall 账目自洽（单树 7 / 3 树 15 / Ghost +2）。
- **门槛**：+11 测试 → 2542 全绿三门槛（与 T009.7 驱动 +17 同会话合并计数）。
