# T021.3 Transition Runtime

> 状态：**done（2026-09-24）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：021.2

## Goal

通用过渡机制：dither / fade 状态机、双表示共存提交、目标表示 Source 就绪排队（sourceReady）、过渡期剔除并集、双表示 DC 增量计数面。机制先在既有链（Mid↔Low 硬切位与 Low 退场）验证，canopy 链 021.7 接入后复验。

## Research Gate

豁免（epic 级记档，非程序化资产生产任务）。

## 待裁决位

- 过渡分型默认：High↔Mid 硬切；Mid/Low→Canopy dither；Canopy→Cull fade out
- 阴影过渡中点切换——本任务只留切换钩子，策略归 021.5；不做双 Shadow 交叉渐变
- 过渡带宽度定义在 metric 空间（候选比例，021.8 锁定）；双表示桶 DC 增量红线 +30（计数面本任务交付、判定 021.8）
- 过渡期 Frustum 边界 = Current 与 Target 的并集，防一侧被提前剔除
- dither 阈值与既有 alphaTest 裁切的合成顺序 = park-shader-agent 交付必答题（简报点名）
- pending 语义并入 target + sourceReady，不设第三字段
- 散布过渡态按 chunk × asset 粒度维护；放置对象允许逐实例 fade

## Scope

- `src/domain/lod/`（过渡状态机）、`src/runtime/` 两链过渡执行与提交、dither shader hook（park-shader-agent）、`tests/`。

## Acceptance

- 远离 / 靠近 / 往返 / 快速拖拽 / 慢速移动 / FOV 改变：无震荡、无重复重建、无闪烁、无明显 pop
- main render / depth / picking 状态同步；DC 增量计数面可观测
- 三门槛按 AGENTS 判据式口径

## 取证路径

`docs/acceptance/t021/021.3/`

## 完成记录

**2026-09-24 执行（Step A 过渡机制 threejs-runtime-agent → Step B dither 材质 park-shader-agent 串行 + 主代理视觉冒烟取证）——完成，三门槛 4129 全绿。**

- **Step A 过渡机制 runtime**：新增 `src/domain/lod/transition.ts`（过渡状态机纯函数零 THREE：分型表〔High↔Mid 与 Mid↔Low 硬切瞬时 / Mid·Low→Canopy 双向 dither / Low·Canopy→Cull fade-out 退场、high·mid→Cull 维持瞬时硬切——streetlamp/GLB 行为零变化〕、metric 步进而非 deltaTime〔§5.2 带宽 metric 空间：帧率无关、可逆、状态机零内部簿记，SelectionState 保持 §10.1 五字段原形〕、双向过渡均 (1,0) 起步 (0,1) 完成〔降档带锚名义线、升档带锚迟滞线——决策翻转点 fade 连续，不引入第二震荡源〕、`TRANSITION_BAND_RATIO = 0.25` 候选常量〔021.8 重锁〕、TransitionCommit 提交决策 + `shadowRepresentation`/`midpointCrossed` 阴影中点切换钩子〔021.5 接线面，本次零 Shadow 行为接入〕）；新增 `src/runtime/instancing/fadeGeometry.ts`（**aFadeOut 属性缝**：InstancedBufferAttribute Float32 itemSize1 非 normalized、语义 = 退场度 0 完整呈现→1 完全退场〔取退场度正是为 GL 缺省 0 = 安全满呈现——aSeed 缺省先例的安全方向版〕、包装 BufferGeometry 共享顶点数据独占实例缓冲、FadeGeometryPool 归还复用禁 dispose）；`InstancedAssetPool.ts`（per-object SelectionState + 客座镜像双表示 + 逐实例 aFadeOut + culled 改 commit 驱动）；`ScatterChunkManager.ts`（chunk×asset 过渡态 + incoming 免合并客座桶 + 双侧 fade + lod.box Union 剔除〔客座并入/拆除确定性重算〕+ 到达回调建客座〔硬切位沿用 pending 确定性重撒、触发改 commit.completed，行为等价〕）；`lodDistribution.ts`（transition { instances, buckets, dualSubmitBuckets } 计数面——红线 +30 判定归 021.8）；`Renderer.ts` 仅 provideSource 闭包 canopy 窄化（回退 high 源，021.7 移除）。衍生缺陷修复一处：到达回调重建后状态机未归位 steady 致下帧跳过重建（两链同修）。
- **Step B dither×alphaTest 材质**（必答题已答，详见 `treeFadeDither.ts` 头注）：新增共享注入模块 `src/runtime/procedural/tree/treeFadeDither.ts`——像素存活 ⟺ ( SDF alpha 经 alphatest_fragment 的 fwidth smoothstep 重映射 > 0 ) ∧ ( IGN ≥ aFadeOut )；注入点 = `#include <alphatest_fragment>` 之后（three 原生 alphahash 槽位；fade 与 alphaTest 是 AND、交换不改存活集）；**A2C 不吃 fade**（fade 是独立 discard 门，不读写 diffuseColor.a——SDF 坡→fwidth→采样掩码值流逐位不变，0.04 坡宽/75% 齿幅纪律不受触碰）；图案 = 屏幕域 IGN（静止相机逐像素逐帧稳定、无时间项零 uniform、风动时图案不沸腾）；**镜像互补设计**：direct（13 树种叶/皮）`ign ≥ fadeOut` 存活 × mirrored（canopy card/trunk，图案 = 1−IGN）`ign ≤ 1−fadeOut` 存活，代入互补恒和 → 两方向同像素恰一侧存活（同 direct 会在中点产生 50% 绝对透底空洞——镜像变体是必需设计）；canopy/各树种 customDepthMaterial **零注入**（阴影过渡走中点切换 §5.4，不做双 Shadow 交叉渐变）；应用面 = 13 树种（含 tree3a）× 叶/皮各一行 + canopy card/trunk（mirrored），共 28 处、深度工厂零触碰；缓存键后缀 `+dither` / `+dither:mirror`；零回退 = 缺属性/值 0 时 `ign < 0` 恒假零 discard（tree3a 基线保护 = 默认态行为零变化）。
- **测试**：新增 `tests/domain/lod/transition.test.ts` 26 例 + `tests/runtime/procedural/tree/treeFadeDither.test.ts` 21 例 + 两链/分布/材质同步改写（逐条记档于各文件头注「改写记档」节与 Step 报告；关键语义迁移：「超远零缩放/隐藏」→「退场带内可见 + aFadeOut=f、终态线外才零缩/隐藏」「退场带内可拾取（§12）、终态不可拾取」；canopy 执行路径以假想声明资产验证——客座桶建立/双侧互补/Union 正交差分/带末迁移/升档 dither；86 处材质缓存键字面量同步 `+dither`）。全量 4129（基线 4073 + 56 新增）。
- **视觉冒烟取证**（`docs/acceptance/t021/021.3/`，主代理派通用代理执行；harness 仿 021.6 最小页 + InstancedMesh 真实消费路径，T018 day 链，celtis low 树 × canopy 同位交叉）：7 帧全过——console/shader 零错误（programs 恒 7，fade 写出零重编译）；溶解渐进单调（0.3485→0.3642→0.3798，目视细颗粒 dither 无整块消失）；中点互补公式逐点吻合（实测 union 与 (1−p)·low+p·canopy 预测误差 ≤0.0004；中点覆盖率 2.18× 单侧 50%——非透底）；零回退位同（带缓冲 0 vs 缺属性 GL 缺省 0 全帧 FNV 位同；材质参数快照未动）；静态稳定（同态 300ms 位同 + 改值往返位同）。观察记档：canopy 满覆盖 > low 满覆盖（卡吞并壳卡间隙设计语义，跨度基准归 021.8）。
- **边界记档**：静止相机停在过渡带内 = 双表示持续并存（dualSubmitBuckets 计数面可观测，021.8 判定；可选对策收窄 W 或带内提前完成——本次未做时间维兜底避免第二状态源）；mid→culled fade 化为潜在扩展位留 021.8；fade-out 反向在迟滞线 51 < 带起点 60 下瞬时满呈现无跳变（迟滞带若 021.8 收窄至两线交叠需复核该不变量，transition.ts 头注已记）；岛诊断 split override 材质不消费 fade（aSeed 同款文档化边界）；非实例绘制读 element 0（同先例）。
- 三门槛：`npm test` 264 文件 / 4129 全绿；`npm run check:layers` 617 文件全过；`npm run typecheck` 零错。
