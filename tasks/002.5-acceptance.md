# T002.5 T002 验收门

> 归属：[T002 Procedural Assets](002-procedural-assets.md) ｜ 状态：**done（2026-09-16）** ｜ 前置：002.1–002.4 全部

## Goal

Epic 级验收（D16：每个大任务完成再验收）：程序化资产全链路 GUI 截图取证 + 全量回归。

## Requirements

- **验收场景脚本**：5 种设施各放多实例（含连续批次与变体）→ 逐项截图取证：库内浏览、Ghost、放置结果、变体差异（同资产一排对比）、合批统计（draw call 实测数）
- **资源纪律**：删除全部对象 + 清场后 console 零错误；再放回验证缓存复用
- **撤销链抽查**：连续批次撤销 / 重做 / 换资产绑定的正确性
- **全量回归**：三重门槛 + 与 T002 开始前基线的测试数对账

## Acceptance

- 截图与 draw call 数字留档（写入完成记录，图存 `docs/` 或任务目录）
- console 零错误；撤销链正确；三重门槛全绿
- 结果同步：TASKS.md T002 行勾选、PROGRESS.md 快照更新

## Constraints

- 取证按 `docs/threejs-debugging.md`：MCP 结构化取数（先过金丝雀）+ 浏览器截图像素验收

## 完成记录（2026-09-16）

**取证通道**：按 `docs/threejs-debugging.md` 先拉 MCP HTTP（:9300）过金丝雀——`renderer_info` 读到 200×140 小地图上下文（错绑定，与 T8.4 记载一致），结构化工具按细则 1.4 弃用，回退浏览器方案（agent-browser 驱动 + 页内 eval 取数 + 截图留档）。视觉模型服务（analyze_image）本会话全程断连（Connection closed，多次重试无效）——像素级判读改由程序化指标代理（见下），截图全部留档 `docs/acceptance/t002/`（14 张）供人工复核。

**① 验收场景与截图取证**（场景：5 资产 41 对象 = 长椅 12 + 路灯 20 + 垃圾桶/消防栓/标识牌各 3；连续批次 + 变体掷骰）

- 库内浏览：facility 分类恰好 5 张程序化卡；「全部」18 卡 GLB/程序化混排（01-browser-facility）；缩略图清空 IndexedDB 后全新生成，全为 PNG dataURL，持久记录实测 1.7–8.4KB
- 搜索：「公园长椅」「路灯」均唯一命中（02 / 02b）
- Ghost：卡片激活 `ed-card--active` + 指针移动 Ghost 亮出（03-ghost-parkbench）
- 放置结果：Objects 41 / Triangles 12.6k ≈ Σ面数×数+底面（05 / 05b），单枚放置耗时 0.5–4ms
- 变体差异（同资产一排对比）：Inspector 逐对象实测 8 只公园长椅——旋转 Y 散布 -145.5°~174.4°、缩放 0.95~1.08、seed 8 枚全异（1115353235 / 1852381110 / 583813920 / …），色相由 seed 确定性复算（04 / 12 截图 + 06-inspector-seed）

**② 合批实测**（主视口 WebGL draw 调用计数，原生 `drawElements*` 原型挂钩，页级按 canvas 归属；窗口后台节流 FPS≈10，各窗口等帧率可比）

| 场景 | 主视口调用/3s | 折合/帧 |
|---|---|---|
| 空场景 | 102 | 3.0 |
| +8 只长椅（Ghost 在） | 1221 | 37（Ghost 预览占 ~17） |
| +12 只长椅（Ghost 收） | 680 | 20 |
| +20 只路灯（同资产连放） | 884 | 26 |
| 全选删除后 | 102 | 回到基线，池全释放 |

判定：**8→12 只同资产实例 draw 调用零增长（680→884 为新增路灯池 +6）；20 连放仅池级成本，实例数无关**——合批生效。Ghost 期间每帧 +17 为预览 Mesh 自身（多材质组 + 阴影 pass），退出即恢复。

**③ 资源纪律与缓存复用**

- 全程 console 零错误（页内 console.error/错误事件/rejection 挂钩计数 0；浏览器侧 console 仅 vite/DevTools debug-info）
- 全选→Delete：41→0 对象、Triangles 归 4、draw 调用精确回落基线 102，无报错
- 文件菜单「新建场景」：历史栈清空（redo 试探无效果）、零报错
- 清场后重放公园长椅：pointerdown 同步耗时 1.1ms（会话构建缓存命中，无重建尖峰），落地正常
- 缩略图两级缓存：清空 IDB → 重载自动重生成并全部持久化（含消防栓 6118B 真值覆写注入的假值）；热缓存重载 2.7s 内 5 缩略图全部就位（持久层命中）。**T002.4 遗留①（垃圾桶缩略图陈旧缓存）验证完毕：清缓存后新几何缩略图正确生成**。（过程中一次「程序化键不落库」经干净重启复验为取证脚本自身 deleteDatabase 事故的残留假象，非缺陷；GLB 懒生成键 streetlight@10876 同场验证通过。）

**④ 撤销链抽查**

- 连续批次：undo 一次撤整批标识牌（41→38），redo 恢复（→41）
- 同 seed 一致性：撤销+重做往返后两帧主画布快照**逐字节相等**；对象 seed 不变（656791432，命令存档不重摇）
- 资产绑定：新会话放置单只长椅后 undo 仅移除该枚（42→41，尾行回到标识牌），不吞并他资产批次

**⑤ 全量回归与对账**：`npm test` **2143/2143** 全绿（158 文件）｜ `check:layers` 401 文件全过 ｜ `typecheck` 干净。对账：T002 前基线 1989 +（24+19+48+63）= **2143** ✓

**遗留与说明**

- 视觉模型服务本会话不可用：近观材质观感（木纹清晰度/漆面磨损等）以 3 张近观截图留档（07/08/12，含像素统计 316–345 色桶、21–29% 饱和占比证明内容充实非空白），**终验留待用户过目**，未自评通过
- T002.4 遗留②（长椅 GLB 与公园长椅并存的内容策展）仍待用户裁定，建议并入 T004 Style Gallery 素材策展
- 取证脚本存 `.zcode/investigate/t0025/`；dev server / chrome / IAB 标签 / MCP 进程均已清理（端口 5173/9299/9300 复核释放）
