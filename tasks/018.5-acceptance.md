# T018.5 验收收官

> 归属：[T018 真实程序化天空与环境光照](018-real-sky-environment.md) ｜ 状态：**in-progress（checkpoint 2026-09-23：终调定值 + 预设落地 + 取证主体完成；余新旧判读收口 / README / 三门槛复验 / 收尾）** ｜ 前置：018.4 done（`__sky` 终调面就绪——移交输入：day/tech 过曝显示域压缩工具面实证 / setIblIntensity 借道重烘刷新 / rebake() flush 同步）

## Goal

T018 epic 验收门执行 + 预设数值表终调收官：①day/tech 过曝处置实操（显示域压缩路径）+ 预设终值落表；②epic Acceptance 十二条逐条对照收口（新旧视觉对比对照 018.0 唯一改前基线 / p95 增量口径裁定 / 资源账 / 回归三门槛）；③完成 T018 全部收尾同步（epic 勾选 / TASKS / PROGRESS / DECISIONS 过曝处置记档）。

## Requirements

- **过曝处置路径裁定（018.3/018.4 移交收口）**：走**显示域压缩**（displayIntensity 天空显示单侧 + iblIntensity 环境光贡献双旋钮，018.2 已交付机制 / 018.4 已实证工具面）——**不走 tone mapping 重审**：D29.3 修订须用户裁定（018.4 任务书口径），本子任务不触发修订；ACES 实证证据（56% 画面改善）已归档 `docs/acceptance/t018/018.3/README.md`，留作潜在 T019 颜色管线立项素材。处置裁定（含弃选理由）记 DECISIONS.md——不修订 D29.3 本体。
- **终调实操（主代理，`__sky` 工作台）**：day/tech displayIntensity 与 iblIntensity 组合扫描取证定值（day 判据：天空脱离 254 饱和现渐变/云、地面/受光物脱离饱和平台、直射光影层次可见——018.3 定论「Preetham HDR × NoToneMapping 大面积 clamp」为处置对象）；dusk/night 终态复核（帧亮度 99.1/47.5 无饱和嫌疑，预期 display 1.0 维持，实测确认）。终调只动显示域两旋钮与 provisional 预设值，**day 太阳角 50.2°/53.1° 零漂移锚永不动**（新旧对比阴影方向零漂移 = epic 验收第 2 条锚点）。
- **预设表落点（threejs-runtime-agent）**：`displayIntensity` 进统一预设面（`EnvironmentPreset` 键 + 四预设值齐备）+ setupSkyEnvironment sky 分支构建期应用（legacy 分支零消费——渐变背景无此语义）；day/tech ibl 终值重锚（environmentPresets 测试「改表须显式重锚」既有口径）；dusk/night/tech provisional 方向断言按终值复核（相对 day 的方向语义随 day 压缩重排——以终调取证为据）。
- **视觉新旧对比（epic 验收第 3 条）**：四预设 × 六主体（夏栎/朴/樟/路灯/GLB/金属）固定机位新侧快照，**018.0 同款协议对称取证**（产品路径 place/view + GLB/金属同款注入 + freezeTime + 同机位常量）→ `docs/acceptance/t018/018.5/`；判读 PBR 环境光/反射可辨改善（018.2 金属/GLB/路灯三主体已证，018.5 以终值全量确认）。
- **p95 增量口径裁定（epic 验收第 9 条）**：**同实例 IBL on/off 差为增量口径**（018.2 建议在案——跨实例漂移 ≈+0.6ms 淹没信号）+ 对照 018.0 基线表抽检（空/100/1000 棵档）；裁定与两判据（≤1.5ms 绝对 / ≤基线 10% 相对）结论记档完成记录——相对判据在高 fps 小帧时 regime 下的可测性边界一并记档。
- **资源账（第 4 条）**：day→dusk→night→tech 循环 ≥5 轮 pmremStats owned=1 / retired 无异常累积 / renderer.info 无线性增长（对照 018.0 legacy 每切换 +1 geo/+2 tex 的改前泄漏面）。
- **内建云静态语义（第 12 条视觉侧）**：终值态冻结双帧云零位移取证（frozen 双帧天空区域逐位一致）；单测已锁 cloudSpeed=0（三门槛内绿）。
- **PMREM 单次耗时（第 10 条）**：终值态 rebake() 实测 ≤100ms（018.2 曾测 30–33ms，终值复核）。
- **不重跑项（D31：完整流程 ≠ 重复完整证据集）**：渐变退役（018.1 代码事实）/ 三一致（018.1 探针+单测）/ PMREM 触发清单与 debounce（018.2/018.4 单测）/ fallback 两语义（018.3 单测+monkey-patch 实证）/ dispose 释放链（018.1/018.3 单测）/ 渲染遍兼容（018.1 实证）——以已落证据 + 三门槛绿为据收口；预设终值变化不触碰上述机制面（仅数值/显示域，机制零改动）。
- **三门槛全绿**（npm test / check:layers / typecheck；基线 4033 / 601 / 零错——预设测试重锚属预期机械调整，断言语义不放松）。

## Scope

- 预期触碰：`src/runtime/environment/environmentPresets.ts`（displayIntensity 键 + 终值）+ `environmentSetup.ts`（构建期应用一行级）+ `tests/runtime/environment/`（重锚/结构断言）+ `docs/acceptance/t018/018.5/`（取证 + tools）+ 任务书/epic/TASKS/PROGRESS/DECISIONS 同步。
- 不碰：tone mapping（D29.3——不修订不偷切）、skyCore / pmremEnvironment / skyRebake / skyTuning 核心语义（018.1–018.4 锁定面零改动——displayIntensity 写入口已有）、fallback 事务语义、cloudSpeed 任何写路径（恒 0）、Shadow Camera（D29.1）、UI/SceneData 持久化、资产材质几何、T006 LOD/散布、渲染循环结构。

## Acceptance

- epic Acceptance 十二条逐条对照收口（含不重跑项的证据引用），结论表落本任务书完成记录。
- 过曝处置裁定记档 DECISIONS.md（显示域压缩选定 + tone mapping 重审弃选理由 + T019 素材指针；D29.3 本体不修订）。
- 四预设 × 六主体新侧快照 + 新旧对比判读记录（018.5 README）。
- p95 增量口径裁定 + 抽检数据；资源账循环数据；冻结云双帧证据；PMREM 耗时读数。
- 三门槛全绿 + 基线更新。

## Constraints

- 派遣：预设表落点 = **threejs-runtime-agent**（数值/键面 + 接线 + 测试重锚）；终调取证与验收判读 = 主代理（浏览器 CDP，复用 t018 tools 协议；判读矛盾时以新 tab 受控复现为裁决——018.4 工具教训）；渲染实质修改主代理不直接编写。
- 取证 SOP：Chrome headless CDP 9333 vsync-off 1920×1080 + Vite 5173（018.0 同环境口径）；进程用完即清。
- `__sky` 为会话态工具，终调值必须落预设表才算交付（不依赖 DEV 面持久化）。

## 进度（checkpoint 2026-09-23，用户指令「先记录进度并提交」时点）

- **终调定值 done**：**day displayIntensity 0.22 / iblIntensity 0.15**（display × ibl 分离扫描 + 组合扫描 + 视觉判读 + 视口分带定量四重依据：d0.22 天空带 luma 154 / r/B 0.508 / 通道饱和 0.0003 与 legacy day 渐变天空〔154.7/0.517〕逐位对齐零裁剪；i0.15 地面饱和占比 0.87→**0** 脱离 018.3 定论的 254 饱和平台、luma 202.3 vs legacy 186.6、lumaStd 44.9→40.9 阴影对比回归、树影左伸可见；金属三球反射在 i0.15 仍可辨——左球镜面蓝天白云+地平线、粗糙度阶梯分明）。**d0.28 弃选**（63% 蓝通道裁剪）；i≥0.2 弃选（饱和平台未脱离）。**tech 复测推翻初始判断 → 同为压缩对**：首轮仅看地面（sat 0/luma 73）判「tech 无需处理」，进一步取证推翻——天空带 24×12 白色分布图 100% 满白（meanRGB 247,249,249、min 通道 ≥240 占比 0.93、饱和度 0.01；太阳 azimuth 70°/elev 60° 与基线机位视向差 ~145° 排除方向性眩光）、金属三球纯白占比 0.64/0.78/0.91。**tech 终值 display 0.2 / ibl 0.15**（天空白占比 0、冷蓝渐变+云可辨、目视成立；金属阶梯恢复可辨 0.23/0.20/0.08、饱和度 0.23–0.30；地面 luma 75→39 回归 legacy 暗蓝语义）。**dusk 白经反打机位实测排除过曝**：同参数视向近太阳（az35）白占比 0.81 vs 背太阳（az240）白占比 0 / 饱和度 0.279——白 = 低太阳正对机位的自然眩光，**dusk/night 维持 display 1 / ibl 0.85·0.35 不变**（压缩反会破坏背离太阳方向的正常黄昏渐变）。
- **预设落地 done（threejs-runtime-agent 交付 + 主代理审查通过）**：`EnvironmentPreset` 增 `displayIntensity` 键（day 0.22 / 余三预设 1）+ day ibl 重锚 0.15 + `setupSkyEnvironment` sky 分支构建期一次 `sky.setDisplayIntensity(preset.displayIntensity)`（fallback 零消费）+ 测试重锚（day 锚显式重锚 / dusk·night·tech 相对断言改绝对值锚定〔day 压缩后排序重排，绝对锚为加严〕/ setup 测试新增 uniform 应用断言 + day 最低 ibl 排序锁）。三门槛 **4034 / 601 / 零错**（agent 自验）。
- **取证主体 done**：24 帧新侧快照（四预设×六主体，018.0 同款协议）+ 接线 live 确认（day 0.22/0.15 读回）；p95 四预设三档（空/100/1000：1000 棵 3.8–3.9ms vs 018.0 legacy 4.4–4.5）；**同实例 IBL on/off 三轮交替：on 3.70–4.00 / off 3.60–3.90，增量 ≈+0.1~0.2ms**；**资源账 6 轮×4 预设循环：geo/tex/prog 恒定 38/5/21 零增长、pmrem owned=1**（legacy 时代每切换 +1geo/+2tex 泄漏对照面消解）；**冻结云双帧**：120 万视口像素仅 6 像素 ±1 噪点（云位移量级不符——静态实证）；**warm rebake 0.6–0.9ms**（baked 计数核验真烘；含 generator 重编译首烘 ~30ms 归 018.2 证据）。console 全程零意外错误。
- **工具教训（记档）**：Vite dev server 模块图对单文件陈旧（watcher 漏事件，environmentPresets.ts 已新而 environmentSetup.ts 过期）——跨会话常驻 dev server 上取证前必须 curl 验证下发模块含最新改动；本次首拍 24 帧在旧接线下作废重拍（浪费一轮）。
- **余项**：新旧 24 对分带对照表已出（t0185-compare.log）→ 判读收口；018.5 README；三门槛主代理复验；epic 勾选/TASKS/PROGRESS/DECISIONS（过曝处置裁定：显示域压缩选定 + tone mapping 重审弃选理由 + T019 素材指针）。
