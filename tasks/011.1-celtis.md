# T011.1 朴树全链（首树）

> 归属：[T011 第一批阔叶乔木](011-broadleaf-trees.md) ｜ 状态：**done（2026-09-20，跨日会话：09-19 开发日 + 09-20 视觉判定收尾日）** ｜ 前置：T010 5/5 + T006 5/5（epic 级前置，见 epic 头）

## Goal

朴树（Celtis sinensis，榆科朴属）生产资产 `asset_tree_celtis`：Research Gate → Reference Spec → 阔叶家族契约实例化 → 几何 / 材质 / 8 槽形态向量 / LOD 三档 / Shadow 通道 → 固定机位视觉验证。作为阔叶家族**第二实例（方法复制首跑）**，发现并记档家族契约缺口（增量修订归 011.5，本任务不修订契约）。

## Research Gate 判定（D26，开工前留痕）

- **判定：强制调研**。命中不得豁免六情形之：①高真实要求（SOP §2 树种辨识度以 Spec 身份/识别特征节为判据）；②类型显著影响形态（朴树 vs 夏栎：叶形卵形三出脉 vs 倒卵形裂片、干皮与冠形语言均异——换色复用 prohibited，SOP Step 0）；⑤资产将成同类生产基准（阔叶家族第二实例，后续树种照此复制）。
- **执行**：通用子代理加载 asset-research 技能 → `docs/research/celtis-reference.md`；主代理硬数值独立抽查 + 照片终审（008.6 返修规程：候选照片 URL 自 API JSON 提权威扩展名、两次独立视觉提问一致方定名）。
- **Spec Version 锚点：1.0**——开发 Agent 开工前校验任务书引用版本与 Spec 顶部一致。

## Requirements

- **Step 0 调研落盘**：八节骨架 + plant 域扩展节（`.zcode/skills/asset-research/references/plant-schema.md` v0.2）；关键事实两轴标注（Form / Evidence Status）；结构尺度 → 植物志（FRPS/FOC 优先），形态材质变体 → 真实照片直接视觉证据；Unknown 显式留档（影响生产决策的关键 Unknown 不得静默）。
- **Step 1 结构分析**：Spec → 夏栎方法基线对照表——算法可直接继承项 / 需按朴树改写项 / Spec Unknown 项（新资产无「现状」，以夏栎复制基线为参照系）；每条改写映射实现面（几何/参数/材质）。
- **Step 2 参数化**：`tree/celtis/celtisShapeProfile.ts`——BroadleafShapeProfile 全字段填朴树数值，逐字段 Spec 依据 + Evidence Status + 共性标注（【家族共性候选】沿用 /【朴树特有】新证据）；工程设定无现实基准的明确标注不编造；结构计数类跨槽恒等；slot-0 锚点 + 7 槽差量展开（009.3 语法）。
- **Step 3 几何（procedural-asset-agent）**：`tree/celtis/celtisGeometry.ts`——夏栎方法复制 + 朴树算法细节（干形/冠形/枝姿/树皮起伏按 Spec 与 profile）；LOD 三档同流派生（同 rng 流 / Mid⊂High 掩码口径 / Low 簇位壳卡）；入口 `assets/asset_tree_celtis.asset.ts`（meta：taxonomy broadleaf / shapeFamily size 8 / levels 三档 / triangleCount = slot-0 High 实数 / proceduralProfile 全槽实测带；build slot→profile 路由 + level 透传 + customDepthMaterial 契约通道）。
- **Step 3 材质（park-shader-agent，与几何文件集不相交并行）**：`tree/celtis/celtisMaterials.ts`——朴树叶 SDF（卵形 + 三出脉基部 + 叶缘齿——以 Spec 叶形节为准）+ 叶色双面区分 + 背光透射 + 风动两层（aSeed/aBend 契约字段沿用）+ 树皮配方（朴树皮型按 Spec，与橡树脊沟语言分化）+ 深度材质同源 GLSL + 9 键分档（customProgramCacheKey 档位唯一，High/Mid/Low × 皮/叶/深度）。
- **Step 3 DEV 出图面（threejs-runtime-agent，合并后）**：`tree/celtis/celtisStage.ts`——mountSlots / view / mountLevels 同构夏栎（import.meta.env.DEV 守卫，生产零痕迹）。
- **Step 4 视觉验证（主代理）**：固定机位取证（SOP §3：N12/M25/F50/B25/BS22/SC15/L35/C7/T26/P42 按检查单适用项取用）+ 十项检查单逐项结论 + 视觉模型核验关键帧 + LOD 档间连续（三档 32m 全景 + 60m 远距）；取证落 `docs/acceptance/t011/011.1/`。
- **测试**：对称 tree3a 测试组织（tests/runtime/procedural/tree/celtis*）——结构（皮面数/rng 消费恒等/贴地）、LOD（三档不变量/预算带/掩码⊂）、Materials（SDF/分档键/深度同源）、ShapeSlots（8 槽差异/计数恒等）、Stage（DEV 守卫）；入口 meta/路由测试。
- **验收基线**：预算 = 家族行 High ≤40000 / Mid 6000–10000 / Low 1500–3000 实测带落带内；夏栎/GLB/旧资产零变化（既有测试全绿即证）。

## Acceptance

- `npm test` / `check:layers` / `typecheck` 三门槛全绿（基线 2657 零回归）
- 十项检查单适用项全过（✓/✗/记档分流）；console 零错误零警告
- Spec 1.0 落盘 + 主代理抽查过；契约缺口清单记档（本任务零修订）
- 取证 `docs/acceptance/t011/011.1/`（README 判定表 + 证据图 + tools）

## Constraints

- 不扩 ProceduralBuild 公共签名（slot 路由在 build 内）；D19 契约第一锁；每次 build 全部 new（含深度材质）；家族契约文件零触碰
- 参考图不进 git / 不进运行时；`screenshots/ref-tmp/` 命名 ref-celtis-*.jpg
- 主代理不直接写渲染实现；派遣 + diff 审查 + 视觉验收
- 视觉裁定 = 机器判定 + 用户异议窗口开放（D23.7 默认，epic 级口径见任务书 epic）

## 完成记录

**2026-09-20 done**（2026-09-19 开发日 + 跨日恢复完成视觉判定与收尾；取证 `docs/acceptance/t011/011.1/`——README 判定表 + 30 证据图 + 6 工具）

- **Step 0 Research Gate（09-19）**：强制调研 ✓——通用子代理（asset-research 技能）产出 `docs/research/celtis-reference.md` **Spec 1.0**（八节骨架 + plant 域扩展节 schema v0.2；来源 7+2 项：FRPS/FOC 经 iPlant 数据端点 + OSU 园艺库 + iNaturalist 7 张双验入库 + 双验弃用记档 10 张）；子代理轮整树照全弃 → **主代理补充轮闭合结构性缺口**：图搜苗圃/百科整树 ×2 双问一致入库（[8][9]），冠幅比 0.8–0.9 / 干高占比 0.35–0.45 / 骨架枝 5–6 / 仰角 45–65° 由 Unknown 升 Inferred（主代理终审记档节在卷）；**硬数值抽查**：FOC 端点回源核对逐项一致（叶 3–10 × 3.5–6cm / 齿限上半部 0–16 侧 / 叶柄 3–10mm / 果 5–7(–8)mm / to 20m）。参考图 ref-celtis-*.jpg 9 张不入 git。
- **Step 1/2 结构分析与参数化**：Spec → 夏栎基线对照落两文件头注（干高域上移 / 骨架 6+中庸领导枝 0.50 / 横展 46–64° / 末梢近水平摊出+微垂坠 / 树皮低浮雕短斑 {4,5,6}+drift16 / 叶卡 0.07–0.11m × 1.3–2.0）；`celtisShapeProfile.ts` = BroadleafShapeProfile **零修改实例化**，逐字段 Spec 依据 + 三类标注；**干高涌现回推**（T009.3 教训应验：attachMin 初值 0.78 涌现冠底 0.51 → 以实测 0.363 回推挂高段 0.52）。
- **Step 3 开发（三子代理并行/接力，文件集不相交）**：几何+参数面+入口（procedural-asset-agent，celtisGeometry/celtisShapeProfile/asset_tree_celtis.asset + 3 测试）∥ 材质（park-shader-agent，celtisMaterials + 测试）；Stage（threejs-runtime-agent，celtisStage + 测试 + bootstrap `window.__celtis` 装配——恢复其权限补装配段）。导出契约冻结下并行合并一次通过。
- **实测账目**：8 槽 × 3 档 High 31556–39366 / Mid 6782–9262 / Low 2154–2758 全落家族预算带；slot-0 High 39366（皮 24178 恒定 + 叶 15188/卡 7594/保留簇 597）；rng 消费 155072 三档恒等；minY 恒 0；视觉冠底/实高 0.363 ∈ Spec 域 0.35–0.45、冠幅比 0.862 ∈ 0.8–0.9。
- **新资产登表（主代理机械收尾）**：bootstrap/phase1.acceptance/assetTaxonomy 三测试注册清单 +1（12 资产）。
- **测试基建修正（记档）**：夏栎 tree3aShapeSlots 确定性测试在全量并行负载下挤爆 30s 超时（celtis 套件加入后 CPU 争用，17.9s 单跑 → >30s 争用态，复现 3 次非偶发）——几何四测试文件（tree3a/celtis × ShapeSlots/Lod）重超时 30s→120s，**断言语义零变化**，全量复跑稳定全绿。
- **Step 4 视觉判定（主代理，两轮校准后终判）**：首轮取证发现**齿与三出脉低于可辨阈值**（视觉模型 + 离屏着色器探针双判据——探针证实着色器全部生效，问题定位为有效对比度：中轴 +6.5% 被 sRGB 压缩）→ **Step 4 校准**（齿幅度 0.040→0.060 顶格 + 脉权重上调；该 agent 会话中断于用量上限，编辑已落盘自洽——typecheck/36 测试全过核实）→ **Step 4b 定稿**（新 park-shader-agent 实例：脉色/带宽/满权 + 齿门控收窄；齿三增强选项否决理由与物理论证记档模块头）→ 探针终值 **+13.9%**（判据 ≥12%）、宏观与 3.5m 均 faint but present。终态全批复拍 30 帧：**十项检查单适用项全过**（齿=掩码级实现 + 物理等价投影记档、暗叶 25–30% 记档、苔痕边缘过记档、4 条低严重度观察项）、风动三证据像素判定过（run 0.97% 冠部 / frozen pctChangedAny=0 / aSeed 相位差测试锁）、console 零错误零警告（批次内收集器回读 []）、LOD 档间连续 + Low 远距口径过；性能分流 011.5。
- **越权产物处置记档（如实）**：中断的校准 agent 在其会话内越权写有取证 README 与本完成记录草稿（描述其会话内核验通道与 Step 4b 前的判定）——主代理以终态权威版覆盖两文件；其中 4 条低严重度观察项经采纳保留（判定不受影响）。
- **契约缺口清单（011.5 输入，本任务零修订——几何 agent 权威记档）**：①偏冠相干方位偏置无契约字段（两实例偏冠槽均靠 rank 主导+相位运气，单种子质心断言信噪比低）②干长比率未字段化（trunk_height_ratio 是 plant 域标准字段，但干长在两实例均为几何硬编码 rng 域——跨树种调干高须改几何代码）③crownWidthRatio 参数→涌现 bbox 比映射带姿态耦合（校准口径记档非缺口）。另：SDF 包络「最宽点位置+先端形态」两参数化为家族泛化候选（夏栎常数指数/朴树变指数）。
- **三门槛（终态）**：`npm test` **2746 全绿**（基线 2657 +89：celtisStructure 14 / celtisLod 7 / celtisShapeSlots 11 / celtisMaterials 36 / celtisStage 21）/ `check:layers` **475**（465+10）/ `typecheck` 零错；夏栎/GLB/旧资产零回归（既有测试全绿即证）；家族契约文件零触碰。
- **进程清理**：Chrome（CDP 9333）/ Vite dev 收尾停净（见 epic 进度）。
