# T021.5 Shadow Representation

> 状态：**done（2026-09-24 执行，见完成记录）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：021.1（可与 021.2–021.4 并行）

## Goal

阴影策略层：cast / receive / depth 三字段按表示驱动，落地 simplified 与 off 两档深度表示与 Canopy Depth Material 契约，消费 021.3 的过渡中点切换钩子。

## Research Gate

豁免（epic 级记档，非程序化资产生产任务）。

## 待裁决位

- 三字段模型：cast（是否进 shadow pass）/ receive（主渲染是否采样阴影）/ depth（深度表示 full·simplified·none）
- 初始策略：High 沿用现状全开；Mid 深度 full 或 simplified 留 021.8 A/B；Low 与 Canopy = simplified；Cull = off
- canopy 受影初始关闭——现状两链建网格 cast/receive 统一 true、与档位无关，本任务把设置权收归策略层，canopy receive=false 是真实行为变化、必须进 021.8 A/B 复核
- Canopy Depth Material 只保冠层轮廓 + 主要空隙 + 基本体量；远景 shadow pass 禁止完整叶片级 SDF（交付归 park-shader-agent）
- Shadow Camera 常量与 T018 环境、太阳方向零触碰（冻结域）
- 剔除后无残影（shadow caster 随表示退场同步移除）

## Scope

- `src/runtime/` 阴影策略层与池建网格设置改策略驱动、Canopy Depth Material（park-shader-agent）、customDepthMaterial 契约扩展、`tests/`。

## Acceptance

- 各表示阴影策略可独立断言；过渡中点切换钩子接通
- 远景 shadow pass 无叶片级 SDF 深度计算（材质面断言 / 取证）
- 三门槛按 AGENTS 判据式口径

## 取证路径

`docs/acceptance/t021/021.5/`

## 完成记录

**2026-09-24 执行（Step A threejs-runtime-agent 单专业面——Canopy Depth Material 已由 021.6 工厂交付、本任务纯契约消费，无 park-shader-agent 面遗留 → Step B 视觉取证主代理执行：通用子代理派遣撞用量上限改直跑，harness 页面为中断子代理产物经审查续用 + 探针亮底参考两轮修正）——完成，三门槛 4160 全绿。**

- **domain 策略层**：新增 `src/domain/lod/shadowPolicy.ts`——`SHADOW_POLICY` 初始表（high/mid = {cast, receive, depth:'full'}；low = {cast, receive, 'simplified'}；canopy = {cast, **receive=false**, 'simplified'}）+ `CULLED_SHADOW_POLICY`（off）+ `shadowPolicyOf(outcome, table?)` 查询（入参宽化 LodSelectionOutcome、可注入覆盖 = 021.8 A/B 通道、脏表防御收 off）+ `isShadowCasterFor(rep, shadowRep)` 过渡期 cast 判定原语（统一规则 = policy.cast ∧ rep === commit.shadowRepresentation——021.3 钩子的消费面）。零 THREE。
- **depth 三档映射原语**：`InstancedAssetPool.shadowDepthMaterialOf(rep, source)` 两链共用——full → 挂 source.customDepthMaterial（现状契约不变）；simplified → canopy 挂（021.6 深度材质即轮廓级构造）/ **low 不挂（源提供 LOW SDF 深度材质也跳过 → three 缺省实心几何深度，零 SDF 计算）**；none → 不挂。
- **两链接线**：放置链三建网格点（singleMesh / instancedMesh / split 亮网格）+ `PoolEntry.shadowRepresentation` 缓存 + `stepEntryTransition` 每帧 `setEntryShadowRepresentation`（连续派生幂等、**不消费 midpointCrossed 边沿**〔留诊断面，无状态推导优先〕）+ `refreshSubmitVisibility` 扩展（cast = ∃renderable ∧ isShadowCasterFor、receive = policy.receive ∧ ∃renderable、culled 双 false belt-and-braces）；散布链三建网格点 + `ChunkLodState.shadowRepresentation` + `refreshShadowCast`（自有/客座 per-chunk 精确 + **合并桶成员 OR 语义**〔midpoint 错开数帧的短暂双投，远场 texel ~16cm 不可辨，021.8 观察点记档〕）+ `mergedBucketCasts`；`lodDistribution.ts` 注释现值口径 → 真值口径（计数逻辑零改动自动跟随）。
- **生产足迹（唯一可见行为变化）**：13 树种 low 表示影从 SDF 叶形裁切变实心壳卡影——D41 已裁定初始策略，与 canopy receive=false 同组记档 021.8 A/B 复核位。canopy / midpoint 路径生产不可达（canopy 源路由 021.7）→ 假想声明资产测试验证（021.3 先例）。streetlamp / GLB / tree3a 文件零变化（tree3a 三档快照全绿——池级 attach 不在快照面；其 materials.ts 陈旧注释由主代理机械性更新）。
- **测试（+19 净增）**：新增 shadowPolicy.test 11（表值一次锁全量 / culled off / 覆盖注入 / 脏表防御 / 中点两侧恰一侧 / fade-out 语义锚点 / 纯函数性）+ InstancedAssetPool.shadow.test 4 + ScatterChunkManager.shadow.test 4（三表示三字段 / low 跳过源深度材质〔源刻意携带加压〕/ 假想 canopy：dither 中点 mesh 级恰一侧 + receive=false + 深度挂载 + 计数 / fade-out 持续到 culled 零残影 / split 亮网格 / **合并桶 OR**：错开机位构造同桶成员跨中点）。改写 2 文件记档于头注：InstancedAssetPool.lod.test（dither f=0.25 期 shadowCasterInstances 2→1——恰一侧 caster 是 §5.4 本意行为）+ batch.test 三处（稳态真值 = 现值，数值零变化仅注释升级）。
- **视觉取证**（`docs/acceptance/t021/021.5/`，九帧全 PASS）：harness 策略层原语直消费（cast/receive/depth/中点摆位全走交付函数）+ T018 day 链 + D29.1 冻结常量照抄。挂载面快照 = low「源提供 `celtis:leaf-depth:low` 但 mounted=null」机制证据；三态特写像素账（low 覆盖 0.086 > high 0.062、σ 反向）；**中点换形**：before mid SDF 影 9979 px → after canopy 剪影影 19669 px（+97%）、反事实双投 23485 px 对照排除双影；culled 零残影 0 px；九态 console/shader 零错误零警告。
- **主代理裁定记档**：① low simplified = 不挂 SDF 深度材质（静态影取舍沿现状——现有 SDF 深度材质本就不含风动；canopy 靠专属材质含 xz 风摆达成同档）；② 中点切换用 shadowRepresentation 连续派生而非 midpointCrossed 事件；③ 合并桶 OR 语义（拒「过渡中 chunk 退出合并」——DC 尖峰比远场短暂双投更贵）。
- 三门槛：`npm test` 267 文件 / 4160 全绿（基线 4141 + 19）；`npm run check:layers` 621 全过；`npm run typecheck` 零错。
