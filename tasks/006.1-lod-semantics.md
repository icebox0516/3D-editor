# T006.1 LOD 公共语义层

> 归属：[T006 Asset Runtime LOD](006-lod-chunking.md) ｜ 状态：**done 2026-09-19** ｜ 前置：T009.6（level 契约与首个多档资产）✓

## Goal

LOD 公共语义层落地（D27.5/D27.12）：Profile 声明契约最小化口径 + 选档评估器 domain 纯函数 + Runtime 全局策略常量与 LOD 总开关语义。本任务不接渲染链（接链 = 006.3）。

## Requirements

- **Profile 最小契约（D27.12）**：沿用 `levels: [{id}]` 声明形态；不引入 screenSize / maxDistance / triangleBudget / castShadow / pickable 等无实际消费者的字段；调度阈值归 Runtime 全局策略常量，不进资产 Profile
- **选档评估器纯函数（D27.5）**：输入 = 纯数据（相机位姿 / fovY / 正交标志 + 资产包围球半径或 chunk 代表距离与代表 scale），输出 = representation 档位；落 `src/domain` 零 THREE（check:layers 强制），node 可测；支持不完整 LOD 链（资产只声明 High/Low 时跳档正确取最近已声明档）
- **度量语义（D27.2）**：归一化视距 d/r（张角）为主口径；正交退化口径（几何尺寸 / 正交视高）；阈值常量以候选值落地并显式标注「实测锁定前为候选」（D27 候选栏）
- **hysteresis**：档位切换阈值带迟滞，相机在边界附近来回不抖动换档
- **LOD 总开关语义**：全局开关（关 = 全 High）在本层定义，消费在 006.3
- **档位派生态纪律（D27.6）**：评估器无状态、结果每帧可重算，不缓存进持久状态

## Acceptance

- 评估器纯函数单测覆盖：透视/正交、迟滞环、不完整链跳档、开关语义、代表 scale 参与归一化
- `npm test` / `check:layers` / `typecheck` 全绿

## Constraints

- 本任务不接渲染链、不改任何 Runtime 渲染行为；实现按 AGENTS.md「多 Agent 按 Step 派遣」执行（domain 纯函数属通用实现）

## 完成记录

**done 2026-09-19**（通用子代理按 Step 派遣实现 + 主代理审查验收，纯 domain 新增零 Runtime 触碰——不接渲染链符合本任务边界）。

**交付物**（4 新文件 + 1 单行导出）：

- `src/domain/lod/lodPolicy.ts`——`LodThresholds` 接口 + `LOD_THRESHOLDS` 候选常量（highToMid 6 / midToLow 16 / lowToCulled 60 / hysteresisBand 0.15）。头注显式标注 **D27 候选值，实测锁定前为候选**（待 006.4/006.5），附标定参照（夏栎 r≈5m、fov 50° → ≈64m/172m/643m 视距）；调度阈值归 Runtime 全局常量不进资产 Profile（D27.12 硬裁定落注释）。
- `src/domain/lod/lodEvaluation.ts`——选档评估器纯函数（零 THREE，node 可测）：
  - **统一度量 m = 视口高归一化视距**：透视 `(d/r)·tan(fovY/2)` / 正交 `orthoHeight/(2r)`；m=1 恰为资产直径占满视口高，越小越近。与 lod-spec §4.1 主口径 d/r 只差 tan 折算常数（fov=90° 严格相等），实现 §4.2「两相机口径同一单调语义、阈值带连续可复用」；fovY 消费落实 D27.5 输入面。
  - `normalizedViewDistance`（度量独立可测，含 radius×scale≤0 / fovY∉(0,π) 防御抛错）+ `resolveDeclaredLevel`（不完整链跳档：序数最近已声明档，**等距 tie-break 取更低档**——保守省面，「远处降档优先」§1）+ `evaluateLodRepresentation`（主评估器）。
  - **迟滞 = 单边**（降档过名义线立即、升档需越过名义上界 × (1−band)，带内保持；升档跨多档一步到位；current 缺省 = 名义档起步）——方向依据「预算内档位保守」治理约束记档头注。
  - **LOD 总开关**（本层定义、006.3 消费）：false = 目标恒 `resolveDeclaredLevel('high')`（正常资产即全 High）且 culled 一并旁路——回退对比与兜底语义（D27.13）；奇异声明（无 high）回最近已声明档，保持「Runtime 只请求已声明档」纪律（§6）取舍已记注释。
  - culled 不跳档（任何资产含单档均可被超远裁剪）；frustum cull 不在评估器职责（时序 §6：块剔除→frustum cull→选档，本 culled 仅超远距口径）。
- `src/domain/lod/index.ts` + `src/domain/index.ts` 导出行（头注模块列举同步）。
- `tests/domain/lod/lodEvaluation.test.ts`——38 条：透视/正交口径（含 m=1 满屏锚点两式、fovY 参与选档）、代表 scale 参与归一化、名义边界穿越、迟滞环全路径（含任务书环路用例与带线严格性）、不完整链跳档（tie-break/单档/culled 线不跳档/缺省单档）、开关语义（含奇异声明回归锁）、纯函数性、输入防御、常量一次锁全量 + thresholds 覆写。阈值穿越断言一律以 LOD_THRESHOLDS 为参照构造相对边界（006.4/006.5 锁值改常量不碎测试语义）。

**验收**：三门槛全绿——`npm test` 183 文件 / **2585 全过**（基线 2547 + 38 零回归）、`check:layers` 453 文件零违规（449 + 4 新文件，domain 零 THREE 受检）、`typecheck` 零错误。

**解锁**：006.2（非植物第二资产档位验证）与 006.3（块×档分桶+换档——本层 API 的 Runtime 接线消费方）可并行。
