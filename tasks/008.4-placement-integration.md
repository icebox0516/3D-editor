# T008.4 放置全链路集成

> 归属：[T008 程序化树木](008-procedural-tree.md) ｜ 状态：**done（2026-09-18；+22 测试 → 2443 全绿；浏览器冒烟零错误）** ｜ 前置：T008.1 + T008.2 + T008.3，且视觉锚点门已过（2026-09-18 过）

## Goal

`asset_tree_3a` 进产品：入库可浏览、点击放置、撤销/重做、存档往返、Inspector seed 重掷——兑现「点击资产即可在场景中生成一棵完整树木」。

## Requirements

- **入库 + 缩略图**：资产库入列（category = plant）；缩略图一张（slot-0 代表形态，走既有离屏快照机制；008.5 扩槽不增缩略图）
- **点击放置**：点击资产**那刻掷 seed S** → 槽路由（008.1 shapeSlotOf）→ Ghost 借对应 sourceKey 的缓存 Source（**Ghost 预览即最终形态**）→ 确认放置落同一 seed 进 `asset.seed`
- **撤销/重做**：放置/删除走既有 Command 链
- **存档往返**：seed 落盘、加载后 build 同槽重建**逐位一致**（io 序列化校验 + 往返单测）
- **复制定义（D19.8 精确语义）**：复制保留原 assetId + seed → 完全相同形态与实例参数，**仅 transform 可独立编辑**（不自动换 seed）
- **Inspector 最小面**：seed 只读行 + 「重掷」按钮（Dices 图标，模式照抄 T003.3 区域 seed 重掷）——走 Command 可撤销；重掷 = 新 seed 同时重算 shapeSlot + instanceRng（新 seed 允许路由回同一 slot：此时共享同一 Source、仅实例表现变化——正常行为非 bug）；结构参数不进 UI
- **交互闭环**：选资产 → 放置 → 看真实效果 → 重掷换一棵 → Gizmo 调位置姿态

## Scope

- 预期触碰：放置流程 seed 掷骰接入、Ghost 路由、io 序列化（asset.seed 已有范式，预计零 schema 改动）、InspectorPanel / 缩略图管线消费
- 不碰：契约层（008.1 冻结）、几何/材质（锚点已定稿）

## Acceptance

- 端到端：入库可见缩略图 → 点击放置出树（Ghost = 所得）→ 重掷按新 seed 重算（允许落回同槽仅换实例参数）→ 复制得孪生（仅 transform 可调）→ undo/redo 全链复原 → 存档重载逐位一致
- 撤销链作用域正确（重掷/复制/删除皆可撤销）
- `npm test` / `check:layers` / `typecheck` 全绿；浏览器冒烟 console 零错误

## Constraints

- three 分层；UI 遵循 `src/ui/styles/` 设计系统；一切持久变更经 Command（Scene 唯一数据源）
- 主代理主导 + 按需小派发（放置链路多为既有通路复用）

## 完成记录

### 2026-09-18 完成（放置全链路闭环）

- **探查先行**（Explore 代理，放置链路地图）：入库/缩略图（无参 build=slot-0）/放置 Command 链/seed 掷骰时机/池分桶跨池迁移/存档透传/复制孪生语义（offsetClone 深拷贝天然满足 D19.8）/Inspector seed 只读行——**全部零改动复用**；缺口三处：①Ghost 不携 seed（PreviewManager 不传 → 8 槽 7/8 概率预览≠所得，本任务核心接缝）②model seed 重掷无命令（UpdateObjectCommand 白名单无 asset）③无 Tree3a 放置链 e2e。
- **Step A**（threejs-runtime-agent）：`PreviewManager.showGhost(assetId, t, seed?)` + `(assetId, seed)` 逐值去重（挂载后同参仅更 transform、占位盒在途同参不重复取源不换令牌）+ `GhostObjectProvider.provideGhostObject(assetId, seed?)` 透传（AssetSourceRouter 端签名本就绪）+ `getGhostSeed()` 诊断位 + 6 测试。边界记档：seed undefined vs 0 逐值不等触发重建（幂等冗余取源，不做归一化）。
- **Step B/C**（主代理，editor/ui/domain 零 THREE）：
  - `ports.ts` PreviewPort.showGhost 增可选 seed（契约注记：实现方按 (assetId,seed) 去重）
  - `PlacementTool`：初显与连放 roll 后均携 `rolledSeed`（连放换源 = 下一枚新槽形态预览；GLB/未掷 seed → undefined 行为零变化）
  - `domain/assets/variants.ts` 新增 `resampleVariantTransform`：重掷采样差换算——scale ×(new/old)、rotY +(new−old)、位置与 X/Z 旋转不动；用户 gizmo 编辑（相对增量）天然保留；往返对合（撤销数学根基）；variants 缺省/全 0 → 恒等拷贝
  - `editor/commands/ChangeAssetSeedCommand.ts`：预计算载荷 `{seed, transform}`（CommandContext 无 registries，沿 ChangePresetCommand 先例）；守卫（目标不存在/非 model/无 seed → false 零副作用）；before/after 深拷贝快照，undo/redo 幂等
  - `InspectorPanel.tsx` MetadataBody variant-seed 行加 Dices 重掷按钮（模式照抄 ScatterParamsForm；行值仍只读）
- **Step D 测试**（2421 → **2443**，176 文件）：PreviewManager.ghost +6（Step A）｜PlacementTool.variants +1（Ghost seed=落地 seed、连放逐枚换源逐位对应、GLB undefined 透传）｜domain variants +5（确定性/采样差精确/恒等/缺省/往返对合）｜ChangeAssetSeedCommand +5（替换语义/undo-redo/守卫/canExecute/快照独立性）｜e2e `tests/app/placement.tree3a.test.ts` +5（放置 Ghost 一致、重掷+手调保留+undo/redo、复制孪生 D19.8、三历史逐层回退重做全复原、存档往返逐位+二次保存字节级一致）。
- **三重门槛**（主代理亲自复核）：`npm test` 176 文件 / 2443 全绿；`check:layers` 440 文件 OK（+2 新测试文件）；`typecheck` 零错误。
- **浏览器冒烟**（agent-browser + dev server 5179 即用即停）：资产库点「夏栎」→ 状态栏进放置态 → 视口鼠标点击放置 → **Ghost 目检为真树形态**（树干+树冠，非占位盒——seed 槽路由生效）→ 点选树 Inspector 出「变体 Seed 1014101499（只读）+ 重掷按钮」→ 重掷 → **990463549** → Ctrl+Z → **恢复 1014101499**；console 全程零错误零警告。3 截图存 `docs/acceptance/t008/008.4/`（ghost / placed / rerolled）。
- **取舍记档**：①重掷「重算 instanceRng」精确化为 transform 采样差换算（增量保留用户手调——比整体重采样更符合「仅 transform 可独立编辑」精神）；②连放重掷后 Ghost 重建有一次占位盒先行（新槽 Source 构建期间，毫秒级；不做旧形态保帧替换——诚实预览下一棵）；③拖放入库对象无 seed → Inspector 无 seed 行无按钮（无变体通道，确定性语义）；④散布路径仍恒 slot-0（T003 线，Renderer 散布 provider 无 seed——008.5 前如需多槽另立任务接线）。
