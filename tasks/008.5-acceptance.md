# T008.5 T008 收官验收门（修订版）

> 归属：[T008 程序化树木](008-procedural-tree.md) ｜ 状态：**done（2026-09-19，T008 收官 6/6）** ｜ 前置：**T009 全部子任务完成**（2026-09-18 修订：后置于 T009 之后执行——形态设计职责移 009.3，本任务只做验证与收官，避免 8 槽质量对齐做两遍，D20）

## Goal

T008 epic 收官验收门：8 槽统一质量基准**验证**（非设计）+ 全链路验收 + 同 sourceKey 共享验证（本契约设计的核心价值证明）+ 取证存档。

## Requirements

- **8 槽统一质量验证**：8 槽（T009.3 交付的形态向量组合）对齐锚点定稿基准逐槽过检——本任务只验证不调参，不达标退回 T009 对应任务
- **全链路验收**：入库 → 放置 → 重掷（重掷结果在 8 个 shapeSlot 上呈统计分布即可；不要求单次重掷必然切换槽位——同槽重掷合法）→ 复制孪生 → undo/redo → 存档往返逐位一致 → Gizmo 编辑
- **同 sourceKey 多实例一致性验证（核心项）**：随机放置 ≥6 棵**同槽**树——确认 Geometry/Material 全程共享（仅一份 Source）、实例间**仅允许 instanceMatrix / instanceColor / aSeed 存在实例级差异**（不得通过 per-instance uniform、独立 Geometry、独立 Material 或额外 Mesh 产生形态差异）、桶内保持**单一 InstancedMesh × 2 draw calls**（renderer.info 实证）
- **零错误五拆口径**（代替粗粒度「console 零错误」）：
  1. 运行时无异常
  2. 资源无重复 build（同 sourceKey 二次请求走缓存）
  3. Ghost 不触发 dispose（借缓存契约不破）
  4. 放置/删除/Undo/Redo 全程无资源泄漏（dispose 链 + 内存观察）
  5. 同 sourceKey 不重复创建 Geometry/Material
- **取证存档**：固定机位 + 固定曝光/环境光 + 固定相机距离（锚点门同款纪律）；近/中/远 + 8 槽全景 + 合批证据（draw call 数）存 `docs/acceptance/t008/`
- **收尾**：完成记录 + TASKS/PROGRESS 同步 + commit（T008 6/6 收官）

## Scope

- 验收驱动（agent-browser/截图）；无新架构面；不承担形态设计（009.3 职责）

## Acceptance

- 上述全项过 = T008 epic 收官（T008 6/6）
- `npm test` / `check:layers` / `typecheck` 全绿
- 进程用完即清（dev server / 浏览器标签不留跨会话残留）

## Constraints

- 观感基准 = 008.3 锚点定稿记录 + 008.6 参考对照表（8 槽与锚点同质量线）
- 主代理执行验收门

## 完成记录

**2026-09-19 完成（主代理执行验收门）**：全项过，**T008 epic 收官 6/6**。取证 `docs/acceptance/t008/008.5/`（README + 7 图 + 存档样本 JSON）；零代码改动（纯验收会话）。

- **全链路（UI 产品路径）**：入库缩略图（23 卡含夏栎，离屏快照树形）→ 点击放置（Ghost 即真树形态 32428 tri 非占位盒，携 seed 槽路由）→ 重掷 ×16+首放 **17 样本经真实 domain `shapeSlotOf` 离线对账 8 槽全覆盖** {0:2,1:2,2:3,3:2,4:2,5:3,6:2,7:1}（含同槽重掷合法）→ undo ×16 / redo ×16 seed 逐位回放 → Ctrl+D ×5 孪生（seed 全等、变换独立，存档 JSON 逐字段验证）→ Gizmo 拖 X 轴改位置后 Ctrl+Z 逐位复原 → 导出 JSON（blob 拦截）→ **真重载** → 导入重建 Objects=6 / seed / 坐标逐位一致（字节级往返由 e2e 既有锁定）。
- **同 sourceKey 核心项（三次独立场景实证）**：6 棵同槽树 drawCalls **恒 7**（env3+单桶 4=2 组×主/影，不随实例数涨）、geometries 恒 5（全新会话 3 env+1 槽源）、triangles ×6——手放链路 / 重载+导入重建 / 驱动面+Ctrl+D 复放各一次；实例级差异仅 instanceMatrix/instanceColor/aSeed 通道（008.1 硬测试架构锁定 + 孪生仅 matrix 异 + 混播同桶共染）。
- **零错误五拆全过**：①console/page errors 全程零（~150 步）；②无重复 build（Ghost 换槽/重掷槽间切换/21 棵全槽 = 11 geo 有界）；③Ghost 不 dispose（开关循环计数全持、缓存命中零重建）；④23 轮 place/clear 无泄漏（textures/programs 恒定；geometries 增量 = 槽源缓存 D19.4 设计内 8 槽有界，铺满后 31 恒定；删除后 dc 回 3 实例缓冲随池释放）；⑤同 sourceKey 恒一账目。**记档**：首次 Gizmo 拖拽懒创建 TransformControls helper（+13 dc/+224 tri，切回选择工具即回落；geo 缓存有界不累积）——非泄漏，池侧账目以 drawCalls/objects 为准。
- **8 槽统一质量验证**：`mountSlots`+`viewSlots` 42m 全景机器核验 8 棵可辨/形态明显不同/同种语言/无渲染异常（与 009.3 用户裁定门定稿一致）；锚点 slot-0 三距离 12/25/50m 固定机位（35°/8°+freezeTime）。
- **回归**：三重门槛 181 文件 / **2542 全绿**、check:layers 446 文件、typecheck 零错误；进程清理完毕（dev server 5190 / 浏览器 / chrome-for-testing 已停，临时槽分布探针测试已删）。
- **执行记档（验收方法学）**：①主画布可交互区被底部浏览器/左面板压缩且坐标随面板展开态变化——指针必须落在 canvas getBoundingClientRect 内（Ghost 首验曾因指针在画布外误判未渲染）；②analyze_image 视觉工具对粗特征（树/卡片数量级）可靠、对精确 UI 元素（gizmo 箭头方位）会幻觉——凡结构数据可得一律以结构数据为准，像素仅作辅证（本次 gizmo 定位用颜色像素扫描完成）；③agent-browser 合成点击对部分按钮（重掷）不触发 handler，回退 DOM `el.click()`；④下载类菜单动作被 CDP 拦截取消，存档取证改 `URL.createObjectURL` 钩子拦 blob。
