# T009.6 夏栎 LOD 三档

> 归属：[T009 夏栎结构真实性与生产化](009-tree-realism.md) ｜ 状态：**done（2026-09-19）** ｜ 前置：T009.2 + T009.3 + T009.4（几何终态）

## Goal

夏栎 High / Mid / Low 三档 build：levels 从占位变实装（夏栎侧），档间身份一致（同一棵树远近都是那棵树）；**预算实测锁定为本任务输出**。

## Requirements

- **三档质量取舍**：Mid 降枝条段数 / 叶簇数量 / 叶卡数量 / Shader 复杂度；Low 只保树冠轮廓 + 颜色层次 + 整体风格（不保持内部枝条）
- **预算锁定**：以候选区间（High 30–40K / Mid 6–10K / Low 1.5–3K 三角形）为初始参考，结合最终结构、性能实测与观感锁定正式预算，写回 asset meta（预算制，D19.8 口径——三档实际面数须符合锁定值）
- **level 契约（防实现者自行设计）**：level 作为 Procedural Build 的可选 Runtime 参数扩展——`build({ seed?, preset?, level? })`；**sourceKey 仍只表达 assetId + preset + shapeSlot**，Runtime Cache 以「sourceKey + level」为档位缓存维度；level 不参与 shapeSlot / morphSeed / sourceKey 计算；旧资产 `build()` 无参调用继续兼容
- **枚举定名（D27.7）**：level 枚举本期定死三值 `'high' | 'mid' | 'low'`——把 `ProceduralLevelDescriptor`（AssetDescriptor.ts）与 procedural/types.ts 相关注释中的占位 `'medium'` 改为 `'mid'`（对齐文档口径；当前零消费者、改名零成本，注释同步改防类型-注释漂移）；proxy/impostor 不进类型枚举（规范语义位归 010.3）、culled 是调度结果不进枚举
- **Cache 注释同步**：落地时把 ProceduralSourceCache 契约注释更新为双维缓存语义——「形态身份由 sourceKey 定义，具体几何 Source 由 sourceKey + level 决定」；**不修改 sourceKeyOf() 本身、不把 level 掺入形态身份**（D23.2）
- **档位缓存**：档位几何独立缓存、独立释放
- **档间一致**：轮廓 / 体量 / 颜色一致，档间切换无身份变化
- **T006 接缝**：选档 / 距离切换 / 分块消费归 T006（006.1 公共语义层 / 006.3 块×档分桶+换档，D27 结构）实装；本任务交付档位声明 + 可按参数强制档位生成验证，不自带切换逻辑

## Scope

- 预期触碰：`broadleafGeometry.ts`（档位参数）、`tree3aMaterials.ts`（Shader 档）、`asset_tree_3a.asset.ts` meta levels、ProceduralSourceCache（level 维度缓存）
- 不碰：T006 的切换/分块实现、契约第一锁

## Acceptance

- 三档可生成、实际面数符合锁定预算、法线健康
- 档间色/形连续（强制档位渲染取证，截图比对辅助）
- 档位独立缓存 / 释放验证；同 seed 逐位复现延续；`npm test` / `check:layers` / `typecheck` 全绿

## Constraints

- **LOD Source 在 Runtime 中增加独立 level 维度；不得改变 D19 sourceKey 的形态身份语义**（不得把 level 掺进形态身份）
- LOD 是统一 Asset Runtime 能力而非「植物 LOD」——不同资产不同预算（D20）

## 完成记录

**done 2026-09-19**（凌晨定时会话；+32 测试 → 2514 全绿三门槛；取证 `docs/acceptance/t009/009.6/` 7 图）。

**开工状态说明（防误判）**：本会话开工时工作区已有前一会话遗留的未提交半成品——契约层（`ProceduralLevel` 枚举 / `ProceduralBuildParams.level` / `ProceduralLevelDescriptor` 'medium'→'mid' D27.7 改名，全仓零 'medium' 残留已验）、ProceduralSourceCache 双维缓存（`sourceKey::level` 内部后缀 + `resolveEntry` 单一真相 + `evict` 单档精确释放）及其 +7 测试、broadleafGeometry 三档发射计划（`lodPlanFor`/`thinStations`/Mid 掩码/Low 壳卡）与临时探针 `__lod-probe.test.ts`。本会话主代理审查该半成品（质量达标）、跑探针锁定预算输入，随后按 Step 派遣三子代理补完：**park-shader-agent**（tree3aMaterials 三工厂 `level` 可选参——旧单体 BODY 拆段拼装、High 六段拼回与改前逐字节一致且以改前全文 fixture 测试锁死；叶 Mid 去脉三线/叶团、叶 Low 用 `TREE3A_LEAF_SDF_LOW` 零噪声采样去透光、皮 Mid 去节疤、皮 Low 去板块采样/节疤/苔痕（板块乘子均值化常量 `vec3(0.985,0.965,0.94)`）、深度 Mid=High SDF / Low=Low SDF、customProgramCacheKey 9 键分档（High 三键沿用原键）、风动三档同源零改动；+10 测试）；**threejs-runtime-agent**（tree3aStage `mountLevels`/`viewLevels`/`viewLevel` + `stats.levels` 逐档账目 + `deps.build` 注入 seam——强制档位生成验证面，T006 接缝交付；+5 测试）；**procedural-asset-agent**（asset build level 透传路由 + `meta.levels` 三档化（D27 最小化 `[{id}]`）+ 模块头预算锁定账目 + 新建 broadleafLod.test.ts 7 测试正式化探针不变量 + asset 路由测试 3 个 + 删探针）。

- **预算锁定（本任务输出，预算制 D19.8）**：High ≤ 40000 / Mid 6000–10000 / Low 1500–3000（三角形，皮+叶）；8 槽 × 3 档实测带 High 28754–38780（slot-6 最低/slot-7 最高）、Mid 6414–9650、Low 1662–2150——Mid/Low 落候选带内按带锁入，High 按上限锁入（slot-6 实测 28754 略低于候选带下限 30K，面数预算意义在上限；皮恒 20724/3882/330）。**记档修正**：009.3 完成记录「总面 32424–38780」与自身「叶卡 4015–9028」内部矛盾（4015 卡 → 28754 面），009.4 已证拓扑恒等、009.6 探针 + High 逐位不动测试确认现状即 009.3 终态——历史记录下限为笔误，非回归。
- **档间不变量（测试锁定）**：三档同 rng 流（消费恒等 177234 快照——发射省略不省略消费）；簇表/channels/leafCandidates 跨档逐位全等；Mid 存活卡 ⊂ High 逐位同位（XZ 12 分量块匹配 + Y 差恒为贴地平移差 ≤3cm——树皮起伏在不同径向采样下的 minY 微差致整树贴地平移档间不同，B 代理发现并记档，bbox 容差口径内）；Low 壳卡 = 保留簇 × 2；minY 三档恒 0；同 seed 同档逐位复现；法线无 NaN；档间 bbox XZ/总高差 ≤ 0.4m。
- **契约面（Requirements 对账）**：level 枚举三值定死（D27.7）✓；Cache 双维注释同步 + sourceKeyOf 零改动 ✓；档位独立缓存/独立释放（evict）✓；旧资产 `build()` 无参兼容（缺省 high 逐位不变，asset 缺省路径测试锁定）✓；T006 接缝 = 档位声明 + mountLevels 强制档位验证，不带切换逻辑 ✓。
- **视觉裁定（主代理 agent-browser 固定机位，7 图）**：slot-0 三档 32m 全景（档间干倾/冠偏同姿态、颜色连续无跳变、无渲染异常）+ 三档 25m 特写 + mid/low 12m 近距复核 + slot-7（最大面数槽）32m 全景。Mid 五项全过（叶簇感/叶形 SDF 保真/逐叶变奏透光/透枝/无异常）；Low 剪影完整、颜色层次**存在但对比含蓄**（亮暗绿交错 + 冠下暗化可辨、非死平；叶团项均值化后乘子对比下降所致——Low 定位远距档，12m 近距口径下已可辨，**009.7 验收时以远距口径复核此项**）。
- 状态栏旁观：DEV 舞台三树同框 90.9k 三角 / FPS ≈10（chrome-for-testing 无 GPU 加速 + 编辑器视口，非产品路径口径）——性能验收归 009.7（RTX 2080 Ti 档）。
- **下一步**：009.7 夏栎四类验收门解锁（009.5 公共 Shadow 仍可并行，汇入 009.7）。
