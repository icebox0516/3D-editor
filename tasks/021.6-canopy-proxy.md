# T021.6 Broadleaf Canopy Proxy

> 状态：**pending（2026-09-23 立项）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：021.1（几何面独立，可与 021.2–021.4 并行）

## Goal

统一远景冠层表示：先从 13 个树种几何文件抽出共享冠层场契约（现各文件独立定义、无共享类型），再实现 BroadleafCanopyProxy（几何 + 材质 + 深度材质），首批接入 T011 全部树种。目标不是更低面数的树，而是最低成本保持树冠轮廓、体量、绿色覆盖率与树种级冠形。

## Research Gate

豁免（epic 级记档：非新程序化资产——从现有 T008/T011 族与各树 Reference Spec 派生，现实事实已在既有 Spec 留痕）。

## 待裁决位

- 面数预算 ≤500 / 树，含简化树干预柱（对照 Low 档再压一个量级）；锁定后回写 lod-spec §5.2 预算表 canopy 列
- 必须从现有 ClusterRecord / Low 档壳卡逻辑派生；禁止另建独立树冠生成体系；禁止每树种自写一套远景算法
- 风动契约：同一 uTime 驱动 + 首日声明并消费 aSeed / aBend（与高中档同相位原则一致）；散布几何缺 aSeed 属性时按缺省零——与散布链高中档当前行为逐位一致，D20.4 修复落地后零改动自动获得逐实例相位；禁止坐标哈希等第三套相位机制
- 材质简化受光：主光 lambert + IBL 环境项、树种级冠色 + 内外明暗梯度、共享 T018 envMap / intensity 域；禁止 unlit
- 散布侧按 assetId 粒度派生（散布链本无 shapeSlot 维度；树种级冠形差异即为远景保留目标）
- 保留外密内疏与基本阴影体量；不保留远景不可辨枝条 / 单叶细节 / 叶片 SDF
- Impostor 不实装：Canopy + 园区可见范围不达预算才立项后续子任务

## Scope

- `src/runtime/procedural/tree/` 共享冠层场契约 + BroadleafCanopyProxy 生成模块、13 树种接入点、canopy 材质与深度材质工厂（材质面 park-shader-agent / 几何面 procedural-asset-agent）、`docs/procedural-assets/lod-spec.md` 预算表、`tests/`（面数带 + 风相位一致 + 树种冠形差异 + tree3a 零回退）。

## Acceptance

- 面数带锁定并回写预算表；与高中档风相位一致有断言
- 13 树种冠形横向可辨（基线帧同源比较）
- tree3a 零回退基线不受触碰影响
- 三门槛按 AGENTS 判据式口径

## 取证路径

`docs/acceptance/t021/021.6/`

## 完成记录

**2026-09-23 执行（两 Step：几何面 procedural-asset-agent + 材质面 park-shader-agent，与 021.2 并行——主代理裁定文件集不相交）——完成。**

**Step 1 几何面**：

- 新文件：`src/runtime/procedural/tree/broadleaf/broadleafClusterField.ts`（共享冠层场契约 `BroadleafClusterRecord` + stats 出口形态 `BroadleafClusterStatsRecord`，纯类型零运行时，沿 broadleafShapeProfile 模式）、`src/runtime/procedural/tree/broadleafCanopyProxy.ts`（统一几何工厂 `buildBroadleafCanopyGeometry(assetId, seed?)` + 13 树种接入表 `BROADLEAF_CANOPY_ASSET_IDS` + 冠层场收割 `harvestBroadleafCanopyField`）、`tests/runtime/procedural/tree/broadleafCanopyProxy.test.ts`（8 用例）。
- 13 个 `*Geometry.ts` 机械收编：删除本地 `interface ClusterRecord`（13 份定义逐字节相同）原位换 `type ClusterRecord = BroadleafClusterRecord` 别名导入——纯类型层变更、生成结果零改动，13 文件净 diff 43 插 / 108 删。
- 派生链（零 rng 后处理，同输入逐位同输出）：seed → slot → profile（与各资产 profileForSeed 同口径）→ 树种 Low 档 → stats.clusters + 地面偏移 Δ + 干基径 → 冠参考系 → k 中心最远点选簇（七极值种子）→ 70 双卡壳带 + 86 单卡体内 → 包络钳制膨胀 → Low 壳卡同构发射 + 干柱 → minY 精确 0。
- 预算锁定：**恒 487 面**（干柱 35 + 冠卡 452〔70 双卡簇×4 + 86 单卡簇×2〕；簇位 < 156 才退化，13 树种 8 槽实测 292–822 簇无退化槽）；对照 Low 档 1586–2858 压一个量级；**lod-spec §5.2 预算表新增 Canopy 列（≤500）已回写**。
- 风动属性契约（几何面）：aSeed 恒 0（与散布 High/Mid 缺属性 GL 缺省 0 逐位一致，档间 0=0 同相；放置链池后续挂逐实例 InstancedBufferAttribute 自动覆盖——D20.4 落地后三档零改动同享，无第三套相位机制）；aBend 与 Low 壳卡同公式同常数（逐位同值测试锁）；aLeafRand 簇心 sin 散列同式同常数；aCrownQ ∈ [0,1] 冠内归一径向深度（内外明暗梯度通道）。组序恰 2：组 0 干柱 / 组 1 冠卡。
- 冠形差异基准 = Mid 过渡对：canopy 冠包络 13 树种实测落 Mid 跨度 ±0.7m 带（High 不作紧基准——花果树种 High 含果序外伸）；13 树种形态比两两互异，Mid 形态比差 ≥0.2 树种对 canopy 序 100% 保持。

**Step 2 材质面**：

- 新文件：`src/runtime/procedural/tree/broadleafCanopyMaterials.ts`（成套材质工厂 [干柱, 冠卡] + Canopy Depth Material，按 assetId 13 树种参数表）、`tests/runtime/procedural/tree/broadleafCanopyMaterials.test.ts`（10 用例，含 drift-lock 逐数对账）。
- 受光模型（§6.4）：MeshStandardMaterial 族——主光 lambert 吃场景 DirectionalLight、IBL 自动吃 T018 scene.environment/environmentIntensity（材质零 override，预设切换全跟随，禁 unlit 落点）；树种级冠色按 assetId 查表；aCrownQ 壳亮芯暗梯度（0.74+0.26·q）+ 透光壳层加权（0.35+0.65·q，canopy 独有精化）；aLeafRand 色相/明度/透光变奏（同该树种叶材质公式同常数——共享簇与 Low 卡逐位同 tint，dither 过渡连续）。
- 风动：onBeforeCompile + `<begin_vertex>` 后作用 transformed + replaceOnce 缺失即抛 + customProgramCacheKey 逐树种唯一——与现有风动同构；13 树种风常数（hash K/C、锚高、摆频）静态转录 + **drift-lock 测试**（假 shader 注入对 species 材质真实 GLSL 逐数对账，树种侧调参未同步即红）；uTime 桥接 = 成套三材质共享同一 uTime 对象（card/trunk 挂材质级 uniforms、depth 绑 shader.uniforms——服务不遍历 customDepthMaterial）→ 影 pass 风摆与主渲染同相同帧。
- Canopy Depth Material（§七）：MeshDepthMaterial + RGBADepthPacking，**无 SDF / 无 alphaTest / 无噪声采样**（canopy 卡本身即冠层壳带，卡间空隙由几何天然保留；测试断言注入后片元与模板逐字相等）；xz 风摆进深度顶点与主渲染同相；法向微扑不进（≤1cm 级 vs 影 texel ~16cm 不可辨，记档取舍）。
- 基线帧取证（`docs/acceptance/t021/021.6/`）：最小 harness 无 Runtime 接线、T018 真实环境链（PMREM / intensity 0.15）、同源机位——13 树种帧 ×13 + 全景 ×1 + README；0 shader 错误。主代理视觉抽查（全景 + salix 单帧）：13/13 主干剪影齐全、冠形两极可辨（salix 11.74m 宽垂冠 ↔ ginkgo 4.99m 窄塔）、受光立体非剪影、无渲染异常。
- Runtime 接线边界遵守：无 RepresentationSourceRouter / CanopySourceCache / provideSource 改动（全归 021.7）；tree3a 零触碰（三档快照锁全绿 = 零回退证明）。

**三门槛（主代理合流后统一）**：`npm test` 262 文件 **4073 全绿**（021.1 后基线 4041 + 净 32：两线新增 36 − 改写合并 4）；`npm run check:layers` **612 文件**全绿；`npm run typecheck` 零错。

**遗留与风险记档**：① canopy 参数表 = species 常数静态副本（两份数据，drift-lock CI 拦截漂移；未来频繁调参可收编共享导出——涉 13 个 *Materials.ts 超出本任务最小改动）；② 深度材质依赖成套消费（单独挂 depth 失去 uTime 更新源——021.7 接线注意）；③ 透光壳层加权致 dither 过渡期 canopy 背光略暗于 Mid（差异仅冠心）——021.8 观察点；④ salix 垂帘条纹质感不进 canopy（簇场保留宽垂冠剪影；如 021.8 判身份损失，修正方向 = 卡形态数据驱动化，非每树种算法）；⑤ koelreuteria 类果序外伸树种 Mid↔Canopy 差 −0.68m 实测在带内——021.8 dither 标定复核。
