# T021.2 Selection Runtime

> 状态：**pending（2026-09-23 立项）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：021.1

## Goal

选档语义从三档 level 链切换到表示链：新链候选阈值结构、屏占比解释口径落调试与验收面、选档粒度（放置逐对象 / 散布四维）与代表口径接线。度量公式与稳定基准球消费零改动。

## Research Gate

豁免（epic 级记档，非程序化资产生产任务）。

## 待裁决位

- 度量公式维持现状（代码与规范逐操作符一致，不得顺手改）
- 屏占比口径 = 1/m（直径 / 视口高），仅作调试与验收解释、不作选档输入
- 新链候选初值直接继承 legacy 三数映射（highToMid / midToCanopy / canopyToCulled）+ 迟滞带 0.15，全部标注候选、重锁归 021.8；旧三数仅存对照注释
- 散布粒度写全四维（region × chunk × asset）；代表 scale 取桶内最大实例
- 基准球消费不变：同一实例任意表示下选档度量恒定（既有断言保持）

## Scope

- `src/domain/lod/lodPolicy.ts`（阈值结构候选化）、`src/domain/lod/lodEvaluation.ts`（口径注释与调试输出）、两链选档接线面、`tests/`（选档 / 迟滞 / 稳定基准断言随语义同步改写并逐条记档）。

## Acceptance

- 新链候选常量落位且源码内标注候选性质；散布四维粒度与代表口径有断言
- 既有「任意表示下度量恒定」类断言语义保持（改写后等价通过）
- 三门槛按 AGENTS 判据式口径

## 取证路径

`docs/acceptance/t021/021.2/`

## 完成记录

**2026-09-23 执行（threejs-runtime-agent）——完成；全量 npm test / check:layers / typecheck 由主代理两线合流后统一跑（本记录含定向自证结果）。**

- 新增断言（无新源文件）：canopy 名义区间落位（(midToCanopy, canopyToCulled] 四区划分 + canopy↔mid / canopy↔culled 边界迟滞 + 升档跨档直达）、跳档映射泛化（resolveDeclaredRepresentation：canopy 承接 low / 直达 canopy / 泛化 tie-break 取更远端 / 全四表示链 low 非必经）、表示能力驱动组合（13 树种 levels 派生分支 canopy 不可达 = 正确行为 / representations 声明优先 / GLB 缺省单档 + 超远 culled）、假想 canopy 声明资产两链持有语义（021.7 接线前不迁移、canopy 源零请求、迟滞参考已记录 + legacy 同机位对照迁 low）、散布四维粒度（region 维：同块键同资产两 region 独立选档；asset 维：同块跨资产基准半径差分裂）、代表 scale 全集口径（seed 搜索 fixture：low 抽稀剔除桶内最大实例后读数仍按全集最大——误用抽稀集将跨 canopyToCulled 线误裁，判别前提在测试内自证）。
- 修改（选档语义面）：`lodPolicy.ts`（LodThresholds 新链 {highToMid, midToCanopy, canopyToCulled} + band 0.15，初值 6/16/60 legacy 同值直承；头注候选化「021.8 A/B 重锁」+ T006.5 历史锁定记档保留 + 6/16/60 ↔ 16.7%/6.25%/1.67% 屏占比对照）；`lodEvaluation.ts`（输入 declaredLevels → representations（effectiveRepresentationChain 产物，声明优先/派生回退语义保持）；nominal 四区 high/mid/canopy/culled——canopy 名义区间落位解除不可达、low 无名义区间（跳档承接，注释记档）；resolveDeclaredLevel → resolveDeclaredRepresentation（REPRESENTATION_ORDER 泛化，tie-break 取更远端）；nominalUpperBoundOf：low/canopy → midToCanopy、culled → canopyToCulled（初值下与 legacy 逐位一致）；头注补 screenFraction = 1/m 解释口径（不作选档输入）；度量公式 normalizedViewDistance 逐操作符零改动）；`InstancedAssetPool.ts` / `ScatterChunkManager.ts`（getDeclaredLevels/getAssetLevels → getRepresentationCapability + 每资产有效链缓存（帧路径零重复归一）；canopy 产出持有注释更新（021.7 前现状，真实资产不可达）；散布 maxScale 全集口径收紧：抽稀前撒点集（maxScaleOf，选档输入与密度解耦——原实现按抽稀后当档集累积，非均匀 scaleRange 下档间读数漂移，属本次口径修正）；散布粒度核实：现状结构性即 region × chunk × asset 四维（source 即 region 维），零结构改造、注释与断言记档）；`Renderer.ts`（注入点 declaredLevelsOf → representationCapabilityOf：meta.representations + levels 双字段投影，两链共用）；`lodView.ts` / `lodDistribution.ts`（1/m 解释口径注释落调试输出面；分布 canopy 键消费侧无需接线——currentLod 泛型流经，真实资产恒 0 直至 021.6/021.7）。
- 测试改写逐条记档：见四个测试文件头注「T021.2 改写记档」节（lodEvaluation.test / InstancedAssetPool.lod.test / ScatterChunkManager.lod.test / ScatterChunkManager.batch.test），模式统一 = 输入字段/阈值字段名迁移（同值直承 16/60）+「m 刚过 midToLow → low」→「m 刚过 midToCanopy → low（canopy 名义档跳档承接）」等价迁移；既有「任意表示下度量恒定」类断言（两链稳定基准 4+4 用例）语义保持、字段名迁移后等价通过。
- 定向自证：`npx vitest run tests/domain/lod`（74 过）+ `tests/domain` 全目录（18 文件 277 过）+ 选档相关 8 文件（lodEvaluation/lodPolicy 经由 / representation / batchPolicy / 两链 lod + batch / lodDistribution / lodView，127 过）+ 间接消费 6 文件（InstancedAssetPool 基线 / RuntimeViewport 两链 / ScatterChunkManager 基线 / RegionScatterSync / RenderLoop，122 过）；`npm run typecheck` 零错；`npm run check:layers` 610 文件全绿（含并行线新增文件）。纯选档语义任务（node 可测），无视觉验收面，未拉 dev server。
- 对方文件错误：无（typecheck/check:layers 全绿，并行线 tree/** 半成品未报错）。
