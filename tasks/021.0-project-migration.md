# T021.0 立项与文档迁移

> 状态：**done（2026-09-23 立项 + 完成同会话）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：D41 裁定（grilling 三轮 16 题 + 独立对抗审核四组必改 + 用户终确认）

## Goal

把用户根目录草稿 `lod升级.md`（未跟踪工作文件）经三轮 grilling 与对抗审核修正后的定案，一次性落盘为规范与任务书体系：运行面架构规范新文档、lod-spec 过渡修订、epic 与八份子任务书、D41 决策记档、T006 / T006.6 处置收口、TASKS / PROGRESS 挂行，草稿迁移完成后删除。

## Research Gate

豁免（epic 级记档，非程序化资产生产任务）。

## 待裁决位

- 编号顺延 T021（T019 为颜色管线语义保留，D29.3 / D39 引用；T020 已占用）
- 文档分面：运行面新文档 representation-runtime.md + 声明面 lod-spec.md 收窄（两面互指，各自唯一真相源）
- lod-spec 021.0 只做过渡修订（真相源声明改写 + Proxy 条款 + 被取代表述行内注），内容性修订随 021.1 / 021.6——满足 D40「已生效未同步窗口为零」（行内注即时生效，改写归执行）
- 草稿删除不留档：仓库无归档机制、草稿为未跟踪用户工作文件、用户明示裁定；全部有效内容已迁入规范与任务书
- T019 在 TASKS 补占位行（审核发现 T020 完成记录称「TASKS/PROGRESS 同步」实未同步——不回改 T020 历史，占位行本会话补齐）

## Scope

- 新建：`docs/procedural-assets/representation-runtime.md`、`tasks/021-lod-representation.md` + `021.0`–`021.8` 九份。
- 修订：`docs/procedural-assets/lod-spec.md`（五处过渡）、`DECISIONS.md`（D41 正文 + 索引行 + D27 / D28 索引 Note + D28.2 行内注）、`TASKS.md`（T006 / T006.6 收口 + T019 占位 + T021 挂行）、`PROGRESS.md` 快照、`tasks/006.6-lod-reference-bounds.md` 迁移记档、`tasks/006-lod-chunking.md` 勾选与处置记档。
- 删除：根目录 `lod升级.md`。

## Acceptance

- 九项落盘清单全部完成；`check:tasks` 报告产出且新文件无信号①命中（预期信号记档于完成记录）
- `check:layers` 与 `typecheck` 恒跑通过；无代码执行面变化，`npm test` 按判据式不强制

## 取证路径

`docs/acceptance/t021/021.0/`（本任务为纯文档，检查器输出即取证）

## 完成记录

**done 2026-09-23（单会话，主代理执行——纯文档零代码，不涉渲染实质修改）**

1. **九项清单全落**：① `representation-runtime.md` 新建（十六节：背景取代 / 六概念 / 表示模型 / 选档 / 过渡 / Canopy / 阴影 / 密度 / 批次 / 状态与键 / 路由契约 / 编辑态与拾取 / 帧时序统计 / 验收口径 / 禁止清单 / 扩展契约与路由）② `lod-spec.md` 五处过渡（行 3 真相源声明收窄 + §2.1 枚举决策门注 + §3 Canopy 转正注 + §4 选档快照注 + §6 接缝注）③ epic + 021.1–021.8 共十份任务书（Lean 形态，待裁决位全部差异摘要化）④ TASKS.md（T006 部分取代注 + 006.6 收口迁 Done + T021 挂行 + **T019 占位行补登**）+ PROGRESS.md 快照 ⑤ DECISIONS.md **D41** 正文十二条 + 索引行 + D27/D28 索引 Note 修订 + D28.2 正文行内注（审核必改项①全采纳）⑥ `006.6-lod-reference-bounds.md` 四处划线迁移注（头部 / Requirements Step 3 / Acceptance / Constraints）+ `006-lod-chunking.md` 勾选行收口 + 进度处置行（必改项③）⑦ 根目录 `lod升级.md` 删除（迁移完成）⑧ 检查器验证（下条）⑨ 遗留无。
2. **验证证据**：`check:tasks` 扫描 82 份（72 + 新 10）——**新 021 系十份零 finding**（信号①②③全零）；信号①全局零命中（representation-runtime.md 入参考集未与任何任务书节标题冲突）；信号②全局仅历史遗留 `004-style-gallery.md` 5.1% 一处；信号③ warning 全部为历史 011.x / 018.x 多基线交叉（与 T020 首跑记档同形态，D40 历史隔离）；**信号④ DECISIONS 索引双向核对 42 == 42 零漏登零幽灵行**（D41 首登即同步）。`check:layers` 604 通过；`typecheck` 零错；`npm test` 按判据式不强制（零代码执行面变化——仅 docs / tasks / 管理文件与草稿删除）。
3. **偏离与事故**：TASKS.md 首次编辑一处 old_string 不匹配（T006.5 行实际文本与重建不符），拆分重做即时修正，无残留；无其他偏离。
4. **遗留**：021.1–021.8 八个子任务待执行（依赖链见 epic；启动即挂 TASKS Current）。
5. **审核追记（同会话，落盘后全新只读审核子代理九项对抗验收）**：总判定「需修正」两项，均已即时修正——① representation-runtime.md §10.2「现存多档资产均已声明」断言与代码不符（streetlamp 两档 levels 无 shapeFamily = `assetId::level` 中间分支现存唯一消费者，`streetlamp.asset.ts:61`），改为如实表述并给 021.7 增加「021.1 收编前置、完成前禁删分支」约束，防双档缓存静默塌缩；② lod-spec §6 表注指定的 021.7 同步改写义务补进 021.7 Scope 与 epic Requirements（原漏接，会形成未来时点「已生效未同步」）。其余七项全过：关键数值四处一致、源码抽查逐项命中、Lean 合规零 finding、依赖链逐份吻合、D31.1 逐 hunk 合规、checker 声明复核属实。低优先级两则处置：信号③枚举句以本追记补正（warning 实亦分布于 002.5 / 003.3 / 006.x / 008.x / 009 / 020 等历史文件，「本次变更零新增 warning」实质主张经行位核对成立）；D28.2 行内注书写瑕疵顺手澄清（外层日期拆为同日修订 + 追记两段）。修正后 check:tasks 复跑：82 份、021 系零 finding、DECISIONS 索引 42==42、warning/normal 计数与修正前一致（全部历史信号）。
