# T021.1 Representation 契约层

> 状态：**pending（2026-09-23 立项）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：021.0（done）

## Goal

把 D41 表示语义落为代码契约：类型联合原地演化（旧 `LodRepresentation` 退役）、`culled` 移出联合转提交状态、AssetDescriptor 能力声明字段、Source 契约扩展 bounds、以及「新增表示 = 扩类型 + 注册 provider」的开放式扩展结构。本任务只立契约与类型面，不改选档与渲染行为。

## Research Gate

豁免（epic 级记档，非程序化资产生产任务）。

## 待裁决位

- 联合类型四值、不含 impostor；`culled` 独立为 submit state 类型
- 原地演化一次改齐，不搞新旧双轨过渡期（domain 纯函数层测试覆盖充分）
- `ProceduralLevel` 三值保留、语义收窄为构建能力；声明走独立 `representations` 字段、`levels` 不污染
- SelectionBounds（恒 High 基准球）与 RenderBounds 分别命名；RenderBounds 随 Source 成套、球形、Cache 拥有池引用
- 新类型与状态形状落 `src/domain/lod/`（语义在 domain、执行在 runtime 的既有分层）

## Scope

- `src/domain/lod/`（类型 / SelectionState 形状 / 策略类型）、`src/domain/assets/AssetDescriptor.ts`、`src/runtime/procedural` 与 `loaders` 契约面（InstanceSource 返回形状）、`docs/procedural-assets/lod-spec.md` §2/§3 声明面条款改写、`tests/domain/lod/` 新契约测试。

## Acceptance

- 有效链按能力生成三态断言（canopy 能力链 / 无 canopy 链 / 单档链）
- 旧类型引用全量迁移无残留（typecheck 零错即证）；lod-spec 声明面条款与代码一致
- 三门槛按 AGENTS 判据式口径

## 取证路径

`docs/acceptance/t021/021.1/`

## 完成记录

**2026-09-23 执行（threejs-runtime-agent）——完成，三门槛全绿。**

- 新文件：`src/domain/lod/representation.ts`（RuntimeRepresentation 四值联合 / LodSubmitState='culled' 提交终态 / LodSelectionOutcome 产出联合 / SelectionState（§10.1 原文）/ ShadowPolicy / RenderBounds / REPRESENTATION_ORDER + effectiveRepresentationChain 能力驱动有效链纯函数；零回引 domain/assets——文件 DAG 保持 lodEvaluation → AssetDescriptor → representation → core 单向）、`src/runtime/loaders/RepresentationSourceRouter.ts`（§十六 路由表与 provider 类型，不接线）、`tests/domain/lod/representation.test.ts`（17 用例：联合锁 / 有效链三态 / 派生与优先级 / 乱序归一 / 类型形状 expectTypeOf）。
- 修改：AssetDescriptor（representations? 字段 + ProceduralLevel 注释收窄为 Asset Build Capability）、lodEvaluation（LodRepresentation 退役 → LodSelectionOutcome；canopy 序数=3/culled=4、canopy 名义上界类型完备分支——运行时不可达，行为逐位不变）、InstancedAssetPool（InstanceSource.bounds?: RenderBounds 契约 + currentLod 类型迁移 + canopy 不可达防御收窄）、ScatterChunkManager（current 类型迁移 + 同款防御）、lodDistribution（键联合补 canopy 键位、计数逻辑零改动——canopy 恒 0 直至 021.2/021.6）、lodReference（SelectionBounds 命名锚点）、streetlamp.asset（meta 增 representations: ['high','low']——纯声明无消费者，ProceduralSourceCache 键逻辑一行未改）、lod-spec.md §2/§3 声明面条款改写（双轨类型表 / 缺省三态 / streetlamp 收编记档 / Culled 提交状态表述）。
- 既有测试机械改写：lodEvaluation.test.ts（仅类型名迁移 + run 返回注解，行为断言一字未改）；lodDistribution.test.ts（键位补 canopy: 0 共 6 处字面量 + 头注，计数断言数值不变）。
- 三门槛：`npm test` 4041 全绿（HEAD 基线 4024 + 新增 17）；`npm run check:layers` 607 文件全绿；`npm run typecheck` 零错（= LodRepresentation 类型引用零残留即证）。纯契约任务无视觉验收面，未拉 dev server。
- 主代理验收（2026-09-23）：diff 全量审查（契约落位对 D41 §三/§四.3/§七/§十/§十一/§十六逐节核对）+ 三门槛亲自复跑全绿 + `LodRepresentation` grep 复核（残留仅注释历史记档）；取证落盘 `docs/acceptance/t021/021.1/README.md`。基线数说明：PROGRESS 快照 4023 → HEAD c6204be 实际 4024（+1 漂移早于本任务，非本次引入）。
