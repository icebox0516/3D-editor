# T010.2 元数据与分类契约

> 归属：[T010 程序化资产公共能力与规范](010-asset-capability.md) ｜ 状态：**done**（2026-09-19） ｜ 前置：T010.1

## Goal

公共资产元数据只扩分类维度，为 T016 资产管理器「大类 → 小类/族 → 具体资产」浏览铺路；植物语义不得泄漏进公共协议。

## Requirements

- **公共层（AssetDescriptor）**：大类 enum（植物 / 建筑 / 车辆 / 自然 / 人 / 公共设施 / 设备 …）+ 可选 family（broadleaf / conifer / shrub …）；现有 11 个程序化资产归类映射（plant / facility / dev → 大类 + family）
- **植物语义隔离**：heightRange / crownWidthRange 等放 procedural 专属扩展（如 `proceduralProfile?: { heightRange?: Range; widthRange?: Range; }`）或 family 层元数据——crownWidth 等植物术语不得成为全局字段（消防车 / 建筑 / 湖泊 / 路灯不应被迫回答「冠幅是什么」）
- **资产管理器消费预留**：分类契约文档化（供 T016 直接消费），不实装任何 UI——非重做 Asset Runtime

## Scope

- 预期触碰：AssetDescriptor 类型、各资产 meta 归类、契约文档
- 不碰：UI、运行时行为

## Acceptance

- 大类 / family 枚举落型；11 资产映射完成且现有 UI 资产栏分组行为不变（或迁移方案经用户裁定）
- `npm test` / `check:layers` / `typecheck` 全绿

## Constraints

- 分类字段为浏览语义服务，不承载行为分支（渲染/放置行为不得按大类 if-else）

## 完成记录

**done 2026-09-19**（procedural-asset-agent 交付；三门槛 2547 / 449 / typecheck 零错全绿）

- **类型契约**：新文件 `src/domain/assets/taxonomy.ts`（domain/assets 层，零 THREE）——大类 8 值 `ASSET_TAXONOMY_CATEGORIES`（plant/building/vehicle/character/facility/device/nature/dev）+ family 6 值 `ASSET_TAXONOMY_FAMILIES`（broadleaf/conifer/shrub + public-facility/road-facility/fire-safety），const 元组 + 派生联合（元组即类型即契约测试运行时校验源，单一真相）；`AssetTaxonomy { category; family? }`。`AssetDescriptor.ts` 的 `ProceduralAssetMeta` 增 `taxonomy`（**必填**——一次定契约避免二次迁移，D22）与 `proceduralProfile?`（`heightRange?`/`widthRange?` 通用维度语义，`Range {min,max}` 闭区间，米）。GLB 侧 `ModelAsset` 不动（映射策略文档化归 T016）。
- **大类值域依据**（每值有消费者证据，不投机扩值）：plant/building/vehicle/character/device ← 程序化 11 资产 + GLB manifest 8 目录分类（`assets/manifest.json` / `scan-assets.mjs` CATEGORY_LABELS）；facility ← 程序化 5 设施 + GLB 三设施细分目录在大类层归并（family 保粒度，即 T016 三级浏览「设施→消防设施→消防栓」直接成立）；**nature = 预留值，当前 0 资产**，唯一依据 = D21 决策点名近期消费者（固定尺寸湖泊，T016/T017）；dev ← asset_seedstack（浏览器产品栏 category 'dev' 过滤语义同源）。
- **设施细分粒度选型**：GLB 现行 8 目录 slug 不一一升大类——public-facility/road-facility/fire-safety 三者归并 facility 大类、以 family 承接细分（slug 原样复用零翻译），依 D22 示例值域（公共设施为一个大类）+ T016 三级浏览结构 + 大类数量可管理三重理由；映射表落 §6。
- **11 资产归类映射**：plant×5（tree_3a/oak→broadleaf、pine→conifer、shrub→shrub、flower 不填 family——草本/地被族 T015 落地时再定值，无诚实族归属不占位）；facility×5（streetlamp/signpost→road-facility、parkbench/trashbin→public-facility、hydrant→fire-safety）；seedstack→dev（无族可归，不填 family 与 profile）。oak 如实归 broadleaf：分类只认树种不认实现代际（003.4 旧实现冻结返工中，不重做）。
- **proceduralProfile 数值全部实测**（不投机造数）：临时探针逐资产构建细模档几何取包围盒（tree_3a 全 8 槽，其余单形态；h 0.39–7.00 / w 0.33–5.15 等，tree_3a 跨槽带 h 7.39–9.12 / w 4.44–9.80），两位小数舍入落 meta；seedstack 不声明（DEV 资产不进产品浏览）。**发现并记档两处头注口径差**：① tree_3a 模块头「总高 7.4–8.5m、冠幅 5.4–6.6m」为 T008.2 slot-0 锚点描述（slot-0 实测 8.483/6.147 落带内），profile 按全槽实测带口径填（slot-3 展开型宽 9.80 如实入带）；② shrub 头注「丛幅≈1.9m」为估算，实测 1.69 以实测为准——均写进资产 meta 注释与规范 §5。
- **植物语义隔离落型**：crownWidth 等植物术语未成为任何公共字段——profile 只收 height/width 通用维度语（消防栓/路灯照常回答）；冠幅语义归家族契约层（`tree/broadleaf` 契约已有 crown* 字段），浏览侧换 widthRange 通用语（D20.6/D22 禁则入规范 §8）。
- **契约测试** `tests/runtime/procedural/assets/assetTaxonomy.test.ts`（+5 测试）：① 枚举值域校验（全部收割 meta 的 category/family 落值域）；② 11 资产映射整表锁（`EXPECTED_TAXONOMY` 与收割清单逐资产一致——新资产必须登表，与 taxonomy 必填的编译约束构成双闸）；③ family↔大类配对锁（`Record<AssetTaxonomyFamily, AssetTaxonomyCategory>` 完整性 = 新增 family 不同步配对即编译红）；④ profile 数值纪律（min≤max 正数有限）；⑤ 实测落带（跨形态槽全枚举构建，ε=0.01 舍入容差 + minY=0 贴地前提——几何演化越带即红，逼声明同步）。
- **契约文档** `docs/procedural-assets/metadata-taxonomy.md`（organization.md 姊妹篇，表格 + 短判据 + 可执行规则）：两正交维度对照表（category 现行 UI 分组键不动 vs taxonomy T016 三级寻址，同名不同物消歧 + taxonomy.family ≠ shapeFamily 消歧）、大类/family 值域证据表、11 资产映射表（含实测带）、GLB 侧映射策略（目录 slug→taxonomy 映射表，T016 装配时派生、manifest 与扫描脚本不动）、扩展流程五步（依据先行→值域落型→配对登记→映射登表→三门槛）、禁则四条（行为分支禁令/植物术语禁令/不投机/不迁移 category）。
- **验收数据**：`npm test` 2547 通过 / 182 文件（基线 2542 + 5 新契约测试，零回退）；`check:layers` 449 文件全过（+taxonomy.ts +assetTaxonomy.test.ts）；`typecheck` 零错。UI 行为不变的构造性依据：零 ui/ 文件改动，`meta.category` 字段与 browserModel/ContentBrowser 消费路径原样（taxonomy 为新增正交字段，浏览器无读取点）；零运行时行为改动（11 资产 build/几何/材质零触碰，仅 meta 数据面增量）。
- **偏离记档**：① 任务书 Requirements 示例 `crownWidthRange` 未落任何字段——按「植物术语不进公共协议」精神收紧为 widthRange 通用语（消防栓/路灯被迫回答冠幅即违 D20.6），冠幅语义留家族契约层，规范 §8 立禁则；② `nature` 零资产值入域（任务书与 D22 示例均含「自然」）——依据 D21 决策点名而非现存资产，规范 §3 显式标注「预留值，当前 0 资产」供 T016/T017 复核；③ taxonomy 波及 6 个测试文件的 ProceduralAssetMeta 构造 fixtures（PlacementTool.variants / AssetRegistry / AssetSourceRouter ×3 / ThumbnailCache / ProceduralSourceCache / routes——必填字段的机械补齐，替身资产按原型归 facility、临时管线资产归 dev，逻辑零改动）。
