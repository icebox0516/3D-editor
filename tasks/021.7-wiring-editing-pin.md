# T021.7 两链接线 + 编辑态 pin

> 状态：**pending（2026-09-23 立项）** ｜ Epic：[021-lod-representation](021-lod-representation.md) ｜ 前置：021.2 + 021.3 + 021.4 + 021.5 + 021.6

## Goal

放置与散布两链切到表示键与统一路由门面，落地编辑态优先级 pin 层与过渡拾取，全链跑通 High→Mid→Canopy→Cull。

## Research Gate

豁免（epic 级记档，非程序化资产生产任务）。

## 待裁决位

- 放置桶键 `sourceKey::representation`、散布桶键 region × chunk × asset × representation；`culled` 是提交状态不是桶类型
- 源缓存维度统一 sourceKey + representation；三分支收敛为二（声明能力 → sourceKey::representation；未声明 → 纯 assetId）；「未声明多档」中间态删除的前置 = streetlamp（唯一现存消费者，两档无 shapeFamily）先经 021.1 representations 声明收编
- 散布 provideSource 签名从 level 参数化为 representation；GLB file 分支恒 high 不变（D28.5）
- RepresentationSourceRouter 为现有路由之上的门面（provideRepresentationSource → {geometry, material, customDepthMaterial?, bounds}），渐进扩展不重写
- 编辑态 pin 集：选中 / 变换中 / gizmo 目标 / 相机聚焦飞行目标（focusObjects）——每帧强制高档 + 全量阴影 + fade 满值；编辑结束恢复正常调度；零 Scene 数据改动
- Ghost / 预览沿用固定高档现状规范（D27.14），不进 pin 机制
- 拾取身份跨表示不变；过渡期仍可拾取；真剔除后不可拾取

## Scope

- `src/runtime/instancing/InstancedAssetPool.ts`、`src/runtime/scatter/ScatterChunkManager.ts`、`src/runtime/loaders/AssetSourceRouter.ts` 门面、编辑态 pin 层（Scheduler 面）、`src/runtime/RuntimeObjectMap` 消费核对、`tests/` + 兼容回归面、`docs/procedural-assets/lod-spec.md` §6 接缝表与 §2.4 缓存键表述同步改写（表注既定义务，勿漏）。

## Acceptance

- 全链换档序列单调无震荡（canopy 链复验 021.3 机制）；双表示过渡期拾取正常
- 编辑态四类目标强制高档可断言、退出恢复；Ghost / 预览 / 撤销重做 / 渲染模式零回归
- 三门槛按 AGENTS 判据式口径

## 取证路径

`docs/acceptance/t021/021.7/`

## 完成记录

（待执行）
