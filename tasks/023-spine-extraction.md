# T023 生产脊柱 spine 提取（workflow 体系升级：spine + 方法剖面）

状态：pending ｜ 立项 2026-09-24（资产库扩充方向共识会话，用户裁定）

## Goal

把 tree.md 中跨生产方法恒真的执行规则提取为 `spine.md` 通用层，tree.md 剖面化为乔木方法正文，SKILL.md 路由升级为「生产复杂度判据 → workflow 选择」——为后续 simple-asset.md（轻量级，首例批次触发）等新 workflow 提供零复制、零漂移的公共基座。

## 前置

- 2026-09-24 共识（本任务书待裁决位 = 共识快照）
- 无代码前置；建议先于 T025 设施首例批次（第一个新 workflow 消费者）完成

## 待裁决位（2026-09-24 用户裁定，执行时不再复议）

1. 提取时机 = 现在（路线图已确认多方法消费者，D20.3 反预建前提不成立）
2. 保守提取纪律：只迁「证明与方法无关」的条款；拿不准的留 tree.md 标注；D36 §5 分裂判据兜底
3. spine 不固化 tree 的 Step 执行者映射——spine 写「每个 Step 定义职责 / 输入 / 输出 / 执行角色槽；具体 Step 集合与 Agent 映射由 Workflow 按生产方法决定」
4. 生产复杂度 = 路由判据，不建全局枚举（simple / standard / complete 不成为流程实体；命名避让 `ProceduralLevel` LOD 档位语义）
5. 中等级不预建实体：灌木 T014 立项时按 D36 §5 裁定路由
6. simple-asset.md 不预建：首个简单资产批次（T025）立项时创建，引用 spine 只写差异剖面
7. spine 变更档位 = 同步机制升格三级最高档（生产方法结构变化：主代理提案 + 用户确认）
8. T002.4 批量先例升格通用口径：同方法同批次简单资产可一个任务批量交付

## Scope

### A. 新建 `.zcode/skills/asset-production/spine.md`

迁入（抽象化后）：

- **Step 编排骨架 = 规则不是映射**：角色槽定义（research 通道 / 主代理 / 几何 / 材质 / runtime / 独立验证）+ 跨方法恒真规则（Research Gate 先于依赖现实对象的开发 Step〔D26〕/ 主代理不直接写渲染实现 / 并行 Step 文件集不相交 + 冻结接口声明 / 中断即未验证重验）
- **read-set 消费链**：四环节 + 三列化表格规范（Path / Section / Mode）+ 派遣简报形状 + 观察钩子（只迁规则与格式，各 Step 必读表留各 workflow）
- **测试纪律**：D40 五者准入 + 已覆盖公共能力不因新增资产复制 + 三门槛判据式引用 + 逐子任务执行模式引用
- **同步机制**：四问筛选器 + 五向分流 + 升格三级 + 完成记录收口 + 索引同步（「本文件」改「所属 Workflow」）
- **抽象版三必做**：①现实/目标身份确认（结论记档）②统一基线验证（固定机位 + 固定灯光 + 冻结时间基线帧；具体参数由 Workflow/验收 SOP 拥有）③问题与契约缺口记档（有无都记；有族归族门、无族归任务收口）+ 疑点触发机制 + 判读纪律抽象（console 零错误零警告通用；「LOD 档间连续」标适用时）+ 校准护栏（一轮默认两轮封顶、上收不硬磨）+ 人工裁定门例外条款（默认不触发）
- **族级门七项骨架（带适用性）**：横向一致性 / 差异成立 / 混合组合场景 / 性能与预算 / LOD（适用时）/ 契约收口（有 family 时）/ 回归；family-specific 检查项归各 Epic 定义
- **新家族首例流程**（D37 六步，任何 workflow 的新 family 通用）

### B. tree.md 剖面化改写

保留乔木方法剖面：适用判定（单干树木）/ Family 参数位 / 器官账目判定 / slot-0 锚点 + 槽差量纪律 / 树几何与 LOD 档位派生 / SDF 叶·树皮·风动两层·深度材质同源 / 树 Stage / 树 read-set 必读表 / 风动三证据 / ShapeSlots 分件示例 / M25 + day + freezeTime 具体参数。删与 spine 重复段落，改引用锚。

### C. `asset-production/SKILL.md` 路由升级

路由表加 spine 声明行；路由两步化：判生产复杂度/方法 → 选 workflow（D36 §5 分裂判据兜底）。

### D. AGENTS.md 同步

「程序化资产生产任务」行的技能结构描述补 spine 层（入口路由 → spine + workflows/<name>.md）。

### E. DECISIONS.md 记档（两段增补文本为定稿原文，照录）

**D36 增补**：

> Spine 不拥有任何资产语义，只拥有跨 Workflow 的执行规则；Workflow 负责生产方法；Task 负责实例差异。

并记：workflow 体系升级为「spine + 方法剖面」两层；提取时机由「第二 workflow 触发」提前为「路线图确认多方法后立即」（2026-09-24 用户裁定）。

**D30 增补（最低可信证据原则，用户起草原文照录）**：

> **最低可信证据原则：** 每个资产都必须具备最低可信证据，证据深度与资产生产复杂度匹配。生产复杂度仅作为路由判据，不建立全局固定的"简单 / 标准 / 完整"流程实体；具体执行深度由所选 Workflow 定义。Spine 只承载跨 Workflow 的共性执行规则，不拥有复杂度定义。
>
> **单资产验收：** 轻量资产同样执行三必做，但采用轻量证据形式：①身份 / 目标符合性一行结论；②每资产一张统一基线帧；③契约 / 公共能力缺口有无记档。复杂资产按对应 Workflow 执行更深验证；疑点、特殊风险与新能力按需增加证据。
>
> **新 Production Family 首例：** 首例必须完成该 Workflow 定义的首例验收深度，不再统一要求树木级完整 SOP；若为树木等复杂方法，执行其完整首例流程；若为简单资产 Workflow，则执行轻量但完整的首例验收。后续同 Family 按增量验收；族级集体验收仅在对应 Epic 定义且适用时执行。
>
> **批量资产：** 多个同方法、同批次简单资产可由一个任务批量交付，共用 Workflow、验收方法与测试基础设施；单资产仅在出现新增行为、结构 / 参数 / 材质语义、新契约、新风险或公共能力新边界时增加对应验证与测试，不因资产数量机械复制证据。

## 特殊 Acceptance

- 纯技能 / 文档面：`check:layers` / `typecheck` 恒跑；`npm test` 按三门槛判据（预期不触代码执行面则不强制，触则全量）
- `npm run check:tasks` report-only 不新增 warning（任务书禁复述纪律）
- 提取完整性：tree.md 与 spine.md 逐段 diff 无同文重复；spine.md 零资产语义词汇（树种 / 槽位 / SDF / 树皮 / 风动等乔木词汇不出现——角色槽与适用性开关表述除外）
- 引用完整性：tree.md 剖面化后各 Step 仍可经 spine 锚点还原完整流程（走查一遍 Step 0–4 read-set 链）

## 取证路径

`docs/acceptance/T023/`（独立任务，无 epic）
