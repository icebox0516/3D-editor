# T006.5 T006 验收门

> 归属：[T006 Asset Runtime LOD](006-lod-chunking.md) ｜ 状态：**done 2026-09-19** ｜ 前置：006.1–006.4

## Goal

双档性能验收（D10）+ 七项观测报表 + 范围门判定（D27）——「性能希望」变「性能治理」的最终取证。

## Requirements

- **园区档**：≤ 2 万散布实例、核显 1080p 连续渲染 ≥ 30fps
- **城市档**：≤ 10 万散布实例、独显 ≥ 30fps；压力场景入独立测试资产（可复跑，不依赖 T003 样式链解冻，D27.11）
- **七项观测报表（D27.9）**：FPS / frame time p95·p99 / draw calls / triangles / visible instances / LOD 分布（各档实例数 + 各档桶数双口径）/ 各 Representation 桶数；GPU 耗时有则记录不做成承诺；renderLoopStats 保持零 THREE 零 DOM 边界
- **范围门判定（D27.3）**：城市档 triangles / draw calls / frame time p95 任一超预算 → Impostor 升级必做，结论写进完成记录并立项后续
- **对照组（D27.13）**：同场景 LOD 开/关对照留档；不回溯 009.7 基线
- **回归**：三重门槛 + 基线对账；LOD 关闭形态与 T003 验收结果对比（无退化）

## Acceptance

- 双档数字达标并留档；报表完整；范围门结论明确（达标 或 Impostor 立项）；回归绿；同步 TASKS.md / PROGRESS.md

## Constraints

- 取证按 `docs/threejs-debugging.md`（MCP 金丝雀 + 截图；性能数字 MCP 取数 + 像素双证）；LOD 分布 DEV 可视化并入本任务取证（D27.14）

## 完成记录

**2026-09-19 done（主代理执行验收门，取证工具脚本 + 常量候选→锁定标注——零生产行为改动）**。取证 `docs/acceptance/t006/006.5/`（README + 38 截图 + 6 工具 + 4 原始 log）。环境：RTX 2080 Ti / Chrome 152 手动窗口化实例（vsync-off 严格保守口径沿 009.7；虚拟显示器 rAF ~10Hz 不可测 vsync 开启态，记档）/ 金丝雀过（主 canvas ≫ 小地图）。

**① 双档性能验收（D10）**：
- **城市档达标**：10 万路灯（103,988 实例登记口径 / 4,096 块，006.4 同参复跑逐位对账；D27.11 独立资产）——三机位 175/187/190 FPS（p95 6.7–7.3ms ≤ 33.3ms）、drawCalls 峰值 **531 ≤ 650**。
- **园区档达标（2080 Ti 实测 + 核显限制记档）**：⚠ 本机 i5-13400 核显 **BIOS 禁用**（设备级确认仅 OrayIddDriver + 2080 Ti；GpuPreference 注册表切换无效——无核显设备可切；禁用独显在远控虚拟显示器环境不可接受）——园区档在 2080 Ti 实测：2 万夏栎（16,772 棵登记口径，LOD 四档和 17,125——≤2 万线）三机位 **206/221/229 FPS**（p95 ≤7.0ms）+ 2 万路灯对照 272–309 FPS，1080p（setDeviceMetricsOverride 1920×1080，canvas 1292×932 记档）。**核显保守外推判读**（README ⑤：GPU 帧时外推 21–39ms + CPU 2–3ms → 24–42fps **30 线临界**）+ **遗留核显环境复验**——若不过，治理面 = 常量级调整（LOD_THRESHOLDS 更保守 / keep.low 下调，与数值解耦无架构改动）。

**② 七项观测报表（D27.9）**：全项留档（city/park-log.json）——FPS / frame time p50·p95·p99·max（p99 = 取证脚本页面侧 nearest-rank 采样器注入，sampleFrames 只有 p95，验收工具不改 src）/ drawCalls / triangles / visible instances / LOD 分布双口径（实例+桶）/ 各 Representation 桶数；GPU 时间 **unavailable**（EXT_disjoint_timer_query_webgl2 未插桩，不作伪精确，沿 009.7）；renderLoopStats 零 THREE 零 DOM 边界未动。

**③ 范围门判定（D27.3）——三项全达标，Impostor 不立项**：drawCalls 531 ≤ 650（81.7% 用量）/ triangles 0.58M @ 175fps / p95 7.3ms（22% 用量）→ Proxy/Impostor 维持规范预留位（lod-spec §3）；≥50 万实例量级时重开范围门。

**④ 对照组（D27.13）**：双档 LOD on/off 同场景同机位留档——城市档 339 calls/384K tris/182fps ↔ off 7,773 calls/10.9M tris/35fps（p95 36.1ms）；**树场景 271 calls/6.4M tris/207fps ↔ off 2,199 calls/1.84 亿 tris/17fps（p95 83.7ms）**——2 万棵全保真不可用 → LOD 调度流畅（12.2×帧率），「性能希望变性能治理」的最终实证。

**⑤ 换档跳变验收 + 人工复核窗口收口（D27.10）**：2 万树推拉 d=60→600 十九帧序列（穿越全档界）——选档迁移**单调无震荡**（H 109→0、tris 8.8M→2.5M 平滑收敛）；18 对相邻帧 diff 严格单调递减无尖峰 + LOD off 同款运动基线对照（on 25.97/14.70/6.25% vs off 27.44/21.16/13.64%——**换档增量不超纯运动视差**）；视觉模型核验三关键帧（近景完整/中远无分层条带/中景无空洞带无裁剪线）——判「无 pop/无闪烁/无整屏突变」。**人工复核窗口随验收门收口：机器判定 + 视觉核验为收官判据，用户观感异议窗口继续开放**（沿 008.6/009.7 先例，可回溯本目录 38 图 + 006.3 的 58 图重开微调）。

**⑥ LOD 分布 DEV 可视化（D27.14）**：取证注入 DOM overlay（不进产品代码）——calls/tris + 双口径 H/M/L/C 实时面板随 19 帧序列截图入画留档；数据出口 Renderer.getLodDistribution()（006.4 交付）消费确认。

**⑦ 策略常量候选→锁定（T006.5 实测锁定记档，数值不变）**：LOD_THRESHOLDS 6/16/60/0.15（依据：分布合理 + 序列无震荡 + 视觉无分层）+ BATCH_POLICY 650/5000/1·1·0.5/32/2（依据：峰值 531+余量 / 抽稀视觉无空洞 / 桶压缩正常）——lodPolicy/batchPolicy/budgetAlert/lod-spec/两测试断言描述同步更新（⚠→✅，纯注释零行为变更）；数值变更纪律不变（重开实测记档 + 一次锁全量断言连带）。

**⑧ 回归**：三门槛 **2657/191 全绿（基线零回归——本任务代码改动仅注释与断言描述文字）/ check:layers 465 / typecheck 零错**；LOD off = 恒 High + culled 旁路 = 语义等价 T003 时代行为（006.3 开关回退单测 + 本任务 off 侧 4 截图渲染完整）——T003 验收结论无退化延续；009.7 基线不回溯（D27.13）。

**T006 epic 5/5 收官**：通用 Asset Runtime LOD 完整交付——语义层（006.1）→ 非植物验证（006.2）→ 两链分桶换档（006.3）→ 批次控制（006.4）→ 双档验收 + 常量锁定（006.5）。遗留移交：核显环境复验（园区档临界外推）；散布 seed 路由修复（D20.4 维持 T003 解冻后独立任务）。下一步 T011 阔叶乔木族建设（Runtime LOD 就绪）。
